# Dispatch Assignment — worker_m1

## Identity
- Role: teamwork_preview_worker (HTML Anchor & Regex Fix Specialist)
- Working Directory: C:\Users\danat\Desktop\dvachbot\.agents\worker_m1
- Target Project Directory: C:\Users\danat\Desktop\dvachbot
- Original Request File: C:\Users\danat\Desktop\dvachbot\.agents\ORIGINAL_REQUEST.md
- Scope Document: C:\Users\danat\Desktop\dvachbot\.agents\orchestrator\PROJECT.md
- Survey Findings: C:\Users\danat\Desktop\dvachbot\.agents\explorer_r1\handoff.md

## Objective — Milestone 1 (M1): HTML Anchor Rendering Fix
Implement the fixes for corrupted HTML anchor tag rendering across backend and frontend code bases.

Specifically:
1. **Harden URL Regex Patterns**:
   - In backend (`site_tgach/main.py`, `Dubsite_tgach/main.py`, `broadcaster.py`, `common/text_utils.py`), update `URL_PATTERN` regex so it excludes HTML entities (`&`, `#`, `;`), quote characters, and Cyrillic/punctuation delimiters at link boundaries.
   - In frontend (`site_tgach/static/js/main.src.js` and `site_tgach/static/js/main.js`), update `linkRegex` in `formatTextGlobal` and `parseTextEffects` to stop at HTML entity delimiters (`&`, `#`, `;`).
2. **Prevent Double-Parsing & Nested Anchors**:
   - In `parseTextEffects` (`main.src.js` & `main.js`), check `container.dataset.parsed === 'true'` before doing regex replacements. If `container.innerHTML` already contains `<a href="...">` elements created server-side by `format_post_text`, do NOT re-parse URLs inside existing `<a>` text nodes.
3. **Attribute Protection & Quote Sanitization**:
   - Ensure all `href` attributes generated by backend `format_post_text` and frontend `formatTextGlobal` are cleanly enclosed in quotes, correctly escaped, and do not capture trailing quotes or Cyrillic text (e.g. `'>ТГАЧ`).
4. **Synchronization**:
   - Symmetrically apply all JavaScript changes to both `site_tgach/static/js/main.src.js` AND `site_tgach/static/js/main.js`.
5. **Testing & Verification**:
   - Create or run a Python test script (e.g. `pytest tests/test_html_anchors.py` or running a standalone verification script) passing strings like `>>1234 https://domain.com/b/res/343717.html'>ТГАЧ` through `format_post_text` to verify that `href` attributes are clean and free of `&#039;`, `&gt;`, or corrupted text. Document test commands and results in `handoff.md`.

## Mandatory Integrity Warning
DO NOT CHEAT. All implementations must be genuine. DO NOT hardcode test results, create dummy/facade implementations, or circumvent the intended task. A teamwork_preview_auditor will independently verify your work. Integrity violations WILL be detected and your work WILL be rejected.

## Output Requirements
Write your handoff report to C:\Users\danat\Desktop\dvachbot\.agents\worker_m1\handoff.md including:
- Files modified and exact changes made
- Build and test commands executed and their full output
- Proof that `>>1234 Текст` and URLs do not produce corrupted HTML anchor tags
