=== Matches for 'downloadCurrentFile' (2) ===
--- Line 1266 ---
   1251:         document.body.style.overflow = '';
   1252:         const mascot = document.getElementById('mascot-wrapper');
   1253:         if (mascot && SettingsManager.current.showMascot) {
   1254:             mascot.style.display = '';
   1255:         }
   1256:         closeModal();
   1257:     },
   1258:     createDOM() {
   1259:         this.overlay = document.createElement('div');
   1260:         this.overlay.className = 'media-stream-overlay';
   1261:         this.overlay.style.height = '100dvh'; 
   1262:         this.overlay.innerHTML = `
   1263:             <div class="ms-top-bar">
   1264:                 <button class="ms-thread-btn" onclick="MediaStreamManager.goToThread()">${t('go_to_thread')}</button>
   1265:                 <div class="ms-controls-right">
-> 1266:                     <button id="ms-download-btn" class="ms-icon-btn" onclick="MediaStreamManager.downloadCurrentFile()" title="${t('iv_download', '???????')}">?</button>
   1267:                     <button id="ms-tags-btn" class="ms-icon-btn" onclick="MediaStreamManager.showCurrentFileTags()" title="${t('tags_btn_hint', '???????? ????')}">??</button>
   1268:                     <button id="ms-rand-toggle" class="ms-icon-btn" onclick="MediaStreamManager.toggleRandom()" title="Shuffle Mode">
   1269:                         ??
   1270:                     </button>
   1271:                     <button class="ms-icon-btn ms-close-btn" onclick="MediaStreamManager.close()" title="${t('ms_close_title')}">
   1272:                         &times;
   1273:                     </button>
   1274:                 </div>
   1275:             </div>
   1276:             <div class="ms-nav-btn ms-prev" onclick="MediaStreamManager.prev()">?</div>
   1277:             <div class="ms-nav-btn ms-next" onclick="MediaStreamManager.next()">?</div>
   1278:             <div class="ms-container" id="ms-view"></div>
   1279:             <div class="ms-info-layer" id="ms-info"></div>
   1280:         `;
   1281:         document.body.appendChild(this.overlay);
--- Line 1521 ---
   1506:             idx = Math.floor(Math.random() * this.playlist.length);
   1507:         } while (idx === this.currentIndex);
   1508:         this.render(idx, 'up');
   1509:     },
   1510:     goToThread() {
   1511:         const item = this.playlist[this.currentIndex];
   1512:         if (item) {
   1513:             if (!item.post.thread_id) {
   1514:                 if (typeof showToast === 'function') showToast(t('ms_chat_post_no_thread', "???? ???? ?? ???? (??? ?????)"));
   1515:                 return;
   1516:             }
   1517:             const url = `/${this.boardId}/res/${item.post.thread_id}.html#post-${item.post.id}`;
   1518:             window.open(url, '_blank');
   1519:         }
   1520:     },
-> 1521:     async downloadCurrentFile() {
   1522:         const item = this.playlist[this.currentIndex];
   1523:         if (!item) return;
   1524:         const btn = document.getElementById('ms-download-btn');
   1525:         if (!btn) return;
   1526:         const originalText = btn.textContent;
   1527:         btn.textContent = '?';
   1528:         btn.style.pointerEvents = 'none';
   1529:         const targetDownloadUrl = item.file.original_file_id ? `/files/${item.file.original_file_id}` : item.file.original_url;
   1530:         try {
   1531:             const response = await fetch(targetDownloadUrl);
   1532:             if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);
   1533:             showToast(t('download_started', '?????????? ????????...'), 1500);
   1534:             const blob = await response.blob();
   1535:             const mimeType = blob.type;
   1536:             let extension = '';

=== Matches for 'showCurrentFileTags' (2) ===
--- Line 1267 ---
   1252:         const mascot = document.getElementById('mascot-wrapper');
   1253:         if (mascot && SettingsManager.current.showMascot) {
   1254:             mascot.style.display = '';
   1255:         }
   1256:         closeModal();
   1257:     },
   1258:     createDOM() {
   1259:         this.overlay = document.createElement('div');
   1260:         this.overlay.className = 'media-stream-overlay';
   1261:         this.overlay.style.height = '100dvh'; 
   1262:         this.overlay.innerHTML = `
   1263:             <div class="ms-top-bar">
   1264:                 <button class="ms-thread-btn" onclick="MediaStreamManager.goToThread()">${t('go_to_thread')}</button>
   1265:                 <div class="ms-controls-right">
   1266:                     <button id="ms-download-btn" class="ms-icon-btn" onclick="MediaStreamManager.downloadCurrentFile()" title="${t('iv_download', '???????')}">?</button>
-> 1267:                     <button id="ms-tags-btn" class="ms-icon-btn" onclick="MediaStreamManager.showCurrentFileTags()" title="${t('tags_btn_hint', '???????? ????')}">??</button>
   1268:                     <button id="ms-rand-toggle" class="ms-icon-btn" onclick="MediaStreamManager.toggleRandom()" title="Shuffle Mode">
   1269:                         ??
   1270:                     </button>
   1271:                     <button class="ms-icon-btn ms-close-btn" onclick="MediaStreamManager.close()" title="${t('ms_close_title')}">
   1272:                         &times;
   1273:                     </button>
   1274:                 </div>
   1275:             </div>
   1276:             <div class="ms-nav-btn ms-prev" onclick="MediaStreamManager.prev()">?</div>
   1277:             <div class="ms-nav-btn ms-next" onclick="MediaStreamManager.next()">?</div>
   1278:             <div class="ms-container" id="ms-view"></div>
   1279:             <div class="ms-info-layer" id="ms-info"></div>
   1280:         `;
   1281:         document.body.appendChild(this.overlay);
   1282:         
--- Line 1573 ---
   1558:             a.href = objectUrl;
   1559:             a.download = finalFilename;
   1560:             document.body.appendChild(a);
   1561:             a.click();
   1562:             document.body.removeChild(a);
   1563:             URL.revokeObjectURL(objectUrl);
   1564:         } catch (err) {
   1565:             console.error('Download failed:', err);
   1566:             showToast(t('download_err', '?????? ??????????'));
   1567:             window.open(targetDownloadUrl, '_blank');
   1568:         } finally {
   1569:             btn.textContent = originalText;
   1570:             btn.style.pointerEvents = 'auto';
   1571:         }
   1572:     },
-> 1573:     showCurrentFileTags() {
   1574:         const item = this.playlist[this.currentIndex];
   1575:         if (!item || !item.file) return;
   1576:         let fileId = item.file.original_file_id;
   1577:         const targetItemUrl = item.file.original_file_id ? `/files/${item.file.original_file_id}` : item.file.original_url;
   1578:         if (!fileId && targetItemUrl) {
   1579:             try {
   1580:                 const filename = targetItemUrl.split('/').pop().split('?')[0];
   1581:                 const parts = filename.split('.');
   1582:                 if (parts.length > 1) parts.pop();
   1583:                 fileId = parts.join('.');
   1584:             } catch (e) {
   1585:                 console.error('Failed to extract file ID:', e);
   1586:             }
   1587:         }
   1588: 

=== Matches for 'preload' (43) ===
--- Line 158 ---
   143:             if (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) {
   144:                 theme = 'dark';
   145:             } else {
   146:                 theme = 'light';
   147:             }
   148:         }
   149: 
   150:         if (theme) {
   151:             document.documentElement.classList.add('theme-' + theme);
   152:             const hexColor = themeColors[theme] || themeColors['light'];
   153:             document.documentElement.style.backgroundColor = hexColor;
   154:             const loader = document.getElementById('app-loader');
   155:             if (loader) loader.style.backgroundColor = hexColor;
   156:         }
   157:     } catch (e) {
-> 158:         console.error('Theme preloader failed:', e);
   159:     }
   160: })();
   161: 
   162: window.safeInit = (name, fn) => {
   163:     try {
   164:         if (typeof fn === 'function') {
   165:             fn();
   166:             console.log(`? [Init] ${name}`);
   167:         } else {
   168:             console.warn(`?? [Init] ${name} skipped: function not found`);
   169:         }
   170:     } catch (e) {
   171:         console.error(`? [Init] ${name} failed:`, e);
   172:     }
   173: };
--- Line 824 ---
   809:                 data.content.files.forEach((f, index) => {
   810:                     const wrapper = document.createElement('label');
   811:                     wrapper.style.cssText = 'position: relative; display: inline-block; width: 60px; height: 60px; margin: 2px; cursor: pointer; border: 1px solid transparent;';
   812:                     const thumbUrl = (f.thumbnail_file_id ? `/files/${f.thumbnail_file_id}` : (f.original_file_id ? `/files/${f.original_file_id}` : (f.thumbnail_url || f.original_url)));
   813:                     const checkbox = document.createElement('input');
   814:                     checkbox.type = 'checkbox';
   815:                     checkbox.value = index;
   816:                     checkbox.style.cssText = 'position: absolute; top: 0; left: 0; opacity: 0; width: 100%; height: 100%; cursor: pointer; z-index: 2;';
   817:                     let mediaEl;
   818:                     const isVideo = f.type === 'video' || f.type === 'gif' || (thumbUrl && thumbUrl.match(/\.(mp4|webm)$/i));
   819:                     if (isVideo) {
   820:                         mediaEl = document.createElement('video');
   821:                         mediaEl.src = thumbUrl;
   822:                         mediaEl.muted = true;
   823:                         mediaEl.loop = true;
-> 824:                         mediaEl.preload = 'metadata'; 
   825:                     } else {
   826:                         mediaEl = document.createElement('img');
   827:                         mediaEl.src = thumbUrl;
   828:                     }
   829:                     mediaEl.referrerPolicy = "no-referrer";
   830:                     mediaEl.style.cssText = 'width: 100%; height: 100%; object-fit: cover; border-radius: 4px; opacity: 1; transition: all 0.2s;';
   831:                     const targetEl = mediaEl; 
   832:                     checkbox.onchange = () => {
   833:                         if (checkbox.checked) {
   834:                             targetEl.style.opacity = '0.3';
   835:                             targetEl.style.filter = 'grayscale(100%)';
   836:                             wrapper.style.borderColor = 'red';
   837:                         } else {
   838:                             targetEl.style.opacity = '1';
   839:                             targetEl.style.filter = 'none';
--- Line 1436 ---
   1421:         const isVideo = ['video', 'gif', 'animation', 'video_note'].includes(item.file.type);
   1422:         const url = (item.file.original_file_id ? `/files/${item.file.original_file_id}` : item.file.original_url);
   1423:         if (isVideo) {
   1424:             el = document.createElement('video');
   1425:             el.referrerPolicy = "no-referrer";
   1426:             el.loop = true;
   1427:             el.controls = true;
   1428:             el.onplay = () => {
   1429:                 if (typeof AudioManager !== 'undefined') AudioManager.stopOthers(el);
   1430:             };
   1431: 
   1432:             const connection = navigator.connection || navigator.mozConnection || navigator.webkitConnection;
   1433:             const saveData = connection ? connection.saveData : false;
   1434:             if (saveData) {
   1435:                 el.autoplay = false;
-> 1436:                 el.preload = 'none';
   1437:                 showToast(t('eco_mode_active', "???????? ???????: ???????? ????????"), 2000);
   1438:             } else {
   1439:                 el.autoplay = true;
   1440:                 el.playsInline = true;
   1441:             }
   1442:             el.muted = false;
   1443:             this.loadVideoWithRetry(el, item);
   1444:         } else {
   1445:             el = document.createElement('img');
   1446:             el.referrerPolicy = "no-referrer";
   1447:             el.src = url;
   1448:         }
   1449:         const isCensored = item.post.content.is_censored === true;
   1450:         const blurClass = isCensored ? ' blurred-media' : '';
   1451:         el.className = 'ms-content ' + (direction === 'up' ? 'slide-up' : 'slide-down') + blurClass;
--- Line 1461 ---
   1446:             el.referrerPolicy = "no-referrer";
   1447:             el.src = url;
   1448:         }
   1449:         const isCensored = item.post.content.is_censored === true;
   1450:         const blurClass = isCensored ? ' blurred-media' : '';
   1451:         el.className = 'ms-content ' + (direction === 'up' ? 'slide-up' : 'slide-down') + blurClass;
   1452:         container.appendChild(el);
   1453:         if (!this.isRandomMode) {
   1454:             [1, 2].forEach(offset => {
   1455:                 const nextItem = this.playlist[index + offset];
   1456:                 if (nextItem) {
   1457:                     const nextUrl = nextItem.file.original_file_id ? `/files/${nextItem.file.original_file_id}` : nextItem.file.original_url;
   1458:                     const isNextVid = ['video', 'gif', 'animation', 'video_note'].includes(nextItem.file.type);
   1459:                     if (isNextVid) {
   1460:                         const v = document.createElement('video');
-> 1461:                         v.preload = 'auto';
   1462:                         v.src = nextUrl;
   1463:                         if(typeof v.referrerPolicy !== 'undefined') v.referrerPolicy = "no-referrer";
   1464:                     } else {
   1465:                         const img = new Image();
   1466:                         if(typeof img.referrerPolicy !== 'undefined') img.referrerPolicy = "no-referrer";
   1467:                         img.src = nextUrl;
   1468:                     }
   1469:                 }
   1470:             });
   1471:         }
   1472:         const rawText = item.post.content.text || '';
   1473:         let formattedText = window.formatTextGlobal(rawText);
   1474:         const date = new Date(item.post.timestamp * 1000).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'});
   1475:         info.innerHTML = `
   1476:             <div class="ms-meta">
--- Line 4944 ---
   4929:         return list.length > 0 ? list[Math.floor(Math.random() * list.length)] : "...";
   4930:     },
   4931:     init() {
   4932:         if (window.isStaticMascotActive) return;
   4933:         if (this.initialized) return; 
   4934:         if (!SettingsManager.current.showMascot) return;
   4935:         if (document.body.classList.contains('page-roulette')) return;
   4936:         if (document.body.classList.contains('page-random-img')) return;
   4937:         if (document.querySelector('.catalog-grid')) return;
   4938:         if (location.pathname.includes('/catalog/')) return;
   4939:         this.initialized = true; 
   4940:         this.createDOM();
   4941:         if (window.SettingsManager && !window.SettingsManager.current.showMascot) {
   4942:             this.wrapper.style.display = 'none';
   4943:         }
-> 4944:         this.preloadEssentials();
   4945:         this.startPhysicsLoop();
   4946:         this.startLogicLoop();
   4947:         this.addFeedingListeners();
   4948:         this.initFormObservers();
   4949:         const events = ['mousedown', 'touchstart'];
   4950:         events.forEach(evt => this.body.addEventListener(evt, (e) => this.onDragStart(e), { passive: false }));
   4951:         const moveEvents = ['mousemove', 'touchmove'];
   4952:         moveEvents.forEach(evt => {
   4953:             const listener = (e) => {
   4954:                 this.onDragMove(e);
   4955:                 if(e.type === 'mousemove') this.onMouseMove(e);
   4956:             };
   4957:             window.addEventListener(evt, listener, { passive: false });
   4958:         });
   4959:         const endEvents = ['mouseup', 'touchend', 'touchcancel'];
--- Line 5092 ---
   5077:             document.body.appendChild(this.wrapper);
   5078:         }
   5079:         this.wrapper.style.height = 'min(350px, 25vh)';
   5080:         this.wrapper.style.width = '240px';
   5081:         this.body = document.createElement('div');
   5082:         this.body.className = 'mascot-body';
   5083:         this.bubble = document.createElement('div');
   5084:         this.bubble.className = 'mascot-bubble';
   5085:         this.imgBack = document.createElement('img');
   5086:         this.imgBack.className = 'mascot-img hidden';
   5087:         this.imgFront = document.createElement('img');
   5088:         this.imgFront.className = 'mascot-img visible';
   5089:         this.body.append(this.imgBack, this.imgFront);
   5090:         this.wrapper.append(this.bubble, this.body);
   5091:     },
-> 5092:     preloadEssentials() {
   5093:         const toLoad = [...this.db.idle, ...this.db.walk, ...this.db.drag_start];
   5094:         toLoad.forEach(src => { const i = new Image(); i.src = this.basePath + src; });
   5095:     },
   5096:     setImg(filename, instant = false) {
   5097:         if (!this.imgFront || !this.imgBack) return;
   5098:         if (!filename) return;
   5099:         if (this.isLocked && !instant) return;
   5100: 
   5101:         if (this.noMirror.includes(filename)) {
   5102:             this.forceNoMirror = true;
   5103:         } else {
   5104:             this.forceNoMirror = false;
   5105:         }
   5106: 
   5107:         const currentFile = this.imgFront.src.split('/').pop();
--- Line 9373 ---
   9358:                 contentEl.src = item.url;
   9359:                 contentEl.classList.remove('is-preview');
   9360:                 if (loader) loader.style.display = 'none';
   9361:             };
   9362:             fullImg.onerror = () => {
   9363:                 if (loader) loader.style.display = 'none';
   9364:             };
   9365:             fullImg.src = item.url;
   9366:         }
   9367:         
   9368:         contentEl.style.transform = 'translate3d(0, 0, 0) scale(1)';
   9369:         
   9370:         nextLayer.innerHTML = '';
   9371:         nextLayer.appendChild(contentEl);
   9372:         
-> 9373:         const preloadOffsets = [-1, 1, 2, 3, 4];
   9374:         preloadOffsets.forEach(offset => {
   9375:             const nIdx = idx + offset;
   9376:             if (nIdx >= 0 && nIdx < this.items.length) {
   9377:                 const neighbor = this.items[nIdx];
   9378:                 if (neighbor.type === 'video') {
   9379:                     const v = document.createElement('video');
   9380:                     v.preload = 'auto';
   9381:                     v.muted = true;
   9382:                     v.src = neighbor.url;
   9383:                 } else {
   9384:                     const img = new Image();
   9385:                     img.src = neighbor.url;
   9386:                 }
   9387:             }
   9388:         });
--- Line 9374 ---
   9359:                 contentEl.classList.remove('is-preview');
   9360:                 if (loader) loader.style.display = 'none';
   9361:             };
   9362:             fullImg.onerror = () => {
   9363:                 if (loader) loader.style.display = 'none';
   9364:             };
   9365:             fullImg.src = item.url;
   9366:         }
   9367:         
   9368:         contentEl.style.transform = 'translate3d(0, 0, 0) scale(1)';
   9369:         
   9370:         nextLayer.innerHTML = '';
   9371:         nextLayer.appendChild(contentEl);
   9372:         
   9373:         const preloadOffsets = [-1, 1, 2, 3, 4];
-> 9374:         preloadOffsets.forEach(offset => {
   9375:             const nIdx = idx + offset;
   9376:             if (nIdx >= 0 && nIdx < this.items.length) {
   9377:                 const neighbor = this.items[nIdx];
   9378:                 if (neighbor.type === 'video') {
   9379:                     const v = document.createElement('video');
   9380:                     v.preload = 'auto';
   9381:                     v.muted = true;
   9382:                     v.src = neighbor.url;
   9383:                 } else {
   9384:                     const img = new Image();
   9385:                     img.src = neighbor.url;
   9386:                 }
   9387:             }
   9388:         });
   9389:     },
--- Line 9380 ---
   9365:             fullImg.src = item.url;
   9366:         }
   9367:         
   9368:         contentEl.style.transform = 'translate3d(0, 0, 0) scale(1)';
   9369:         
   9370:         nextLayer.innerHTML = '';
   9371:         nextLayer.appendChild(contentEl);
   9372:         
   9373:         const preloadOffsets = [-1, 1, 2, 3, 4];
   9374:         preloadOffsets.forEach(offset => {
   9375:             const nIdx = idx + offset;
   9376:             if (nIdx >= 0 && nIdx < this.items.length) {
   9377:                 const neighbor = this.items[nIdx];
   9378:                 if (neighbor.type === 'video') {
   9379:                     const v = document.createElement('video');
-> 9380:                     v.preload = 'auto';
   9381:                     v.muted = true;
   9382:                     v.src = neighbor.url;
   9383:                 } else {
   9384:                     const img = new Image();
   9385:                     img.src = neighbor.url;
   9386:                 }
   9387:             }
   9388:         });
   9389:     },
   9390:     cleanupLayer(layer) {
   9391:         const media = layer.querySelectorAll('video, audio, img');
   9392:         media.forEach(el => {
   9393:             if (el.tagName === 'VIDEO' || el.tagName === 'AUDIO') {
   9394:                 el.pause();
   9395:                 el.removeAttribute('src');
--- Line 9397 ---
   9382:                     v.src = neighbor.url;
   9383:                 } else {
   9384:                     const img = new Image();
   9385:                     img.src = neighbor.url;
   9386:                 }
   9387:             }
   9388:         });
   9389:     },
   9390:     cleanupLayer(layer) {
   9391:         const media = layer.querySelectorAll('video, audio, img');
   9392:         media.forEach(el => {
   9393:             if (el.tagName === 'VIDEO' || el.tagName === 'AUDIO') {
   9394:                 el.pause();
   9395:                 el.removeAttribute('src');
   9396:                 el.removeAttribute('poster');
-> 9397:                 el.preload = 'none';
   9398:                 el.load();
   9399:             }
   9400:             el.src = "";
   9401:             el.remove();
   9402:         });
   9403: 
   9404:         layer.innerHTML = '';
   9405:     },
   9406:     performCrossfade(oldLayer, newLayer) {
   9407:         oldLayer.classList.remove('active');
   9408:         newLayer.classList.add('active');
   9409:         
   9410:         this.activeLayer = 1 - this.activeLayer;
   9411:         
   9412:         setTimeout(() => {
--- Line 10130 ---
   10115:         });
   10116:         scope.querySelectorAll('.custom-audio-player').forEach(player => {
   10117:             this.initPlayer(player);
   10118:         });
   10119:     },
   10120:     createPlayer(oldEl, src, name) {
   10121:         const safeName = (name || t('audio_def_name', 'Audio'))
   10122:             .replace(/<[^>]*>?/gm, '')
   10123:             .replace(/"/g, '&quot;');
   10124:             
   10125:         const info = this.parseAudioName(safeName);
   10126:         const uid = 'aud-' + Math.random().toString(36).substr(2, 9);
   10127:         const div = document.createElement('div');
   10128:         div.className = 'custom-audio-player';
   10129:         div.innerHTML = `
-> 10130:             <audio id="${uid}" preload="metadata" playsinline webkit-playsinline><source src="${src}"></audio>
   10131:             <button class="cap-play-btn" data-target="${uid}" type="button">?</button>
   10132:             <div class="cap-body">
   10133:                 <div class="cap-title" title="${safeName}">
   10134:                     ${info.artist ? `<span class="cap-artist">${info.artist}</span>` : ''}
   10135:                     <span class="cap-track">${info.track}</span>
   10136:                 </div>
   10137:                 <div class="cap-controls-row">
   10138:                     <div class="cap-info"><span class="cap-time">00:00</span></div>
   10139:                     <div class="cap-progress-container" data-target="${uid}"><div class="cap-wave-base"></div><div class="cap-wave-progress"><div class="cap-wave-base cap-wave-played"></div></div></div>
   10140:                     <div class="cap-info"><button class="cap-speed-btn" data-target="${uid}" type="button">1x</button></div>
   10141:                     <a href="${src}" download="${safeName}" class="cap-download-btn" title="${t('download_title', '???????')}">?</a>
   10142:                 </div>
   10143:             </div>`;
   10144:         
   10145:         if (oldEl && oldEl.parentNode) {
--- Line 11052 ---
   11037:                 // Moved up to be available for video_note as well
   11038:                 let blurHtml = '';
   11039:                 if (f.blurhash) {
   11040:                     blurHtml = `<canvas class="blurhash-canvas" data-hash="${esc(f.blurhash)}" width="32" height="32"></canvas>`;
   11041:                 }
   11042:                 let bayanHtml = '';
   11043:                 const dCount = parseInt(f.dupe_count || 0);
   11044:                 if (dCount > 1) {
   11045:                     bayanHtml = `<div class="bayan-text" title="???? ???? ??? ?????????? ${dCount} ???">?? ${t('label_duplicate', '??????')} (${dCount})</div>`;
   11046:                 }
   11047: 
   11048:                 if (['audio', 'voice'].includes(f.type)) {
   11049:                      const uniqueId = `audio-${data.id}-${Math.random().toString(36).substr(2, 5)}`;
   11050:                      const audioInfo = AudioManager.parseAudioName(filename);
   11051:                      audContent += `<div class="custom-audio-player" id="player-${uniqueId}" data-src="${safeUrl}">
-> 11052:                         <audio id="${uniqueId}" preload="none"><source src="${safeUrl}" type="${esc(f.mime_type) || 'audio/mpeg'}"></audio>
   11053:                         <button class="cap-play-btn" data-target="${uniqueId}" type="button">?</button>
   11054:                         <div class="cap-body">
   11055:                             <div class="cap-title" title="${filename}">
   11056:                                 ${audioInfo.artist ? `<span class="cap-artist">${audioInfo.artist}</span>` : ''}
   11057:                                 <span class="cap-track">${audioInfo.track}</span>
   11058:                             </div>
   11059:                             <div class="cap-controls-row">
   11060:                                 <div class="cap-info"><span class="cap-time">00:00</span></div>
   11061:                                 <div class="cap-progress-container" data-target="${uniqueId}"><div class="cap-wave-base"></div><div class="cap-wave-progress"><div class="cap-wave-base cap-wave-played"></div></div></div>
   11062:                                 <div class="cap-info"><button class="cap-speed-btn" data-target="${uniqueId}" type="button">1x</button></div>
   11063:                                 <a href="${safeUrl}" download="${filename}" class="cap-download-btn" title="${t('download_title', '???????')}">?</a>
   11064:                             </div>
   11065:                         </div>
   11066:                     </div>`;
   11067:                 }
--- Line 11090 ---
   11075:                             <a href="${safeUrl}" class="gallery-trigger" data-type="${isWebm ? 'video' : 'image'}" data-filename="${filename}" ${fileIdAttr}>
   11076:                                 ${isWebm 
   11077:                                     ? `<video class="post-sticker${blurClass}" src="${safeUrl}" autoplay loop muted playsinline webkit-playsinline style="max-height: 150px; width: auto; background: transparent; display: block;" referrerpolicy="no-referrer"></video>`
   11078:                                     : `<img src="${placeholder}" data-src="${safeThumb || safeUrl}" class="post-sticker lazy-load${blurClass}" style="max-height: 150px;" onclick="event.stopPropagation();" referrerpolicy="no-referrer">`
   11079:                                 }
   11080:                             </a>
   11081:                         </div>`;
   11082:                     }
   11083:                     else if (f.type === 'video_note') {
   11084:                         if (type === 'chat') {
   11085:                             imgContent += `<video class="post-video video-note lazy-load${blurClass}" data-src="${safeUrl}" ${safeThumb ? `poster="${safeThumb}"` : ''} autoplay loop muted playsinline onclick="event.preventDefault(); event.stopPropagation(); this.paused ? this.play() : this.pause();" style="border-radius: 50%; object-fit: cover;" referrerpolicy="no-referrer"></video>`;
   11086:                         } else {
   11087:                             const badgeType = 'VIDEO'; 
   11088:                             imgContent += `<div class="file-thumb lazy-media-wrapper" data-src="${safeUrl}" data-filename="${filename}" data-type="video">
   11089:                                 ${blurHtml || ''}
-> 11090:                                 <video class="post-image lazy-load${blurClass}" data-src="${safeUrl}" ${safeThumb ? `poster="${safeThumb}"` : ''} preload="metadata" muted playsinline loop style="object-fit: cover; width: 100%; height: 100%; background: #000;"></video>
   11091:                                 <span class="lazy-badge">${badgeType}</span>
   11092:                                 ${bayanHtml || ''}
   11093:                             </div>`;
   11094:                         }
   11095:                     }
   11096:                     else if (['image', 'photo', 'video', 'gif', 'animation', 'video_note'].includes(f.type)) {
   11097:                         const isVid = ['video', 'gif', 'animation', 'video_note'].includes(f.type);
   11098:                         
   11099:                         if (isVid) {
   11100:                             const badgeType = (f.type === 'gif' || f.type === 'animation') ? 'GIF' : 'VIDEO';
   11101:                             imgContent += `<div class="file-thumb lazy-media-wrapper" data-src="${safeUrl}" data-filename="${filename}" data-type="${f.type === 'gif' ? 'gif' : 'video'}">
   11102:                                 ${blurHtml}
   11103:                                 <video class="post-image lazy-load${blurClass}" data-src="${safeUrl}" ${safeThumb ? `poster="${safeThumb}"` : ''} preload="metadata" muted playsinline loop style="object-fit: cover; width: 100%; height: 100%; background: #000;"></video>
   11104:                                 <span class="lazy-badge">${badgeType}</span>
   11105:                                 ${bayanHtml}
--- Line 11103 ---
   11088:                             imgContent += `<div class="file-thumb lazy-media-wrapper" data-src="${safeUrl}" data-filename="${filename}" data-type="video">
   11089:                                 ${blurHtml || ''}
   11090:                                 <video class="post-image lazy-load${blurClass}" data-src="${safeUrl}" ${safeThumb ? `poster="${safeThumb}"` : ''} preload="metadata" muted playsinline loop style="object-fit: cover; width: 100%; height: 100%; background: #000;"></video>
   11091:                                 <span class="lazy-badge">${badgeType}</span>
   11092:                                 ${bayanHtml || ''}
   11093:                             </div>`;
   11094:                         }
   11095:                     }
   11096:                     else if (['image', 'photo', 'video', 'gif', 'animation', 'video_note'].includes(f.type)) {
   11097:                         const isVid = ['video', 'gif', 'animation', 'video_note'].includes(f.type);
   11098:                         
   11099:                         if (isVid) {
   11100:                             const badgeType = (f.type === 'gif' || f.type === 'animation') ? 'GIF' : 'VIDEO';
   11101:                             imgContent += `<div class="file-thumb lazy-media-wrapper" data-src="${safeUrl}" data-filename="${filename}" data-type="${f.type === 'gif' ? 'gif' : 'video'}">
   11102:                                 ${blurHtml}
-> 11103:                                 <video class="post-image lazy-load${blurClass}" data-src="${safeUrl}" ${safeThumb ? `poster="${safeThumb}"` : ''} preload="metadata" muted playsinline loop style="object-fit: cover; width: 100%; height: 100%; background: #000;"></video>
   11104:                                 <span class="lazy-badge">${badgeType}</span>
   11105:                                 ${bayanHtml}
   11106:                             </div>`;
   11107:                         } else {
   11108:                             imgContent += `<a href="${safeUrl}" class="file-thumb" data-filename="${filename}" data-src="${safeUrl}" data-type="image" ${fileIdAttr}>
   11109:                                 ${blurHtml}
   11110:                                 <img src="${placeholder}" data-src="${safeThumb || safeUrl}" class="post-image lazy-load${blurClass}" alt="img" referrerpolicy="no-referrer" onload="this.classList.add('loaded')">
   11111:                                 ${bayanHtml}
   11112:                             </a>`;
   11113:                         }
   11114:                     }
   11115:                 }
   11116:             });
   11117:             if (imgContent) imagesHtml = `<div class="post-files-container post-gallery">${imgContent}</div>`;
   11118:             if (audContent) audiosHtml = `<div class="post-audios-container">${audContent}</div>`;
--- Line 11261 ---
   11246:         const files = data.content.files || [];
   11247:         if (files.length > 0) {
   11248:             const f = files[0];
   11249:             const isVid = ['video', 'gif', 'animation', 'video_note'].includes(f.type);
   11250:             const mediaUrl = (f.original_file_id ? `/files/${f.original_file_id}` : f.original_url) || "";
   11251:             const thumbUrl = (f.thumbnail_file_id ? `/files/${f.thumbnail_file_id}` : (f.original_file_id ? `/files/${f.original_file_id}` : (f.thumbnail_url || f.original_url))) || "";
   11252:             
   11253:             if (typeof FailedMediaCache !== 'undefined' && ((mediaUrl && FailedMediaCache.isFailed(mediaUrl)) || (thumbUrl && FailedMediaCache.isFailed(thumbUrl)))) {
   11254:                 thumbHtml = `<div class="catalog-thumb broken-media" style="background-color: #1e1e1e; display:flex; align-items:center; justify-content:center;"><span style="font-size:2em">??</span></div>`;
   11255:             } else if (isVid) {
   11256:                 const vidUrl = mediaUrl;
   11257:                 const posterUrl = thumbUrl || mediaUrl;
   11258:                 if (vidUrl) {
   11259:                     thumbHtml = `
   11260:                         <div class="catalog-thumb lazy-media-wrapper" data-src="${vidUrl}" data-file-id="${f.original_file_id || ''}" data-type="video" style="background-color: #000;">
-> 11261:                             <video class="lazy-load${blurClass}" preload="metadata" muted playsinline loop data-src="${vidUrl}" ${posterUrl ? `poster="${posterUrl}"` : ''} style="width: 100%; height: 100%; object-fit: cover;"></video>
   11262:                             <span class="lazy-badge" style="position:absolute; bottom:5px; right:5px; background:rgba(0,0,0,0.6); color:white; padding:2px 4px; font-size:10px; border-radius:3px;">VIDEO</span>
   11263:                         </div>`;
   11264:                 } else {
   11265:                      thumbHtml = `<div class="catalog-thumb" style="background-color: ${bgColor}; display:flex; align-items:center; justify-content:center;"><span style="font-size:2em">?</span></div>`;
   11266:                 }
   11267:             } else {
   11268:                 const imgUrl = thumbUrl || mediaUrl;
   11269:                 if (imgUrl) {
   11270:                     thumbHtml = `<div class="catalog-thumb"><img src="data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7" data-src="${imgUrl}" class="lazy-load${blurClass}" style="width: 100%; height: 100%; object-fit: cover;" referrerpolicy="no-referrer"></div>`;
   11271:                 } else {
   11272:                     thumbHtml = `<div class="catalog-thumb" style="background-color: ${bgColor}; display:flex; align-items:center; justify-content:center;"><span style="font-size:2em">??</span></div>`;
   11273:                 }
   11274:             }
   11275:         } else {
   11276:             thumbHtml = `<div class="catalog-thumb" style="background-color: ${bgColor};"><div class="catalog-ambient"><span>?</span><small>${esc(data.content.text).substring(0, 40)}</small></div></div>`;
--- Line 12238 ---
   12223:         if (!this.loader) return;
   12224:         if (show) {
   12225:             this.loader.style.display = 'block';
   12226:             this.video.style.opacity = '0.5';
   12227:         } else {
   12228:             this.loader.style.display = 'none';
   12229:             this.video.style.opacity = '1';
   12230:         }
   12231:     },
   12232:     async goNext() {
   12233:         if (this.isLoading) return;
   12234:         if (this.currentIndex < this.historyStack.length - 1) {
   12235:             this.currentIndex++;
   12236:             this.playVideo(this.historyStack[this.currentIndex]);
   12237:             this.updateButtons();
-> 12238:             this.preloadNext(); 
   12239:             return;
   12240:         }
   12241:         this.isLoading = true;
   12242:         this.toggleLoader(true);
   12243:         this.video.pause();
   12244:         
   12245:         let data;
   12246:         if (!this.preloadBuffer) this.preloadBuffer = [];
   12247:         
   12248:         if (this.nextVideoData) {
   12249:             this.preloadBuffer.unshift(this.nextVideoData);
   12250:             this.nextVideoData = null;
   12251:         }
   12252: 
   12253:         if (this.preloadBuffer.length > 0) {
--- Line 12246 ---
   12231:     },
   12232:     async goNext() {
   12233:         if (this.isLoading) return;
   12234:         if (this.currentIndex < this.historyStack.length - 1) {
   12235:             this.currentIndex++;
   12236:             this.playVideo(this.historyStack[this.currentIndex]);
   12237:             this.updateButtons();
   12238:             this.preloadNext(); 
   12239:             return;
   12240:         }
   12241:         this.isLoading = true;
   12242:         this.toggleLoader(true);
   12243:         this.video.pause();
   12244:         
   12245:         let data;
-> 12246:         if (!this.preloadBuffer) this.preloadBuffer = [];
   12247:         
   12248:         if (this.nextVideoData) {
   12249:             this.preloadBuffer.unshift(this.nextVideoData);
   12250:             this.nextVideoData = null;
   12251:         }
   12252: 
   12253:         if (this.preloadBuffer.length > 0) {
   12254:             data = this.preloadBuffer.shift();
   12255:         } else {
   12256:             data = await this.loadVideoData();
   12257:         }
   12258: 
   12259:         this.isLoading = false;
   12260:         if (data) {
   12261:             this.historyStack.push(data);
--- Line 12249 ---
   12234:         if (this.currentIndex < this.historyStack.length - 1) {
   12235:             this.currentIndex++;
   12236:             this.playVideo(this.historyStack[this.currentIndex]);
   12237:             this.updateButtons();
   12238:             this.preloadNext(); 
   12239:             return;
   12240:         }
   12241:         this.isLoading = true;
   12242:         this.toggleLoader(true);
   12243:         this.video.pause();
   12244:         
   12245:         let data;
   12246:         if (!this.preloadBuffer) this.preloadBuffer = [];
   12247:         
   12248:         if (this.nextVideoData) {
-> 12249:             this.preloadBuffer.unshift(this.nextVideoData);
   12250:             this.nextVideoData = null;
   12251:         }
   12252: 
   12253:         if (this.preloadBuffer.length > 0) {
   12254:             data = this.preloadBuffer.shift();
   12255:         } else {
   12256:             data = await this.loadVideoData();
   12257:         }
   12258: 
   12259:         this.isLoading = false;
   12260:         if (data) {
   12261:             this.historyStack.push(data);
   12262:             this.currentIndex++;
   12263:             this.playVideo(data);
   12264:             this.updateButtons();
--- Line 12253 ---
   12238:             this.preloadNext(); 
   12239:             return;
   12240:         }
   12241:         this.isLoading = true;
   12242:         this.toggleLoader(true);
   12243:         this.video.pause();
   12244:         
   12245:         let data;
   12246:         if (!this.preloadBuffer) this.preloadBuffer = [];
   12247:         
   12248:         if (this.nextVideoData) {
   12249:             this.preloadBuffer.unshift(this.nextVideoData);
   12250:             this.nextVideoData = null;
   12251:         }
   12252: 
-> 12253:         if (this.preloadBuffer.length > 0) {
   12254:             data = this.preloadBuffer.shift();
   12255:         } else {
   12256:             data = await this.loadVideoData();
   12257:         }
   12258: 
   12259:         this.isLoading = false;
   12260:         if (data) {
   12261:             this.historyStack.push(data);
   12262:             this.currentIndex++;
   12263:             this.playVideo(data);
   12264:             this.updateButtons();
   12265:             this.preloadNext();
   12266:         } else {
   12267:             setTimeout(() => this.goNext(), 2000);
   12268:         }
--- Line 12254 ---
   12239:             return;
   12240:         }
   12241:         this.isLoading = true;
   12242:         this.toggleLoader(true);
   12243:         this.video.pause();
   12244:         
   12245:         let data;
   12246:         if (!this.preloadBuffer) this.preloadBuffer = [];
   12247:         
   12248:         if (this.nextVideoData) {
   12249:             this.preloadBuffer.unshift(this.nextVideoData);
   12250:             this.nextVideoData = null;
   12251:         }
   12252: 
   12253:         if (this.preloadBuffer.length > 0) {
-> 12254:             data = this.preloadBuffer.shift();
   12255:         } else {
   12256:             data = await this.loadVideoData();
   12257:         }
   12258: 
   12259:         this.isLoading = false;
   12260:         if (data) {
   12261:             this.historyStack.push(data);
   12262:             this.currentIndex++;
   12263:             this.playVideo(data);
   12264:             this.updateButtons();
   12265:             this.preloadNext();
   12266:         } else {
   12267:             setTimeout(() => this.goNext(), 2000);
   12268:         }
   12269:     },
--- Line 12265 ---
   12250:             this.nextVideoData = null;
   12251:         }
   12252: 
   12253:         if (this.preloadBuffer.length > 0) {
   12254:             data = this.preloadBuffer.shift();
   12255:         } else {
   12256:             data = await this.loadVideoData();
   12257:         }
   12258: 
   12259:         this.isLoading = false;
   12260:         if (data) {
   12261:             this.historyStack.push(data);
   12262:             this.currentIndex++;
   12263:             this.playVideo(data);
   12264:             this.updateButtons();
-> 12265:             this.preloadNext();
   12266:         } else {
   12267:             setTimeout(() => this.goNext(), 2000);
   12268:         }
   12269:     },
   12270:     goPrev() {
   12271:         if (this.currentIndex > 0) {
   12272:             this.currentIndex--;
   12273:             this.playVideo(this.historyStack[this.currentIndex]);
   12274:             this.updateButtons();
   12275:         }
   12276:     },
   12277:     updateButtons() {
   12278:         if (!this.prevBtn) return;
   12279:         if (this.currentIndex > 0) {
   12280:             this.prevBtn.style.opacity = '1';
--- Line 12310 ---
   12295:                     const boards = JSON.parse(saved).join(',');
   12296:                     if (boards) url += `?boards=${boards}`;
   12297:                 }
   12298:             } catch (e) { console.warn('Filter read error', e); }
   12299:             
   12300:             console.log('[Roulette] Loading:', url);
   12301:             
   12302:             const res = await fetch(url);
   12303:             if (!res.ok) throw new Error();
   12304:             return await res.json();
   12305:         } catch (e) {
   12306:             console.error("Failed to load video metadata");
   12307:             return null;
   12308:         }
   12309:     },
-> 12310:     async preloadNext() {
   12311:         if (!this.preloadBuffer) this.preloadBuffer = [];
   12312:         if (this.isPreloading) return;
   12313:         this.isPreloading = true;
   12314:         try {
   12315:             while (this.preloadBuffer.length < 3) {
   12316:                 const data = await this.loadVideoData();
   12317:                 if (data) {
   12318:                     const preloader = document.createElement('video');
   12319:                     preloader.preload = 'auto';
   12320:                     preloader.src = data.src;
   12321:                     preloader.muted = true;
   12322:                     preloader.load(); 
   12323:                     data._preloaderElement = preloader;
   12324:                     if (data.poster) new Image().src = data.poster;
   12325:                     this.preloadBuffer.push(data);
--- Line 12311 ---
   12296:                     if (boards) url += `?boards=${boards}`;
   12297:                 }
   12298:             } catch (e) { console.warn('Filter read error', e); }
   12299:             
   12300:             console.log('[Roulette] Loading:', url);
   12301:             
   12302:             const res = await fetch(url);
   12303:             if (!res.ok) throw new Error();
   12304:             return await res.json();
   12305:         } catch (e) {
   12306:             console.error("Failed to load video metadata");
   12307:             return null;
   12308:         }
   12309:     },
   12310:     async preloadNext() {
-> 12311:         if (!this.preloadBuffer) this.preloadBuffer = [];
   12312:         if (this.isPreloading) return;
   12313:         this.isPreloading = true;
   12314:         try {
   12315:             while (this.preloadBuffer.length < 3) {
   12316:                 const data = await this.loadVideoData();
   12317:                 if (data) {
   12318:                     const preloader = document.createElement('video');
   12319:                     preloader.preload = 'auto';
   12320:                     preloader.src = data.src;
   12321:                     preloader.muted = true;
   12322:                     preloader.load(); 
   12323:                     data._preloaderElement = preloader;
   12324:                     if (data.poster) new Image().src = data.poster;
   12325:                     this.preloadBuffer.push(data);
   12326:                 } else break;
--- Line 12315 ---
   12300:             console.log('[Roulette] Loading:', url);
   12301:             
   12302:             const res = await fetch(url);
   12303:             if (!res.ok) throw new Error();
   12304:             return await res.json();
   12305:         } catch (e) {
   12306:             console.error("Failed to load video metadata");
   12307:             return null;
   12308:         }
   12309:     },
   12310:     async preloadNext() {
   12311:         if (!this.preloadBuffer) this.preloadBuffer = [];
   12312:         if (this.isPreloading) return;
   12313:         this.isPreloading = true;
   12314:         try {
-> 12315:             while (this.preloadBuffer.length < 3) {
   12316:                 const data = await this.loadVideoData();
   12317:                 if (data) {
   12318:                     const preloader = document.createElement('video');
   12319:                     preloader.preload = 'auto';
   12320:                     preloader.src = data.src;
   12321:                     preloader.muted = true;
   12322:                     preloader.load(); 
   12323:                     data._preloaderElement = preloader;
   12324:                     if (data.poster) new Image().src = data.poster;
   12325:                     this.preloadBuffer.push(data);
   12326:                 } else break;
   12327:             }
   12328:         } catch(e) {} finally { this.isPreloading = false; }
   12329:     },
   12330:     playVideo(data) {
--- Line 12318 ---
   12303:             if (!res.ok) throw new Error();
   12304:             return await res.json();
   12305:         } catch (e) {
   12306:             console.error("Failed to load video metadata");
   12307:             return null;
   12308:         }
   12309:     },
   12310:     async preloadNext() {
   12311:         if (!this.preloadBuffer) this.preloadBuffer = [];
   12312:         if (this.isPreloading) return;
   12313:         this.isPreloading = true;
   12314:         try {
   12315:             while (this.preloadBuffer.length < 3) {
   12316:                 const data = await this.loadVideoData();
   12317:                 if (data) {
-> 12318:                     const preloader = document.createElement('video');
   12319:                     preloader.preload = 'auto';
   12320:                     preloader.src = data.src;
   12321:                     preloader.muted = true;
   12322:                     preloader.load(); 
   12323:                     data._preloaderElement = preloader;
   12324:                     if (data.poster) new Image().src = data.poster;
   12325:                     this.preloadBuffer.push(data);
   12326:                 } else break;
   12327:             }
   12328:         } catch(e) {} finally { this.isPreloading = false; }
   12329:     },
   12330:     playVideo(data) {
   12331:         if (this.currentBlobUrl) {
   12332:             URL.revokeObjectURL(this.currentBlobUrl);
   12333:             this.currentBlobUrl = null;
--- Line 12319 ---
   12304:             return await res.json();
   12305:         } catch (e) {
   12306:             console.error("Failed to load video metadata");
   12307:             return null;
   12308:         }
   12309:     },
   12310:     async preloadNext() {
   12311:         if (!this.preloadBuffer) this.preloadBuffer = [];
   12312:         if (this.isPreloading) return;
   12313:         this.isPreloading = true;
   12314:         try {
   12315:             while (this.preloadBuffer.length < 3) {
   12316:                 const data = await this.loadVideoData();
   12317:                 if (data) {
   12318:                     const preloader = document.createElement('video');
-> 12319:                     preloader.preload = 'auto';
   12320:                     preloader.src = data.src;
   12321:                     preloader.muted = true;
   12322:                     preloader.load(); 
   12323:                     data._preloaderElement = preloader;
   12324:                     if (data.poster) new Image().src = data.poster;
   12325:                     this.preloadBuffer.push(data);
   12326:                 } else break;
   12327:             }
   12328:         } catch(e) {} finally { this.isPreloading = false; }
   12329:     },
   12330:     playVideo(data) {
   12331:         if (this.currentBlobUrl) {
   12332:             URL.revokeObjectURL(this.currentBlobUrl);
   12333:             this.currentBlobUrl = null;
   12334:         }
--- Line 12320 ---
   12305:         } catch (e) {
   12306:             console.error("Failed to load video metadata");
   12307:             return null;
   12308:         }
   12309:     },
   12310:     async preloadNext() {
   12311:         if (!this.preloadBuffer) this.preloadBuffer = [];
   12312:         if (this.isPreloading) return;
   12313:         this.isPreloading = true;
   12314:         try {
   12315:             while (this.preloadBuffer.length < 3) {
   12316:                 const data = await this.loadVideoData();
   12317:                 if (data) {
   12318:                     const preloader = document.createElement('video');
   12319:                     preloader.preload = 'auto';
-> 12320:                     preloader.src = data.src;
   12321:                     preloader.muted = true;
   12322:                     preloader.load(); 
   12323:                     data._preloaderElement = preloader;
   12324:                     if (data.poster) new Image().src = data.poster;
   12325:                     this.preloadBuffer.push(data);
   12326:                 } else break;
   12327:             }
   12328:         } catch(e) {} finally { this.isPreloading = false; }
   12329:     },
   12330:     playVideo(data) {
   12331:         if (this.currentBlobUrl) {
   12332:             URL.revokeObjectURL(this.currentBlobUrl);
   12333:             this.currentBlobUrl = null;
   12334:         }
   12335:         if (this.currentPreloader) {
--- Line 12321 ---
   12306:             console.error("Failed to load video metadata");
   12307:             return null;
   12308:         }
   12309:     },
   12310:     async preloadNext() {
   12311:         if (!this.preloadBuffer) this.preloadBuffer = [];
   12312:         if (this.isPreloading) return;
   12313:         this.isPreloading = true;
   12314:         try {
   12315:             while (this.preloadBuffer.length < 3) {
   12316:                 const data = await this.loadVideoData();
   12317:                 if (data) {
   12318:                     const preloader = document.createElement('video');
   12319:                     preloader.preload = 'auto';
   12320:                     preloader.src = data.src;
-> 12321:                     preloader.muted = true;
   12322:                     preloader.load(); 
   12323:                     data._preloaderElement = preloader;
   12324:                     if (data.poster) new Image().src = data.poster;
   12325:                     this.preloadBuffer.push(data);
   12326:                 } else break;
   12327:             }
   12328:         } catch(e) {} finally { this.isPreloading = false; }
   12329:     },
   12330:     playVideo(data) {
   12331:         if (this.currentBlobUrl) {
   12332:             URL.revokeObjectURL(this.currentBlobUrl);
   12333:             this.currentBlobUrl = null;
   12334:         }
   12335:         if (this.currentPreloader) {
   12336:             this.currentPreloader.src = "";
--- Line 12322 ---
   12307:             return null;
   12308:         }
   12309:     },
   12310:     async preloadNext() {
   12311:         if (!this.preloadBuffer) this.preloadBuffer = [];
   12312:         if (this.isPreloading) return;
   12313:         this.isPreloading = true;
   12314:         try {
   12315:             while (this.preloadBuffer.length < 3) {
   12316:                 const data = await this.loadVideoData();
   12317:                 if (data) {
   12318:                     const preloader = document.createElement('video');
   12319:                     preloader.preload = 'auto';
   12320:                     preloader.src = data.src;
   12321:                     preloader.muted = true;
-> 12322:                     preloader.load(); 
   12323:                     data._preloaderElement = preloader;
   12324:                     if (data.poster) new Image().src = data.poster;
   12325:                     this.preloadBuffer.push(data);
   12326:                 } else break;
   12327:             }
   12328:         } catch(e) {} finally { this.isPreloading = false; }
   12329:     },
   12330:     playVideo(data) {
   12331:         if (this.currentBlobUrl) {
   12332:             URL.revokeObjectURL(this.currentBlobUrl);
   12333:             this.currentBlobUrl = null;
   12334:         }
   12335:         if (this.currentPreloader) {
   12336:             this.currentPreloader.src = "";
   12337:             this.currentPreloader.load();
--- Line 12323 ---
   12308:         }
   12309:     },
   12310:     async preloadNext() {
   12311:         if (!this.preloadBuffer) this.preloadBuffer = [];
   12312:         if (this.isPreloading) return;
   12313:         this.isPreloading = true;
   12314:         try {
   12315:             while (this.preloadBuffer.length < 3) {
   12316:                 const data = await this.loadVideoData();
   12317:                 if (data) {
   12318:                     const preloader = document.createElement('video');
   12319:                     preloader.preload = 'auto';
   12320:                     preloader.src = data.src;
   12321:                     preloader.muted = true;
   12322:                     preloader.load(); 
-> 12323:                     data._preloaderElement = preloader;
   12324:                     if (data.poster) new Image().src = data.poster;
   12325:                     this.preloadBuffer.push(data);
   12326:                 } else break;
   12327:             }
   12328:         } catch(e) {} finally { this.isPreloading = false; }
   12329:     },
   12330:     playVideo(data) {
   12331:         if (this.currentBlobUrl) {
   12332:             URL.revokeObjectURL(this.currentBlobUrl);
   12333:             this.currentBlobUrl = null;
   12334:         }
   12335:         if (this.currentPreloader) {
   12336:             this.currentPreloader.src = "";
   12337:             this.currentPreloader.load();
   12338:             this.currentPreloader = null;
--- Line 12325 ---
   12310:     async preloadNext() {
   12311:         if (!this.preloadBuffer) this.preloadBuffer = [];
   12312:         if (this.isPreloading) return;
   12313:         this.isPreloading = true;
   12314:         try {
   12315:             while (this.preloadBuffer.length < 3) {
   12316:                 const data = await this.loadVideoData();
   12317:                 if (data) {
   12318:                     const preloader = document.createElement('video');
   12319:                     preloader.preload = 'auto';
   12320:                     preloader.src = data.src;
   12321:                     preloader.muted = true;
   12322:                     preloader.load(); 
   12323:                     data._preloaderElement = preloader;
   12324:                     if (data.poster) new Image().src = data.poster;
-> 12325:                     this.preloadBuffer.push(data);
   12326:                 } else break;
   12327:             }
   12328:         } catch(e) {} finally { this.isPreloading = false; }
   12329:     },
   12330:     playVideo(data) {
   12331:         if (this.currentBlobUrl) {
   12332:             URL.revokeObjectURL(this.currentBlobUrl);
   12333:             this.currentBlobUrl = null;
   12334:         }
   12335:         if (this.currentPreloader) {
   12336:             this.currentPreloader.src = "";
   12337:             this.currentPreloader.load();
   12338:             this.currentPreloader = null;
   12339:         }
   12340:         if (data._preloaderElement) {
--- Line 12340 ---
   12325:                     this.preloadBuffer.push(data);
   12326:                 } else break;
   12327:             }
   12328:         } catch(e) {} finally { this.isPreloading = false; }
   12329:     },
   12330:     playVideo(data) {
   12331:         if (this.currentBlobUrl) {
   12332:             URL.revokeObjectURL(this.currentBlobUrl);
   12333:             this.currentBlobUrl = null;
   12334:         }
   12335:         if (this.currentPreloader) {
   12336:             this.currentPreloader.src = "";
   12337:             this.currentPreloader.load();
   12338:             this.currentPreloader = null;
   12339:         }
-> 12340:         if (data._preloaderElement) {
   12341:             this.currentPreloader = data._preloaderElement;
   12342:             delete data._preloaderElement; 
   12343:         }
   12344:         this.video.classList.remove('blurred-media', 'revealed');
   12345:         if (data.is_censored) {
   12346:             this.video.classList.add('blurred-media');
   12347:         }
   12348:         this.video.src = data.src;
   12349:         this.video.poster = data.poster || '';
   12350:         this.video.muted = this.userMuted;
   12351:         const textEl = document.getElementById('r-text');
   12352:         if (textEl) textEl.innerHTML = window.formatTextGlobal(data.text);
   12353:         const metaEl = document.getElementById('r-meta');
   12354:         if (metaEl) metaEl.textContent = `/${data.board_id}/ #${data.post_id}`;
   12355:         const linkBtn = document.getElementById('r-link-btn');
--- Line 12341 ---
   12326:                 } else break;
   12327:             }
   12328:         } catch(e) {} finally { this.isPreloading = false; }
   12329:     },
   12330:     playVideo(data) {
   12331:         if (this.currentBlobUrl) {
   12332:             URL.revokeObjectURL(this.currentBlobUrl);
   12333:             this.currentBlobUrl = null;
   12334:         }
   12335:         if (this.currentPreloader) {
   12336:             this.currentPreloader.src = "";
   12337:             this.currentPreloader.load();
   12338:             this.currentPreloader = null;
   12339:         }
   12340:         if (data._preloaderElement) {
-> 12341:             this.currentPreloader = data._preloaderElement;
   12342:             delete data._preloaderElement; 
   12343:         }
   12344:         this.video.classList.remove('blurred-media', 'revealed');
   12345:         if (data.is_censored) {
   12346:             this.video.classList.add('blurred-media');
   12347:         }
   12348:         this.video.src = data.src;
   12349:         this.video.poster = data.poster || '';
   12350:         this.video.muted = this.userMuted;
   12351:         const textEl = document.getElementById('r-text');
   12352:         if (textEl) textEl.innerHTML = window.formatTextGlobal(data.text);
   12353:         const metaEl = document.getElementById('r-meta');
   12354:         if (metaEl) metaEl.textContent = `/${data.board_id}/ #${data.post_id}`;
   12355:         const linkBtn = document.getElementById('r-link-btn');
   12356:         if (linkBtn) {
--- Line 12342 ---
   12327:             }
   12328:         } catch(e) {} finally { this.isPreloading = false; }
   12329:     },
   12330:     playVideo(data) {
   12331:         if (this.currentBlobUrl) {
   12332:             URL.revokeObjectURL(this.currentBlobUrl);
   12333:             this.currentBlobUrl = null;
   12334:         }
   12335:         if (this.currentPreloader) {
   12336:             this.currentPreloader.src = "";
   12337:             this.currentPreloader.load();
   12338:             this.currentPreloader = null;
   12339:         }
   12340:         if (data._preloaderElement) {
   12341:             this.currentPreloader = data._preloaderElement;
-> 12342:             delete data._preloaderElement; 
   12343:         }
   12344:         this.video.classList.remove('blurred-media', 'revealed');
   12345:         if (data.is_censored) {
   12346:             this.video.classList.add('blurred-media');
   12347:         }
   12348:         this.video.src = data.src;
   12349:         this.video.poster = data.poster || '';
   12350:         this.video.muted = this.userMuted;
   12351:         const textEl = document.getElementById('r-text');
   12352:         if (textEl) textEl.innerHTML = window.formatTextGlobal(data.text);
   12353:         const metaEl = document.getElementById('r-meta');
   12354:         if (metaEl) metaEl.textContent = `/${data.board_id}/ #${data.post_id}`;
   12355:         const linkBtn = document.getElementById('r-link-btn');
   12356:         if (linkBtn) {
   12357:             if (data.thread_id) {
--- Line 12386 ---
   12371:             }).catch(e => {
   12372:                 if (e.name === 'AbortError') return;
   12373:                 console.log("Autoplay blocked or failed, muting...", e);
   12374:                 this.video.muted = true;
   12375:                 this.userMuted = true;
   12376:                 this.video.play().catch(() => {
   12377:                     this.toggleLoader(false);
   12378:                 });
   12379:                 showToast(t('roulette_auto_hint'), 6000);
   12380:             });
   12381:         }
   12382:     }
   12383: };
   12384: window.RouletteManager = RouletteManager;       
   12385: const ImageRouletteManager = {
-> 12386:     preloadBuffer: [],
   12387:     isPreloading: false,
   12388: 
   12389:     init() {
   12390:         if (!document.body.classList.contains('page-random-img')) return;
   12391:         this.container = document.getElementById('ri-container');
   12392:         this.img = document.getElementById('ri-img');
   12393:         this.errorCount = 0; 
   12394:         this.img.onerror = () => {
   12395:             this.errorCount++;
   12396:             console.warn("Image 404 in roulette, skipping...");
   12397:             if (this.errorCount > 16) {
   12398:                 showToast(t('net_error_stop', '??????? ????? ??????. ????????? ????????.'));
   12399:                 this.toggleLoader(false);
   12400:                 this.errorCount = 0;
   12401:                 return;
--- Line 12565 ---
   12550:             console.warn("Image 404 in roulette, skipping...");
   12551:             setTimeout(() => this.goNext(), 500);
   12552:         };
   12553:         this.goNext();
   12554:     },
   12555: 
   12556:     toggleUI() {
   12557:         this.uiLayer.classList.toggle('ui-hidden');
   12558:     },
   12559: 
   12560:     toggleLoader(show) {
   12561:         if (this.loader) this.loader.style.display = show ? 'block' : 'none';
   12562:         if (this.img) this.img.style.opacity = show ? '0.5' : '1';
   12563:     },
   12564: 
-> 12565:     async preloadNext() {
   12566:         if (this.isPreloading) return;
   12567:         this.isPreloading = true;
   12568:         try {
   12569:             let url = '/api/img/next';
   12570:             try {
   12571:                 const saved = localStorage.getItem('tgach_img_filter');
   12572:                 if (saved) url += `?boards=${JSON.parse(saved).join(',')}`;
   12573:             } catch (e) {}
   12574: 
   12575:             while (this.preloadBuffer.length < 3) {
   12576:                 const res = await fetch(url);
   12577:                 if (res.ok) {
   12578:                     const data = await res.json();
   12579:                     if (!data.error) {
   12580:                         const temp = new Image();
--- Line 12575 ---
   12560:     toggleLoader(show) {
   12561:         if (this.loader) this.loader.style.display = show ? 'block' : 'none';
   12562:         if (this.img) this.img.style.opacity = show ? '0.5' : '1';
   12563:     },
   12564: 
   12565:     async preloadNext() {
   12566:         if (this.isPreloading) return;
   12567:         this.isPreloading = true;
   12568:         try {
   12569:             let url = '/api/img/next';
   12570:             try {
   12571:                 const saved = localStorage.getItem('tgach_img_filter');
   12572:                 if (saved) url += `?boards=${JSON.parse(saved).join(',')}`;
   12573:             } catch (e) {}
   12574: 
-> 12575:             while (this.preloadBuffer.length < 3) {
   12576:                 const res = await fetch(url);
   12577:                 if (res.ok) {
   12578:                     const data = await res.json();
   12579:                     if (!data.error) {
   12580:                         const temp = new Image();
   12581:                         temp.src = data.src;
   12582:                         this.preloadBuffer.push(data);
   12583:                     } else {
   12584:                         break;
   12585:                     }
   12586:                 } else {
   12587:                     break;
   12588:                 }
   12589:             }
   12590:         } catch (e) {
--- Line 12582 ---
   12567:         this.isPreloading = true;
   12568:         try {
   12569:             let url = '/api/img/next';
   12570:             try {
   12571:                 const saved = localStorage.getItem('tgach_img_filter');
   12572:                 if (saved) url += `?boards=${JSON.parse(saved).join(',')}`;
   12573:             } catch (e) {}
   12574: 
   12575:             while (this.preloadBuffer.length < 3) {
   12576:                 const res = await fetch(url);
   12577:                 if (res.ok) {
   12578:                     const data = await res.json();
   12579:                     if (!data.error) {
   12580:                         const temp = new Image();
   12581:                         temp.src = data.src;
-> 12582:                         this.preloadBuffer.push(data);
   12583:                     } else {
   12584:                         break;
   12585:                     }
   12586:                 } else {
   12587:                     break;
   12588:                 }
   12589:             }
   12590:         } catch (e) {
   12591:             console.error("Preload error", e);
   12592:         } finally {
   12593:             this.isPreloading = false;
   12594:         }
   12595:     },
   12596: 
   12597:     async goNext() {
--- Line 12603 ---
   12588:                 }
   12589:             }
   12590:         } catch (e) {
   12591:             console.error("Preload error", e);
   12592:         } finally {
   12593:             this.isPreloading = false;
   12594:         }
   12595:     },
   12596: 
   12597:     async goNext() {
   12598:         if (this.isLoading) return;
   12599:         if (this.currentIndex < this.historyStack.length - 1) {
   12600:             this.currentIndex++;
   12601:             this.render(this.historyStack[this.currentIndex]);
   12602:             if (this.currentIndex === this.historyStack.length - 1) {
-> 12603:                 this.preloadNext();
   12604:             }
   12605:             return;
   12606:         }
   12607:         if (this.preloadBuffer.length > 0) {
   12608:             const item = this.preloadBuffer.shift();
   12609:             this.pushToHistory(item);
   12610:             this.render(item);
   12611:             this.preloadNext();
   12612:             return;
   12613:         }
   12614: 
   12615:         this.isLoading = true;
   12616:         this.toggleLoader(true);
   12617: 
   12618:         try {
--- Line 12607 ---
   12592:         } finally {
   12593:             this.isPreloading = false;
   12594:         }
   12595:     },
   12596: 
   12597:     async goNext() {
   12598:         if (this.isLoading) return;
   12599:         if (this.currentIndex < this.historyStack.length - 1) {
   12600:             this.currentIndex++;
   12601:             this.render(this.historyStack[this.currentIndex]);
   12602:             if (this.currentIndex === this.historyStack.length - 1) {
   12603:                 this.preloadNext();
   12604:             }
   12605:             return;
   12606:         }
-> 12607:         if (this.preloadBuffer.length > 0) {
   12608:             const item = this.preloadBuffer.shift();
   12609:             this.pushToHistory(item);
   12610:             this.render(item);
   12611:             this.preloadNext();
   12612:             return;
   12613:         }
   12614: 
   12615:         this.isLoading = true;
   12616:         this.toggleLoader(true);
   12617: 
   12618:         try {
   12619:             let url = '/api/img/next';
   12620:             try {
   12621:                 const saved = localStorage.getItem('tgach_img_filter');
   12622:                 if (saved) url += `?boards=${JSON.parse(saved).join(',')}`;
--- Line 12608 ---
   12593:             this.isPreloading = false;
   12594:         }
   12595:     },
   12596: 
   12597:     async goNext() {
   12598:         if (this.isLoading) return;
   12599:         if (this.currentIndex < this.historyStack.length - 1) {
   12600:             this.currentIndex++;
   12601:             this.render(this.historyStack[this.currentIndex]);
   12602:             if (this.currentIndex === this.historyStack.length - 1) {
   12603:                 this.preloadNext();
   12604:             }
   12605:             return;
   12606:         }
   12607:         if (this.preloadBuffer.length > 0) {
-> 12608:             const item = this.preloadBuffer.shift();
   12609:             this.pushToHistory(item);
   12610:             this.render(item);
   12611:             this.preloadNext();
   12612:             return;
   12613:         }
   12614: 
   12615:         this.isLoading = true;
   12616:         this.toggleLoader(true);
   12617: 
   12618:         try {
   12619:             let url = '/api/img/next';
   12620:             try {
   12621:                 const saved = localStorage.getItem('tgach_img_filter');
   12622:                 if (saved) url += `?boards=${JSON.parse(saved).join(',')}`;
   12623:             } catch (e) {}
--- Line 12611 ---
   12596: 
   12597:     async goNext() {
   12598:         if (this.isLoading) return;
   12599:         if (this.currentIndex < this.historyStack.length - 1) {
   12600:             this.currentIndex++;
   12601:             this.render(this.historyStack[this.currentIndex]);
   12602:             if (this.currentIndex === this.historyStack.length - 1) {
   12603:                 this.preloadNext();
   12604:             }
   12605:             return;
   12606:         }
   12607:         if (this.preloadBuffer.length > 0) {
   12608:             const item = this.preloadBuffer.shift();
   12609:             this.pushToHistory(item);
   12610:             this.render(item);
-> 12611:             this.preloadNext();
   12612:             return;
   12613:         }
   12614: 
   12615:         this.isLoading = true;
   12616:         this.toggleLoader(true);
   12617: 
   12618:         try {
   12619:             let url = '/api/img/next';
   12620:             try {
   12621:                 const saved = localStorage.getItem('tgach_img_filter');
   12622:                 if (saved) url += `?boards=${JSON.parse(saved).join(',')}`;
   12623:             } catch (e) {}
   12624: 
   12625:             const res = await fetch(url);
   12626:             if (!res.ok) throw new Error();
--- Line 12636 ---
   12621:                 const saved = localStorage.getItem('tgach_img_filter');
   12622:                 if (saved) url += `?boards=${JSON.parse(saved).join(',')}`;
   12623:             } catch (e) {}
   12624: 
   12625:             const res = await fetch(url);
   12626:             if (!res.ok) throw new Error();
   12627:             const data = await res.json();
   12628:             
   12629:             if (data.error) {
   12630:                 if (typeof showToast === 'function') showToast("???????? ??????????");
   12631:                 return;
   12632:             }
   12633: 
   12634:             this.pushToHistory(data);
   12635:             this.render(data);
-> 12636:             this.preloadNext();
   12637:         } catch (e) {
   12638:             console.error(e);
   12639:             setTimeout(() => this.goNext(), 1000);
   12640:         } finally {
   12641:             this.isLoading = false;
   12642:             this.toggleLoader(false);
   12643:         }
   12644:     },
   12645: 
   12646:     pushToHistory(item) {
   12647:         this.historyStack.push(item);
   12648:         this.currentIndex++;
   12649:         if (this.historyStack.length > 50) {
   12650:             this.historyStack.shift();
   12651:             this.currentIndex--;

=== Matches for 'files/' (13) ===
--- Line 812 ---
   797:         modal.style.display = 'block';
   798:         modal.style.zIndex = '10051';
   799:         requestAnimationFrame(() => modal.classList.add('active'));
   800:         document.body.classList.add('modal-open');
   801:         try {
   802:             const res = await fetch(`/api/post/${postNum}`);
   803:             if (!res.ok) throw new Error('Failed to fetch post');
   804:             const data = await res.json();
   805:             numInput.value = postNum;
   806:             textInput.value = data.content.text || '';
   807:             fileContainer.innerHTML = '';
   808:             if (data.content.files && data.content.files.length > 0) {
   809:                 data.content.files.forEach((f, index) => {
   810:                     const wrapper = document.createElement('label');
   811:                     wrapper.style.cssText = 'position: relative; display: inline-block; width: 60px; height: 60px; margin: 2px; cursor: pointer; border: 1px solid transparent;';
-> 812:                     const thumbUrl = (f.thumbnail_file_id ? `/files/${f.thumbnail_file_id}` : (f.original_file_id ? `/files/${f.original_file_id}` : (f.thumbnail_url || f.original_url)));
   813:                     const checkbox = document.createElement('input');
   814:                     checkbox.type = 'checkbox';
   815:                     checkbox.value = index;
   816:                     checkbox.style.cssText = 'position: absolute; top: 0; left: 0; opacity: 0; width: 100%; height: 100%; cursor: pointer; z-index: 2;';
   817:                     let mediaEl;
   818:                     const isVideo = f.type === 'video' || f.type === 'gif' || (thumbUrl && thumbUrl.match(/\.(mp4|webm)$/i));
   819:                     if (isVideo) {
   820:                         mediaEl = document.createElement('video');
   821:                         mediaEl.src = thumbUrl;
   822:                         mediaEl.muted = true;
   823:                         mediaEl.loop = true;
   824:                         mediaEl.preload = 'metadata'; 
   825:                     } else {
   826:                         mediaEl = document.createElement('img');
   827:                         mediaEl.src = thumbUrl;
--- Line 1366 ---
   1351:                         this.playlist.push({
   1352:                             file: file,
   1353:                             post: post
   1354:                         });
   1355:                     });
   1356:                 }
   1357:             });
   1358:             this.page++;
   1359:         } catch (e) {
   1360:             showToast(t('stash_load_error'));
   1361:         } finally {
   1362:             this.isLoading = false;
   1363:         }
   1364:     },
   1365:     loadVideoWithRetry(videoEl, item, attempt = 0) {
-> 1366:         const itemOrigUrl = (item.file.original_file_id ? `/files/${item.file.original_file_id}` : item.file.original_url);
   1367:         const sources = item.file.sources || [itemOrigUrl];
   1368:         videoEl.onerror = () => {
   1369:             if (attempt + 1 < sources.length) {
   1370:                 console.log(`[Stream] Mirror failed, trying mirror ${attempt + 1}`);
   1371:                 this.loadVideoWithRetry(videoEl, item, attempt + 1);
   1372:             } else {
   1373:                 if (typeof FailedMediaCache !== 'undefined') {
   1374:                     sources.forEach(s => FailedMediaCache.markFailed(s));
   1375:                 }
   1376:                 console.error('[Stream] All sources failed');
   1377:                 if (typeof showToast === 'function') showToast(t('ms_video_404', '????? ??????????'), 2000);
   1378:             }
   1379:         };
   1380:         if (typeof FailedMediaCache !== 'undefined' && sources[attempt] && FailedMediaCache.isFailed(sources[attempt])) {
   1381:             console.error('[Stream] Media already marked failed');
--- Line 1422 ---
   1407:             return;
   1408:         }
   1409:         this.currentIndex = index;
   1410:         const item = this.playlist[index];
   1411:         const container = document.getElementById('ms-view');
   1412:         const info = document.getElementById('ms-info');
   1413:         const oldVideo = container.querySelector('video');
   1414:         if (oldVideo) {
   1415:             oldVideo.pause();
   1416:             oldVideo.removeAttribute('src');
   1417:             oldVideo.load();
   1418:         }
   1419:         container.innerHTML = '';
   1420:         let el;
   1421:         const isVideo = ['video', 'gif', 'animation', 'video_note'].includes(item.file.type);
-> 1422:         const url = (item.file.original_file_id ? `/files/${item.file.original_file_id}` : item.file.original_url);
   1423:         if (isVideo) {
   1424:             el = document.createElement('video');
   1425:             el.referrerPolicy = "no-referrer";
   1426:             el.loop = true;
   1427:             el.controls = true;
   1428:             el.onplay = () => {
   1429:                 if (typeof AudioManager !== 'undefined') AudioManager.stopOthers(el);
   1430:             };
   1431: 
   1432:             const connection = navigator.connection || navigator.mozConnection || navigator.webkitConnection;
   1433:             const saveData = connection ? connection.saveData : false;
   1434:             if (saveData) {
   1435:                 el.autoplay = false;
   1436:                 el.preload = 'none';
   1437:                 showToast(t('eco_mode_active', "???????? ???????: ???????? ????????"), 2000);
--- Line 1457 ---
   1442:             el.muted = false;
   1443:             this.loadVideoWithRetry(el, item);
   1444:         } else {
   1445:             el = document.createElement('img');
   1446:             el.referrerPolicy = "no-referrer";
   1447:             el.src = url;
   1448:         }
   1449:         const isCensored = item.post.content.is_censored === true;
   1450:         const blurClass = isCensored ? ' blurred-media' : '';
   1451:         el.className = 'ms-content ' + (direction === 'up' ? 'slide-up' : 'slide-down') + blurClass;
   1452:         container.appendChild(el);
   1453:         if (!this.isRandomMode) {
   1454:             [1, 2].forEach(offset => {
   1455:                 const nextItem = this.playlist[index + offset];
   1456:                 if (nextItem) {
-> 1457:                     const nextUrl = nextItem.file.original_file_id ? `/files/${nextItem.file.original_file_id}` : nextItem.file.original_url;
   1458:                     const isNextVid = ['video', 'gif', 'animation', 'video_note'].includes(nextItem.file.type);
   1459:                     if (isNextVid) {
   1460:                         const v = document.createElement('video');
   1461:                         v.preload = 'auto';
   1462:                         v.src = nextUrl;
   1463:                         if(typeof v.referrerPolicy !== 'undefined') v.referrerPolicy = "no-referrer";
   1464:                     } else {
   1465:                         const img = new Image();
   1466:                         if(typeof img.referrerPolicy !== 'undefined') img.referrerPolicy = "no-referrer";
   1467:                         img.src = nextUrl;
   1468:                     }
   1469:                 }
   1470:             });
   1471:         }
   1472:         const rawText = item.post.content.text || '';
--- Line 1529 ---
   1514:                 if (typeof showToast === 'function') showToast(t('ms_chat_post_no_thread', "???? ???? ?? ???? (??? ?????)"));
   1515:                 return;
   1516:             }
   1517:             const url = `/${this.boardId}/res/${item.post.thread_id}.html#post-${item.post.id}`;
   1518:             window.open(url, '_blank');
   1519:         }
   1520:     },
   1521:     async downloadCurrentFile() {
   1522:         const item = this.playlist[this.currentIndex];
   1523:         if (!item) return;
   1524:         const btn = document.getElementById('ms-download-btn');
   1525:         if (!btn) return;
   1526:         const originalText = btn.textContent;
   1527:         btn.textContent = '?';
   1528:         btn.style.pointerEvents = 'none';
-> 1529:         const targetDownloadUrl = item.file.original_file_id ? `/files/${item.file.original_file_id}` : item.file.original_url;
   1530:         try {
   1531:             const response = await fetch(targetDownloadUrl);
   1532:             if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);
   1533:             showToast(t('download_started', '?????????? ????????...'), 1500);
   1534:             const blob = await response.blob();
   1535:             const mimeType = blob.type;
   1536:             let extension = '';
   1537:             const mimeMap = {
   1538:                 'video/mp4': '.mp4', 'video/webm': '.webm', 'video/quicktime': '.mov',
   1539:                 'image/jpeg': '.jpg', 'image/png': '.png', 'image/gif': '.gif', 'image/webp': '.webp'
   1540:             };
   1541:             extension = mimeMap[mimeType] || '';
   1542:             if (!extension && item.file.filename) {
   1543:                 const match = item.file.filename.match(/\.([a-z0-9]{2,5})$/i);
   1544:                 if (match) extension = match[0];
--- Line 1577 ---
   1562:             document.body.removeChild(a);
   1563:             URL.revokeObjectURL(objectUrl);
   1564:         } catch (err) {
   1565:             console.error('Download failed:', err);
   1566:             showToast(t('download_err', '?????? ??????????'));
   1567:             window.open(targetDownloadUrl, '_blank');
   1568:         } finally {
   1569:             btn.textContent = originalText;
   1570:             btn.style.pointerEvents = 'auto';
   1571:         }
   1572:     },
   1573:     showCurrentFileTags() {
   1574:         const item = this.playlist[this.currentIndex];
   1575:         if (!item || !item.file) return;
   1576:         let fileId = item.file.original_file_id;
-> 1577:         const targetItemUrl = item.file.original_file_id ? `/files/${item.file.original_file_id}` : item.file.original_url;
   1578:         if (!fileId && targetItemUrl) {
   1579:             try {
   1580:                 const filename = targetItemUrl.split('/').pop().split('?')[0];
   1581:                 const parts = filename.split('.');
   1582:                 if (parts.length > 1) parts.pop();
   1583:                 fileId = parts.join('.');
   1584:             } catch (e) {
   1585:                 console.error('Failed to extract file ID:', e);
   1586:             }
   1587:         }
   1588: 
   1589:         if (!fileId) {
   1590:             if (typeof showToast === 'function') showToast(t('err_no_file_id', 'File ID ?? ??????, ???? ??????????'));
   1591:             return;
   1592:         }
--- Line 11013 ---
   10998:         let audiosHtml = '';
   10999:         if (data.content.files?.length) {
   11000:             // NEW: Censorship check
   11001:             const isCensored = data.content.is_censored === true;
   11002:             const blurClass = isCensored ? ' blurred-media' : '';
   11003: 
   11004:             let imgContent = '';
   11005:             let audContent = '';
   11006:             data.content.files.forEach(f => {
   11007:                 let mirrorAttrs = '';
   11008:                 if (f._mirrors) {
   11009:                     for (const type in f._mirrors) {
   11010:                         mirrorAttrs += ` data-mirror-${type}="${esc(f._mirrors[type])}"`;
   11011:                     }
   11012:                 }
-> 11013:                 const url = (f.original_file_id ? `/files/${f.original_file_id}` : f.original_url) || "";
   11014:                 let thumbCandidate = (f.thumbnail_file_id ? `/files/${f.thumbnail_file_id}` : (f.original_file_id ? `/files/${f.original_file_id}` : (f.thumbnail_url || f.original_url))) || null;
   11015: 
   11016:                 if (typeof FailedMediaCache !== 'undefined' && url && FailedMediaCache.isFailed(url)) {
   11017:                     imgContent += `<div class="file-thumb broken-media" style="display:flex; align-items:center; justify-content:center; width:100%; height:100%; min-height:80px; background:#1e1e1e; color:#888; font-size:1.2em;">?? Media Unavailable</div>`;
   11018:                     return;
   11019:                 }
   11020:                 if (thumbCandidate && typeof FailedMediaCache !== 'undefined' && FailedMediaCache.isFailed(thumbCandidate)) {
   11021:                     thumbCandidate = url;
   11022:                 }
   11023:                 const isVideoType = ['video', 'gif', 'animation', 'video_note'].includes(f.type);
   11024:                 if (!thumbCandidate) {
   11025:                     if (isVideoType) {
   11026:                         thumbCandidate = '';
   11027:                     } else {
   11028:                         thumbCandidate = url; 
--- Line 11014 ---
   10999:         if (data.content.files?.length) {
   11000:             // NEW: Censorship check
   11001:             const isCensored = data.content.is_censored === true;
   11002:             const blurClass = isCensored ? ' blurred-media' : '';
   11003: 
   11004:             let imgContent = '';
   11005:             let audContent = '';
   11006:             data.content.files.forEach(f => {
   11007:                 let mirrorAttrs = '';
   11008:                 if (f._mirrors) {
   11009:                     for (const type in f._mirrors) {
   11010:                         mirrorAttrs += ` data-mirror-${type}="${esc(f._mirrors[type])}"`;
   11011:                     }
   11012:                 }
   11013:                 const url = (f.original_file_id ? `/files/${f.original_file_id}` : f.original_url) || "";
-> 11014:                 let thumbCandidate = (f.thumbnail_file_id ? `/files/${f.thumbnail_file_id}` : (f.original_file_id ? `/files/${f.original_file_id}` : (f.thumbnail_url || f.original_url))) || null;
   11015: 
   11016:                 if (typeof FailedMediaCache !== 'undefined' && url && FailedMediaCache.isFailed(url)) {
   11017:                     imgContent += `<div class="file-thumb broken-media" style="display:flex; align-items:center; justify-content:center; width:100%; height:100%; min-height:80px; background:#1e1e1e; color:#888; font-size:1.2em;">?? Media Unavailable</div>`;
   11018:                     return;
   11019:                 }
   11020:                 if (thumbCandidate && typeof FailedMediaCache !== 'undefined' && FailedMediaCache.isFailed(thumbCandidate)) {
   11021:                     thumbCandidate = url;
   11022:                 }
   11023:                 const isVideoType = ['video', 'gif', 'animation', 'video_note'].includes(f.type);
   11024:                 if (!thumbCandidate) {
   11025:                     if (isVideoType) {
   11026:                         thumbCandidate = '';
   11027:                     } else {
   11028:                         thumbCandidate = url; 
   11029:                     }
--- Line 11250 ---
   11235:     createCatalogCard(data) {
   11236:         const esc = (str) => String(str || "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
   11237:         const threadId = data.id;
   11238:         const hue = (parseInt(threadId) * 137) % 360;
   11239:         const bgColor = `hsl(${hue}, 60%, 40%)`;
   11240:         
   11241:         // Censor check for catalog
   11242:         const isCensored = data.content.is_censored === true;
   11243:         const blurClass = isCensored ? ' blurred-media' : '';
   11244: 
   11245:         let thumbHtml = '';
   11246:         const files = data.content.files || [];
   11247:         if (files.length > 0) {
   11248:             const f = files[0];
   11249:             const isVid = ['video', 'gif', 'animation', 'video_note'].includes(f.type);
-> 11250:             const mediaUrl = (f.original_file_id ? `/files/${f.original_file_id}` : f.original_url) || "";
   11251:             const thumbUrl = (f.thumbnail_file_id ? `/files/${f.thumbnail_file_id}` : (f.original_file_id ? `/files/${f.original_file_id}` : (f.thumbnail_url || f.original_url))) || "";
   11252:             
   11253:             if (typeof FailedMediaCache !== 'undefined' && ((mediaUrl && FailedMediaCache.isFailed(mediaUrl)) || (thumbUrl && FailedMediaCache.isFailed(thumbUrl)))) {
   11254:                 thumbHtml = `<div class="catalog-thumb broken-media" style="background-color: #1e1e1e; display:flex; align-items:center; justify-content:center;"><span style="font-size:2em">??</span></div>`;
   11255:             } else if (isVid) {
   11256:                 const vidUrl = mediaUrl;
   11257:                 const posterUrl = thumbUrl || mediaUrl;
   11258:                 if (vidUrl) {
   11259:                     thumbHtml = `
   11260:                         <div class="catalog-thumb lazy-media-wrapper" data-src="${vidUrl}" data-file-id="${f.original_file_id || ''}" data-type="video" style="background-color: #000;">
   11261:                             <video class="lazy-load${blurClass}" preload="metadata" muted playsinline loop data-src="${vidUrl}" ${posterUrl ? `poster="${posterUrl}"` : ''} style="width: 100%; height: 100%; object-fit: cover;"></video>
   11262:                             <span class="lazy-badge" style="position:absolute; bottom:5px; right:5px; background:rgba(0,0,0,0.6); color:white; padding:2px 4px; font-size:10px; border-radius:3px;">VIDEO</span>
   11263:                         </div>`;
   11264:                 } else {
   11265:                      thumbHtml = `<div class="catalog-thumb" style="background-color: ${bgColor}; display:flex; align-items:center; justify-content:center;"><span style="font-size:2em">?</span></div>`;
--- Line 11251 ---
   11236:         const esc = (str) => String(str || "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
   11237:         const threadId = data.id;
   11238:         const hue = (parseInt(threadId) * 137) % 360;
   11239:         const bgColor = `hsl(${hue}, 60%, 40%)`;
   11240:         
   11241:         // Censor check for catalog
   11242:         const isCensored = data.content.is_censored === true;
   11243:         const blurClass = isCensored ? ' blurred-media' : '';
   11244: 
   11245:         let thumbHtml = '';
   11246:         const files = data.content.files || [];
   11247:         if (files.length > 0) {
   11248:             const f = files[0];
   11249:             const isVid = ['video', 'gif', 'animation', 'video_note'].includes(f.type);
   11250:             const mediaUrl = (f.original_file_id ? `/files/${f.original_file_id}` : f.original_url) || "";
-> 11251:             const thumbUrl = (f.thumbnail_file_id ? `/files/${f.thumbnail_file_id}` : (f.original_file_id ? `/files/${f.original_file_id}` : (f.thumbnail_url || f.original_url))) || "";
   11252:             
   11253:             if (typeof FailedMediaCache !== 'undefined' && ((mediaUrl && FailedMediaCache.isFailed(mediaUrl)) || (thumbUrl && FailedMediaCache.isFailed(thumbUrl)))) {
   11254:                 thumbHtml = `<div class="catalog-thumb broken-media" style="background-color: #1e1e1e; display:flex; align-items:center; justify-content:center;"><span style="font-size:2em">??</span></div>`;
   11255:             } else if (isVid) {
   11256:                 const vidUrl = mediaUrl;
   11257:                 const posterUrl = thumbUrl || mediaUrl;
   11258:                 if (vidUrl) {
   11259:                     thumbHtml = `
   11260:                         <div class="catalog-thumb lazy-media-wrapper" data-src="${vidUrl}" data-file-id="${f.original_file_id || ''}" data-type="video" style="background-color: #000;">
   11261:                             <video class="lazy-load${blurClass}" preload="metadata" muted playsinline loop data-src="${vidUrl}" ${posterUrl ? `poster="${posterUrl}"` : ''} style="width: 100%; height: 100%; object-fit: cover;"></video>
   11262:                             <span class="lazy-badge" style="position:absolute; bottom:5px; right:5px; background:rgba(0,0,0,0.6); color:white; padding:2px 4px; font-size:10px; border-radius:3px;">VIDEO</span>
   11263:                         </div>`;
   11264:                 } else {
   11265:                      thumbHtml = `<div class="catalog-thumb" style="background-color: ${bgColor}; display:flex; align-items:center; justify-content:center;"><span style="font-size:2em">?</span></div>`;
   11266:                 }
--- Line 11498 ---
   11483:     if (!originalUrl) {
   11484:         if (typeof FailedMediaCache !== 'undefined' && currentSrc) FailedMediaCache.markFailed(currentSrc);
   11485:         renderStaticError();
   11486:         return;
   11487:     }
   11488: 
   11489:     // Try fallback from thumbnail / external mirror to originalUrl or local proxy endpoint
   11490:     if (!img.dataset.triedFallback) {
   11491:         img.dataset.triedFallback = "true";
   11492:         let fallbackUrl = null;
   11493:         if (originalUrl && originalUrl !== currentSrc && (typeof FailedMediaCache === 'undefined' || !FailedMediaCache.isFailed(originalUrl))) {
   11494:             fallbackUrl = originalUrl;
   11495:         } else {
   11496:             const fileId = img.dataset.fileId || (parent ? parent.dataset.fileId : null);
   11497:             if (fileId) {
-> 11498:                 const localUrl = `/files/${fileId}`;
   11499:                 if (localUrl !== currentSrc && localUrl !== originalUrl && (typeof FailedMediaCache === 'undefined' || !FailedMediaCache.isFailed(localUrl))) {
   11500:                     fallbackUrl = localUrl;
   11501:                 }
   11502:             }
   11503:         }
   11504: 
   11505:         if (fallbackUrl) {
   11506:             if (typeof FailedMediaCache !== 'undefined' && currentSrc) {
   11507:                 FailedMediaCache.markFailed(currentSrc);
   11508:             }
   11509:             delete img.dataset.finalError;
   11510:             img.onerror = function() { handleImageError(img); };
   11511:             img.src = fallbackUrl;
   11512:             return;
   11513:         }
--- Line 11524 ---
   11509:             delete img.dataset.finalError;
   11510:             img.onerror = function() { handleImageError(img); };
   11511:             img.src = fallbackUrl;
   11512:             return;
   11513:         }
   11514:     }
   11515: 
   11516:     let failedType = null;
   11517:     if (currentSrc.includes("iili.io")) failedType = "freeimage";
   11518:     else if (currentSrc.includes("ibb.co")) failedType = "imgbb";
   11519:     else if (currentSrc.includes("pixhost.to")) failedType = "pixhost";
   11520:     else if (currentSrc.includes("catbox.moe")) failedType = "catbox";
   11521:     else if (currentSrc.includes("0x0.st")) failedType = "0x0";
   11522:     else if (currentSrc.includes("telegram.org")) failedType = "telegram";
   11523: 
-> 11524:     const isLocalFile = currentSrc.includes("/files/") || originalUrl.includes("/files/") || !failedType;
   11525: 
   11526:     if (isLocalFile) {
   11527:         if (typeof FailedMediaCache !== 'undefined') {
   11528:             FailedMediaCache.markFailed(originalUrl);
   11529:             FailedMediaCache.markFailed(currentSrc);
   11530:         }
   11531:         renderStaticError();
   11532:         return;
   11533:     }
   11534: 
   11535:     let skipped = img.dataset.skippedHosts ? img.dataset.skippedHosts.split(",") : [];
   11536:     if (failedType && !skipped.includes(failedType)) {
   11537:         skipped.push(failedType);
   11538:     }
   11539:     img.dataset.skippedHosts = skipped.join(",");
--- Line 13824 ---
   13809:             const grid = document.querySelector('.gallery-grid');
   13810:             images.forEach(img => {
   13811:                 const wrapper = document.createElement('div');
   13812:                 wrapper.className = 'gallery-item-wrapper';
   13813:                 
   13814:                 const thumb = document.createElement('div');
   13815:                 thumb.className = 'gallery-thumb';
   13816:                 
   13817:                 const link = document.createElement('a');
   13818:                 link.href = `/${img.parent_board_id}/res/${img.parent_post_id}.html#post-${img.parent_post_id}`;
   13819:                 link.className = 'file-thumb';
   13820:                 link.dataset.filename = img.filename;
   13821:                 
   13822:                 const image = document.createElement('img');
   13823:                 image.className = 'post-image lazy-load';
-> 13824:                 image.dataset.src = (img.thumbnail_file_id ? `/files/${img.thumbnail_file_id}` : (img.original_file_id ? `/files/${img.original_file_id}` : (img.thumbnail_url || img.original_url)));
   13825:                 image.src = "data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7";
   13826:                 
   13827:                 link.appendChild(image);
   13828:                 thumb.appendChild(link);
   13829:                 wrapper.appendChild(thumb);
   13830:                 grid.appendChild(wrapper);
   13831:                 
   13832:                 if (typeof SmartLoader !== 'undefined') SmartLoader.scan(wrapper);
   13833:             });
   13834:             
   13835:             if (typeof GalleryManager !== 'undefined') GalleryManager.updatePlaylist();
   13836:             
   13837:         } catch (e) {
   13838:             console.error("Tag scroll error:", e);
   13839:         } finally {

=== Matches for 'original_file_id' (15) ===
--- Line 812 ---
   797:         modal.style.display = 'block';
   798:         modal.style.zIndex = '10051';
   799:         requestAnimationFrame(() => modal.classList.add('active'));
   800:         document.body.classList.add('modal-open');
   801:         try {
   802:             const res = await fetch(`/api/post/${postNum}`);
   803:             if (!res.ok) throw new Error('Failed to fetch post');
   804:             const data = await res.json();
   805:             numInput.value = postNum;
   806:             textInput.value = data.content.text || '';
   807:             fileContainer.innerHTML = '';
   808:             if (data.content.files && data.content.files.length > 0) {
   809:                 data.content.files.forEach((f, index) => {
   810:                     const wrapper = document.createElement('label');
   811:                     wrapper.style.cssText = 'position: relative; display: inline-block; width: 60px; height: 60px; margin: 2px; cursor: pointer; border: 1px solid transparent;';
-> 812:                     const thumbUrl = (f.thumbnail_file_id ? `/files/${f.thumbnail_file_id}` : (f.original_file_id ? `/files/${f.original_file_id}` : (f.thumbnail_url || f.original_url)));
   813:                     const checkbox = document.createElement('input');
   814:                     checkbox.type = 'checkbox';
   815:                     checkbox.value = index;
   816:                     checkbox.style.cssText = 'position: absolute; top: 0; left: 0; opacity: 0; width: 100%; height: 100%; cursor: pointer; z-index: 2;';
   817:                     let mediaEl;
   818:                     const isVideo = f.type === 'video' || f.type === 'gif' || (thumbUrl && thumbUrl.match(/\.(mp4|webm)$/i));
   819:                     if (isVideo) {
   820:                         mediaEl = document.createElement('video');
   821:                         mediaEl.src = thumbUrl;
   822:                         mediaEl.muted = true;
   823:                         mediaEl.loop = true;
   824:                         mediaEl.preload = 'metadata'; 
   825:                     } else {
   826:                         mediaEl = document.createElement('img');
   827:                         mediaEl.src = thumbUrl;
--- Line 1366 ---
   1351:                         this.playlist.push({
   1352:                             file: file,
   1353:                             post: post
   1354:                         });
   1355:                     });
   1356:                 }
   1357:             });
   1358:             this.page++;
   1359:         } catch (e) {
   1360:             showToast(t('stash_load_error'));
   1361:         } finally {
   1362:             this.isLoading = false;
   1363:         }
   1364:     },
   1365:     loadVideoWithRetry(videoEl, item, attempt = 0) {
-> 1366:         const itemOrigUrl = (item.file.original_file_id ? `/files/${item.file.original_file_id}` : item.file.original_url);
   1367:         const sources = item.file.sources || [itemOrigUrl];
   1368:         videoEl.onerror = () => {
   1369:             if (attempt + 1 < sources.length) {
   1370:                 console.log(`[Stream] Mirror failed, trying mirror ${attempt + 1}`);
   1371:                 this.loadVideoWithRetry(videoEl, item, attempt + 1);
   1372:             } else {
   1373:                 if (typeof FailedMediaCache !== 'undefined') {
   1374:                     sources.forEach(s => FailedMediaCache.markFailed(s));
   1375:                 }
   1376:                 console.error('[Stream] All sources failed');
   1377:                 if (typeof showToast === 'function') showToast(t('ms_video_404', '????? ??????????'), 2000);
   1378:             }
   1379:         };
   1380:         if (typeof FailedMediaCache !== 'undefined' && sources[attempt] && FailedMediaCache.isFailed(sources[attempt])) {
   1381:             console.error('[Stream] Media already marked failed');
--- Line 1422 ---
   1407:             return;
   1408:         }
   1409:         this.currentIndex = index;
   1410:         const item = this.playlist[index];
   1411:         const container = document.getElementById('ms-view');
   1412:         const info = document.getElementById('ms-info');
   1413:         const oldVideo = container.querySelector('video');
   1414:         if (oldVideo) {
   1415:             oldVideo.pause();
   1416:             oldVideo.removeAttribute('src');
   1417:             oldVideo.load();
   1418:         }
   1419:         container.innerHTML = '';
   1420:         let el;
   1421:         const isVideo = ['video', 'gif', 'animation', 'video_note'].includes(item.file.type);
-> 1422:         const url = (item.file.original_file_id ? `/files/${item.file.original_file_id}` : item.file.original_url);
   1423:         if (isVideo) {
   1424:             el = document.createElement('video');
   1425:             el.referrerPolicy = "no-referrer";
   1426:             el.loop = true;
   1427:             el.controls = true;
   1428:             el.onplay = () => {
   1429:                 if (typeof AudioManager !== 'undefined') AudioManager.stopOthers(el);
   1430:             };
   1431: 
   1432:             const connection = navigator.connection || navigator.mozConnection || navigator.webkitConnection;
   1433:             const saveData = connection ? connection.saveData : false;
   1434:             if (saveData) {
   1435:                 el.autoplay = false;
   1436:                 el.preload = 'none';
   1437:                 showToast(t('eco_mode_active', "???????? ???????: ???????? ????????"), 2000);
--- Line 1457 ---
   1442:             el.muted = false;
   1443:             this.loadVideoWithRetry(el, item);
   1444:         } else {
   1445:             el = document.createElement('img');
   1446:             el.referrerPolicy = "no-referrer";
   1447:             el.src = url;
   1448:         }
   1449:         const isCensored = item.post.content.is_censored === true;
   1450:         const blurClass = isCensored ? ' blurred-media' : '';
   1451:         el.className = 'ms-content ' + (direction === 'up' ? 'slide-up' : 'slide-down') + blurClass;
   1452:         container.appendChild(el);
   1453:         if (!this.isRandomMode) {
   1454:             [1, 2].forEach(offset => {
   1455:                 const nextItem = this.playlist[index + offset];
   1456:                 if (nextItem) {
-> 1457:                     const nextUrl = nextItem.file.original_file_id ? `/files/${nextItem.file.original_file_id}` : nextItem.file.original_url;
   1458:                     const isNextVid = ['video', 'gif', 'animation', 'video_note'].includes(nextItem.file.type);
   1459:                     if (isNextVid) {
   1460:                         const v = document.createElement('video');
   1461:                         v.preload = 'auto';
   1462:                         v.src = nextUrl;
   1463:                         if(typeof v.referrerPolicy !== 'undefined') v.referrerPolicy = "no-referrer";
   1464:                     } else {
   1465:                         const img = new Image();
   1466:                         if(typeof img.referrerPolicy !== 'undefined') img.referrerPolicy = "no-referrer";
   1467:                         img.src = nextUrl;
   1468:                     }
   1469:                 }
   1470:             });
   1471:         }
   1472:         const rawText = item.post.content.text || '';
--- Line 1529 ---
   1514:                 if (typeof showToast === 'function') showToast(t('ms_chat_post_no_thread', "???? ???? ?? ???? (??? ?????)"));
   1515:                 return;
   1516:             }
   1517:             const url = `/${this.boardId}/res/${item.post.thread_id}.html#post-${item.post.id}`;
   1518:             window.open(url, '_blank');
   1519:         }
   1520:     },
   1521:     async downloadCurrentFile() {
   1522:         const item = this.playlist[this.currentIndex];
   1523:         if (!item) return;
   1524:         const btn = document.getElementById('ms-download-btn');
   1525:         if (!btn) return;
   1526:         const originalText = btn.textContent;
   1527:         btn.textContent = '?';
   1528:         btn.style.pointerEvents = 'none';
-> 1529:         const targetDownloadUrl = item.file.original_file_id ? `/files/${item.file.original_file_id}` : item.file.original_url;
   1530:         try {
   1531:             const response = await fetch(targetDownloadUrl);
   1532:             if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);
   1533:             showToast(t('download_started', '?????????? ????????...'), 1500);
   1534:             const blob = await response.blob();
   1535:             const mimeType = blob.type;
   1536:             let extension = '';
   1537:             const mimeMap = {
   1538:                 'video/mp4': '.mp4', 'video/webm': '.webm', 'video/quicktime': '.mov',
   1539:                 'image/jpeg': '.jpg', 'image/png': '.png', 'image/gif': '.gif', 'image/webp': '.webp'
   1540:             };
   1541:             extension = mimeMap[mimeType] || '';
   1542:             if (!extension && item.file.filename) {
   1543:                 const match = item.file.filename.match(/\.([a-z0-9]{2,5})$/i);
   1544:                 if (match) extension = match[0];
--- Line 1576 ---
   1561:             a.click();
   1562:             document.body.removeChild(a);
   1563:             URL.revokeObjectURL(objectUrl);
   1564:         } catch (err) {
   1565:             console.error('Download failed:', err);
   1566:             showToast(t('download_err', '?????? ??????????'));
   1567:             window.open(targetDownloadUrl, '_blank');
   1568:         } finally {
   1569:             btn.textContent = originalText;
   1570:             btn.style.pointerEvents = 'auto';
   1571:         }
   1572:     },
   1573:     showCurrentFileTags() {
   1574:         const item = this.playlist[this.currentIndex];
   1575:         if (!item || !item.file) return;
-> 1576:         let fileId = item.file.original_file_id;
   1577:         const targetItemUrl = item.file.original_file_id ? `/files/${item.file.original_file_id}` : item.file.original_url;
   1578:         if (!fileId && targetItemUrl) {
   1579:             try {
   1580:                 const filename = targetItemUrl.split('/').pop().split('?')[0];
   1581:                 const parts = filename.split('.');
   1582:                 if (parts.length > 1) parts.pop();
   1583:                 fileId = parts.join('.');
   1584:             } catch (e) {
   1585:                 console.error('Failed to extract file ID:', e);
   1586:             }
   1587:         }
   1588: 
   1589:         if (!fileId) {
   1590:             if (typeof showToast === 'function') showToast(t('err_no_file_id', 'File ID ?? ??????, ???? ??????????'));
   1591:             return;
--- Line 1577 ---
   1562:             document.body.removeChild(a);
   1563:             URL.revokeObjectURL(objectUrl);
   1564:         } catch (err) {
   1565:             console.error('Download failed:', err);
   1566:             showToast(t('download_err', '?????? ??????????'));
   1567:             window.open(targetDownloadUrl, '_blank');
   1568:         } finally {
   1569:             btn.textContent = originalText;
   1570:             btn.style.pointerEvents = 'auto';
   1571:         }
   1572:     },
   1573:     showCurrentFileTags() {
   1574:         const item = this.playlist[this.currentIndex];
   1575:         if (!item || !item.file) return;
   1576:         let fileId = item.file.original_file_id;
-> 1577:         const targetItemUrl = item.file.original_file_id ? `/files/${item.file.original_file_id}` : item.file.original_url;
   1578:         if (!fileId && targetItemUrl) {
   1579:             try {
   1580:                 const filename = targetItemUrl.split('/').pop().split('?')[0];
   1581:                 const parts = filename.split('.');
   1582:                 if (parts.length > 1) parts.pop();
   1583:                 fileId = parts.join('.');
   1584:             } catch (e) {
   1585:                 console.error('Failed to extract file ID:', e);
   1586:             }
   1587:         }
   1588: 
   1589:         if (!fileId) {
   1590:             if (typeof showToast === 'function') showToast(t('err_no_file_id', 'File ID ?? ??????, ???? ??????????'));
   1591:             return;
   1592:         }
--- Line 11013 ---
   10998:         let audiosHtml = '';
   10999:         if (data.content.files?.length) {
   11000:             // NEW: Censorship check
   11001:             const isCensored = data.content.is_censored === true;
   11002:             const blurClass = isCensored ? ' blurred-media' : '';
   11003: 
   11004:             let imgContent = '';
   11005:             let audContent = '';
   11006:             data.content.files.forEach(f => {
   11007:                 let mirrorAttrs = '';
   11008:                 if (f._mirrors) {
   11009:                     for (const type in f._mirrors) {
   11010:                         mirrorAttrs += ` data-mirror-${type}="${esc(f._mirrors[type])}"`;
   11011:                     }
   11012:                 }
-> 11013:                 const url = (f.original_file_id ? `/files/${f.original_file_id}` : f.original_url) || "";
   11014:                 let thumbCandidate = (f.thumbnail_file_id ? `/files/${f.thumbnail_file_id}` : (f.original_file_id ? `/files/${f.original_file_id}` : (f.thumbnail_url || f.original_url))) || null;
   11015: 
   11016:                 if (typeof FailedMediaCache !== 'undefined' && url && FailedMediaCache.isFailed(url)) {
   11017:                     imgContent += `<div class="file-thumb broken-media" style="display:flex; align-items:center; justify-content:center; width:100%; height:100%; min-height:80px; background:#1e1e1e; color:#888; font-size:1.2em;">?? Media Unavailable</div>`;
   11018:                     return;
   11019:                 }
   11020:                 if (thumbCandidate && typeof FailedMediaCache !== 'undefined' && FailedMediaCache.isFailed(thumbCandidate)) {
   11021:                     thumbCandidate = url;
   11022:                 }
   11023:                 const isVideoType = ['video', 'gif', 'animation', 'video_note'].includes(f.type);
   11024:                 if (!thumbCandidate) {
   11025:                     if (isVideoType) {
   11026:                         thumbCandidate = '';
   11027:                     } else {
   11028:                         thumbCandidate = url; 
--- Line 11014 ---
   10999:         if (data.content.files?.length) {
   11000:             // NEW: Censorship check
   11001:             const isCensored = data.content.is_censored === true;
   11002:             const blurClass = isCensored ? ' blurred-media' : '';
   11003: 
   11004:             let imgContent = '';
   11005:             let audContent = '';
   11006:             data.content.files.forEach(f => {
   11007:                 let mirrorAttrs = '';
   11008:                 if (f._mirrors) {
   11009:                     for (const type in f._mirrors) {
   11010:                         mirrorAttrs += ` data-mirror-${type}="${esc(f._mirrors[type])}"`;
   11011:                     }
   11012:                 }
   11013:                 const url = (f.original_file_id ? `/files/${f.original_file_id}` : f.original_url) || "";
-> 11014:                 let thumbCandidate = (f.thumbnail_file_id ? `/files/${f.thumbnail_file_id}` : (f.original_file_id ? `/files/${f.original_file_id}` : (f.thumbnail_url || f.original_url))) || null;
   11015: 
   11016:                 if (typeof FailedMediaCache !== 'undefined' && url && FailedMediaCache.isFailed(url)) {
   11017:                     imgContent += `<div class="file-thumb broken-media" style="display:flex; align-items:center; justify-content:center; width:100%; height:100%; min-height:80px; background:#1e1e1e; color:#888; font-size:1.2em;">?? Media Unavailable</div>`;
   11018:                     return;
   11019:                 }
   11020:                 if (thumbCandidate && typeof FailedMediaCache !== 'undefined' && FailedMediaCache.isFailed(thumbCandidate)) {
   11021:                     thumbCandidate = url;
   11022:                 }
   11023:                 const isVideoType = ['video', 'gif', 'animation', 'video_note'].includes(f.type);
   11024:                 if (!thumbCandidate) {
   11025:                     if (isVideoType) {
   11026:                         thumbCandidate = '';
   11027:                     } else {
   11028:                         thumbCandidate = url; 
   11029:                     }
--- Line 11035 ---
   11020:                 if (thumbCandidate && typeof FailedMediaCache !== 'undefined' && FailedMediaCache.isFailed(thumbCandidate)) {
   11021:                     thumbCandidate = url;
   11022:                 }
   11023:                 const isVideoType = ['video', 'gif', 'animation', 'video_note'].includes(f.type);
   11024:                 if (!thumbCandidate) {
   11025:                     if (isVideoType) {
   11026:                         thumbCandidate = '';
   11027:                     } else {
   11028:                         thumbCandidate = url; 
   11029:                     }
   11030:                 }
   11031:                 const safeThumb = thumbCandidate ? esc(thumbCandidate) : '';
   11032:                 const safeUrl = esc(url);
   11033:                 const filename = esc(f.filename || t('default_filename'));
   11034:                 const placeholder = "data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7";
-> 11035:                 const fileIdAttr = f.original_file_id ? `data-file-id="${esc(f.original_file_id)}"` : '';
   11036: 
   11037:                 // Moved up to be available for video_note as well
   11038:                 let blurHtml = '';
   11039:                 if (f.blurhash) {
   11040:                     blurHtml = `<canvas class="blurhash-canvas" data-hash="${esc(f.blurhash)}" width="32" height="32"></canvas>`;
   11041:                 }
   11042:                 let bayanHtml = '';
   11043:                 const dCount = parseInt(f.dupe_count || 0);
   11044:                 if (dCount > 1) {
   11045:                     bayanHtml = `<div class="bayan-text" title="???? ???? ??? ?????????? ${dCount} ???">?? ${t('label_duplicate', '??????')} (${dCount})</div>`;
   11046:                 }
   11047: 
   11048:                 if (['audio', 'voice'].includes(f.type)) {
   11049:                      const uniqueId = `audio-${data.id}-${Math.random().toString(36).substr(2, 5)}`;
   11050:                      const audioInfo = AudioManager.parseAudioName(filename);
--- Line 11250 ---
   11235:     createCatalogCard(data) {
   11236:         const esc = (str) => String(str || "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
   11237:         const threadId = data.id;
   11238:         const hue = (parseInt(threadId) * 137) % 360;
   11239:         const bgColor = `hsl(${hue}, 60%, 40%)`;
   11240:         
   11241:         // Censor check for catalog
   11242:         const isCensored = data.content.is_censored === true;
   11243:         const blurClass = isCensored ? ' blurred-media' : '';
   11244: 
   11245:         let thumbHtml = '';
   11246:         const files = data.content.files || [];
   11247:         if (files.length > 0) {
   11248:             const f = files[0];
   11249:             const isVid = ['video', 'gif', 'animation', 'video_note'].includes(f.type);
-> 11250:             const mediaUrl = (f.original_file_id ? `/files/${f.original_file_id}` : f.original_url) || "";
   11251:             const thumbUrl = (f.thumbnail_file_id ? `/files/${f.thumbnail_file_id}` : (f.original_file_id ? `/files/${f.original_file_id}` : (f.thumbnail_url || f.original_url))) || "";
   11252:             
   11253:             if (typeof FailedMediaCache !== 'undefined' && ((mediaUrl && FailedMediaCache.isFailed(mediaUrl)) || (thumbUrl && FailedMediaCache.isFailed(thumbUrl)))) {
   11254:                 thumbHtml = `<div class="catalog-thumb broken-media" style="background-color: #1e1e1e; display:flex; align-items:center; justify-content:center;"><span style="font-size:2em">??</span></div>`;
   11255:             } else if (isVid) {
   11256:                 const vidUrl = mediaUrl;
   11257:                 const posterUrl = thumbUrl || mediaUrl;
   11258:                 if (vidUrl) {
   11259:                     thumbHtml = `
   11260:                         <div class="catalog-thumb lazy-media-wrapper" data-src="${vidUrl}" data-file-id="${f.original_file_id || ''}" data-type="video" style="background-color: #000;">
   11261:                             <video class="lazy-load${blurClass}" preload="metadata" muted playsinline loop data-src="${vidUrl}" ${posterUrl ? `poster="${posterUrl}"` : ''} style="width: 100%; height: 100%; object-fit: cover;"></video>
   11262:                             <span class="lazy-badge" style="position:absolute; bottom:5px; right:5px; background:rgba(0,0,0,0.6); color:white; padding:2px 4px; font-size:10px; border-radius:3px;">VIDEO</span>
   11263:                         </div>`;
   11264:                 } else {
   11265:                      thumbHtml = `<div class="catalog-thumb" style="background-color: ${bgColor}; display:flex; align-items:center; justify-content:center;"><span style="font-size:2em">?</span></div>`;
--- Line 11251 ---
   11236:         const esc = (str) => String(str || "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
   11237:         const threadId = data.id;
   11238:         const hue = (parseInt(threadId) * 137) % 360;
   11239:         const bgColor = `hsl(${hue}, 60%, 40%)`;
   11240:         
   11241:         // Censor check for catalog
   11242:         const isCensored = data.content.is_censored === true;
   11243:         const blurClass = isCensored ? ' blurred-media' : '';
   11244: 
   11245:         let thumbHtml = '';
   11246:         const files = data.content.files || [];
   11247:         if (files.length > 0) {
   11248:             const f = files[0];
   11249:             const isVid = ['video', 'gif', 'animation', 'video_note'].includes(f.type);
   11250:             const mediaUrl = (f.original_file_id ? `/files/${f.original_file_id}` : f.original_url) || "";
-> 11251:             const thumbUrl = (f.thumbnail_file_id ? `/files/${f.thumbnail_file_id}` : (f.original_file_id ? `/files/${f.original_file_id}` : (f.thumbnail_url || f.original_url))) || "";
   11252:             
   11253:             if (typeof FailedMediaCache !== 'undefined' && ((mediaUrl && FailedMediaCache.isFailed(mediaUrl)) || (thumbUrl && FailedMediaCache.isFailed(thumbUrl)))) {
   11254:                 thumbHtml = `<div class="catalog-thumb broken-media" style="background-color: #1e1e1e; display:flex; align-items:center; justify-content:center;"><span style="font-size:2em">??</span></div>`;
   11255:             } else if (isVid) {
   11256:                 const vidUrl = mediaUrl;
   11257:                 const posterUrl = thumbUrl || mediaUrl;
   11258:                 if (vidUrl) {
   11259:                     thumbHtml = `
   11260:                         <div class="catalog-thumb lazy-media-wrapper" data-src="${vidUrl}" data-file-id="${f.original_file_id || ''}" data-type="video" style="background-color: #000;">
   11261:                             <video class="lazy-load${blurClass}" preload="metadata" muted playsinline loop data-src="${vidUrl}" ${posterUrl ? `poster="${posterUrl}"` : ''} style="width: 100%; height: 100%; object-fit: cover;"></video>
   11262:                             <span class="lazy-badge" style="position:absolute; bottom:5px; right:5px; background:rgba(0,0,0,0.6); color:white; padding:2px 4px; font-size:10px; border-radius:3px;">VIDEO</span>
   11263:                         </div>`;
   11264:                 } else {
   11265:                      thumbHtml = `<div class="catalog-thumb" style="background-color: ${bgColor}; display:flex; align-items:center; justify-content:center;"><span style="font-size:2em">?</span></div>`;
   11266:                 }
--- Line 11260 ---
   11245:         let thumbHtml = '';
   11246:         const files = data.content.files || [];
   11247:         if (files.length > 0) {
   11248:             const f = files[0];
   11249:             const isVid = ['video', 'gif', 'animation', 'video_note'].includes(f.type);
   11250:             const mediaUrl = (f.original_file_id ? `/files/${f.original_file_id}` : f.original_url) || "";
   11251:             const thumbUrl = (f.thumbnail_file_id ? `/files/${f.thumbnail_file_id}` : (f.original_file_id ? `/files/${f.original_file_id}` : (f.thumbnail_url || f.original_url))) || "";
   11252:             
   11253:             if (typeof FailedMediaCache !== 'undefined' && ((mediaUrl && FailedMediaCache.isFailed(mediaUrl)) || (thumbUrl && FailedMediaCache.isFailed(thumbUrl)))) {
   11254:                 thumbHtml = `<div class="catalog-thumb broken-media" style="background-color: #1e1e1e; display:flex; align-items:center; justify-content:center;"><span style="font-size:2em">??</span></div>`;
   11255:             } else if (isVid) {
   11256:                 const vidUrl = mediaUrl;
   11257:                 const posterUrl = thumbUrl || mediaUrl;
   11258:                 if (vidUrl) {
   11259:                     thumbHtml = `
-> 11260:                         <div class="catalog-thumb lazy-media-wrapper" data-src="${vidUrl}" data-file-id="${f.original_file_id || ''}" data-type="video" style="background-color: #000;">
   11261:                             <video class="lazy-load${blurClass}" preload="metadata" muted playsinline loop data-src="${vidUrl}" ${posterUrl ? `poster="${posterUrl}"` : ''} style="width: 100%; height: 100%; object-fit: cover;"></video>
   11262:                             <span class="lazy-badge" style="position:absolute; bottom:5px; right:5px; background:rgba(0,0,0,0.6); color:white; padding:2px 4px; font-size:10px; border-radius:3px;">VIDEO</span>
   11263:                         </div>`;
   11264:                 } else {
   11265:                      thumbHtml = `<div class="catalog-thumb" style="background-color: ${bgColor}; display:flex; align-items:center; justify-content:center;"><span style="font-size:2em">?</span></div>`;
   11266:                 }
   11267:             } else {
   11268:                 const imgUrl = thumbUrl || mediaUrl;
   11269:                 if (imgUrl) {
   11270:                     thumbHtml = `<div class="catalog-thumb"><img src="data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7" data-src="${imgUrl}" class="lazy-load${blurClass}" style="width: 100%; height: 100%; object-fit: cover;" referrerpolicy="no-referrer"></div>`;
   11271:                 } else {
   11272:                     thumbHtml = `<div class="catalog-thumb" style="background-color: ${bgColor}; display:flex; align-items:center; justify-content:center;"><span style="font-size:2em">??</span></div>`;
   11273:                 }
   11274:             }
   11275:         } else {
--- Line 12427 ---
   12412:         this.metaEl = document.getElementById('ri-meta');
   12413:         this.linkBtn = document.getElementById('ri-link-btn');
   12414:         this.prevBtn = document.getElementById('ri-prev-btn');
   12415:         this.nextBtn = document.getElementById('ri-next-btn');
   12416:         this.prevBtnMobile = document.getElementById('ri-prev-btn-mobile');
   12417:         this.nextBtnMobile = document.getElementById('ri-next-btn-mobile');
   12418:         this.linkBtnMobile = document.getElementById('ri-link-btn-mobile');
   12419:         this.tagsBtn = document.getElementById('ri-tags-btn');
   12420:         this.dlBtn = document.getElementById('ri-download-btn');
   12421: 
   12422:         if (this.tagsBtn) {
   12423:             this.tagsBtn.onclick = (e) => {
   12424:                 e.stopPropagation();
   12425:                 if (this.currentData) {
   12426:                     const fid = this.currentData.file_id || 
-> 12427:                                 this.currentData.original_file_id || 
   12428:                                 (this.currentData.src ? (this.currentData.src.match(/\/files\/([^\/]+)/)?.[1]) : null);
   12429:                     
   12430:                     GalleryManager.items = [{ url: this.currentData.src, fileId: fid }];
   12431:                     GalleryManager.index = 0;
   12432:                     GalleryManager.showTagsModal();
   12433:                 }
   12434:             };
   12435:         }
   12436: 
   12437:         if (this.dlBtn) {
   12438:             this.dlBtn.onclick = async (e) => {
   12439:                 e.preventDefault();
   12440:                 e.stopPropagation();
   12441:                 if (!this.currentData) return;
   12442: 
--- Line 13824 ---
   13809:             const grid = document.querySelector('.gallery-grid');
   13810:             images.forEach(img => {
   13811:                 const wrapper = document.createElement('div');
   13812:                 wrapper.className = 'gallery-item-wrapper';
   13813:                 
   13814:                 const thumb = document.createElement('div');
   13815:                 thumb.className = 'gallery-thumb';
   13816:                 
   13817:                 const link = document.createElement('a');
   13818:                 link.href = `/${img.parent_board_id}/res/${img.parent_post_id}.html#post-${img.parent_post_id}`;
   13819:                 link.className = 'file-thumb';
   13820:                 link.dataset.filename = img.filename;
   13821:                 
   13822:                 const image = document.createElement('img');
   13823:                 image.className = 'post-image lazy-load';
-> 13824:                 image.dataset.src = (img.thumbnail_file_id ? `/files/${img.thumbnail_file_id}` : (img.original_file_id ? `/files/${img.original_file_id}` : (img.thumbnail_url || img.original_url)));
   13825:                 image.src = "data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7";
   13826:                 
   13827:                 link.appendChild(image);
   13828:                 thumb.appendChild(link);
   13829:                 wrapper.appendChild(thumb);
   13830:                 grid.appendChild(wrapper);
   13831:                 
   13832:                 if (typeof SmartLoader !== 'undefined') SmartLoader.scan(wrapper);
   13833:             });
   13834:             
   13835:             if (typeof GalleryManager !== 'undefined') GalleryManager.updatePlaylist();
   13836:             
   13837:         } catch (e) {
   13838:             console.error("Tag scroll error:", e);
   13839:         } finally {