/*
............/´¯/)................(\¯`\..................
.........../...//....ЗДОХНИ.......\\...\................
........../...//......ФАШИСТ.......\\...\...............
...../´¯/..../´¯\.ЕБАНЫй......../¯` \....\¯`\............
.././.../..../..../.|_......._|.\....\....\...\.\.........
(.(....(....(..../..)..)…...(..(.\....)....)....).).......
.\................\/.../......\...\/................/......
..\.................. /.........\................../......







    ⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⣀⣠⡀⠤⠤⡤⠤⣀⡄⣀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⣀⠴⣋⡭⠴⠌⠃⠹⠴⠧⣒⡴⢡⠛⡢⣄⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢀⣴⠝⠊⠁⠀⠀⠀⠀⠀⠀⠀⠀⠈⠑⠫⣔⢢⢳⡄⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⡾⠃⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠻⡤⢻⡄⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠃⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠘⣆⢳⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢸⡍⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⣤⣤⣄⠀⠀⠀⠀⠀⣀⣀⡀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢾⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⡇⢰⠀⣇⣀⣀⣀⣠⣫⣔⣩⣓⣤⣀⣀⣀⣀⠀⠀⠀⠀⠀⠀⢀⡏⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢀⡠⠤⡔⣒⢒⠳⡌⣍⠻⡇⢨⠐⡏⣧⠙⡁⣧⣹⠁⡏⢸⢹⡌⢏⢹⣌⡗⣄⠢⡔⢠⠒⡞⠤⢤⣀⡀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⢀⣠⠴⡒⢭⠣⣙⢢⠱⣌⣪⠵⢖⡨⠱⣇⠸⢀⣇⠛⢠⠄⣿⢿⠐⠇⢸⡸⠃⣎⢸⣹⡿⣶⢷⢾⡇⡹⢌⠓⡆⢦⢩⡑⠲⠤⣀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⢀⣤⠲⢍⡱⢊⡱⣊⢴⡳⢛⡍⠳⣌⠚⡤⢣⢍⡛⣛⠫⣙⢛⢻⣟⡋⣿⠛⡇⢸⡙⢏⡛⢻⡘⣛⠛⣛⢛⢣⡑⢎⡱⢚⠤⢣⠜⣡⢋⡌⡓⢤⡀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⣠⣔⠯⠔⠓⠊⣑⠛⠉⢰⢅⢣⠣⢜⡱⢌⠳⣌⠣⢎⠴⣡⠓⡌⢎⣽⠀⠓⠼⣳⡖⠯⡙⢢⡙⢢⠱⡌⠳⡌⢎⠦⣉⠦⡱⣉⢎⡱⢊⡕⢪⠔⡩⢆⡹⢦⡀⠀⠀⠀⠀⠀⠀⠀
⠘⠉⠀⠀⠀⡠⠊⠀⠀⠀⠀⠉⢚⠇⠺⠐⠉⠖⠌⠓⠬⢒⣥⢙⡘⢆⡚⠀⠀⠀⠀⠈⠉⠙⠃⠘⠃⠳⠌⠳⣘⠌⡖⡡⢎⡑⢆⠎⡔⢣⠜⣡⢚⡱⢊⠴⣡⠙⣦⠀⠀⠀⠀⠀⠀
⠀⠀⠀⡠⠊⠀⠀⠀⠀⠀⠀⣰⡁⣠⠠⢤⢄⡒⢢⠒⣌⢣⡏⢦⢙⡂⣯⡀⢤⠠⣄⠢⡔⣂⠖⣒⢲⡒⡔⣒⠰⢢⠌⡅⢎⡜⣌⠚⣌⢣⠚⡤⢣⠜⣩⠒⡥⡙⡌⢧⠀⠀⠀⠀⠀
⠀⢠⠊⠀⠀⢀⣀⠤⢔⣲⠯⡑⢦⢡⢋⠖⣌⠎⡥⢋⡔⣟⡘⢆⠣⣜⢾⢌⢣⠣⣌⠳⡇⡜⡸⢄⡟⡦⠱⣌⢣⢃⠞⡘⢦⡘⢤⢋⡔⢣⡙⡔⢣⠚⡔⣫⠔⡱⢌⡍⢧⠀⠀⠀⠀
⡠⢁⢠⠔⣪⠱⣈⠞⡼⣃⢎⡱⢊⠦⣉⠞⢤⢋⡔⢣⢼⡣⠜⣌⢣⡝⢸⠌⣆⠳⣌⢺⡕⡬⡑⢎⡇⢣⡓⡔⣊⣮⢘⡱⢢⠍⣆⠣⡜⣡⠚⣌⢣⡙⡌⣷⢊⡕⢪⠜⡌⡇⠀⠀⠀
⢔⡉⢦⡙⣄⣳⢇⡾⡓⡌⢦⠱⣉⡖⣡⠚⡤⢣⠜⣡⣾⢡⢋⡔⣺⠀⢸⡘⢤⠓⡌⢾⣱⢻⡘⢼⠁⢸⡔⡱⢸⣟⡦⣑⠣⡚⢤⠓⡜⢤⢋⠤⣷⠰⡡⣟⢢⠜⣡⠚⡔⣋⠀⠀⠀
⠬⣘⡢⠕⡽⣑⡾⢡⠓⡜⢢⢓⡼⢱⢡⢋⡔⢣⠚⣴⣿⣢⣧⣜⡇⠀⠐⡏⢦⡙⣌⡻⣼⣻⠘⡼⠀⠈⡧⣱⠶⢾⢻⡤⣧⣙⠤⣋⠜⡢⢍⠲⣹⡇⡱⢿⡌⢎⠥⣋⠴⣹⠀⠀⠀
⡶⠋⠀⡞⡱⣾⢡⢣⡙⣌⠣⣾⢃⠇⣎⠲⣌⠣⣽⣟⣿⢡⠒⣼⠀⠈⠀⣿⠰⡘⢤⡟⣵⣯⠘⡷⠀⠀⢳⠡⢎⢽⠨⣳⡔⣩⠻⣦⡙⠴⣉⠖⣹⣧⠱⣿⡘⡌⢖⡡⢒⡱⡆⠀⠀
⠀⠀⣼⢡⣿⢃⠎⢦⠱⢌⣲⡏⡜⡸⢄⠳⣌⠱⣿⢮⣿⢄⢫⡇⠀⠀⠀⢱⢣⡙⢆⡿⣱⢿⣈⡇⠀⠀⠘⣧⢊⢼⠀⠣⢿⡠⢓⠬⣑⠣⡜⡘⢼⣧⣹⢿⠰⣉⠦⣑⢣⠒⡇⠀⠀
⠀⣸⢣⠋⡜⣌⠺⡰⣉⢶⣿⡘⠴⡑⢎⡱⢌⣹⢷⡿⢸⢌⣺⠀⠀⠀⠀⠈⡧⠜⣌⠷⣏⣿⣸⠁⠀⠀⠀⢹⡌⢲⠄⠀⠭⢷⣉⠖⣡⠓⡬⡑⢎⣽⣛⣾⠱⣌⠲⡡⢎⡱⡇⠀⠀
⠀⣮⠊⢰⠱⣌⢣⠱⣌⡿⣧⡘⢥⡙⢦⡑⢎⣾⣻⠱⠸⡆⡷⠀⠀⠀⠀⠀⢹⡜⡌⣿⢺⣼⣻⠀⠀⠀⠀⠘⣯⢸⡇⠀⠈⢎⢷⡜⢤⢋⠴⣉⠆⣿⣟⣾⢑⡎⡱⡑⢎⡔⡇⠀⠀
⣸⠃⠀⣨⠱⣌⢒⠣⣼⡻⣇⠜⣢⠙⡴⡘⢬⣷⡏⠃⠀⡧⠇⠀⠀⠀⠀⠀⠈⢷⢌⡳⢯⣾⡝⠀⠀⠀⠀⠀⠹⡼⡁⠀⠀⠈⢊⢿⢠⢋⠖⣡⠚⣼⣻⣼⢾⡡⢣⡙⢢⠜⡇⠀⠀
⡇⠀⠀⢹⠶⡈⡎⢱⣏⢷⡇⠾⣀⠏⣰⠉⣾⣹⠹⠀⠰⢿⠀⠀⠶⠶⠶⡀⠀⠈⣶⢹⡷⣾⡇⠀⠀⠀⠀⣈⣁⣹⡷⠆⠰⠆⢀⡎⣇⡉⠾⣀⠏⣰⢿⣾⢿⣰⢇⡸⢁⡎⡇⠀⠀
⠁⠀⠀⢻⡰⢱⡘⣹⣎⢺⡇⠳⣌⠚⡤⡙⣶⡟⣷⣶⣶⣿⣿⣿⣿⣿⣿⠏⠀⠀⠸⣧⢟⡶⡇⠀⠀⠀⠀⠺⣿⣿⣿⣿⣶⣶⣦⣴⣽⣮⣱⢌⡚⢬⣿⢾⣯⠿⣠⢃⠧⣘⠃⠀⠀
⠀⠀⠀⣳⢌⠣⡜⡹⢤⠇⣏⠳⣈⠳⡰⢡⣿⠁⣿⠀⣸⣿⣿⣿⣿⣿⣿⠄⠀⠀⠀⠹⣯⢞⡇⠀⠀⠀⠀⠀⣾⣿⣿⣿⣿⣿⣿⣿⠛⣿⡟⠢⢍⠆⣿⣿⣺⡗⠤⢣⡚⢼⠀⠀⠀
⠀⠀⠀⢺⢨⠱⡌⡱⡞⠀⢡⡓⣌⢣⠱⡡⣾⡆⠙⡇⠈⠀⠈⣿⣾⣿⣾⠀⠀⠀⠀⠀⠹⣯⡇⠀⠀⠀⠀⠀⠁⠀⢹⣷⣿⣯⣿⡿⢀⡏⣿⡑⢪⠜⣽⡿⡽⠮⣕⠩⢷⢺⠀⠀⠀
⠀⠀⠀⢸⡄⠳⡌⡽⠀⠀⠘⡕⡌⢆⢣⡑⣿⣻⡀⠰⠘⣤⡶⢿⣿⡾⣿⡀⠀⠀⠀⠀⠀⠹⣷⠀⠀⠀⠀⠀⣶⣤⣾⣽⠷⠻⣷⡇⢈⠀⣿⡏⢦⠙⣺⣿⣃⡀⠘⣧⠚⣯⠀⠀⠀
⠀⠀⠀⠈⣇⠳⢌⡇⠀⠀⠀⢹⡘⡌⢦⢹⣿⡹⣇⠀⠀⢻⣇⠀⠁⢂⣼⠆⠀⠀⠀⠀⠀⠀⠹⠄⠀⠀⠀⠀⢻⣿⠀⠀⠁⢠⡿⢀⠂⢸⡷⣻⡆⡹⢼⡟⠉⠁⠀⣹⢮⡹⡄⠀⠀
⠀⠀⠀⠀⢹⡘⢼⠁⠀⠀⠀⠈⣷⠘⢦⢹⣧⢻⣽⡀⠀⠀⠻⠿⠚⠛⠉⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠈⠛⠳⠶⠾⠿⠇⠁⠀⣸⡟⣵⣿⠰⣹⡇⠀⠀⠀⡼⢂⣷⡵⡀⠀
⠀⠀⠀⠀⠀⢏⡺⠀⠀⠀⠀⠀⠸⣍⢆⣻⢾⡹⣞⡇⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠇⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢠⡀⠀⡿⣞⡵⢾⣗⢰⡇⠀⢀⡼⢕⠣⡇⠱⣧⠀
⠀⠀⠀⠀⠀⠘⡅⠀⠀⠀⠀⠀⠀⢸⡆⣿⢫⢷⣹⣧⠀⠀⠀⠀⠀⠀⠀⠀⠀⣄⣀⠤⢄⣀⢀⣀⣄⠀⠀⠀⠀⠀⠀⠀⠀⠀⠈⠀⢰⣟⢧⣻⢭⣿⡸⣇⠺⣍⠒⣍⠲⡅⠀⠘⠀
⠀⠀⠀⠀⠀⠀⠉⠀⠀⠀⠀⠀⠀⢸⡹⣼⣯⢳⢧⢿⡀⠀⠀⠀⠀⠀⠀⠀⠀⢻⠁⢎⠰⡀⢆⠰⠈⢧⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢸⣏⠿⣜⣳⢞⣿⡇⢣⠆⡍⢆⢣⡇⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢸⠰⣿⢏⡟⣮⢯⢹⣶⣄⡀⠀⠀⠀⠀⠀⠘⣧⠌⣂⠱⡈⢆⠩⣘⠆⠀⠀⠀⠀⠀⠀⣀⣴⣶⡿⣝⢯⣝⣮⣛⣿⣿⢠⢋⡜⡌⢆⡇⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢸⠱⣹⢾⡹⢧⢿⢸⣿⡾⣹⢟⣶⣤⣤⣀⡀⠈⠳⢄⠢⡁⢆⢡⠞⠀⠀⠀⣀⣤⢶⣞⡟⣿⢾⣻⡝⣺⢞⡶⣻⣳⡟⢤⠣⡜⣘⠲⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠘⡴⡙⣮⢻⣝⡾⢸⣷⡿⣝⡾⠟⠾⠞⠷⠿⠻⢶⣦⡌⢭⠬⡁⠤⠐⢊⠉⢼⣟⣾⣼⣹⢻⣟⣳⢍⡿⣎⢷⣻⣗⣻⢆⠳⡘⢤⢋⠄⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⡧⣉⣷⡻⣼⣹⡾⠗⠋⠀⠀⣈⠐⠄⣂⠰⢀⠂⠄⡙⠿⣒⣆⠄⣁⠂⠌⢼⣿⣳⣻⠽⣿⡿⢋⣼⡻⣼⢫⣿⡼⣽⡎⡱⣉⠦⣉⡆⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢰⡗⢤⠹⣷⡽⠋⠀⠀⠀⡀⠰⢀⠊⠔⡀⠒⠤⢈⠔⡀⢂⠙⢦⠛⡲⡬⢀⠚⠻⢿⣫⢝⡿⢡⢫⡷⣝⡧⣟⡿⣜⣳⢿⡑⢤⠓⡤⡇⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⣼⣏⠦⣉⡿⠁⠌⡑⠒⡈⠤⠁⡄⠌⡐⠠⢉⠐⡀⠆⡈⠤⠈⠌⣷⡌⣳⠄⡈⢤⡿⡜⣾⢡⢃⣿⢳⡝⡾⣽⡟⡽⣎⣿⡎⠦⣙⠰⡇⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⣿⣳⠜⣼⠁⠌⡐⠠⡁⠰⢀⠡⠐⡐⠠⢁⠂⠰⠠⠘⣀⠂⢅⣒⣼⡙⡏⠀⢤⡿⢼⣹⡏⢦⣹⢷⣫⢽⡳⣟⣻⢿⣼⢞⣿⠰⣡⢃⡇⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢸⣯⣽⢾⠷⡈⠄⡡⠂⢁⢂⠁⢂⠡⢀⠑⠠⠌⢂⢁⠂⠤⢘⡎⠉⠹⡑⢿⡠⣼⡝⣣⡿⡘⢦⣟⢮⡳⢯⣽⠟⢁⡤⣚⢿⣾⡇⢆⠣⡆⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢸⢶⡟⠁⠀⣇⠌⠠⠑⡀⢊⠐⡄⠒⠈⠄⡡⠐⡈⠠⠘⣀⡞⠑⢄⠀⠘⡄⢻⡧⣝⣳⠣⣍⣟⡮⢯⣝⣯⣃⡴⢏⡾⠏⢁⡠⢿⣌⠣⣇⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢸⡟⠀⡴⠉⠘⡎⠠⠑⣀⠢⠁⠄⠡⠌⡐⠠⠑⣀⠢⠁⢤⣇⠀⠈⢣⠀⢙⡀⢻⣼⢏⠲⣼⡻⣼⢻⣜⡿⣣⡽⠋⢀⡴⢯⣹⣧⣿⡂⣯⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⡚⠀⠎⠀⠀⢸⢁⠂⠅⡄⢂⠩⠐⡐⠂⠤⢁⠒⡀⠢⢁⡞⠀⢣⠀⠈⡅⠀⢃⠀⡿⣌⣽⡳⡽⣎⡷⡾⣽⠋⢀⡴⡟⢿⡧⠋⠀⢸⡃⡇⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢠⠃⢸⠀⠀⠀⣾⡳⣌⠐⢠⠁⠌⡁⠆⡉⠐⠂⡄⢁⢒⣤⡟⠀⠈⡀⠀⢸⠀⠘⠄⣻⢽⣞⣵⢻⡵⣻⠟⠁⡴⣏⣿⡽⠋⠀⠀⠀⠀⡇⡇⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⣟⡆⠁⠀⠀⢸⠀⠙⠦⣽⠲⠬⣐⣀⡢⢐⣉⣰⣴⠾⠛⠋⢹⠀⠀⢇⠀⠀⠃⠀⢀⡽⣎⣿⢮⣗⣻⠋⣠⠾⡵⣹⠞⠀⠀⠀⠀⠀⢀⡇⣗⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠐⣏⢷⡀⠀⠀⠸⡄⡁⠂⠌⠉⣷⣿⣿⣿⣿⣿⣿⣿⠛⠒⠒⠃⡇⠀⠘⠀⠀⠀⣠⢞⡳⢮⣽⢷⣺⢏⢴⣫⢳⡽⠃⠀⠀⠀⠀⠀⠀⢸⠆⡏⠤⠠⠠



*/
































console.log('main.js loaded successfully');

(function() {
    try {
        const themeColors = {
            'light': '#f8faff', 'dark': '#1a1d21', 'cyberpunk': '#0d0221', 'crypto': '#000000',
            'conan': '#0c0502', 'win95': '#008080', 'gameboy': '#8bac0f', 'adventure': '#4fc3f7',
            'luxury': '#0a0a0a', 'toxic': '#1a001a', 'shaft': '#111111', 'nord': '#2e3440',
            'gruvbox': '#282828', 'discord': '#202225', 'lain': '#f0f0f0', 'dracula': '#282a36',
            'sakura': '#fdf6f6', 'ukraine': '#e1e8ff', 'soviet': '#f5f1e7', 'paper': '#f3e9d2',
            'terminal': '#000000', 'ocean': '#0b192f', 'forest': '#2f3d34', 'gothic': '#100c13', 'noir': '#1a1a1a'
        };

        let theme = localStorage.getItem('theme');
        try {
            const settings = JSON.parse(localStorage.getItem('user_settings') || '{}');
            if (settings.theme) theme = settings.theme;
        } catch(e) {}
        if (!theme) {
            if (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) {
                theme = 'dark';
            } else {
                theme = 'light';
            }
        }

        if (theme) {
            document.documentElement.classList.add('theme-' + theme);
            const hexColor = themeColors[theme] || themeColors['light'];
            document.documentElement.style.backgroundColor = hexColor;
            const loader = document.getElementById('app-loader');
            if (loader) loader.style.backgroundColor = hexColor;
        }
    } catch (e) {
        console.error('Theme preloader failed:', e);
    }
})();

window.safeInit = (name, fn) => {
    try {
        if (typeof fn === 'function') {
            fn();
            console.log(`✅ [Init] ${name}`);
        } else {
            console.warn(`⚠️ [Init] ${name} skipped: function not found`);
        }
    } catch (e) {
        console.error(`❌ [Init] ${name} failed:`, e);
    }
};
window.alert = showToast;
window.globalVideoVolume = parseFloat(localStorage.getItem('user_volume') || '0.5');
let _volSaveTimer;
window.saveVolume = (vol) => {
    window.globalVideoVolume = vol;
    clearTimeout(_volSaveTimer);
    _volSaveTimer = setTimeout(() => {
        localStorage.setItem('user_volume', vol);
    }, 500);
};
window.addEventListener('storage', (e) => {
    if (e.key === 'user_volume') {
        window.globalVideoVolume = parseFloat(e.newValue || '0.5');
        document.querySelectorAll('video, audio').forEach(el => {
            if (!el.paused) el.volume = window.globalVideoVolume;
        });
        const slider = document.getElementById('dp-volume');
        if (slider) slider.value = window.globalVideoVolume;
    }
});
const debounce = (func, wait) => {
    let timeout;
    return function executedFunction(...args) {
        const later = () => {
            clearTimeout(timeout);
            func(...args);
        };
        clearTimeout(timeout);
        timeout = setTimeout(later, wait);
    };
};
const CONSTANTS = {
    MAX_FILE_SIZE: 40 * 1024 * 1024,
    MAX_FILES: 5,
    TOAST_DURATION: 3000,
    AUDIO_EXTENSIONS: /\.(mp3|ogg|wav|m4a|opus|flac|aac)$/i,
    YOUTUBE_REGEX: /(?:https?:\/\/)?(?:www\.)?(?:youtube\.com\/(?:[^\/\s]+\/\S+\/|(?:v|e(?:mbed)?)\/|\S*?[?&]v=)|youtu\.be\/)([a-zA-Z0-9_-]{11})/gi,
    CAPS_REGEX: /([А-ЯA-Z0-9\s\W]{5,})([.!?]|$)/g,
    HISTORY_KEY: 'viewed_threads_history',
    HIDDEN_POSTS_KEY: 'hidden_posts',
    SETTINGS_KEY: 'user_settings',
    FAV_KEY: 'favourite_threads'
};

let UI_TRANSLATIONS = { ru: {}, en: {}, jp: {} };
window.formatTextGlobal = (text, opId = null, boardId = null, threadId = null) => {
    if (!text) return "";
    let s = text.replace(/&/g, "&amp;")
                .replace(/</g, "&lt;")
                .replace(/>/g, "&gt;")
                .replace(/"/g, "&quot;")
                .replace(/'/g, "&#039;");
    
    const linkRegex = /(?<!["'=])(https?:\/\/[^\s<"']+)/g;
    s = s.replace(linkRegex, '<a href="$1" target="_blank" rel="noopener" class="auto-link">$1</a>');

    s = s.replace(
        /&gt;&gt;(\d+)/g,
        (match, num) => {
            const isOp = opId && String(num) === String(opId);
            const label = isOp ? `&gt;&gt;${num} (OP)` : `&gt;&gt;${num}`;
            const tId = threadId || opId;
            const isThreadPage = tId && location.pathname.includes(`/res/${tId}.html`);
            const href = isThreadPage ? `#post-${num}` : `/${boardId || 'b'}/res/${tId || num}.html#post-${num}`;
            
            return `<a href="${href}" class="post-link" onclick="event.stopPropagation();" data-post-num="${num}">${label}</a>`;
        }
    );

    s = s.replace(/\[b\](.*?)\[\/b\]/gis, '<b>$1</b>');
    s = s.replace(/\[i\](.*?)\[\/i\]/gis, '<i>$1</i>');
    s = s.replace(/\[s\](.*?)\[\/s\]/gis, '<s>$1</s>');
    s = s.replace(/\[u\](.*?)\[\/u\]/gis, '<u>$1</u>');
    s = s.replace(/\[code\](.*?)\[\/code\]/gis, '<code>$1</code>');
    s = s.replace(/\|\|(.*?)\|\|/gis, '<span class="spoiler">$1</span>');
    s = s.replace(/\[shake\](.*?)\[\/shake\]/gis, '<span class="effect-shake">$1</span>');
    s = s.replace(/\[rainbow\](.*?)\[\/rainbow\]/gis, '<span class="effect-rainbow">$1</span>');
    s = s.replace(/\[blur\](.*?)\[\/blur\]/gis, '<span class="effect-blur">$1</span>');
    s = s.replace(/\[glitch\](.*?)\[\/glitch\]/gis, (match, content) => {
        const clean = content.replace(/<[^>]*>/g, ''); 
        return `<span class="effect-glitch" data-text="${clean}">${content}</span>`;
    });

    s = s.split('\n').map(line => {
        const trimmed = line.trim();
        if (trimmed.startsWith('&gt;') && !trimmed.startsWith('&gt;&gt;')) {
            return `<span class="greentext">${line}</span>`;
        }
        return line;
    }).join('<br>');
    

    function closeUnclosedTags(html) {
        const div = document.createElement('div');
        div.innerHTML = html;
        return div.innerHTML;
    }
    s = closeUnclosedTags(s);

    return s;
};
async function loadTranslations() {
    try {
        const res = await fetch('/api/locales');
        if (res.ok) {
            UI_TRANSLATIONS = await res.json();
            console.log('✅ Локализация загружена из locales.py');
        }
    } catch (e) {
        console.error('Ошибка загрузки локализации:', e);
    }
}
window.t = function(key, defaultText = "") {
    const lang = document.documentElement.lang || localStorage.getItem('lang') || 'ru';
    return UI_TRANSLATIONS[lang]?.[key] || UI_TRANSLATIONS['ru'][key] || defaultText || key;
};
const vibrate = (pattern = 10) => {
    if (navigator.vibrate) navigator.vibrate(pattern);
};
const UserState = {
    userId: document.body.dataset.userId || null,
    postIds: new Set(JSON.parse(localStorage.getItem('my_post_ids') || '[]')),
    hiddenPosts: new Set(JSON.parse(localStorage.getItem(CONSTANTS.HIDDEN_POSTS_KEY) || '[]'))
};
const applyAdaptiveFontSize = (postElement) => {
    const textContainer = postElement.querySelector('.post-text-content .post-text');
    if (!textContainer) return;
    const len = textContainer.textContent.trim().length;
    postElement.classList.remove('text-size-short', 'text-size-medium', 'text-size-long');
    if (len <= 100) {
        postElement.classList.add('text-size-short');
    } else if (len > 100 && len <= 400) {
        postElement.classList.add('text-size-medium');
    } else {
        postElement.classList.add('text-size-long');
    }
};
const processEmbeds = (el) => {
    const textDiv = el.querySelector('.post-text-content') || el;
    if (textDiv.dataset.embedsProcessed) return;
    const links = textDiv.querySelectorAll('a');
    links.forEach(link => {
        const match = link.href.match(CONSTANTS.YOUTUBE_REGEX);
        if (match && match[1]) {
            const vid = match[1];
            const uid = `embed-${vid}-${Math.random().toString(36).substr(2, 5)}`;
            const wrapper = document.createElement('div');
            wrapper.className = 'youtube-embed';
            wrapper.innerHTML = `
                <div class="yt-thumb" style="background-image: url('https://img.youtube.com/vi/${vid}/hqdefault.jpg')">
                    <div class="yt-play-btn">▶</div>
                </div>
                <div id="${uid}" class="youtube-iframe-container"></div>
            `;
            wrapper.onclick = function() {
                const container = this.querySelector('.youtube-iframe-container');
                container.style.display = 'block';
                container.innerHTML = `<iframe src="https://www.youtube.com/embed/${vid}?autoplay=1" frameborder="0" allowfullscreen allow="autoplay"></iframe>`;
                this.querySelector('.yt-thumb').style.display = 'none';
                this.classList.add('active');
            };
            link.after(wrapper);
        }
    });
    textDiv.dataset.embedsProcessed = 'true';
};
const removePostFromDOM = (postNum) => {
    const post = document.getElementById(`post-${postNum}`);
    if (!post) return;
    post.classList.add('is-deleting');
    setTimeout(() => {
        const threadContainer = post.closest('.thread-container');
        if (threadContainer && post.id === `post-${threadContainer.id.replace('thread-', '')}`) {
            const separator = threadContainer.nextElementSibling;
            if (separator && separator.tagName === 'HR') separator.remove();
            threadContainer.remove();
        } else {
            post.remove();
        }
    }, 500);
};
const formatTime = (seconds) => {
    if (!seconds || !isFinite(seconds)) return "00:00";
    const m = Math.floor(seconds / 60);
    const s = Math.floor(seconds % 60);
    return `${m.toString().padStart(2, '0')}:${s.toString().padStart(2, '0')}`;
};
const formatTimestamp = (unixTimestamp) => {
    const date = new Date(unixTimestamp * 1000);
    const lang = document.documentElement.lang || 'ru';
    const currentBoard = document.body.dataset.currentBoard;
    const isWh40k = currentBoard === 'wh40k';

    const pad = (n) => n.toString().padStart(2, '0');
    const day = pad(date.getDate());
    const month = pad(date.getMonth() + 1);
    const year = date.getFullYear().toString();
    const shortYear = year.slice(-2);
    
    const hours = pad(date.getHours());
    const minutes = pad(date.getMinutes());
    const seconds = pad(date.getSeconds());
    if (isWh40k) {
        const start = new Date(date.getFullYear(), 0, 0);
        const diff = date - start;
        const oneDay = 1000 * 60 * 60 * 24;
        const dayOfYear = Math.floor(diff / oneDay);
        const checkNum = Math.floor((dayOfYear / 365) * 1000).toString().padStart(3, '0');
        return `[${hours}:${minutes}] 0.${checkNum}.${shortYear}.M42`; 
    }

    let dateStr = '';
    if (lang === 'jp') {
        dateStr = `${year}/${month}/${day}`;
    } else if (lang === 'en') {
        dateStr = `${month}/${day}/${year}`;
    } else {
        dateStr = `${day}.${month}.${year}`;
    }
    
    const timeStr = window.innerWidth <= 768 ? `${hours}:${minutes}` : `${hours}:${minutes}:${seconds}`;
    return `${dateStr} ${timeStr}`;
};
const isElementInViewport = (el) => {
    const rect = el.getBoundingClientRect();
    return (
        rect.top >= 0 && rect.left >= 0 &&
        rect.bottom <= (window.innerHeight || document.documentElement.clientHeight) &&
        rect.right <= (window.innerWidth || document.documentElement.clientWidth)
    );
};
const blobToBase64 = (blob) => {
    return new Promise((resolve, reject) => {
        const reader = new FileReader();
        reader.onloadend = () => resolve(reader.result);
        reader.onerror = reject;
        reader.readAsDataURL(blob);
    });
};
function checkIfVideoIsNote(video) {
    if (video.classList.contains('is-gif') || video.classList.contains('is-regular-video') || video.classList.contains('video-note')) return;
    const w = video.videoWidth;
    const h = video.videoHeight;
    if (w > 0 && h > 0 && Math.abs(w - h) < 10 && video.duration < 61) {
        video.classList.add('post-video', 'video-note');
        video.removeAttribute('controls');
        video.muted = true;
        video.loop = true;
        video.playsInline = true;
        video.pause();
    }
}
let notificationSound = new Audio('/static/sounds/not.mp3');
notificationSound.volume = 0.5;
function showToast(message, duration = CONSTANTS.TOAST_DURATION) {
    let container = document.getElementById('toast-container');
    if (!container) {
        container = document.createElement('div');
        container.id = 'toast-container';
        document.body.appendChild(container);
    }
    while (container.children.length >= 3) {
        container.removeChild(container.firstChild);
    }
    const existing = Array.from(container.children).find(t => t.textContent === message);
    if (existing) {
        existing.classList.remove('show');
        void existing.offsetWidth;
        existing.classList.add('show');
        return;
    }

    while (container.children.length >= 4) {
        container.id = 'toast-container';
        document.body.appendChild(container);
    }
    while (container.children.length >= 4) {
        container.removeChild(container.firstChild);
    }
    const toast = document.createElement('div');
    toast.className = 'toast';
    toast.textContent = message;
    container.appendChild(toast);
    requestAnimationFrame(() => toast.classList.add('show'));
    setTimeout(() => {
        toast.classList.remove('show');
        setTimeout(() => toast.remove(), 300);
    }, duration);
}
const sendDesktopNotification = (title, body) => {
    if (!("Notification" in window)) return;
    if (Notification.permission === "granted") {
        new Notification(title, { body, icon: '/static/icon.png' });
    } else if (Notification.permission !== "denied") {
        Notification.requestPermission().then(perm => {
            if (perm === "granted") new Notification(title, { body });
        });
    }
};
const ReadTracker = {
    init() {
        if (!location.pathname.includes('/res/')) return;
        if ('scrollRestoration' in history) {
            history.scrollRestoration = 'manual';
        }
        const pathParts = location.pathname.split('/');
        const boardId = pathParts[1];
        const threadId = pathParts[3].replace('.html', '');
        this.storageKey = `read_marker_${boardId}_${threadId}`;
        this.scrollKey = `scroll_pos_${boardId}_${threadId}`;
        if (Math.random() < 0.1) {
            try {
                Object.keys(localStorage).forEach(key => {
                    if ((key.startsWith('read_marker_') || key.startsWith('scroll_pos_')) && key !== this.storageKey && key !== this.scrollKey) {
                        if (localStorage.length > 500) localStorage.removeItem(key);
                    }
                });
            } catch(e) {}
        }
        setTimeout(() => this.restorePosition(), 100);
        const save = () => this.savePosition();
        document.addEventListener('visibilitychange', () => { if (document.hidden) save(); });
        window.addEventListener('beforeunload', save);
        document.addEventListener('click', (e) => {
            if (e.target.tagName === 'A') save();
        });
        setInterval(save, 5000);
    },
    savePosition() {
        const posts = Array.from(document.querySelectorAll('.post'));
        if (posts.length === 0) return;
        const viewportHeight = window.innerHeight;
        let lastVisibleId = null;
        for (let i = posts.length - 1; i >= 0; i--) {
            const rect = posts[i].getBoundingClientRect();
            if (rect.top < viewportHeight) {
                lastVisibleId = posts[i].id.replace('post-', '');
                break;
            }
        }
        if (lastVisibleId) {
            localStorage.setItem(this.storageKey, lastVisibleId);
        }
        localStorage.setItem(this.scrollKey, window.scrollY);
    },
    restorePosition() {
        const savedScroll = localStorage.getItem(this.scrollKey);
        if (savedScroll) {
            window.scrollTo(0, parseInt(savedScroll));
        }
        const lastReadId = localStorage.getItem(this.storageKey);
        if (!lastReadId) return;
        const posts = Array.from(document.querySelectorAll('.post'));
        if (posts.length === 0) return;
        const firstUnread = posts.find(p => {
            const pid = parseInt(p.id.replace('post-', ''));
            return pid > parseInt(lastReadId);
        });
        if (firstUnread) {
            const sep = document.createElement('div');
            sep.className = 'unread-separator';
            sep.innerHTML = '<span>' + t('unread_separator_text', 'Новые сообщения') + '</span>';
            firstUnread.before(sep);
            if (!savedScroll) {
                setTimeout(() => {
                    sep.scrollIntoView({ behavior: 'auto', block: 'center' });
                }, 150);
            }
        }
    }
};
const BackButtonManager = {
    stack: [],
    init() {
        window.addEventListener('popstate', (e) => {
            if (this.stack.length > 0) {
                const mode = this.stack.pop();
                this.closeWindow(mode);
            }
        });
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape' && this.stack.length > 0) {
                e.preventDefault();
                history.back();
            }
        });
    },
    push(mode) {
        if (this.stack.length > 0 && this.stack[this.stack.length - 1] === mode) {
            return;
        }
        this.stack.push(mode);
        history.pushState({ modalOpen: mode }, "", "");
    },
    closeWindow(mode) {
        if (mode === 'gallery' && typeof GalleryManager !== 'undefined') GalleryManager.close(true);
        if (mode === 'media' && typeof MediaStreamManager !== 'undefined') MediaStreamManager.close();
        if (mode === 'mediawall' && typeof MediaWallManager !== 'undefined') MediaWallManager.close();
        if (mode === 'form' && typeof FormManager !== 'undefined') FormManager.hideFloating(true);
        if (mode === 'videopopup' && typeof VideoPopupManager !== 'undefined') VideoPopupManager.close(true);
        if (mode === 'modal') closeModal();
    },
    triggerClose(mode) {
        if (this.stack.length > 0 && this.stack[this.stack.length - 1] === mode) {
            history.back();
        } else {
            this.closeWindow(mode);
        }
    }
};
const AlertManager = {
    init() {
        if (document.body.dataset.userId) {
            this.check();
            setInterval(() => {
                if (!document.hidden) this.check();
            }, 300000);
        }
    },
    async check() {
        try {
            const res = await fetch('/api/my-alerts');
            if (!res.ok) return;
            const alerts = await res.json();
            if (alerts && alerts.length > 0) {
                this.show(alerts[0]);
            }
        } catch (e) {
            console.error("Alert check failed", e);
        }
    },
    show(alert) {
        if (document.getElementById('system-alert-modal')) return;
        const safeContent = window.formatTextGlobal(alert.content);
        
        const modalHtml = `
            <div id="system-alert-modal" class="modal" style="display:block; z-index: 10000; border: 2px solid var(--accent-primary); box-shadow: 0 0 20px rgba(0,0,0,0.5);">
                <h3 style="color: var(--accent-primary); margin-top:0;">${t('alert_title')}</h3>
                ${alert.image_url ? `<img src="${alert.image_url}" referrerpolicy="no-referrer" style="max-width:100%; border-radius:4px; margin-bottom:10px;">` : ''}
                <div style="font-size: 1.1em; margin-bottom: 20px; line-height: 1.5;">
                    ${safeContent}
                </div>
                ${alert.btn_text ? `
                    <a href="${alert.btn_link || '#'}" target="_blank" class="btn btn-primary" style="width:100%; display:block; text-align:center; margin-bottom:10px;">
                        ${alert.btn_text}
                    </a>
                ` : ''}
                <button id="alert-close-btn" class="btn btn-secondary" style="width: 100%;">${t('alert_read_btn')}</button>
            </div>
            <div id="system-alert-overlay" class="modal-overlay" style="display:block; z-index: 9999;"></div>
        `;
        document.body.insertAdjacentHTML('beforeend', modalHtml);
        document.body.style.overflow = 'hidden';
        document.getElementById('alert-close-btn').onclick = async () => {
            try {
                await fetch('/api/my-alerts/read', {
                    method: 'POST',
                    headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify({ alert_id: alert.id })
                });
            } catch(e) {}
            document.getElementById('system-alert-modal').remove();
            document.getElementById('system-alert-overlay').remove();
            document.body.style.overflow = '';
            setTimeout(() => this.check(), 1000);
        };
    }
};
const ReportManager = {
    init() {
        document.body.addEventListener('click', (e) => {
            if (e.target.classList.contains('action-report')) {
                e.preventDefault();
                if (e.target.classList.contains('action-reported')) return;
                const postNum = e.target.dataset.postNum;
                const modal = document.getElementById('report-modal');
                if (modal) {
                    const form = modal.querySelector('form');
                    if (form) form.reset();
                    const input = modal.querySelector('#report-post-num-input');
                    const display = modal.querySelector('#report-post-num');
                    if (input) input.value = postNum;
                    if (display) display.textContent = `#${postNum}`;
                    openModal('report-modal');
                    this.currentBtn = e.target;
                }
            }
        });
        const form = document.getElementById('report-form');
        if (form) {
            form.addEventListener('submit', async (e) => {
                e.preventDefault();
                const formData = new FormData(form);
                const data = Object.fromEntries(formData.entries());
                data.post_num = parseInt(data.post_num);
                try {
                    const res = await fetch('/api/report', {
                        method: 'POST',
                        headers: {'Content-Type': 'application/json'},
                        body: JSON.stringify(data)
                    });
                    if (res.ok) {
                        showToast(t('toast_reported'));
                        closeModal();
                        form.reset();
                        if (this.currentBtn) {
                            this.currentBtn.textContent = t('report_btn_sent');
                            this.currentBtn.classList.add('action-reported');
                            this.currentBtn.style.opacity = '0.5';
                            this.currentBtn.style.cursor = 'default';
                        }
                    } else {
                        const err = await res.json();
                        showToast(`${t('error_prefix')}${err.detail}`);
                    }
                } catch (e) {
                    showToast(t('network_error'));
                }
            });
        }
    }
};
const StealthEditor = {
    init() {
        document.body.addEventListener('click', (e) => {
            const btn = e.target.closest('.action-stealth-edit');
            if (!btn) return;
            e.preventDefault();
            e.stopPropagation();
            const postNum = btn.dataset.postNum;
            this.open(postNum);
        });
        const form = document.getElementById('stealth-edit-form');
        if (form && !form.dataset.listening) {
            form.addEventListener('submit', (e) => this.submit(e));
            form.dataset.listening = 'true';
        }
    },
    async open(postNum) {
        const modal = document.getElementById('stealth-edit-modal');
        const overlay = document.getElementById('modal-overlay');
        const textInput = document.getElementById('se-text');
        const numInput = document.getElementById('se-post-num');
        const fileContainer = document.getElementById('se-current-files');
        if (!modal || !textInput) {
            console.error('[Stealth] Modal elements not found in DOM');
            return;
        }
        textInput.value = t('loading');
        fileContainer.innerHTML = '';
        if (overlay) {
            overlay.style.display = 'block';
            overlay.style.zIndex = '10050';
            requestAnimationFrame(() => overlay.classList.add('active'));
        }
        modal.style.display = 'block';
        modal.style.zIndex = '10051';
        requestAnimationFrame(() => modal.classList.add('active'));
        document.body.classList.add('modal-open');
        try {
            const res = await fetch(`/api/post/${postNum}`);
            if (!res.ok) throw new Error('Failed to fetch post');
            const data = await res.json();
            numInput.value = postNum;
            textInput.value = data.content.text || '';
            fileContainer.innerHTML = '';
            if (data.content.files && data.content.files.length > 0) {
                data.content.files.forEach((f, index) => {
                    const wrapper = document.createElement('label');
                    wrapper.style.cssText = 'position: relative; display: inline-block; width: 60px; height: 60px; margin: 2px; cursor: pointer; border: 1px solid transparent;';
                    const thumbUrl = f.thumbnail_url || f.original_url;
                    const checkbox = document.createElement('input');
                    checkbox.type = 'checkbox';
                    checkbox.value = index;
                    checkbox.style.cssText = 'position: absolute; top: 0; left: 0; opacity: 0; width: 100%; height: 100%; cursor: pointer; z-index: 2;';
                    let mediaEl;
                    const isVideo = f.type === 'video' || f.type === 'gif' || (thumbUrl && thumbUrl.match(/\.(mp4|webm)$/i));
                    if (isVideo) {
                        mediaEl = document.createElement('video');
                        mediaEl.src = thumbUrl;
                        mediaEl.muted = true;
                        mediaEl.loop = true;
                        mediaEl.preload = 'metadata'; 
                    } else {
                        mediaEl = document.createElement('img');
                        mediaEl.src = thumbUrl;
                    }
                    mediaEl.referrerPolicy = "no-referrer";
                    mediaEl.style.cssText = 'width: 100%; height: 100%; object-fit: cover; border-radius: 4px; opacity: 1; transition: all 0.2s;';
                    const targetEl = mediaEl; 
                    checkbox.onchange = () => {
                        if (checkbox.checked) {
                            targetEl.style.opacity = '0.3';
                            targetEl.style.filter = 'grayscale(100%)';
                            wrapper.style.borderColor = 'red';
                        } else {
                            targetEl.style.opacity = '1';
                            targetEl.style.filter = 'none';
                            wrapper.style.borderColor = 'transparent';
                        }
                    };
                    wrapper.appendChild(checkbox);
                    wrapper.appendChild(targetEl);
                    fileContainer.appendChild(wrapper);
                });
            } else {
                fileContainer.innerHTML = `<span style="color: gray; font-style: italic; font-size: 0.9em;">${t('stealth_no_files')}</span>`;
            }
        } catch (e) {
            console.error(e);
            if (typeof showToast === 'function') showToast(t('stash_load_error'));
            
            modal.classList.remove('active');
            if (overlay) overlay.classList.remove('active');
            setTimeout(() => {
                modal.style.display = 'none';
                if (overlay) overlay.style.display = 'none';
            }, 300);
        }
    },
    async submit(e) {
        e.preventDefault();
        const form = e.target;
        const formData = new FormData(form);
        const btn = form.querySelector('button[type="submit"]');
        
        const checkboxes = form.querySelectorAll('#se-current-files input[type="checkbox"]:checked');
        const deleteIndices = Array.from(checkboxes).map(cb => parseInt(cb.value));
        formData.append('delete_files', JSON.stringify(deleteIndices));

        if (btn) {
            btn.disabled = true;
            btn.textContent = t('stealth_saving');
        }

        try {
            const res = await fetch('/api/admin/stealth_edit', {
                method: 'POST',
                body: formData
            });

            if (!res.ok) {
                const err = await res.json();
                throw new Error(err.detail || 'Save failed');
            }

            if (typeof showToast === 'function') showToast(t('stealth_done', 'Изменено тихо'));
            
            document.getElementById('stealth-edit-modal').classList.remove('active');
            const overlay = document.getElementById('modal-overlay');
            if (overlay) overlay.classList.remove('active');
            
            setTimeout(() => {
                document.getElementById('stealth-edit-modal').style.display = 'none';
                if (overlay) overlay.style.display = 'none';
                document.body.classList.remove('modal-open');
            }, 300);

        } catch (e) {
            console.error(e);
            if (typeof showToast === 'function') showToast(`${t('error_prefix')} ${e.message}`);
        } finally {
            if (btn) {
                btn.disabled = false;
                btn.textContent = t('stealth_btn');
            }
        }
    }
};
const ReactionManager = {
    activePopup: null,
    emojis: ['👍', '👎', '❤️', '🔥', '🤡', '💩', '🤔', '😱'],
    init() {
        document.body.addEventListener('click', (e) => {
            const btn = e.target.closest('.reaction-btn');
            if (!btn) {
                if (this.activePopup && !e.target.closest('.reaction-popup')) {
                    this.close();
                }
                return;
            }
            e.preventDefault();
            e.stopPropagation();
            const postNum = btn.dataset.postNum;
            if (this.activePopup && this.activePopup.dataset.postNum === postNum) {
                this.close();
                return;
            }
            this.open(btn, postNum);
        });
        document.body.addEventListener('click', async (e) => {
            const emojiItem = e.target.closest('.reaction-popup .emoji');
            if (!emojiItem) return;
            const popup = emojiItem.parentElement;
            const postNum = popup.dataset.postNum;
            const emoji = emojiItem.dataset.emoji;
            this.close();
            try {
                const res = await fetch('/api/react', {
                    method: 'POST',
                    headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify({ post_num: parseInt(postNum), emoji: emoji })
                });
                if (!res.ok) {
                    const err = await res.json();
                    showToast(`${t('error_prefix')}${err.detail}`);
                }
            } catch (err) {
                showToast(t('network_error'));
            }
        });
    },
    open(btn, postNum) {
        this.close();
        const popup = document.createElement('div');
        popup.className = 'reaction-popup';
        popup.dataset.postNum = postNum;
        this.emojis.forEach(char => {
            const span = document.createElement('span');
            span.className = 'emoji';
            span.textContent = char;
            span.dataset.emoji = char;
            popup.appendChild(span);
        });
        btn.parentElement.appendChild(popup);
        this.activePopup = popup;
    },
    close() {
        if (this.activePopup) {
            this.activePopup.remove();
            this.activePopup = null;
        }
    }
};
const PersonalizationManager = {
    init() {
        setTimeout(() => this.sync(), 100);
    },
    async sync() {
        const posts = document.querySelectorAll('.post');
        if (!posts.length) return;
        const idsOnPage = Array.from(posts).map(p => p.id.replace('post-', ''));
        try {
            const res = await fetch('/api/get-my-posts', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify(idsOnPage)
            });
            if (!res.ok) return;
            const myIds = await res.json();
            myIds.forEach(id => {
                UserState.postIds.add(String(id));
                const post = document.getElementById(`post-${id}`);
                if (post) {
                    post.classList.add('is-yours');
                    const header = post.querySelector('.post-header');
                    if (header && !header.querySelector('.you-marker')) {
                        const marker = document.createElement('span');
                        marker.className = 'you-marker';
                        marker.textContent = t('you_marker');
                        const numLink = header.querySelector('.post-num-link');
                        if (numLink) numLink.after(marker);
                        else header.appendChild(marker);
                    }
                }
            });
            localStorage.setItem('my_post_ids', JSON.stringify([...UserState.postIds]));
        } catch (e) {
            console.error("Personalization sync failed", e);
        }
    }
};
const FavouritesManager = {
    threads: new Set(),
    init() {
        try {
            const saved = JSON.parse(localStorage.getItem('favourite_threads')) || [];
            this.threads = new Set(saved);
        } catch (e) { this.threads = new Set(); }
        this.updateUI();
        document.body.addEventListener('click', (e) => {
            const btn = e.target.closest('.favourite-btn');
            if (btn) {
                e.preventDefault();
                e.stopPropagation();
                this.toggle(btn.dataset.threadId);
            }
        });
        const headBtn = document.getElementById('favourites-btn');
        if (headBtn) headBtn.onclick = (e) => { e.preventDefault(); location.href = '/favourites/'; };
        if (document.getElementById('favourites-container')) {
            this.renderPage();
        }
        window.addEventListener('storage', (e) => {
            if (e.key === 'favourite_threads') {
                try {
                    this.threads = new Set(JSON.parse(e.newValue || '[]'));
                    this.updateUI();
                    if (document.getElementById('favourites-container')) {
                        document.getElementById('favourites-container').innerHTML = '';
                        this.renderPage();
                    }
                } catch(err) {}
            }
        });
    },
    toggle(id) {
        if (!id) return;
        const isFavourite = this.threads.has(id);
        if (isFavourite) {
            this.threads.delete(id);
        } else {
            this.threads.add(id);
        }
        this.save();
        document.querySelectorAll(`.favourite-btn[data-thread-id="${id}"]`).forEach(btn => {
            this.updateButtonState(btn, !isFavourite);
            btn.style.transform = 'scale(1.4)';
            setTimeout(() => btn.style.transform = '', 200);
        });
    },
    save() {
        localStorage.setItem('favourite_threads', JSON.stringify([...this.threads]));
    },
    updateUI() {
        document.querySelectorAll('.favourite-btn').forEach(btn => {
            const id = btn.dataset.threadId;
            const active = this.threads.has(id);
            this.updateButtonState(btn, active);
        });
    },
    updateButtonState(btn, isActive) {
        btn.classList.toggle('is-favourite', isActive);
        btn.title = isActive ? t('fav_del') : t('fav_add');
    },
    async renderPage() {
        const container = document.getElementById('favourites-container');
        const loader = document.getElementById('favourites-loading');
        if (!container) return;
        if (!this.threads || this.threads.size === 0) {
            if (loader) loader.style.display = 'none';
            container.innerHTML = `<div style="text-align:center; padding:20px; opacity:0.6;">${t('fav_empty', 'Список избранного пуст')}</div>`;
            return;
        }
        if (loader) loader.style.display = 'block';
        try {
            const res = await fetch('/api/favourites', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({ thread_ids: [...this.threads].reverse() })
            });
            if (!res.ok) throw new Error('Ошибка сервера');
            const data = await res.json();
            container.innerHTML = '';
            if (!data || data.length === 0) {
                container.innerHTML = `<div style="text-align:center; padding:20px; opacity:0.6;">${t('fav_empty', 'Ничего не найдено (возможно, треды удалены)')}</div>`;
            } else {
                data.forEach(thread => {
                    try {
                        const el = ScrollManager.createThreadElement(thread);
                        container.appendChild(el);
                        window.initializePostFeatures(el);
                    } catch (err) {
                        console.error("Ошибка отрисовки треда:", err);
                    }
                });
            }
        } catch (e) {
            console.error("Ошибка загрузки избранного:", e);
            container.innerHTML = `<div style="color:var(--action-danger); text-align:center; padding:20px;">${t('fav_loading_err', 'Ошибка при загрузке данных')}</div>`;
        } finally {
            if (loader) loader.style.display = 'none';
        }
    }
};
const HistoryManager = {
    KEY: 'viewed_threads_history',
    MAX: 50,
    init() {
        if (window.location.pathname.includes('/res/')) {
            this.recordVisit();
        }
        const container = document.getElementById('history-container');
        if (container) {
            this.renderPage(container);
        }
    },
    recordVisit() {
        try {
            const match = window.location.pathname.match(/\/([a-z0-9]+)\/res\/(\d+)\.html/);
            if (!match) return;
            const boardId = match[1];
            const threadId = match[2];
            let history = [];
            try { history = JSON.parse(localStorage.getItem(this.KEY) || '[]'); } catch(e){}
            history = history.filter(h => String(h.threadId) !== String(threadId));
            let cleanTitle = document.title.replace(/^\(\d+\)\s*/, '').replace(/ - ТГАЧ$/, '');
            history.unshift({
                threadId: String(threadId),
                boardId: boardId,
                title: cleanTitle,
                timestamp: Date.now()
            });
            if (history.length > this.MAX) history = history.slice(0, this.MAX);
            localStorage.setItem(this.KEY, JSON.stringify(history));
        } catch (e) { console.error('History logic error', e); }
    },
    renderPage(container) {
        const history = JSON.parse(localStorage.getItem(this.KEY) || '[]');
        const loader = document.getElementById('history-loading');
        const clearBtn = document.getElementById('clear-history-btn');
        
        if (!history.length) {
            if(loader) loader.textContent = t('history_empty');
            return;
        }
        if(loader) loader.style.display = 'none';
        const esc = (str) => {
            return String(str || "")
                .replace(/&/g, "&amp;")
                .replace(/</g, "&lt;")
                .replace(/>/g, "&gt;")
                .replace(/"/g, "&quot;")
                .replace(/'/g, "&#039;");
        };

        history.forEach(h => {
            const div = document.createElement('div');
            div.className = 'history-item';
            const safeBoard = esc(h.boardId);
            const safeTitle = esc(h.title || t('default_thread_title', 'Тред'));
            
            div.innerHTML = `
                <a href="/${safeBoard}/res/${h.threadId}.html" class="history-link">
                    <b>/${safeBoard}/</b> — ${safeTitle}
                </a>
                <span class="history-date">${new Date(h.timestamp).toLocaleString()}</span>
            `;
            container.appendChild(div);
        });
        
        if (clearBtn) {
            clearBtn.onclick = () => {
                if(confirm(t('history_clear_confirm'))) {
                    localStorage.removeItem(this.KEY);
                    location.reload();
                }
            };
        }
    }
};
const MediaStreamManager = {
    playlist: [],
    currentIndex: 0,
    page: 1,
    isLoading: false,
    boardId: null,
    overlay: null,
    touchStartY: 0,
    touchStartX: 0,
    isRandomMode: false, 
    init() {
        const btn = document.getElementById('media-mode-btn');
        if (btn) {
            this.boardId = document.querySelector('input[name="board_id"]')?.value || 'b';
            btn.onclick = (e) => {
                e.preventDefault();
                this.open();
            };
        }
    },
    async open() {
        if (!this.overlay) this.createDOM();
        this.overlay.style.display = 'flex';
        document.body.style.overflow = 'hidden';
        if (typeof BackButtonManager !== 'undefined') {
            BackButtonManager.push('media');
        }
        const mascot = document.getElementById('mascot-wrapper');
        if (mascot) mascot.style.display = 'none';
        if (this.playlist.length === 0) {
            await this.loadMore();
        }
        
        this.isRandomMode = false;
        this.updateRandomBtnState();
        this.render(0);
        if (typeof BackButtonManager !== 'undefined') BackButtonManager.push('media');
        if (window.innerWidth <= 768) {
            setTimeout(() => {
                showToast(t('media_swipe_hint'), 4000);
            }, 500);
        }
    },
    close() {
        if (this.overlay) {
            this.overlay.style.display = 'none';
            const v = this.overlay.querySelector('video');
            if (v) {
                v.pause();
                v.src = "";
                v.load();
            }
            this.playlist = [];
        }
        document.body.style.overflow = '';
        const mascot = document.getElementById('mascot-wrapper');
        if (mascot && SettingsManager.current.showMascot) {
            mascot.style.display = '';
        }
        closeModal();
    },
    createDOM() {
        this.overlay = document.createElement('div');
        this.overlay.className = 'media-stream-overlay';
        this.overlay.style.height = '100dvh'; 
        this.overlay.innerHTML = `
            <div class="ms-top-bar">
                <button class="ms-thread-btn" onclick="MediaStreamManager.goToThread()">${t('go_to_thread')}</button>
                <div class="ms-controls-right">
                    <button id="ms-download-btn" class="ms-icon-btn" onclick="MediaStreamManager.downloadCurrentFile()" title="${t('iv_download', 'Скачать')}">💾</button>
                    <button id="ms-tags-btn" class="ms-icon-btn" onclick="MediaStreamManager.showCurrentFileTags()" title="${t('tags_btn_hint', 'Показать теги')}">🏷️</button>
                    <button id="ms-rand-toggle" class="ms-icon-btn" onclick="MediaStreamManager.toggleRandom()" title="Shuffle Mode">
                        ➡️
                    </button>
                    <button class="ms-icon-btn ms-close-btn" onclick="MediaStreamManager.close()" title="${t('ms_close_title')}">
                        &times;
                    </button>
                </div>
            </div>
            <div class="ms-nav-btn ms-prev" onclick="MediaStreamManager.prev()">‹</div>
            <div class="ms-nav-btn ms-next" onclick="MediaStreamManager.next()">›</div>
            <div class="ms-container" id="ms-view"></div>
            <div class="ms-info-layer" id="ms-info"></div>
        `;
        document.body.appendChild(this.overlay);
        
        this.overlay.addEventListener('touchstart', e => {
            if (e.target.closest('button') || e.target.closest('.ms-nav-btn') || e.target.closest('.ms-top-bar') || e.target.tagName === 'INPUT') return;
            this.touchStartY = e.touches[0].clientY;
            this.touchStartX = e.touches[0].clientX;
        }, {passive: false});
        this.overlay.addEventListener('touchend', e => {
            if (e.target.closest('button') || e.target.closest('.ms-nav-btn')) return;
            const dy = e.changedTouches[0].clientY - this.touchStartY;
            const dx = e.changedTouches[0].clientX - this.touchStartX;
            
            if (Math.abs(dy) > 50 && Math.abs(dy) > Math.abs(dx)) {
                if (dy < 0) this.next();
                else this.prev();
            }
        });
        
        document.addEventListener('keydown', e => {
            if (this.overlay.style.display === 'none') return;
            if (e.key === 'ArrowDown' || e.key === ' ' || e.key === 'ArrowRight') { e.preventDefault(); this.next(); }
            if (e.key === 'ArrowUp' || e.key === 'ArrowLeft') { e.preventDefault(); this.prev(); }
            if (e.key === 'Enter') this.goToThread();
            if (e.key === 'Escape') this.close();
        });
    },
    
    toggleRandom() {
        this.isRandomMode = !this.isRandomMode;
        this.updateRandomBtnState();
        showToast(this.isRandomMode ? t('mode_random') : t('mode_seq'));
    },
    updateRandomBtnState() {
        const btn = document.getElementById('ms-rand-toggle');
        if (!btn) return;
        if (this.isRandomMode) {
            btn.textContent = '🎲';
            btn.classList.add('active-mode');
        } else {
            btn.textContent = '➡️';
            btn.classList.remove('active-mode');
        }
    },
    async loadMore() {
        if (this.isLoading) return;
        this.isLoading = true;
        
        try {
            const res = await fetch(`/api/media-feed/${this.boardId}?page=${this.page}`);
            const posts = await res.json();
            if (posts.length === 0) {
                showToast(t('media_stream_end'));
                this.isLoading = false;
                return;
            }
            posts.forEach(post => {
                if (post.content.files) {
                    post.content.files.forEach(file => {
                        this.playlist.push({
                            file: file,
                            post: post
                        });
                    });
                }
            });
            this.page++;
        } catch (e) {
            showToast(t('stash_load_error'));
        } finally {
            this.isLoading = false;
        }
    },
    loadVideoWithRetry(videoEl, item, attempt = 0) {
        const sources = item.file.sources || [item.file.original_url];
        videoEl.onerror = () => {
            if (attempt + 1 < sources.length) {
                console.log(`[Stream] Mirror failed, trying mirror ${attempt + 1}`);
                this.loadVideoWithRetry(videoEl, item, attempt + 1);
            } else {
                const sep = sources[0].includes('?') ? '&' : '?';
                if (!videoEl.dataset.retriedOnce) {
                    videoEl.dataset.retriedOnce = "true";
                    videoEl.src = `${sources[0]}${sep}retry=${Date.now()}`;
                    videoEl.play().catch(()=>{});
                } else {
                    console.error('[Stream] All sources failed');
                    if (typeof showToast === 'function') showToast(t('ms_video_404', 'Видео недоступно'), 2000);
                }
            }
        };
        videoEl.src = sources[attempt];
        videoEl.play().catch(() => {
            videoEl.muted = true;
            videoEl.play().catch(()=>{});
        });
    },
    render(index, direction = 'up') {
        if (this.playlist.length > 50 && index > 20) {
            const cut = index - 10;
            this.playlist.splice(0, cut);
            index -= cut;
            console.log(`[Media] Trimmed playlist by ${cut} items`);
        }
        if (index < 0) index = this.playlist.length - 1; 
        if (index >= this.playlist.length) {
            if (!this.isLoading) {
                this.loadMore().then(() => {
                    
                    if (index < this.playlist.length) this.render(index, direction);
                    else this.render(0, direction);
                });
            }
            return;
        }
        this.currentIndex = index;
        const item = this.playlist[index];
        const container = document.getElementById('ms-view');
        const info = document.getElementById('ms-info');
        const oldVideo = container.querySelector('video');
        if (oldVideo) {
            oldVideo.pause();
            oldVideo.removeAttribute('src');
            oldVideo.load();
        }
        container.innerHTML = '';
        let el;
        const isVideo = ['video', 'gif', 'animation', 'video_note'].includes(item.file.type);
        const url = item.file.original_url;
        if (isVideo) {
            el = document.createElement('video');
            el.referrerPolicy = "no-referrer";
            el.loop = true;
            el.controls = true;
            el.onplay = () => {
                if (typeof AudioManager !== 'undefined') AudioManager.stopOthers(el);
            };

            const connection = navigator.connection || navigator.mozConnection || navigator.webkitConnection;
            const saveData = connection ? connection.saveData : false;
            if (saveData) {
                el.autoplay = false;
                el.preload = 'none';
                showToast(t('eco_mode_active', "Экономия трафика: Автоплей выключен"), 2000);
            } else {
                el.autoplay = true;
                el.playsInline = true;
            }
            el.muted = false;
            this.loadVideoWithRetry(el, item);
        } else {
            el = document.createElement('img');
            el.referrerPolicy = "no-referrer";
            el.src = url;
        }
        el.className = 'ms-content ' + (direction === 'up' ? 'slide-up' : 'slide-down');
        container.appendChild(el);
        if (!this.isRandomMode) {
            [1, 2].forEach(offset => {
                const nextItem = this.playlist[index + offset];
                if (nextItem) {
                    const nextUrl = nextItem.file.original_url;
                    const isNextVid = ['video', 'gif', 'animation', 'video_note'].includes(nextItem.file.type);
                    if (isNextVid) {
                        const v = document.createElement('video');
                        v.preload = 'auto';
                        v.src = nextUrl;
                        if(typeof v.referrerPolicy !== 'undefined') v.referrerPolicy = "no-referrer";
                    } else {
                        const img = new Image();
                        if(typeof img.referrerPolicy !== 'undefined') img.referrerPolicy = "no-referrer";
                        img.src = nextUrl;
                    }
                }
            });
        }
        const rawText = item.post.content.text || '';
        let formattedText = window.formatTextGlobal(rawText);
        const date = new Date(item.post.timestamp * 1000).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'});
        info.innerHTML = `
            <div class="ms-meta">
                #${item.post.id} • ${date}
            </div>
            <div class="ms-text" style="white-space: normal; overflow-y: auto;">
                ${formattedText || t('ms_no_text')}
            </div>
        `;
        const threadBtn = this.overlay.querySelector('.ms-thread-btn');
        if (threadBtn) {
            if (item.post.thread_id) {
                threadBtn.style.display = '';
            } else {
                threadBtn.style.display = 'none';
            }
        }
    },
    next() {
        if (this.isRandomMode) {
            this.randomNext();
        } else {
            this.render(this.currentIndex + 1, 'up');
        }
    },
    prev() {
        this.render(this.currentIndex - 1, 'down');
    },
    randomNext() {
        if (this.playlist.length <= 1) return;
        let idx;
        do {
            idx = Math.floor(Math.random() * this.playlist.length);
        } while (idx === this.currentIndex);
        this.render(idx, 'up');
    },
    goToThread() {
        const item = this.playlist[this.currentIndex];
        if (item) {
            if (!item.post.thread_id) {
                if (typeof showToast === 'function') showToast(t('ms_chat_post_no_thread', "Этот пост из чата (нет треда)"));
                return;
            }
            const url = `/${this.boardId}/res/${item.post.thread_id}.html#post-${item.post.id}`;
            window.open(url, '_blank');
        }
    },
    async downloadCurrentFile() {
        const item = this.playlist[this.currentIndex];
        if (!item) return;
        const btn = document.getElementById('ms-download-btn');
        if (!btn) return;
        const originalText = btn.textContent;
        btn.textContent = '⏳';
        btn.style.pointerEvents = 'none';
        try {
            const response = await fetch(item.file.original_url);
            if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);
            showToast(t('download_started', 'Скачивание началось...'), 1500);
            const blob = await response.blob();
            const mimeType = blob.type;
            let extension = '';
            const mimeMap = {
                'video/mp4': '.mp4', 'video/webm': '.webm', 'video/quicktime': '.mov',
                'image/jpeg': '.jpg', 'image/png': '.png', 'image/gif': '.gif', 'image/webp': '.webp'
            };
            extension = mimeMap[mimeType] || '';
            if (!extension && item.file.filename) {
                const match = item.file.filename.match(/\.([a-z0-9]{2,5})$/i);
                if (match) extension = match[0];
            }
            if (!extension) {
                const urlExtMatch = (item.file.original_url || '').match(/\.([a-z0-9]{2,5})(?=[?#]|$)/i);
                if (urlExtMatch) extension = urlExtMatch[0];
            }
            let baseFilename = (item.file.filename || 'video').replace(/\.[^/.]+$/, "");
            if (baseFilename === 'video') {
                baseFilename = `tgach_${item.post.board_id}_${item.post.id}`;
            }
            const finalFilename = baseFilename + (extension || '.mp4');
            const objectUrl = URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.style.display = 'none';
            a.href = objectUrl;
            a.download = finalFilename;
            document.body.appendChild(a);
            a.click();
            document.body.removeChild(a);
            URL.revokeObjectURL(objectUrl);
        } catch (err) {
            console.error('Download failed:', err);
            showToast(t('download_err', 'Ошибка скачивания'));
            window.open(item.file.original_url, '_blank');
        } finally {
            btn.textContent = originalText;
            btn.style.pointerEvents = 'auto';
        }
    },
    showCurrentFileTags() {
        const item = this.playlist[this.currentIndex];
        if (!item || !item.file) return;
        let fileId = item.file.original_file_id;
        if (!fileId && item.file.original_url) {
            try {
                const filename = item.file.original_url.split('/').pop().split('?')[0];
                const parts = filename.split('.');
                if (parts.length > 1) parts.pop();
                fileId = parts.join('.');
            } catch (e) {
                console.error('Failed to extract file ID:', e);
            }
        }

        if (!fileId) {
            if (typeof showToast === 'function') showToast(t('err_no_file_id', 'File ID не найден, теги недоступны'));
            return;
        }
        if (typeof GalleryManager !== 'undefined' && typeof GalleryManager.showTagsModal === 'function') {
            GalleryManager.items = [{ 
                fileId: fileId,
                url: item.file.original_url 
            }];
            GalleryManager.index = 0;
            GalleryManager.showTagsModal();
        } else {
            if (typeof showToast === 'function') showToast('Ошибка: менеджер тегов не загружен.');
        }
    }
};
const MediaWallManager = {
    overlay: null,
    init() {
        const btn = document.getElementById('media-wall-btn');
        if (btn) {
            btn.onclick = (e) => {
                e.preventDefault();
                this.open();
            };
        }
    },
    open() {
        if (!this.overlay) this.createDOM();
        const grid = this.overlay.querySelector('.mw-grid');
        grid.innerHTML = '';
        const thumbs = Array.from(document.querySelectorAll('a.file-thumb'));
        if (thumbs.length === 0) {
            showToast(t('media_wall_empty'));
            return;
        }
        GalleryManager.updatePlaylist();
        thumbs.forEach(thumbLink => {
            const img = thumbLink.querySelector('img');
            const vid = thumbLink.querySelector('video');
            if (!img && !vid) return;
            const item = document.createElement('div');
            item.className = 'mw-item';
            const originalMedia = img || vid;
            const realSrc = originalMedia.dataset.src || originalMedia.src;
            const mediaClone = document.createElement(originalMedia.tagName);
            mediaClone.src = realSrc;
            mediaClone.loading = "lazy";
            mediaClone.removeAttribute('loading');
            mediaClone.classList.remove('lazy-load');
            mediaClone.classList.add('loaded');
            mediaClone.style.opacity = '1';
            mediaClone.style.display = 'block';
            if (mediaClone.tagName === 'VIDEO') {
                mediaClone.removeAttribute('autoplay');
                mediaClone.pause();
                mediaClone.currentTime = 0;
            }
            item.appendChild(mediaClone);
            const targetHref = thumbLink.getAttribute('href');
            const galleryIndex = GalleryManager.items.findIndex(i =>
                i.url === targetHref || decodeURI(i.url) === decodeURI(targetHref)
            );
            item.onclick = (e) => {
                e.stopPropagation();
                GalleryManager.open(galleryIndex !== -1 ? galleryIndex : 0);
            };
            grid.appendChild(item);
        });
        this.overlay.style.display = 'block';
        document.body.style.overflow = 'hidden';
        if (typeof BackButtonManager !== 'undefined') BackButtonManager.push('mediawall');
    },
    close() {
        if (this.overlay) this.overlay.style.display = 'none';
        document.body.style.overflow = '';
    },
    createDOM() {
        this.overlay = document.createElement('div');
        this.overlay.className = 'media-wall-overlay';
        this.overlay.innerHTML = `
            <div class="mw-header">
                <h2>${t('media_wall_header')}</h2>
                <div class="mw-close" onclick="MediaWallManager.close()">×</div>
            </div>
            <div class="mw-grid">
                <!-- Сюда попадут картинки -->
            </div>
        `;
        document.body.appendChild(this.overlay);
    }
};
const BookManager = {
    init() {
        const btn = document.getElementById('book-mode-btn');
        if (btn) {
            btn.addEventListener('click', (e) => {
                e.preventDefault();
                this.startProcess();
            });
        }
    },
    yieldToUI() {
        return new Promise(resolve => setTimeout(resolve, 0));
    },
    async startProcess() {
        if (!confirm(t('book_confirm'))) return;
        showToast(t('book_init'));
        if (typeof pdfMake === 'undefined') {
            try {
                await this.loadScript('https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.2.7/pdfmake.min.js');
                await this.loadScript('https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.2.7/vfs_fonts.js');
            } catch (e) {
                return alert(t('book_lib_err'));
            }
        }
        this.generatePDF();
    },
    loadScript(src) {
        return new Promise((resolve, reject) => {
            const s = document.createElement('script');
            s.src = src;
            s.onload = resolve;
            s.onerror = reject;
            document.head.appendChild(s);
        });
    },
    async generatePDF() {
        try {
            const threadTitle = document.title.replace(' - ТГАЧ', '');
            const posts = Array.from(document.querySelectorAll('.post'));
            const total = posts.length;
            const styles = getComputedStyle(document.body);
            const bgColor = styles.getPropertyValue('--bg-post').trim() || '#ffffff';
            const textColor = styles.getPropertyValue('--text-primary').trim() || '#000000';
            const linkColor = styles.getPropertyValue('--accent-primary').trim() || 'blue';
            const secColor = styles.getPropertyValue('--text-secondary').trim() || 'gray';
            showToast(t('book_analyzing').replace('{0}', total));
            await this.yieldToUI();
            const content = [];
            
            content.push({ text: threadTitle, style: 'header', alignment: 'center', margin: [0, 20, 0, 20], color: textColor });
            let count = 0;
            for (const post of posts) {
                const num = post.id.replace('post-', '');
                const headerEl = post.querySelector('.post-header').cloneNode(true);
                const adminSpan = headerEl.querySelector('span[style*="ff00ff"]');
                if(adminSpan) adminSpan.remove();
                const headerText = headerEl.textContent.replace(/\s+/g, ' ').trim();
                const textContentEl = post.querySelector('.post-text-content');
                const textClone = textContentEl.cloneNode(true);
                textClone.querySelectorAll('button, .embed-toggle, .backlinks-container').forEach(b => b.remove());
                textClone.innerHTML = textClone.innerHTML.replace(/<br\s*\/?>/gi, '\n');
                let rawText = textClone.innerText;
                rawText = rawText
                    .replace(/\[\/?(b|i|s|u|code|shake|rainbow|glitch|blur)\]/gi, '')
                    .replace(/\[glitch\]/gi, '');
                const textStack = [];
                const lines = rawText.split('\n');
                lines.forEach(line => {
                    const l = line.trim();
                    if (!l) return;
                    
                    if (l.startsWith('>')) {
                        textStack.push({ text: l + '\n', color: '#789922', italics: true });
                        return;
                    }
                    const parts = l.split(/(>>\/?\d+)/g);
                    const lineContent = [];
                    parts.forEach(part => {
                        if (!part) return;
                        if (part.match(/^>>\/?\d+$/)) {
                            lineContent.push({ text: part + ' ', color: linkColor, bold: true });
                        } else {
                            lineContent.push({ text: part, color: textColor });
                        }
                    });
                    lineContent.push({ text: '\n' });
                    textStack.push({ text: lineContent });
                });
                const images = post.querySelectorAll('.post-image, .post-sticker');
                const mediaStack = [];
                if (images.length > 0) {
                    let currentRow = [];
                    for (const img of images) {
                        let src = img.src;
                        if (!src || src.startsWith('data:image/gif')) {
                             src = img.dataset.src;
                        }
                        if (src) {
                            const base64 = await this.getBase64Image(src);
                            if (base64) {
                                currentRow.push({
                                    image: base64,
                                    width: 150,
                                    margin: [0, 5, 5, 0]
                                });
                            }
                        }
                        
                        if (currentRow.length === 2) {
                            mediaStack.push({ columns: currentRow, columnGap: 10 });
                            currentRow = [];
                        }
                    }
                    if (currentRow.length > 0) mediaStack.push({ columns: currentRow });
                }
                content.push({
                    stack: [
                        { text: headerText, color: secColor, fontSize: 10, margin: [0, 0, 0, 5] },
                        { stack: mediaStack },
                        { stack: textStack, margin: [0, 5, 0, 0], fontSize: 12 }
                    ],
                    margin: [0, 5, 0, 15],
                    fillColor: bgColor,
                    border: [false, false, false, true],
                    borderColor: '#ccc'
                });
                count++;
                if (count % 10 === 0) {
                    showToast(t('book_typesetting').replace('{0}', Math.round((count / total) * 100)));
                    await this.yieldToUI();
                }
            }
            const docDefinition = {
                content: content,
                pageSize: 'A4',
                pageMargins: [20, 20, 20, 20],
                
                background: function(currentPage) {
                    return {
                        canvas: [
                            { type: 'rect', x: 0, y: 0, w: 595.28, h: 841.89, color: bgColor } 
                        ]
                    };
                },
                defaultStyle: { font: 'Roboto' }, 
                info: { title: threadTitle, author: 'TGACH' }
            };
            pdfMake.createPdf(docDefinition).download(`TGACH_${Date.now()}.pdf`);
            showToast(t('book_done'));
        } catch (e) {
            console.error(e);
            alert(t('gen_error') + " " + e.message);
        }
    },
    async getBase64Image(url) {
        try {
            const controller = new AbortController();
            const id = setTimeout(() => controller.abort(), 3000);
            const response = await fetch(url, { 
                signal: controller.signal,
                credentials: 'same-origin' 
            });
            clearTimeout(id);
            if (!response.ok) return null;
            const blob = await response.blob();
            if (blob.size > 500 * 1024) return null;
            return new Promise((resolve) => {
                const reader = new FileReader();
                reader.onloadend = () => resolve(reader.result);
                reader.onerror = () => resolve(null);
                reader.readAsDataURL(blob);
            });
        } catch (e) {
            return null;
        }
    }
};
const OfflineManager = {
    init() {
        const btn = document.getElementById('save-thread-btn');
        if (btn) {
            btn.addEventListener('click', (e) => {
                e.preventDefault();
                this.saveThread();
            });
        }
    },
    async saveThread() {
        showToast(t('offline_archiving'));
        const clone = document.documentElement.cloneNode(true);
        const selectors = ['script', '#post-form', '.form-container', '.post-actions', '#floating-reply-box', '.modal', '#toast-container'];
        selectors.forEach(s => clone.querySelectorAll(s).forEach(el => el.remove()));
        let css = '';
        for (const sheet of Array.from(document.styleSheets)) {
            try {
                if (sheet.href) {
                    const r = await fetch(sheet.href);
                    css += await r.text();
                } else {
                    for (const rule of Array.from(sheet.cssRules)) css += rule.cssText;
                }
            } catch(e) {}
        }
        const style = document.createElement('style');
        style.textContent = css;
        clone.querySelector('head').innerHTML = '';
        clone.querySelector('head').appendChild(style);
        const imgs = clone.querySelectorAll('img.post-image, img.post-sticker');
        let count = 0;
        const toBase64 = async (url) => {
            const r = await fetch(url);
            const b = await r.blob();
            return new Promise(res => {
                const fr = new FileReader();
                fr.onload = () => res(fr.result);
                fr.readAsDataURL(b);
            });
        };
        for (const img of imgs) {
            try {
                img.src = await toBase64(img.src);
                count++;
                if (count % 5 === 0) {
                    showToast(t('offline_progress').replace('{0}', count).replace('{1}', imgs.length));
                    await new Promise(resolve => setTimeout(resolve, 0));
                }
            } catch(e) {}
        }
        const blob = new Blob(["<!DOCTYPE html>" + clone.outerHTML], {type: 'text/html'});
        const a = document.createElement('a');
        a.href = URL.createObjectURL(blob);
        a.download = `TGACH_Thread_${location.pathname.split('/').pop()}_Offline.html`;
        a.click();
        showToast(t('offline_done'));
    }
};
const CatalogManager = {
    init() {
        const filterInput = document.getElementById('catalog-filter');
        if (filterInput) {
            filterInput.addEventListener('input', debounce((e) => {
                const term = e.target.value.toLowerCase();
                const items = document.querySelectorAll('.catalog-item');
                requestAnimationFrame(() => {
                    items.forEach(item => {
                        const text = item.textContent.toLowerCase();
                        item.style.display = text.includes(term) ? 'flex' : 'none';
                    });
                });
            }, 500));
        }
        const sizeSelect = document.getElementById('catalog-size');
        const grid = document.getElementById('catalog-grid');
        if (sizeSelect && grid) {
            const savedSize = localStorage.getItem('catalog_size') || 'medium';
            sizeSelect.value = savedSize;
            this.setSize(grid, savedSize);
            sizeSelect.addEventListener('change', (e) => {
                const newSize = e.target.value;
                this.setSize(grid, newSize);
                localStorage.setItem('catalog_size', newSize);
            });
        }
        if (filterInput && grid) {
            const observer = new MutationObserver((mutations) => {
                const term = filterInput.value.toLowerCase();
                if (!term) return;
                mutations.forEach(m => {
                    m.addedNodes.forEach(node => {
                        if (node.nodeType === 1 && node.classList.contains('catalog-item')) {
                            const text = node.textContent.toLowerCase();
                            node.style.display = text.includes(term) ? 'flex' : 'none';
                        }
                    });
                });
            });
            observer.observe(grid, { childList: true });
        }
        document.addEventListener('click', (e) => {
            const textBlock = e.target.closest('.catalog-text-content');
            if (textBlock) {
                const isOverflowing = textBlock.scrollHeight > textBlock.clientHeight;
                const isActive = textBlock.classList.contains('is-scrollable');
                if (isOverflowing || isActive) {
                    e.preventDefault();
                    e.stopPropagation();
                    textBlock.classList.toggle('is-scrollable');
                }
            }
        });
        if (grid) {
            const items = grid.querySelectorAll('.catalog-text-content');
            items.forEach(el => {
                if (!el.dataset.parsed) {
                    window.parseTextEffects(el); 
                }
            });
        }
    },
    setSize(grid, size) {
        grid.classList.remove('size-small', 'size-medium', 'size-large');
        if (size !== 'medium') {
            grid.classList.add(`size-${size}`);
        }
    }
};
const PreviewManager = {
    cache: new Map(),
    stack: [],
    openTimer: null,
    closeTimer: null,
    init() {
        document.body.addEventListener('mouseover', (e) => {
            if (window.innerWidth <= 768) return;
            const link = e.target.closest('a.post-num-link, a.post-link');
            const popup = e.target.closest('.post-preview-popup');
            if (link || popup) clearTimeout(this.closeTimer);
            if (link && !link.classList.contains('cross-board-link')) {
                this.onHover(e, link);
            }
        });
        document.body.addEventListener('mouseout', (e) => {
            if (window.innerWidth <= 768) return;
            const link = e.target.closest('a.post-num-link, a.post-link');
            const popup = e.target.closest('.post-preview-popup');
            if (link || popup) {
                this.closeTimer = setTimeout(() => this.closeAll(), 300);
            }
        });
        document.body.addEventListener('click', (e) => {
            if (window.innerWidth > 768) return;
            const link = e.target.closest('a.post-num-link, a.post-link');
            if (!link || link.classList.contains('cross-board-link')) return;
            if (link.closest('.post-actions')) return;
            if (link.classList.contains('post-num-link') && link.closest('.post-header')) {
                return; 
            }
            if (link.classList.contains('preview-active')) {
                return;
            }
            e.preventDefault();
            e.stopPropagation();
            this.onHover(e, link, true);
        });
        document.addEventListener('click', (e) => {
            const popup = e.target.closest('.post-preview-popup');
            if (popup && !e.target.closest('a')) {
                const currentLevel = parseInt(popup.dataset.level);
                this.cleanup(currentLevel + 1);
                return;
            }
            if (!popup && !e.target.closest('a.post-num-link, a.post-link')) {
                this.closeAll();
            }
        });
    },
    onHover(e, link, immediate = false) {
        const postNum = link.dataset.postNum;
        if (!postNum) return;
        const parentPost = link.closest('.post');
        if (parentPost && parentPost.id === `post-${postNum}`) return;
        if (this.stack.some(item => item.num === postNum)) return;
        const parentPopup = link.closest('.post-preview-popup');
        const level = parentPopup ? parseInt(parentPopup.dataset.level) + 1 : 1;
        const showFn = () => {
            this.cleanup(level);
            link.classList.add('preview-active');
            this.show(link, postNum, level);
        };
        if (immediate) {
            showFn();
        } else {
            clearTimeout(this.openTimer);
            this.openTimer = setTimeout(showFn, 200);
        }
    },
    cleanup(fromLevel) {
        for (let i = this.stack.length - 1; i >= 0; i--) {
            if (this.stack[i].level >= fromLevel) {
                const item = this.stack.pop();
                item.el.classList.remove('visible');
                item.sourceLink.classList.remove('preview-active');
                setTimeout(() => item.el.remove(), 150);
            }
        }
    },
    closeAll() {
        this.cleanup(1);
    },
    async show(targetLink, num, level) {
        const popup = document.createElement('div');
        popup.className = `post-preview-popup popup-level-${level}`;
        popup.dataset.level = level;
        popup.innerHTML = `<div class="post" style="padding:10px; margin:0; border:none; background:transparent;">⏳ #${num}...</div>`;
        if (document.body.classList.contains('modal-open')) {
            popup.style.zIndex = '20000';
        }
        document.body.appendChild(popup);
        const rect = targetLink.getBoundingClientRect();
        const popupRect = popup.getBoundingClientRect();
        const viewportW = window.innerWidth;
        const viewportH = window.innerHeight;
        let top, left;
        if (rect.bottom + popupRect.height + 10 < viewportH) {
            top = rect.bottom + window.scrollY + 5;
        } else {
            top = rect.top + window.scrollY - popupRect.height - 5;
        }
        left = rect.left + window.scrollX;
        if (left + popupRect.width > viewportW - 10) {
            left = viewportW - popupRect.width - 10;
        }
        if (left < 10) left = 10;
        popup.style.top = `${top}px`;
        popup.style.left = `${left}px`;
        requestAnimationFrame(() => popup.classList.add('visible'));
        this.stack.push({ el: popup, level: level, num: num, sourceLink: targetLink });
        try {
            const existing = document.getElementById(`post-${num}`);
            if (existing) {
                this.render(popup, num, existing.querySelector('.post-content').innerHTML, true);
                this.recalcPosition(popup, targetLink);
                return;
            }
            let data = this.cache.get(num);
            if (!data) {
                if (this.activeController) this.activeController.abort();
                this.activeController = new AbortController();
                try {
                    const res = await fetch(`/api/post/${num}`, { signal: this.activeController.signal });
                    if (!res.ok) throw new Error('404');
                    data = await res.json();
                    if (this.cache.size > 200) {
                        this.cache.clear();
                        console.log('🧹 [Memory] Preview cache cleared');
                    }

                    this.cache.set(num, data);
                } catch (e) {
                    if (e.name === 'AbortError') return;
                    throw e;
                } finally {
                    this.activeController = null;
                }
            }
            const dummyEl = PostRenderer.create(data, 'preview');
            this.render(popup, num, dummyEl.querySelector('.post-content').innerHTML, false);
            this.recalcPosition(popup, targetLink);
        } catch (e) {
            popup.innerHTML = `<div class="post" style="padding:10px; color:var(--action-danger);">${t('preview_not_found').replace('{0}', num)}</div>`;
        }
    },
    render(popupEl, num, contentHtml, isClone) {
        popupEl.innerHTML = `
            <div class="post" style="border:none; box-shadow:none; margin:0; background:transparent;">
                <div class="post-header">
                    <span class="post-num">#${num}</span>
                    ${isClone ? `<span style="opacity:0.5; font-size:0.8em">${t('preview_on_page')}</span>` : ''}
                </div>
                <div class="post-content">${contentHtml}</div>
            </div>`;
        
        window.initializePostFeatures(popupEl);
        window.parseTextEffects(popupEl);
        if (typeof SmartLoader !== 'undefined') {
            popupEl.querySelectorAll('img.lazy-load, video.lazy-load').forEach(img => {
                SmartLoader.enqueue(img);
            });
        }
    },
    recalcPosition(popup, targetLink) {
        const rect = targetLink.getBoundingClientRect();
        const popupRect = popup.getBoundingClientRect();
        const viewportH = window.innerHeight;
        const currentTop = parseFloat(popup.style.top);
        const linkTopAbs = rect.top + window.scrollY;
        if (currentTop < linkTopAbs) {
             const newTop = linkTopAbs - popupRect.height - 5;
             popup.style.top = `${newTop}px`;
        } else {
            if (rect.bottom + popupRect.height + 10 > viewportH) {
                 const newTop = linkTopAbs - popupRect.height - 5;
                 if (newTop > window.scrollY) {
                     popup.style.top = `${newTop}px`;
                 }
            }
        }
    }
};
const HotkeyManager = {
    init() {
        document.addEventListener('keydown', (e) => this.handleKey(e));
    },
    handleKey(e) {
        const tag = document.activeElement.tagName;
        const isTyping = tag === 'INPUT' || tag === 'TEXTAREA';
        if ((e.ctrlKey || e.metaKey) && e.key === 'Enter') {
            e.preventDefault();
            this.submitForm();
            return;
        }

        if (e.altKey && e.key === 'Enter') {
            e.preventDefault();
            this.submitForm();
            return;
        }

        if (isTyping) return;

        if (e.code === 'KeyR' && !e.ctrlKey && !e.metaKey && !e.altKey) {
            e.preventDefault();
            if (typeof FormManager !== 'undefined') {
                if (document.getElementById('floating-reply-box').style.display !== 'none') {
                    FormManager.floatingTextarea.focus();
                } else {
                    FormManager.focusMainForm('');
                }
            }
        }
        if (e.key === '[' || e.key === ']') {
            this.navigateThreads(e.key === ']');
        }
    },
    submitForm() {
        const floating = document.getElementById('floating-reply-box');
        if (floating && floating.style.display !== 'none') {
            const btn = floating.querySelector('button[type="submit"]');
            if (btn) btn.click();
        } else {
            const main = document.getElementById('post-form');
            const btn = main ? main.querySelector('button[type="submit"]') : null;
            if (btn) btn.click();
        }
    },
    navigateThreads(isNext) {
        const selector = document.querySelector('.catalog-grid') ? '.catalog-item' : '.thread-container';
        const items = Array.from(document.querySelectorAll(selector));
        if (items.length === 0) return;
        const currentIdx = items.findIndex(el => {
            const rect = el.getBoundingClientRect();
            return rect.top >= -100 && rect.top < window.innerHeight / 2;
        });
        let targetIdx = -1;
        if (currentIdx === -1) {
            targetIdx = isNext ? 0 : items.length - 1;
        } else {
            targetIdx = isNext ? currentIdx + 1 : currentIdx - 1;
        }
        if (targetIdx >= 0 && targetIdx < items.length) {
            const target = items[targetIdx];
            const headerOffset = 60;
            const elementPosition = target.getBoundingClientRect().top;
            const offsetPosition = elementPosition + window.pageYOffset - headerOffset;
            window.scrollTo({
                top: offsetPosition,
                behavior: "smooth"
            });
            target.style.transition = 'box-shadow 0.2s';
            const oldShadow = target.style.boxShadow;
            target.style.boxShadow = '0 0 0 2px var(--accent-primary)';
            setTimeout(() => target.style.boxShadow = oldShadow, 500);
        }
    }
};
const ScrollManager = {
    loading: false,
    page: 1,
    finished: false,
    init() {
        const chatList = document.querySelector('.chat-list');
        const threadList = document.querySelector('.thread-list');
        const repliesList = document.querySelector('.replies-list');
        
        this.isChat = !!chatList;
        this.isBoard = !!threadList;
        this.isThread = !!repliesList;
        
        const container = chatList || threadList || repliesList;
        if (!container) return;

        const isSkeleton = document.body.dataset.skeleton === 'true';
        if (isSkeleton) {
            this.page = 0;
            // NEW: Handle thread skeleton (Single Thread View)
            if (this.isThread) {
                this.loadThreadSkeleton();
                return; // Stop here, no scroll observer needed for thread initially
            }
        }

        this.loader = document.getElementById(this.isChat ? 'chat-loading-indicator' : 'threads-loading-indicator');
        const sentinel = document.createElement('div');
        sentinel.id = 'scroll-sentinel';
        sentinel.style.cssText = 'height: 1px; width: 100%; pointer-events: none; clear: both;';
        container.after(sentinel);
        
        const scrollObserver = new IntersectionObserver((entries) => {
            if (entries[0].isIntersecting && !this.loading && !this.finished) {
                this.loadMore();
            }
        }, { 
            rootMargin: '600px',
            threshold: 0.01 
        });
        scrollObserver.observe(sentinel);
        this._observer = scrollObserver;

        if (isSkeleton) {
            this.loadMore();
        } else if (this.isBoard && threadList.children.length === 0 && !document.querySelector('.no-threads-msg')) {
            setTimeout(() => this.loadMore(), 500);
        }
    },
    async loadThreadSkeleton() {
        if (this.loading) return;
        this.loading = true;
        
        const loader = document.getElementById('threads-loading-indicator');
        if (loader) loader.style.display = 'block';

        const boardId = document.body.dataset.currentBoard;
        // Extract thread ID from URL
        const threadIdMatch = location.pathname.match(/\/res\/(\d+)\.html/);
        if (!boardId || !threadIdMatch) return;
        const threadId = threadIdMatch[1];

        try {
            const res = await fetch(`/api/thread/${boardId}/${threadId}`);
            if (!res.ok) throw new Error('API Error');
            const posts = await res.json();
            
            if (loader) loader.style.display = 'none';

            if (!posts || posts.length === 0) return;

            // 1. Render OP Post
            const opData = posts[0];
            const opContainer = document.querySelector('.op-post');
            if (opContainer) {
                // Установка мета-данных ОП-автора ДО рендеринга постов
                const opAuthorId = String(opData.author_id);
                document.body.dataset.opAuthorId = opAuthorId;
                const mainTag = document.querySelector('main');
                if (mainTag) mainTag.dataset.opAuthorId = opAuthorId;

                const newOp = PostRenderer.create(opData, 'thread');
                opContainer.innerHTML = '';
                opContainer.appendChild(newOp);
                
                // Очистка заголовка от HTML и обновление тайтла
                const cleanTitle = (opData.thread_title || opData.content.text || "").replace(/<[^>]+>/g, '').slice(0, 60);
                document.title = `/${boardId}/ - ${cleanTitle || 'Thread #' + opData.id}`;
                
                document.body.dataset.isArchived = opData.is_archived ? 'true' : 'false';
            }

            // 2. Render Replies
            const replies = posts.slice(1);
            const container = document.querySelector('.replies-list');
            const frag = document.createDocumentFragment();
            const newElements = [];
            
            // Add OP to init list
            const opEl = opContainer ? opContainer.querySelector('.post') : null;
            if (opEl) newElements.push(opEl);

            replies.forEach(p => {
                const el = PostRenderer.create(p, 'thread');
                frag.appendChild(el);
                newElements.push(el);
            });
            
            if (container) {
                container.innerHTML = '';
                container.appendChild(frag);
            }

            requestAnimationFrame(() => {
                newElements.forEach(el => window.initializePostFeatures(el));
                if (window.WSManager) WSManager.init(boardId);
                
                // Scroll to anchor
                const hash = window.location.hash;
                if (hash) {
                    const target = document.querySelector(hash);
                    if (target) setTimeout(() => target.scrollIntoView(), 100);
                }
            });

        } catch (e) {
            console.error(e);
            if (loader) loader.innerHTML = '<div style="color:var(--action-danger)">Ошибка загрузки треда</div>';
        } finally {
            this.loading = false;
        }
    },
    async loadMore() {
        if (this.loading) return;
        this.loading = true;
        if(this.loader) this.loader.style.display = 'block';
        
        const boardId = document.querySelector('input[name="board_id"]')?.value;
        if (!boardId) { this.loading = false; return; }

        try {
            const endpoint = this.isChat ? `/api/chat/${boardId}` : `/api/threads/${boardId}`;
            const sort = new URLSearchParams(location.search).get('sort') || 'bump';
            const url = `${endpoint}?page=${this.page}&sort=${sort}`;
            
            const res = await fetch(url);
            if (!res.ok) throw new Error('API Error');
            const data = await res.json();
            
            if (this.loader) this.loader.style.display = 'none';
            
            if (!data || data.length === 0) {
                this.finished = true;
                if (this.page === 0) {
                    const msg = document.getElementById('no-threads-msg');
                    if (msg) msg.style.display = 'block';
                } else {
                    if (this.loader) {
                        this.loader.style.display = 'block';
                        this.loader.textContent = t('scroll_no_more');
                    }
                }
            } else {
                const msg = document.getElementById('no-threads-msg');
                if (msg) msg.style.display = 'none';
                
                const frag = document.createDocumentFragment();
                const container = document.querySelector(this.isChat ? '.chat-list' : '.thread-list');
                const newElements = [];

                data.forEach(item => {
                    const el = this.isChat
                        ? PostRenderer.create(item, 'chat')
                        : this.createThreadElement(item);
                    
                    if (this.isChat) window.parseTextEffects(el);
                    
                    frag.appendChild(el);
                    newElements.push(el);
                });
                
                container.appendChild(frag);
                this.page++;

                requestAnimationFrame(() => {
                    newElements.forEach(el => {
                        window.initializePostFeatures(el);
                    });
                });
            }
        } catch (e) {
            console.error(e);
        } finally {
            this.loading = false;
            if(this.loader && !this.finished) this.loader.style.display = 'none';
        }
    },
    createThreadElement(data) {
        const div = document.createElement('div');
        div.className = 'thread-container';
        div.id = `thread-${data.id}`;
        const opPost = PostRenderer.create(data, 'board');
        div.appendChild(opPost);
        
        const statsDiv = document.createElement('div');
        statsDiv.className = 'thread-stats-container';
        
        const pCount = data.posts_count || 1;
        const aCount = data.anon_count || 1;
        const openText = t('btn_open_thread', 'Открыть');
        const threadUrl = `/${data.board_id}/res/${data.id}.html`;
        const statsText = t('stat_thread_details').replace('{replies}', pCount).replace('{anons}', aCount);
        statsDiv.innerHTML = `${statsText} <a href="${threadUrl}">[${openText}]</a>`;
        div.appendChild(statsDiv);
        
        if (data.latest_replies?.length) {
            const repliesDiv = document.createElement('div');
            repliesDiv.className = 'latest-replies-container';
            data.latest_replies.forEach(r => {
                const rep = PostRenderer.create(r, 'board-reply');
                rep.classList.add('reply-preview');
                repliesDiv.appendChild(rep);
            });
            div.appendChild(repliesDiv);
        }
        div.insertAdjacentHTML('beforeend', '<hr class="thread-separator">');
        checkThreadLimit(div);
        return div;
    }
};
const JumpManager = {
    lastPosition: 0,
    lastPostId: null,
    timer: null,
    container: null,

    init() {
        this.container = document.createElement('div');
        this.container.id = 'jump-return-container';
        document.body.appendChild(this.container);

        this.container.onclick = (e) => {
            e.preventDefault();
            this.goBack();
        };

        document.body.addEventListener('click', (e) => {
            const link = e.target.closest('a[href^="#post-"], .post-link, .post-num-link');
            if (link && !link.classList.contains('cross-board-link')) {
                const sourcePost = this.findCurrentPost();
                this.handleJump(sourcePost);
            }
        });
        window.addEventListener('scroll', () => {
            if (this.container.classList.contains('active')) {
                if (Math.abs(window.scrollY - this.lastPosition) < 150) {
                    this.hide();
                }
            }
        }, { passive: true });
    },

    findCurrentPost() {
        const posts = document.querySelectorAll('.post');
        for (let post of posts) {
            const rect = post.getBoundingClientRect();
            if (rect.top >= -100 && rect.top <= window.innerHeight / 2) {
                return post.id.replace('post-', '');
            }
        }
        return null;
    },

    handleJump(sourceId) {
        const oldY = window.scrollY;
        requestAnimationFrame(() => {
            setTimeout(() => {
                const newY = window.scrollY;
                if (Math.abs(newY - oldY) > 400) {
                    this.lastPosition = oldY;
                    this.lastPostId = sourceId;
                    this.show();
                }
            }, 50);
        });
    },

    show() {
        clearTimeout(this.timer);
        const label = this.lastPostId ? `К посту #${this.lastPostId}` : t('jump_return', 'Вернуться назад');
        this.container.innerHTML = `<button class="jump-return-btn"><span>⬅</span> ${label}</button>`;
        this.container.classList.add('active');
        this.timer = setTimeout(() => this.hide(), 7000);
    },

    hide() {
        this.container.classList.remove('active');
    },

    goBack() {
        window.scrollTo({
            top: this.lastPosition,
            behavior: 'smooth'
        });
        this.hide();
    }
};
const UIManager = {
    init() {
        this.initHeaderHiding();
        this.initScrollTop();
        this.initQuickQuote();
        this.initStickyVideo();
        this.initUserFilter();
        this.initDoubleTap();
        this.initPostFilters();
        const expandBtn = document.getElementById('expand-all-btn');
        if (expandBtn) {
            expandBtn.onclick = (e) => {
                e.preventDefault();
                const isCurrentlyExpanded = expandBtn.textContent.includes('Свернуть');
                const allThumbs = document.querySelectorAll('.file-thumb');
                if (isCurrentlyExpanded) {
                    allThumbs.forEach(thumb => {
                        if (thumb.classList.contains('is-expanded')) {
                            toggleInlineExpansion(thumb);
                        }
                    });
                    expandBtn.textContent = `🖼️ ${t('btn_expand_all', 'Раскрыть все')}`;
                } else {
                    allThumbs.forEach(thumb => {
                        if (!thumb.classList.contains('is-expanded')) {
                            toggleInlineExpansion(thumb);
                        }
                    });
                    expandBtn.textContent = `❌ ${t('btn_collapse_all', 'Свернуть все')}`;
                }
            };
        }
    },
    initHeaderHiding() {
        const header = document.querySelector('header');
        const isTargetPage = location.pathname.includes('/res/') || 
                             document.querySelector('.thread-list') || 
                             document.querySelector('.chat-list') || 
                             location.pathname.includes('/overboard/');
        if (!header || !isTargetPage) return;
        const trigger = document.createElement('div');
        trigger.id = 'header-trigger';
        trigger.title = 'Показать меню';
        document.body.appendChild(trigger);
        const spacer = document.createElement('div');
        let isSticky = false;
        const showHeader = () => {
            if (!isSticky) return;
            spacer.style.height = (header.offsetHeight + 15) + 'px';
            spacer.style.width = '100%';
            if (!spacer.isConnected) header.parentNode.insertBefore(spacer, header);
            header.classList.add('sticky-mode');
            requestAnimationFrame(() => header.classList.add('visible'));
            trigger.classList.remove('active');
        };
        const hideHeader = () => {
            if (!isSticky) return;
            header.classList.remove('visible');
            setTimeout(() => {
                if (!header.classList.contains('visible')) {
                    header.classList.remove('sticky-mode');
                    if (spacer.isConnected) spacer.remove(); 
                    if (window.scrollY > 300) trigger.classList.add('active');
                }
            }, 300);
        };
        window.addEventListener('scroll', () => {
            if (window.scrollY > 300) {
                if (!isSticky) {
                    trigger.classList.add('active');
                    isSticky = true;
                }
            } else {
                if (isSticky) {
                    trigger.classList.remove('active');
                    header.classList.remove('sticky-mode', 'visible');
                    if (spacer.isConnected) spacer.remove(); 
                    isSticky = false;
                }
            }
        }, { passive: true });
        trigger.addEventListener('mouseenter', showHeader);
        trigger.addEventListener('click', (e) => { e.stopPropagation(); showHeader(); });
        header.addEventListener('mouseleave', hideHeader);
        document.addEventListener('click', (e) => {
            if (isSticky && header.classList.contains('visible') && !header.contains(e.target) && e.target !== trigger) {
                hideHeader();
            }
        });
    },
    initScrollTop() {
        const btnUp = document.getElementById('scroll-top-btn');
        if (btnUp) btnUp.remove();
        const btnDown = document.getElementById('scroll-bottom-btn');
        if (btnDown) btnDown.remove();
    },
    initQuickQuote() {
        let btn = document.getElementById('quick-quote-btn');
        if (!btn) {
            btn = document.createElement('div');
            btn.id = 'quick-quote-btn';
            btn.textContent = t('quote_btn');
            document.body.appendChild(btn);
        }
        if (btn.dataset.listening) return;
        btn.dataset.listening = "true";
        let selectedText = '';
        document.addEventListener('mouseup', (e) => {
            if (e.target === btn) return;
            setTimeout(() => {
                const sel = window.getSelection();
                const text = sel.toString().trim();
                const anchor = sel.anchorNode;
                const post = anchor ? (anchor.nodeType === 1 ? anchor : anchor.parentElement).closest('.post') : null;
                if (text.length > 0 && post) {
                    selectedText = text;
                    const range = sel.getRangeAt(0);
                    const rect = range.getBoundingClientRect();
                    btn.style.top = `${rect.top + window.scrollY - 40}px`;
                    btn.style.left = `${rect.left + window.scrollX}px`;
                    btn.style.display = 'block';
                    btn.dataset.postNum = post.id.replace('post-', '');
                    btn.style.animation = 'none';
                    btn.offsetHeight;
                    btn.style.animation = 'popIn 0.2s forwards';
                } else {
                    btn.style.display = 'none';
                }
            }, 10);
        });
        document.addEventListener('mousedown', (e) => {
            if (e.target !== btn) {
                btn.style.display = 'none';
            }
        });
        document.addEventListener('selectionchange', () => {
            const sel = window.getSelection();
            if (!sel || sel.toString().trim().length === 0) {
                btn.style.display = 'none';
            }
        });
        window.addEventListener('scroll', () => {
            if (btn.style.display !== 'none') {
                btn.style.display = 'none';
                window.getSelection().removeAllRanges();
            }
        }, { passive: true });
        btn.addEventListener('mousedown', (e) => {
            e.preventDefault(); e.stopPropagation();
            const num = btn.dataset.postNum;
            const txt = selectedText; 
            const main = document.getElementById('post-form');
            const floatBox = document.getElementById('floating-reply-box');
            const rect = main.getBoundingClientRect();
            
            const isVisible = (
                rect.top >= 0 && rect.left >= 0 &&
                rect.bottom <= (window.innerHeight || document.documentElement.clientHeight)
            );
            
            if (isVisible && floatBox.style.display === 'none') {
                const ta = main.querySelector('textarea');
                FormManager.appendToTextarea(ta, num, txt);
            } else {
                const isOpen = floatBox.style.display !== 'none';
                FormManager.showFloating(isOpen ? null : e, num, txt);
            }
            btn.style.display = 'none';
            setTimeout(() => window.getSelection().removeAllRanges(), 10);
        });
    },
    initStickyVideo() {
        const stickyObs = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                const vid = entry.target;
                const isScrolledPast = entry.boundingClientRect.top < 0;
                if (!entry.isIntersecting && !vid.paused && !vid.ended && isScrolledPast && !vid.classList.contains('video-note')) {
                    vid.classList.add('sticky-mode');
                    if (!vid.nextElementSibling?.classList.contains('sticky-close-btn')) {
                        const btn = document.createElement('button');
                        btn.className = 'sticky-close-btn show';
                        btn.innerHTML = '×';
                        const closeAction = (e) => {
                            e.preventDefault();
                            e.stopPropagation();
                            vid.pause();
                            vid.classList.remove('sticky-mode');
                            btn.remove();
                        };
                        btn.onclick = closeAction;
                        btn.ontouchstart = closeAction;
                        vid.after(btn);
                    } else {
                        vid.nextElementSibling.classList.add('show');
                    }
                } else if (entry.isIntersecting) {
                    vid.classList.remove('sticky-mode');
                    if (vid.nextElementSibling?.classList.contains('sticky-close-btn')) {
                        vid.nextElementSibling.remove();
                    }
                }
            });
        }, { threshold: 0.0 });
        window.observeSticky = (el) => stickyObs.observe(el);
    },
    initPostFilters() {
        const bindFilter = (btnId, bodyClass) => {
            const btn = document.getElementById(btnId);
            if (!btn) return;
            btn.addEventListener('click', () => {
                const isActive = btn.classList.toggle('active');
                document.body.classList.toggle(bodyClass, isActive);
                if (bodyClass === 'my-posts-view') {
                    document.body.classList.remove('replies-to-me-view');
                    document.getElementById('replies-to-me-filter-btn')?.classList.remove('active');
                } else {
                    document.body.classList.remove('my-posts-view');
                    document.getElementById('my-posts-filter-btn')?.classList.remove('active');
                }
            });
        };
        bindFilter('my-posts-filter-btn', 'my-posts-view');
        bindFilter('replies-to-me-filter-btn', 'replies-to-me-view');
    },
    initUserFilter() {
        let lastTapTime = 0;
        let lastTappedId = null;

        document.body.addEventListener('click', (e) => {
            if (e.target.classList.contains('identicon')) {
                const uid = e.target.dataset.userid;
                if (window.innerWidth <= 768) {
                    const now = Date.now();
                    const isDoubleTap = (now - lastTapTime < 400) && (lastTappedId === uid);
                    
                    if (!isDoubleTap) {
                        e.preventDefault();
                        e.stopPropagation();
                        if (typeof SonicID !== 'undefined') SonicID.play(uid);
                        lastTapTime = now;
                        lastTappedId = uid;
                        return;
                    }
                }

                const isActive = document.body.dataset.filteredUser === uid;
                
                if (isActive) {
                    delete document.body.dataset.filteredUser;
                    document.body.classList.remove('filtering-by-id');
                    document.querySelectorAll('.post.filter-highlight').forEach(p => p.classList.remove('filter-highlight'));
                } else {
                    document.body.dataset.filteredUser = uid;
                    document.body.classList.add('filtering-by-id');
                    document.querySelectorAll('.post').forEach(p => {
                        if (p.querySelector(`.identicon[data-userid="${uid}"]`)) {
                            p.classList.add('filter-highlight');
                        } else {
                            p.classList.remove('filter-highlight');
                        }
                    });
                }
            }
        });
    },
    initDoubleTap() {
        let lastTap = 0;
        document.addEventListener('click', (e) => {
            if (e.target.closest('a, button, input, textarea, video, .action-menu, .post-actions')) return;
            
            const post = e.target.closest('.post');
            if (!post) return;
            
            const cur = new Date().getTime();
            const diff = cur - lastTap;
            if (diff < 300 && diff > 50) {
                const selection = window.getSelection().toString();
                if (selection.length > 0) return;
                e.preventDefault();
                const postNum = post.id.replace('post-', '');
                const isThreadPage = location.pathname.includes('/res/');
                post.style.transform = 'scale(0.99)';
                setTimeout(() => post.style.transform = '', 100);
                
                if (isThreadPage) {
                    FormManager.showFloating(e, postNum);
                } else {
                    const menuBtn = post.querySelector('.action-menu-trigger');
                    const boardId = menuBtn?.dataset.boardId || document.body.dataset.currentBoard || 'b';
                    let threadId = menuBtn?.dataset.threadId;
                    
                    if (!threadId || threadId === 'null') threadId = postNum;
                    window.location.href = `/${boardId}/res/${threadId}.html#post-${postNum}`;
                }
            }
            lastTap = cur;
        });
    }
};
const CrossBoardManager = {
    resolve(scope = document) {
        const targets = [];
        scope.querySelectorAll('a[href*="/res/0#post-"]').forEach(a => {
            const match = a.getAttribute('href').match(/^\/([a-z0-9]+)\/res\/0/);
            const board = match ? match[1] : (document.querySelector('input[name="board_id"]')?.value || 'b');
            targets.push({ el: a, num: a.dataset.postNum, board: board });
        });

        if (!targets.length) return;
        const ids = targets.map(t => t.num);
        
        fetch('/api/get-thread-ids', {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify({ post_nums: ids })
        })
        .then(r => r.json())
        .then(map => {
            targets.forEach(({el, num, board}) => {
                const threadId = map[num];
                if (threadId) {
                    el.href = `/${board}/res/${threadId}.html#post-${num}`;
                    el.classList.remove('dead-link');
                    el.title = "";
                } else {
                    el.classList.add('dead-link');
                    el.title = t('post_not_found');
                    el.style.opacity = '0.5';
                }
            });
        })
        .catch(console.error);
    }
};
const BottleManager = {
    init() {
        document.body.addEventListener('click', (e) => {
            if (e.target.classList.contains('action-send-bottle')) {
                e.preventDefault();
                const num = e.target.dataset.postNum;
                const modal = document.getElementById('bottle-modal');
                const input = modal?.querySelector('#bottle-post-num-input');
                if (input) input.value = num;
                openModal('bottle-modal');
            }
        });
        const cancelBtn = document.getElementById('bottle-cancel-btn');
        if (cancelBtn) {
            cancelBtn.onclick = (e) => {
                e.preventDefault();
                closeModal();
            };
        }
        const form = document.getElementById('bottle-form');
        if (form) {
            form.addEventListener('submit', async (e) => {
                e.preventDefault();
                const postNumInput = form.querySelector('#bottle-post-num-input');
                const textInput = form.querySelector('#bottle-text');
                const data = {
                    post_num: parseInt(postNumInput.value),
                    text: textInput.value
                };
                try {
                    const res = await fetch('/api/bottle/send', {
                        method: 'POST',
                        headers: {'Content-Type': 'application/json'},
                        body: JSON.stringify(data)
                    });
                    const json = await res.json();
                    if (res.ok) {
                        showToast(json.message || t('bottle_sent_default'));
                        closeModal();
                        form.reset();
                    } else {
                        showToast(`Ошибка: ${json.detail}`);
                    }
                } catch (err) {
                    showToast(t('network_error'));
                }
            });
        }
        const notifBtn = document.getElementById('bottle-notification-btn');
        if (notifBtn) {
            notifBtn.addEventListener('click', async (e) => {
                e.preventDefault();
                try {
                    const res = await fetch('/api/bottle/read');
                    const data = await res.json();
                    if (res.ok && data.message) {
                        alert(t('bottle_alert_prefix') + data.message);
                        notifBtn.style.display = 'none';
                        const counter = document.getElementById('bottle-count');
                        if (counter) counter.textContent = '';
                    } else {
                        showToast(t('stash_load_error'));
                    }
                } catch (err) {
                    console.error(err);
                    showToast(t('network_error'));
                }
            });
        }
        this.check();
        setInterval(() => {
            if (!document.hidden) this.check();
        }, 120000);
    },
    async check() {
        if (!document.body.dataset.userId) return;
        const btn = document.getElementById('bottle-notification-btn');
        if (!btn) return;
        try {
            const res = await fetch('/api/bottle/count');
            if (res.ok) {
                const data = await res.json();
                const count = data.count || 0;
                const counter = document.getElementById('bottle-count');
                if (count > 0) {
                    btn.style.display = 'flex';
                    if (counter) counter.textContent = count;
                    btn.classList.add('shake-anim');
                } else {
                    btn.style.display = 'none';
                    btn.classList.remove('shake-anim');
                }
            }
        } catch (e) {
        }
    }
};
const CooldownManager = {
    intervals: new Map(), 
    
    getStorageKey(form) {
        const board = form.querySelector('input[name="board_id"]')?.value || 'unknown';
        const modeInput = form.querySelector('input[name="post_mode"]');
        const mode = modeInput ? modeInput.value : 'reply';
        const scope = (mode === 'new_thread') ? 'thread' : 'post';
        return `cooldown_v5_${board}_${scope}`;
    },

    init(formElement) {
        if (!formElement) return;
        this.check(formElement);
        
        document.addEventListener('visibilitychange', () => {
            if (!document.hidden) this.check(formElement);
        });
        window.addEventListener('pageshow', () => this.check(formElement));
    },

    start(formElement, seconds) {
        if (!formElement) return;
        const key = this.getStorageKey(formElement);
        const unlockTime = Date.now() + (seconds * 1000);
        localStorage.setItem(key, unlockTime);
        this.check(formElement);
    },

    check(formElement) {
        const key = this.getStorageKey(formElement);
        const unlockTimeStr = localStorage.getItem(key);
        if (!unlockTimeStr) return;

        const unlockTime = parseInt(unlockTimeStr);
        const now = Date.now();
        const remaining = Math.ceil((unlockTime - now) / 1000);

        if (remaining > 0) {
            this.startUI(formElement, unlockTime);
        } else {
            this.unlockUI(formElement);
            localStorage.removeItem(key);
        }
    },

    startUI(formElement, unlockTime) {
        const submitBtn = formElement.querySelector('button[type="submit"]');
        if (!submitBtn) return;
        
        if (!submitBtn.dataset.originalText) {
            submitBtn.dataset.originalText = submitBtn.textContent;
        }

        submitBtn.disabled = true;
        submitBtn.style.opacity = '0.6';
        submitBtn.style.cursor = 'not-allowed';

        if (this.intervals.has(formElement)) {
            clearInterval(this.intervals.get(formElement));
        }

        const update = () => {
            const now = Date.now();
            const left = Math.ceil((unlockTime - now) / 1000);
            
            if (left <= 0) {
                this.unlockUI(formElement);
                clearInterval(this.intervals.get(formElement));
                this.intervals.delete(formElement);
                localStorage.removeItem(this.getStorageKey(formElement));
            } else {
                submitBtn.textContent = `⏳ ${left}с`;
            }
        };

        update(); 
        const intervalId = setInterval(update, 500); 
        this.intervals.set(formElement, intervalId);
    },

    unlockUI(formElement) {
        const submitBtn = formElement.querySelector('button[type="submit"]');
        if (!submitBtn) return;
        
        submitBtn.disabled = false;
        if (submitBtn.dataset.originalText) {
            submitBtn.textContent = submitBtn.dataset.originalText;
        }
        submitBtn.style.opacity = '';
        submitBtn.style.cursor = '';
        
        if (this.intervals.has(formElement)) {
            clearInterval(this.intervals.get(formElement));
            this.intervals.delete(formElement);
        }
    },

    getSiteMode: () => document.body.dataset.siteMode || 'PRIVATE',
    isLoggedIn: () => !!document.body.dataset.userId
};
const AdminUtils = {
    init() {
        this.initShadowBanForm();
        this.checkFeedbackCount();
    },

    initShadowBanForm() {
        const sbForm = document.getElementById('shadow-ban-form');
        if (sbForm && !sbForm.dataset.initialized) {
            sbForm.dataset.initialized = 'true';
            const newForm = sbForm.cloneNode(true);
            sbForm.parentNode.replaceChild(newForm, sbForm);
            
            newForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const formData = new FormData(newForm);
                const data = {
                    post_num: parseInt(formData.get('post_num')),
                    duration: parseInt(formData.get('duration'))
                };
                try {
                    const res = await fetch('/api/admin/shadow_ban', {
                        method: 'POST',
                        headers: {'Content-Type': 'application/json'},
                        body: JSON.stringify(data)
                    });
                    if (res.ok) {
                        showToast(t('msg_shadow_cast'));
                        document.getElementById('shadow-ban-modal').style.display = 'none';
                        newForm.reset();
                        if (typeof AdminManager !== 'undefined' && typeof AdminManager.loadUsers === 'function') {
                            const select = document.getElementById('admin-board-select');
                            if (select) AdminManager.loadUsers(select.value);
                        }
                    } else {
                        const err = await res.json();
                        showToast(`${t('error_prefix')}${err.detail}`);
                    }
                } catch (err) {
                    showToast(t('network_error'));
                }
            });
        }
    },

    async checkFeedbackCount() {
        if (document.body.dataset.isAdmin !== 'true') return;
        try {
            const res = await fetch('/api/admin/feedback/count');
            const data = await res.json();
            const badge = document.getElementById('inbox-badge');
            if (badge) {
                if (data.count > 0) {
                    badge.textContent = data.count;
                    badge.style.display = 'inline-block';
                } else {
                    badge.style.display = 'none';
                }
            }
        } catch(e) {}
    }
};
const VpnManager = {
    init() {
        if (sessionStorage.getItem('geo_checked')) {
            this.applyLogic(sessionStorage.getItem('user_country'));
            return;
        }
        fetch('https://api.country.is')
            .then(res => res.json())
            .then(data => {
                const country = data.country;
                sessionStorage.setItem('user_country', country);
                sessionStorage.setItem('geo_checked', 'true');
                document.cookie = `user_country=${country}; path=/; max-age=86400; samesite=lax`;
                this.applyLogic(country);
            })
            .catch(err => console.log('Geo check failed', err));
    },
    applyLogic(country) {
        if (country !== 'RU') return;
        const lastSeen = localStorage.getItem('vpn_toast_ts');
        const now = Date.now();
        const oneDay = 24 * 60 * 60 * 1000;
        if (!lastSeen || (now - lastSeen > oneDay)) {
            this.showReminderToast();
            localStorage.setItem('vpn_toast_ts', now);
        }
    },
    
//  showDisclaimer() {
//     const overlay = document.createElement('div');
//      overlay.className = 'vpn-modal-overlay';
//      overlay.innerHTML = `
//          <div class="vpn-modal">



//              <div class="vpn-links">
//                  <a href="https://github.com/hiddify/hiddify-next/releases" target="_blank" class="vpn-btn">🛡️ Hiddify (Best)</a>
//                  <a href="https://github.com/2dust/v2rayNG" target="_blank" class="vpn-btn">📱 v2rayNG (Android)</a>
//                  <a href="https://github.com/yichengchen/clashX" target="_blank" class="vpn-btn">🍏 ClashX (macOS)</a>
//                  <a href="https://t.me/proxy?server=..." target="_blank" class="vpn-btn">✈️ MTProxy</a>
//              </div>


//          </div>
//      `;
//      document.body.appendChild(overlay);
//      document.body.style.overflow = 'hidden';
//
//      document.getElementById('vpn-close-btn').onclick = () => {
//          overlay.remove();
//          document.body.style.overflow = '';
//          localStorage.setItem('vpn_disclaimer_seen', 'true');
//          setTimeout(() => this.showReminderToast(), 1000);
//      };
//  },
    showReminderToast() {
        if (document.querySelector('.vpn-toast')) return;
        const toast = document.createElement('div');
        toast.className = 'vpn-toast';
        toast.innerHTML = `
            <span>${t('vpn_toast_text', 'Плешивая гнида тормозит наш CDN. Если картинки не грузятся — включи VPN!')}</span>
            <span style="font-size: 1.2em; cursor: pointer;">&times;</span>
        `;
        toast.onclick = () => {
            toast.style.opacity = '0';
            setTimeout(() => toast.remove(), 500);
        };
        document.body.appendChild(toast);
        setTimeout(() => {
            if(document.body.contains(toast)) {
                toast.style.opacity = '0';
                setTimeout(() => toast.remove(), 500);
            }
        }, 7000);
    }
};
const BoardStyleManager = {
    styles: ['grid', 'list', 'cards', 'compact'],
    currentStyleIndex: 1,
    init() {
        const grid = document.querySelector('.board-card-grid');
        if (!grid) return;
        const btnSwitch = document.getElementById('style-switch-btn'); 
        const styleNameLabel = document.getElementById('style-name-display');
        const cards = document.querySelectorAll('.board-card');
        cards.forEach(card => {
            const c1 = card.dataset.color1;
            const c2 = card.dataset.color2;
            if (c1 && c2) {
                card.style.setProperty('--base-color', c1);
                card.style.setProperty('--accent-color', c2);
            }
        });
        try {
            const saved = localStorage.getItem('main_page_style_v3');
            if (saved && this.styles.includes(saved)) {
                this.currentStyleIndex = this.styles.indexOf(saved);
            }
        } catch(e) {}
        this.apply(grid, styleNameLabel);
        if (btnSwitch) {
            btnSwitch.onclick = (e) => {
                e.preventDefault();
                this.currentStyleIndex = (this.currentStyleIndex + 1) % this.styles.length;
                this.apply(grid, styleNameLabel);
            };
        }
    },

    apply(grid, label) {
        const styleName = this.styles[this.currentStyleIndex];
        grid.classList.remove('style-list', 'style-cards', 'style-compact');
        if (styleName !== 'grid') {
            grid.classList.add(`style-${styleName}`);
        }
        if (label) {
            const names = {
                'grid': t('style_grid', 'Плитка'),
                'list': t('style_list', 'Список'),
                'cards': t('style_cards', 'Карточки'),
                'compact': t('style_compact', 'Компакт')
            };
            label.textContent = names[styleName] || styleName;
        }
        localStorage.setItem('main_page_style_v3', styleName);
    }
};
function toggleInlineExpansion(link) {
    const img = link.querySelector('img');
    const isVideo = link.dataset.type === 'video' || 
                    link.dataset.type === 'gif' || 
                    link.querySelector('video') || 
                    (link.href && link.href.match(/\.(mp4|webm|mov|mkv)(\?|$)/i));

    if (isVideo) {
        const src = link.href || link.dataset.fullUrl || link.dataset.src;
        let poster = null;
        if (img) poster = img.src;
        else if (link.dataset.thumbSrc) poster = link.dataset.thumbSrc;
        
        if (typeof VideoPopupManager !== 'undefined') {
            VideoPopupManager.open(src, poster);
        } else {
            window.open(src, '_blank');
        }
        return;
    }

    if (!img) return;
    const isExpanded = link.classList.contains('is-expanded');

    if (isExpanded) {
        link.classList.remove('is-expanded');
        link.classList.remove('is-loading-full');
        
        const btn = link.querySelector('.video-close-btn');
        if (btn) btn.remove();

        if (img.tagName === 'IMG' && link.dataset.thumbSrc) {
            img.src = link.dataset.thumbSrc;
        }
        img.style.width = '';
        img.style.height = '';
        img.style.maxWidth = '';
    } else {
        if (img.tagName === 'IMG' && !link.dataset.thumbSrc) {
            link.dataset.thumbSrc = img.src;
        }
        
        link.classList.add('is-expanded');
        link.classList.add('is-loading-full');
        
        let closeBtn = document.createElement('div');
        closeBtn.className = 'video-close-btn';
        closeBtn.innerHTML = '×';
        closeBtn.onclick = (e) => {
            e.preventDefault();
            e.stopPropagation();
            toggleInlineExpansion(link);
        };
        link.appendChild(closeBtn);

        const fullSrc = link.href;
        const tempImg = new Image();
        
        tempImg.onload = () => {
            if (!link.classList.contains('is-expanded')) return;
            img.src = fullSrc;
            link.classList.remove('is-loading-full');
        };
        tempImg.src = fullSrc;
    }
}
const SettingsManager = {
    defaults: {
        fontStyle: 'b',
        fontSize: window.innerWidth <= 768 ? 'small' : 'medium',
        previewSize: 'medium',
        interfaceDensity: 'normal',
        layoutMode: 'center',
        theme: null,
        soundEnabled: true,
        showIdenticons: false,
        dimMode: false,
        selfCaptcha: false,
        showMascot: true,
        showScrollButtons: false
    },
    current: {},
    themeColors: {
        'light': '#f8faff', 'dark': '#1a1d21', 'cyberpunk': '#0d0221', 'crypto': '#000000',
        'conan': '#0c0502', 'win95': '#008080', 'gameboy': '#8bac0f', 'adventure': '#4fc3f7',
        'luxury': '#0a0a0a', 'toxic': '#1a001a', 'shaft': '#111111', 'nord': '#2e3440',
        'gruvbox': '#282828', 'discord': '#202225', 'lain': '#f0f0f0', 'dracula': '#282a36',
        'sakura': '#fdf6f6', 'ukraine': '#e1e8ff', 'soviet': '#f5f1e7', 'paper': '#f3e9d2',
        'terminal': '#000000', 'ocean': '#0b192f', 'forest': '#2f3d34', 'gothic': '#100c13', 'noir': '#1a1a1a'
    },
    init() {
        try {
            let saved = {};
            try {
                saved = JSON.parse(localStorage.getItem(CONSTANTS.SETTINGS_KEY)) || {};
            } catch (err) {
                console.error("Settings corrupted, resetting...", err);
                localStorage.removeItem(CONSTANTS.SETTINGS_KEY);
            }
            const isMobile = window.innerWidth <= 768;
            this.current = { ...this.defaults, ...saved };
            if (saved.showMascot === undefined && isMobile) {
                this.current.showMascot = false;
            }
            if (saved.previewSize === undefined && isMobile) {
                this.current.previewSize = 'small';
            }
            let initialTheme = saved.theme || localStorage.getItem('theme');
            if (!initialTheme) {
                if (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) initialTheme = 'dark';
                else if (window.matchMedia && window.matchMedia('(prefers-color-scheme: light)').matches) initialTheme = 'light';
                else initialTheme = Math.random() < 0.5 ? 'dark' : 'light';
            }
            this.current.theme = initialTheme;
            let initialFontSize = saved.fontSize;
            if (!initialFontSize) {
                initialFontSize = isMobile ? 'small' : 'medium';
            }
            this.current.fontSize = initialFontSize;
            if (!saved.theme) {
                window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', e => {
                    if (!this.current.theme || this.current.theme === 'light' || this.current.theme === 'dark') {
                        this.current.theme = e.matches ? 'dark' : 'light';
                        this.apply();
                    }
                });
            }
            this.apply();
            this.bindUI();
        } catch (e) { 
            console.error("Settings init error", e); 
        }
    },
    apply() {
        const targets = [document.body, document.documentElement];
        targets.forEach(el => {
            const cl = el.classList;
            [...cl].forEach(c => {
                if (/^(font-size-|preview-size-|layout-|theme-|density-|font-style-)/.test(c)) cl.remove(c);
            });
            cl.remove('dim-mode', 'show-identicons');
            cl.add(`font-size-${this.current.fontSize}`, `preview-size-${this.current.previewSize}`, `layout-${this.current.layoutMode}`);
            cl.add(`theme-${this.current.theme}`);
            if (this.current.interfaceDensity === 'compact') cl.add('density-compact');
            if (this.current.fontStyle === 'b') cl.add('font-style-b');
            if (this.current.dimMode) cl.add('dim-mode');
            cl.toggle('show-identicons', this.current.showIdenticons);
        });
        if (this.themeColors[this.current.theme]) {
            document.documentElement.style.backgroundColor = this.themeColors[this.current.theme];
        }

        if (this.current.showIdenticons) setTimeout(generateIdenticons, 10);
        const themeBtn = document.getElementById('theme-toggle');
        const icons = { light: '☀️', dark: '🌙', crypto: '₿', cyberpunk: '🌃', sakura: '🌸', nord: '❄️', gruvbox: '☕', lain: '🌐', discord: '🎮', adventure: '⚔️', conan: '🩸', win95: '💾', gameboy: '👾', luxury: '🥂', toxic: '☢️',  shaft: '⛩️', ukraine: '🇺🇦', soviet: '☭', paper: '📜', terminal: '>_', ocean: '🌊', forest: '🌲', gothic: '🦇', noir: '⚫' };
        if (themeBtn) themeBtn.textContent = icons[this.current.theme] || '🌙';
        const mascotWrapper = document.getElementById('mascot-wrapper');
        if (mascotWrapper) {
            if (this.current.showMascot) {
                mascotWrapper.style.display = '';
            } else {
                mascotWrapper.style.display = 'none';
            }
        }
        const metaThemeColor = document.querySelector('meta[name="theme-color"]');
        if (metaThemeColor) {
            const mapColor = this.themeColors[this.current.theme];
            if (mapColor) {
                metaThemeColor.setAttribute('content', mapColor);
            } else {
                setTimeout(() => {
                    const computedColor = getComputedStyle(document.body).getPropertyValue('--bg-page').trim();
                    if (computedColor) metaThemeColor.setAttribute('content', computedColor);
                }, 50);
            }
        }
        
        const expandBtn = document.getElementById('expand-all-btn');
        if (expandBtn) {
            if (this.current.expandImages) {
                expandBtn.style.display = 'inline-block';
                expandBtn.textContent = `❌ ${t('btn_collapse_all', 'Свернуть все')}`; 
            } else {
                expandBtn.style.display = 'none';
            }
        }
    },
    save() {
        localStorage.setItem(CONSTANTS.SETTINGS_KEY, JSON.stringify(this.current));
        localStorage.setItem('theme', this.current.theme);
        this.apply();
    },
    bindUI() {
        const openBtn = document.getElementById('settings-btn');
        if (openBtn) {
            openBtn.addEventListener('click', (e) => {
                e.preventDefault();
                openModal('settings-modal');
            });
        }
        const closeBtn = document.querySelector('#settings-modal .modal-close-btn') ||
                         document.querySelector('#settings-modal button[onclick*="closeModal"]');
        const allBtns = document.querySelectorAll('#settings-modal button');
        allBtns.forEach(btn => {
            if (btn.innerHTML.includes('times') || btn.textContent.includes('×') || btn.textContent.toLowerCase() === 'закрыть') {
                btn.onclick = (e) => {
                    e.preventDefault();
                    closeModal();
                };
            }
        });
        const form = document.getElementById('settings-form');
        if (!form) return;
        Object.keys(this.current).forEach(key => {
            const el = form.querySelector(`[data-setting="${key}"]`);
            if (!el) return;
            if (el.type === 'checkbox') el.checked = this.current[key];
            else el.value = this.current[key];
        });
        form.addEventListener('change', (e) => {
            const key = e.target.dataset.setting;
            if (key) {
                this.current[key] = e.target.type === 'checkbox' ? e.target.checked : e.target.value;
                this.save();
            }
        });
        const themeBtn = document.getElementById('theme-toggle');
        if (themeBtn) {
            const themes = ['light', 'dark', 'adventure', 'conan', 'win95', 'lain', 'discord', 'gameboy', 'toxic', 'luxury', 'shaft', 'nord', 'gruvbox', 'dracula', 'crypto', 'cyberpunk', 'sakura', 'ukraine', 'soviet', 'paper', 'terminal', 'ocean', 'forest', 'gothic', 'noir'];
            themeBtn.onclick = () => {
                const idx = themes.indexOf(this.current.theme || 'light');
                this.current.theme = themes[(idx + 1) % themes.length];
                this.save();
                const select = document.getElementById('setting-theme');
                if (select) select.value = this.current.theme;
            };
        }
        const mascotRow = document.querySelector('input[data-setting="showMascot"]')?.closest('.settings-row-checkbox');
        if (mascotRow && !document.getElementById('setting-scroll-btns')) {
            const div = document.createElement('div');
            div.className = 'form-group settings-row-checkbox';
            div.innerHTML = `
                <div style="display: flex; justify-content: space-between; align-items: center;">
                    <label for="setting-scroll-btns" style="margin:0;">Кнопки скролла ⤴️</label>
                    <input type="checkbox" id="setting-scroll-btns" data-setting="showScrollButtons">
                </div>
            `;
            mascotRow.after(div);
        }

        const fsCheckbox = document.getElementById('setting-fullscreen');
        if (fsCheckbox) {
            fsCheckbox.checked = !!document.fullscreenElement;
            fsCheckbox.onchange = () => {
                if (fsCheckbox.checked) {
                    document.documentElement.requestFullscreen().catch(e => {
                        fsCheckbox.checked = false;
                        if (typeof showToast === 'function') showToast(t('err_fullscreen', "Не удалось включить полный экран"));
                    });
                } else {
                    if (document.fullscreenElement) document.exitFullscreen();
                }
            };
            document.addEventListener('fullscreenchange', () => {
                fsCheckbox.checked = !!document.fullscreenElement;
            });
        }
    }
};

window.startSpankGame = function() {
    
    const sfx = {
        slaps: [],
        moans: [],
        init: function() {
            
            const slapFiles = ['slap1.mp3', 'slap2.mp3', 'slap3.mp3'];
            const moanFiles = ['moan1.mp3', 'moan2.mp3'];
            
            slapFiles.forEach(f => { const a = new Audio('/static/sounds/spank/' + f); a.volume=0.6; this.slaps.push(a); });
            moanFiles.forEach(f => { const a = new Audio('/static/sounds/spank/' + f); a.volume=0.4; this.moans.push(a); });
        },
        play: function() {
            if (!SettingsManager.current.soundEnabled) return;
            
            if (this.slaps.length > 0) {
                const s = this.slaps[Math.floor(Math.random() * this.slaps.length)];
                
                s.cloneNode(true).play().catch(()=>{});
            }
            
            if (this.moans.length > 0 && Math.random() < 0.3) {
                const m = this.moans[Math.floor(Math.random() * this.moans.length)];
                
                setTimeout(() => {
                    m.cloneNode(true).play().catch(()=>{});
                }, 100);
            }
        }
    };
    
    if (!window.spankSfxLoaded) {
        sfx.init();
        window.spankSfxLoaded = true;
        window.spankSfxInstance = sfx;
    }
    
    let modal = document.getElementById('spank-game-modal');
    if (!modal) {
        const html = `
            <div id="spank-game-modal" class="modal" style="background:#1a1a1a; width: 400px; height: 500px; padding:0; overflow:hidden; z-index:20050;">
                <button class="modal-close-btn" onclick="document.getElementById('spank-game-modal').style.display='none'; document.getElementById('modal-overlay').classList.remove('active');" style="position:absolute; top:10px; right:10px; z-index:100; color:white; background:rgba(0,0,0,0.5); border:none; border-radius:50%; width:30px; height:30px; font-size:20px; cursor:pointer;">&times;</button>
                <div id="spank-game-container" style="width:100%; height:100%;"></div>
            </div>
        `;
        document.body.insertAdjacentHTML('beforeend', html);
        modal = document.getElementById('spank-game-modal');
    }
    const container = document.getElementById('spank-game-container');
    const overlay = document.getElementById('modal-overlay') || document.createElement('div');
    if (!document.getElementById('modal-overlay')) {
        overlay.id = 'modal-overlay';
        document.body.appendChild(overlay);
    }
    
    overlay.style.display = 'block';
    modal.style.display = 'block';
    setTimeout(() => {
        overlay.classList.add('active');
        modal.classList.add('active');
    }, 10);
    const bgImg = "/static/img/captcha/spank_target.webp";
    
    container.innerHTML = `
        <div class="spank-ui" id="game-area" style="position:relative; width:100%; height:100%; overflow:hidden; background:#222; cursor:url('data:image/svg+xml;utf8,<svg xmlns=\\'http://www.w3.org/2000/svg\\' width=\\'40\\' height=\\'48\\' viewport=\\'0 0 100 100\\' style=\\'fill:black;font-size:24px;\\'><text y=\\'50%\\'>🖐️</text></svg>') 16 0, auto;">
            <div style="position:absolute; top:10px; left:10px; color:#ff0055; font-weight:bold; font-size:20px; z-index:60; font-family:monospace;">
                ${t('game_score')}: <span id="spank-score">0</span>
            </div>
            <div class="spank-flash-overlay" id="game-flash"></div>
            <img src="${bgImg}" class="spank-layer layer-base" draggable="false" style="position:absolute; bottom:0; left:50%; transform:translateX(-50%); height:90%;">
            <img src="${bgImg}" class="spank-layer layer-jiggle" id="game-butt" draggable="false" style="position:absolute; bottom:0; left:50%; transform:translateX(-50%); height:90%;">
            <div class="captcha-hint" style="color:rgba(255,255,255,0.5); z-index:50; bottom:10px;">${t('captcha_hint_spank')}</div>
        </div>
    `;
    const area = document.getElementById('game-area');
    const butt = document.getElementById('game-butt');
    const flash = document.getElementById('game-flash');
    const scoreEl = document.getElementById('spank-score');
    let score = 0;
    let cooldown = false;
    let lastX=0, lastY=0, lastTime=0;
    const doSpank = (x, y) => {
        if (cooldown) return;
        cooldown = true;
        score++;
        scoreEl.textContent = score;
        if (navigator.vibrate) navigator.vibrate(40);
        SpankAudioManager.play();
        
        butt.classList.remove('hit-left', 'hit-right', 'hit-center');
        void butt.offsetWidth;
        const r = Math.random();
        let animClass = 'hit-center';
        if (r < 0.2) animClass = 'hit-left';
        else if (r < 0.4) animClass = 'hit-right';
        butt.classList.add(animClass);
        butt.style.animationDuration = (animClass === 'hit-center' ? 0.3 : 0.35 + Math.random() * 0.1) + 's';
        
        flash.style.transition = 'none';
        flash.classList.add('active');
        setTimeout(() => {
            flash.style.transition = 'opacity 0.5s ease-out';
            flash.classList.remove('active');
        }, 50);
        
        const rect = area.getBoundingClientRect();
        const relX = x - rect.left;
        const relY = y - rect.top;
        const hand = document.createElement('div');
        hand.className = 'hand-slap';
        hand.textContent = '🖐️';
        hand.style.left = relX + 'px';
        hand.style.top = relY + 'px';
        hand.style.transform = `translate(-50%, -50%) rotate(${Math.random() * 60 - 30}deg)`;
        area.appendChild(hand);
        setTimeout(() => hand.remove(), 200);
        const fx = document.createElement('div');
        fx.className = 'smack-fx';
        const words = t('spank_sfx') || ["SMACK!", "PLAP!", "SLAP!", "AHH~", "🍑", "BAM!"];
        fx.textContent = words[Math.floor(Math.random() * words.length)];
        fx.style.left = relX + 'px';
        fx.style.top = relY + 'px';
        area.appendChild(fx);
        setTimeout(() => fx.remove(), 500);
        setTimeout(() => { cooldown = false; }, 400);
    };
    const handleTap = (e) => {
        if(e.cancelable) e.preventDefault();
        const cx = e.touches ? e.touches[0].clientX : e.clientX;
        const cy = e.touches ? e.touches[0].clientY : e.clientY;
        doSpank(cx, cy);
    };
    const handleMove = (e) => {
        const cx = e.touches ? e.touches[0].clientX : e.clientX;
        const cy = e.touches ? e.touches[0].clientY : e.clientY;
        const now = Date.now();
        if (lastTime > 0) {
            const speed = Math.hypot(cx - lastX, cy - lastY) / (now - lastTime);
            const rect = area.getBoundingClientRect();
            if (speed > 1.5 && !cooldown && (cy - rect.top) > rect.height * 0.4) {
                doSpank(cx, cy);
            }
        }
        lastX = cx; lastY = cy; lastTime = now;
    };
    area.addEventListener('mousedown', handleTap);
    area.addEventListener('touchstart', handleTap, {passive: false});
    area.addEventListener('mousemove', handleMove);
    area.addEventListener('touchmove', handleMove, {passive: true});
    area.addEventListener('mouseleave', () => { lastTime = 0; });
};
window.showThreadSummary = async (threadId) => {
    const btn = document.querySelector(`.summary-btn[onclick*="${threadId}"]`);
    const originalText = btn.textContent;
    try {
        btn.textContent = '⏳';
        btn.style.cursor = 'wait';
        const res = await fetch(`/api/thread/${threadId}/summary`);
        if (!res.ok) throw new Error('Failed');
        const data = await res.json();
        
        if (typeof showToast === 'function') {
            
            const modal = document.createElement('div');
            modal.className = 'modal active';
            modal.style.display = 'block';
            modal.style.zIndex = '20000';
            modal.innerHTML = `
                <div style="background:var(--bg-main); padding:20px; border-radius:8px; border:1px solid var(--accent-primary); max-width:500px; margin: 10% auto; position:relative; box-shadow:0 10px 40px rgba(0,0,0,0.5);">
                    <h3 style="margin-top:0;">${t('summary_title', 'Summary:')}</h3>
                    <!-- FIX: Использование formatTextGlobal для защиты от XSS в ответе нейросети -->
                    <div style="font-size:1.1em; line-height:1.6; margin-bottom:20px;">${window.formatTextGlobal(data.summary)}</div>
                    <button class="btn btn-primary" onclick="this.closest('.modal').remove()" style="width:100%">${t('btn_understood', 'OK')}</button>
                </div>
            `;
            document.body.appendChild(modal);
        } else {
            alert(`🧠 Суть треда:\n\n${data.summary}`);
        }
    } catch (e) {
        if (typeof showToast === 'function') showToast(t('network_error'));
    } finally {
        btn.textContent = originalText;
        btn.style.cursor = 'pointer';
    }
};
const PoW = {
    async solve(challenge, difficulty) {
        const prefix = "0".repeat(difficulty);
        let nonce = 0;
        const encoder = new TextEncoder();
        while (true) {
            nonce++;
            const str = challenge + nonce;
            const buffer = encoder.encode(str);
            const hashBuffer = await crypto.subtle.digest('SHA-256', buffer);
            const hashArray = Array.from(new Uint8Array(hashBuffer));
            const hashHex = hashArray.map(b => b.toString(16).padStart(2, '0')).join('');
            if (hashHex.startsWith(prefix)) {
                return nonce;
            }
            if (nonce % 500 === 0) {
                await new Promise(r => setTimeout(r, 0));
            }
        }
    },
    async attachToForm(formData, formElement) {
        try {
            const res = await fetch('/api/pow/challenge');
            if (!res.ok) return;
            const data = await res.json();
            const { challenge, difficulty } = data;
            if (!difficulty || difficulty === 0) return;
            const btn = formElement.querySelector('button[type="submit"]');
            let oldText = '';
            if (btn) {
                oldText = btn.textContent;
                btn.disabled = true;
                btn.textContent = t('pow_mining', '🛡️ Mining...');
                btn.style.cursor = 'wait';
            }
            const nonce = await this.solve(challenge, difficulty);
            formData.append('pow_challenge', challenge);
            formData.append('pow_nonce', nonce);
            if (btn) {
                btn.textContent = oldText;
                btn.disabled = false;
                btn.style.cursor = '';
            }
        } catch (e) {
            console.error("PoW Error:", e);
            const btn = formElement.querySelector('button[type="submit"]');
            if (btn) {
                 btn.disabled = false;
                 btn.textContent = 'Error';
            }
        }
    }
};
const SonicID = {
    ctx: null,
    masterGain: null,
    init() {
        if (!this.ctx) {
            const AC = window.AudioContext || window.webkitAudioContext;
            if (AC) {
                this.ctx = new AC();
                this.masterGain = this.ctx.createGain();
                this.masterGain.connect(this.ctx.destination);
            }
        }
    },
    play(userId) {
        if (!this.ctx) this.init();
        if (!this.ctx) return;
        if (window.SettingsManager && SettingsManager.current.soundEnabled === false) return;
        if (this.ctx.state === 'suspended') this.ctx.resume().catch(() => {});

        let h = 0;
        for (let i = 0; i < userId.length; i++) {
            h = Math.imul(31, h) + userId.charCodeAt(i) | 0;
        }
        const seed = Math.abs(h);
        let s = seed;
        const rnd = () => {
            s = (s * 9301 + 49297) % 233280;
            return s / 233280;
        };

        const now = this.ctx.currentTime;
        let vol = window.globalVideoVolume;
        if (typeof vol !== 'number' || !Number.isFinite(vol)) vol = 0.5;
        
        this.masterGain.gain.cancelScheduledValues(now);
        this.masterGain.gain.setValueAtTime(Math.min(vol * 0.3, 0.3), now);

        const scales = [
            [0, 4, 7, 9],       
            [0, 3, 7, 10],      
            [0, 2, 7, 9],       
            [0, 4, 7, 11, 14],  
            [0, 3, 5, 6, 7, 10] 
        ];
        const scale = scales[seed % scales.length];
        const rootKey = 60 + (seed % 12); 

        const motifs = [
            [0.1, 0.1, 0.2],         
            [0.08, 0.08, 0.08, 0.08], 
            [0.15, 0.3],             
            [0.1, 0.1, 0.1, 0.4],    
            [0.2, 0.2, 0.2]          
        ];
        const rhythm = motifs[seed % motifs.length];

        const osc = this.ctx.createOscillator();
        const gain = this.ctx.createGain();
        
        osc.type = (seed % 3 === 0) ? 'triangle' : 'square';
        
        osc.connect(gain);
        gain.connect(this.masterGain);

        let noteTime = now;
        let scaleIdx = Math.floor(rnd() * scale.length); 

        gain.gain.setValueAtTime(0, now);

        for (let i = 0; i < rhythm.length; i++) {
            const dur = rhythm[i];
            
            const move = Math.floor(rnd() * 3) - 1; 
            const jump = (rnd() > 0.8) ? (Math.floor(rnd() * 4) - 2) : 0;
            
            scaleIdx += move + jump;
            
            const octave = Math.floor(scaleIdx / scale.length);
            const idx = ((scaleIdx % scale.length) + scale.length) % scale.length;
            const midi = rootKey + (octave * 12) + scale[idx];
            
            const freq = 440 * Math.pow(2, (midi - 69) / 12);
            
            osc.frequency.setValueAtTime(freq, noteTime);
            
            const attack = 0.01;
            const release = 0.02;
            const sustainDur = dur - attack - release;
            
            gain.gain.linearRampToValueAtTime(1.0, noteTime + attack);
            if (sustainDur > 0) {
                gain.gain.setValueAtTime(1.0, noteTime + attack + sustainDur);
                gain.gain.linearRampToValueAtTime(0, noteTime + dur);
            } else {
                 gain.gain.linearRampToValueAtTime(0, noteTime + dur);
            }

            noteTime += dur;
        }

        osc.start(now);
        osc.stop(noteTime + 0.1);

        setTimeout(() => {
            osc.disconnect();
            gain.disconnect();
        }, (noteTime - now + 0.5) * 1000);
    },
    attach() {
        let lastTime = 0;
        document.body.addEventListener('mouseenter', (e) => {
            if (e.target.classList && e.target.classList.contains('identicon')) {
                const now = Date.now();
                if (now - lastTime > 150) {
                    const uid = e.target.dataset.userid;
                    if (uid) this.play(uid);
                    lastTime = now;
                }
            }
        }, true);
    }
};
const ServerPulse = {
    init() {
        const footer = document.querySelector('footer p');
        if (!footer) return;
        const widget = document.createElement('span');
        widget.id = 'server-pulse';
        widget.style.cssText = "margin-left: 15px; font-family: monospace; font-size: 0.9em; opacity: 0.7; cursor: help;";
        widget.title = "Нагрузка на сервер (ноутбук админа)";
        footer.appendChild(widget);
        this.update();
        setInterval(() => this.update(), 10000); 
    },
    async update() {
        try {
            const res = await fetch('/api/server/pulse');
            if (!res.ok) return;
            const data = await res.json();
            const el = document.getElementById('server-pulse');
            if (!el) return;
            let html = `CPU: ${data.cpu}% | RAM: ${data.ram}%`;
            if (data.temp) {
                const color = data.temp > 80 ? 'red' : (data.temp > 60 ? 'orange' : 'inherit');
                html += ` | <span style="color:${color}">🌡️ ${data.temp}°C</span>`;
            }
            el.innerHTML = html;
        } catch(e) {}
    }
};
const ThreadPlayer = {
    ui: null,
    posts: [],
    currentIndex: 0,
    timer: null,
    baseDelay: 2000, 
    isPlaying: false,
    init() {
        if (document.getElementById('thread-player-controls')) return;
        const div = document.createElement('div');
        div.id = 'thread-player-controls';
        div.innerHTML = `
            <button class="tp-btn" id="tp-stop" title="Стоп">⏹</button>
            <button class="tp-btn" id="tp-play" title="Старт/Пауза">⏯</button>
            <div class="tp-progress" id="tp-counter">0 / 0</div>
            <div class="tp-speed-container" title="Скорость воспроизведения">
                <span>🐌</span>
                <input type="range" id="tp-speed-slider" min="1" max="50" value="10">
                <span>⚡</span>
            </div>
        `;
        document.body.appendChild(div);
        this.ui = div;
        div.querySelector('#tp-stop').onclick = () => this.stop();
        div.querySelector('#tp-play').onclick = () => this.toggle();
        
        const slider = div.querySelector('#tp-speed-slider');
        slider.oninput = () => {
        };
    },
    start() {
        if (!location.pathname.includes('/res/')) return;
        this.init();
        const allPosts = Array.from(document.querySelectorAll('.post'));
        if (allPosts.length < 2) return showToast(t('thread_too_short', "Тред слишком короткий"));
        this.opPost = allPosts[0];
        this.posts = allPosts.slice(1);
        this.currentIndex = 0;
        this.isPlaying = true;
        
        this.posts.forEach(p => {
            p.style.display = 'none';
            p.classList.remove('tp-animate');
        });
        this.ui.classList.add('active');
        this.updateCounter();
        this.opPost.scrollIntoView({ behavior: 'smooth' });
        this._scrollHandler = () => {
            if (this.isAutoScrolling) return;
            if (this.isPlaying) {
                this.pause();
                showToast(t('player_paused_scroll', "Пауза (ручной скролл)"));
            }
        };
        window.addEventListener('wheel', this._scrollHandler, { passive: true });
        window.addEventListener('touchmove', this._scrollHandler, { passive: true });
        
        this.play();
    },
    play() {
        if (this.timer) clearTimeout(this.timer);
        this.isPlaying = true;
        document.getElementById('tp-play').textContent = '⏸';
        const loop = () => {
            if (this.currentIndex >= this.posts.length) {
                this.pause();
                return;
            }
            const post = this.posts[this.currentIndex];
            post.style.display = '';
            post.classList.add('tp-animate');
            this.isAutoScrolling = true;
            const rect = post.getBoundingClientRect();
            if (rect.bottom > window.innerHeight) {
                post.scrollIntoView({ behavior: 'smooth', block: 'center' });
            }
            setTimeout(() => this.isAutoScrolling = false, 500);
            this.currentIndex++;
            this.updateCounter();
            const sliderEl = document.getElementById('tp-speed-slider');
            const sliderVal = sliderEl ? parseInt(sliderEl.value) : 10;
            const delay = Math.max(50, 2000 - (sliderVal * 40)); 
            this.timer = setTimeout(loop, delay);
        };
        loop();
    },
    pause() {
        if (this.timer) clearTimeout(this.timer);
        this.isPlaying = false;
        document.getElementById('tp-play').textContent = '▶';
    },
    toggle() {
        if (this.isPlaying) this.pause();
        else this.play();
    },
    stop() {
        if (this._scrollHandler) {
            window.removeEventListener('wheel', this._scrollHandler);
            window.removeEventListener('touchmove', this._scrollHandler);
        }
        this.pause();
        this.posts.forEach(p => {
            p.style.display = '';
            p.classList.remove('tp-animate');
        });
        this.ui.classList.remove('active');
    },
    updateCounter() {
        document.getElementById('tp-counter').textContent = `${this.currentIndex} / ${this.posts.length}`;
    }
};
const BlurHash = {
    decode(blurhash, canvas, punch=1) {
        if (!blurhash || !canvas) return;
        const width = 32;
        const height = 32;
        
        const pixels = this.decodeBlurHash(blurhash, width, height, punch);
        const ctx = canvas.getContext('2d');
        const imageData = ctx.createImageData(width, height);
        imageData.data.set(pixels);
        ctx.putImageData(imageData, 0, 0);
    },
    decodeBlurHash(blurhash, width, height, punch) {
        const size = blurhash.length;
        if (size < 6) return new Uint8ClampedArray(width * height * 4);
        
        const decode83 = (str) => {
            let val = 0;
            for (let i = 0; i < str.length; i++) {
                const char = str[i];
                const digit = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz#$%*+,-.:;=?@[]^_{|}~".indexOf(char);
                val = val * 83 + digit;
            }
            return val;
        };

        const sizeFlag = decode83(blurhash[0]);
        const numY = Math.floor(sizeFlag / 9) + 1;
        const numX = (sizeFlag % 9) + 1;
        const quantisedMaximumValue = decode83(blurhash[1]);
        const maximumValue = (quantisedMaximumValue + 1) / 166;
        
        const colors = new Array(numX * numY);
        
        for (let i = 0; i < colors.length; i++) {
            if (i === 0) {
                const value = decode83(blurhash.substring(2, 6));
                colors[i] = [
                    (value >> 16) & 255, (value >> 8) & 255, value & 255
                ].map(v => v / 255 * 255);
            } else {
                const value = decode83(blurhash.substring(4 + i * 2, 6 + i * 2));
                colors[i] = [
                    Math.floor(value / (19 * 19)) - 9,
                    Math.floor((value / 19) % 19) - 9,
                    (value % 19) - 9
                ].map(v => v * maximumValue * punch);
            }
        }

        const bytes = new Uint8ClampedArray(width * height * 4);
        for (let y = 0; y < height; y++) {
            for (let x = 0; x < width; x++) {
                let r = 0, g = 0, b = 0;
                for (let j = 0; j < numY; j++) {
                    for (let i = 0; i < numX; i++) {
                        const basis = Math.cos(Math.PI * x * i / width) * Math.cos(Math.PI * y * j / height);
                        const color = colors[i + j * numX];
                        r += color[0] * basis;
                        g += color[1] * basis;
                        b += color[2] * basis;
                    }
                }
                const idx = 4 * (x + y * width);
                bytes[idx] = Math.min(255, Math.max(0, (r)));
                bytes[idx+1] = Math.min(255, Math.max(0, (g)));
                bytes[idx+2] = Math.min(255, Math.max(0, (b)));
                bytes[idx+3] = 255;
            }
        }
        return bytes;
    },
    queue: [],
    isProcessing: false,
    
    addToQueue(canvases) {
        canvases.forEach(c => {
            if (!c.dataset.queued && !c.dataset.drawn) {
                c.dataset.queued = 'true';
                this.queue.push(c);
            }
        });
        this.processQueue();
    },

    processQueue() {
        if (this.isProcessing) return;
        this.isProcessing = true;

        const loop = () => {
            if (this.queue.length === 0) {
                this.isProcessing = false;
                return;
            }
            const start = performance.now();
            while (this.queue.length > 0 && performance.now() - start < 3) {
                const c = this.queue.shift();
                if (c && c.isConnected && !c.dataset.drawn) {
                    this.decode(c.dataset.hash, c);
                    c.dataset.drawn = 'true';
                }
            }
            requestAnimationFrame(loop);
        };
        loop();
    },

    init() {
        this.addToQueue(document.querySelectorAll('canvas.blurhash-canvas'));
        
        if (this.observer) return;
        this.observer = new MutationObserver((mutations) => {
            const newCanvases = [];
            mutations.forEach(m => {
                m.addedNodes.forEach(node => {
                    if (node.nodeType === 1 && node.classList && node.classList.contains('blurhash-canvas')) {
                        newCanvases.push(node);
                    }
                    if (node.querySelectorAll) {
                        node.querySelectorAll('canvas.blurhash-canvas').forEach(c => newCanvases.push(c));
                    }
                });
            });
            if (newCanvases.length > 0) this.addToQueue(newCanvases);
        });
        const container = document.querySelector('.thread-list') || document.querySelector('.chat-list');
        if(container) this.observer.observe(container, { childList: true, subtree: true });
    }
};
const ZalgoRot = {
    init() {
        if (this.observer) return;
        this.processAll();
        this.observer = new MutationObserver((mutations) => {
            mutations.forEach(m => {
                m.addedNodes.forEach(node => {
                    if (node.nodeType === 1 && node.classList.contains('post')) {
                        this.applyRot(node);
                    }
                    if (node.nodeType === 1 && node.querySelectorAll) {
                        node.querySelectorAll('.post').forEach(p => this.applyRot(p));
                    }
                });
            });
        });
        const container = document.querySelector('.thread-list') || document.querySelector('.chat-list');
        if (container) this.observer.observe(container, { childList: true, subtree: true });
    },
    processAll() {
        document.querySelectorAll('.post').forEach(post => this.applyRot(post));
    },
    applyRot(post) {
        const count = parseInt(post.dataset.reports || 0);
        if (count < 1) return;
        if (count >= 2) {
            post.style.opacity = '0.85';
            post.style.filter = 'sepia(0.3)';
        }
        if (count >= 3) {
            post.style.opacity = '0.7';
            post.style.filter = 'sepia(0.6) blur(0.5px)';
            this.zalgoText(post.querySelector('.post-text'));
        }
        if (count >= 5) {
            post.style.opacity = '0.5';
            post.style.filter = 'grayscale(1) blur(1.5px) contrast(1.5)';
            post.style.transform = `rotate(${Math.random() - 0.5}deg)`; 
            const textEl = post.querySelector('.post-text');
            if (textEl) this.zalgoText(textEl, true);
        }
    },
    zalgoText(element, extreme = false) {
        if (!element || element.dataset.rotted) return;
        const walk = (node) => {
            if (node.nodeType === 3) {
                const chars = node.nodeValue.split('');
                const zalgos = ['\u0300', '\u0301', '\u0302', '\u0303', '\u0304', '\u0305', '\u0306', '\u0307'];
                const density = extreme ? 0.15 : 0.05;
                const newText = chars.map(c => {
                    if (Math.random() < density && c.trim()) {
                        return c + zalgos[Math.floor(Math.random() * zalgos.length)];
                    }
                    return c;
                }).join('');
                node.nodeValue = newText;
            } else if (node.nodeType === 1) {
                node.childNodes.forEach(walk);
            }
        };
        walk(element);
        element.classList.add('effect-glitch-text');
        element.dataset.rotted = 'true';
    }
};
safeInit('ZalgoRot', () => ZalgoRot.init());
window.checkThreadVibe = async (btn) => {
    if (typeof btn !== 'object') {
        btn = document.querySelector(`.vibe-btn[data-thread-id="${btn}"]`);
    }
    if (!btn) return;
    const threadId = btn.dataset.threadId;
    const oldContent = btn.textContent;
    try {
        btn.textContent = '⏳';
        btn.disabled = true;
        const res = await fetch(`/api/thread/${threadId}/vibe`);
        if (!res.ok) throw new Error();
        const data = await res.json();
        
        btn.textContent = data.icon;
        btn.title = t('vibe_title').replace('{0}', data.vibe);
        
        if (typeof showToast === 'function') {
            showToast(t('vibe_toast').replace('{0}', `${data.vibe} ${data.icon}`));
        }
    } catch (e) {
        btn.textContent = '❌';
        setTimeout(() => {
            btn.textContent = oldContent;
            btn.disabled = false;
        }, 2000);
    }
};
window.sendFeedback = async (form) => {
    const btn = form.querySelector('button[type="submit"]');
    const oldText = btn.textContent;
    btn.disabled = true;
    btn.textContent = t('feedback_sending'); 
    try {
        const formData = Object.fromEntries(new FormData(form).entries());
        if (formData.email) return; 

        const res = await fetch('/api/feedback', {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify(formData)
        });

        if (res.ok) {
            showToast(t('feedback_done')); 
            form.reset();
            form.closest('.modal').style.display = 'none';
            document.getElementById('modal-overlay').style.display = 'none';
        } else {
            const err = await res.json();
            showToast(t('error_prefix') + (err.detail || 'Error'));
        }
    } catch (e) {
        showToast(t('network_error'));
    } finally {
        btn.disabled = false;
        btn.textContent = oldText;
    }
};
window.MyPostsPage = { 
    init() {
        const container = document.getElementById('my-posts-feed');
        const countEl = document.getElementById('mp-total-count');
        console.log("MyPostsPage initializing..."); 
        let ids = [];
        try {
            ids = JSON.parse(localStorage.getItem('my_post_ids') || '[]');
        } catch(e) { ids = []; }
        if (countEl) countEl.textContent = ids.length; 
        if (ids.length === 0) {
            container.innerHTML = `
                <div style="text-align:center; padding:50px; opacity:0.6;">
                    <div style="font-size:3em; margin-bottom:20px;">📭</div>
                    <h3>${t('mp_no_posts_title')}</h3>
                    <p>${t('mp_no_posts_text')}</p>
                    <a href="/" class="btn btn-primary" style="margin-top:20px;">${t('mp_start_btn')}</a>
                </div>
            `;
            return;
        }
        const targetIds = ids.slice(-1000).reverse();
        fetch('/api/my-posts-content', {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify({ post_nums: targetIds })
        })
        .then(r => r.json())
        .then(posts => {
            container.innerHTML = '';
            if (posts.length === 0) {
                container.innerHTML = `<div style="text-align:center; padding:20px;">${t('mp_posts_deleted')}</div>`;
                return;
            }
            posts.forEach(post => {
                const el = PostRenderer.create(post, 'thread');
                const header = el.querySelector('.post-header');
                const boardTag = document.createElement('span');
                boardTag.className = 'is-op-badge';
                boardTag.style.background = 'var(--text-secondary)';
                boardTag.textContent = `/${post.board_id}/`;
                header.prepend(boardTag);
                window.initializePostFeatures(el);
                container.appendChild(el);
            });
        })
        .catch(e => {
            console.error(e);
            container.innerHTML = '<div style="color:red; text-align:center;">Ошибка загрузки данных</div>';
        });
    }
};
const SpankAudioManager = {
    slaps: [],
    moans: [],
    isLoaded: false,
    init() {
        if (this.isLoaded) return;
        
        const slapFiles = ['slap1.mp3', 'slap2.mp3', 'slap3.mp3'];
        const moanFiles = ['moan1.mp3', 'moan2.mp3'];
        
        slapFiles.forEach(f => {
            const a = new Audio('/static/sounds/spank/' + f);
            a.volume = 0.6;
            this.slaps.push(a);
        });
        
        moanFiles.forEach(f => {
            const a = new Audio('/static/sounds/spank/' + f);
            a.volume = 0.4;
            this.moans.push(a);
        });
        this.isLoaded = true;
        console.log('🍒 Spank Audio Loaded');
    },
    play() {
        
        if (window.SettingsManager && !window.SettingsManager.current.soundEnabled) return;
        if (!this.isLoaded) this.init();
        
        if (this.slaps.length > 0) {
            const s = this.slaps[Math.floor(Math.random() * this.slaps.length)];
            
            s.cloneNode(true).play().catch(() => {});
        }
        
        if (this.moans.length > 0 && Math.random() < 0.3) {
            const m = this.moans[Math.floor(Math.random() * this.moans.length)];
            
            setTimeout(() => {
                m.cloneNode(true).play().catch(() => {});
            }, 100 + Math.random() * 100);
        }
    }
};
const MascotManager = {
    wrapper: null,
    body: null,
    imgFront: null,
    imgBack: null,
    bubble: null,
    basePath: '/static/img/mascot/',
    isMobile: false,
    loaded: false,
    mouseThrottle: false,
    visualCooldown: false,
    isLocked: false,
    attachedElement: null,
    floorOffset: 0,
    dragStartTime: 0,
    throwCount: 0,
    lastThrowTime: 0,
    p: {
        x: -150, y: 0,
        vx: 0, vy: 0,
        rot: 0,
        sx: 1, sy: 1,
        gravity: 0.9,
        friction: 0.92,
        floorBounce: 0.4,
        dragInertia: 0.15,
        isDragging: false,
        lastMouseX: 0, lastMouseY: 0
    },
    state: 'idle',
    direction: 1,
    clickCount: 0,
    timers: { logic: null, reset: null, walk: null, feed: null, outfit: null, sequence: null, freeze: null },
    isFrozen: false,
    isPhysicsRunning: false,
    locales: {
        ru: {
            night: ["Не спится? Мне тоже...", "Только ты и я...", "В интернете кто-то не прав?", "Тихо тут.", "Я охраняю твой сон (нет).", "Глаза красные уже.", "Может, спать? Шучу.", "Ночью тут спокойнее.", "Холодно одной...", "Посидим до утра? <3", "Ты тоже сова?", "Глазки не устали?", "Весь мир спит...", "Луна красивая...", "Тс-с... Не буди.", "Время секретиков.", "Мне не страшно с тобой.", "Ещё одну страничку?", "Зомби-режим: ВКЛ."],
            morning: ["Доброе утро, соня!", "Просыпайся, самурай.", "Где мой кофе?", "Солнце слепит!", "Новый день — новые драмы.",  "С добрым утром!", "Улыбнись!", "Хватит спать!", "Ты спал вообще?", "Утро добрым не бывает, но ты старайся.", "Хуй будешь?", "Кофе будешь?", "Ты сегодня рано~", "Выглядишь отлично!", "Начинаем день!", "Потягу-у-ушки!", "Солнышко встало.", "Новые треды ждут.", "Завтрак — это важно.", "Ну еще 5 минуточек...", "Врываемся в этот день!", "У тебя все получится!", "Глаза открой!", "План: скроллить.", "Бодрячком!"],
            day: ["Работаешь?", "Отдохни минутку.", "Я отвлекаю?", "День в разгаре.", "Суета...", "Пей водичку!", "Я тут, рядом.", "Скучаю...", "Погладь меня~", "Спину выпрями!", "Сделай перерыв.", "Хватит работать, иди ко мне.", "Делу время, потехе — я.", "Отвлечемся?", "Солнце светит.", "Много работы?", "Скролл-скролл...", "Я слежу за тобой.", "Обед был?", "Продуктивность топ!", "Начальник идет! Шучу."],
            evening: ["Устал? Отдохни.", "Как прошел день?", "Уютный вечер.", "Наконец-то ты тут.", "Пора расслабиться.", "Чай, плед и борды.", "День был долгим.", "Никакой работы.", "Закат видел?", "Залипнем вместе.", "Глубокие мысли...", "Ты заслужил отдых.", "Ужин был?", "Включай лоу-фай.", "Не грусти.",  "Вечерний вайб.", "Самое время для чая.", "Расскажи, как день прошел.", "Уже стемнело.", "Зажигай лампу.", "Уютненько.", "Никуда не спешим.", "Вечерние треды — самые ламповые.", "Усталость...", "Отдыхай, ты молодец.", "Кино посмотрим?", "Музыку потише.", "Город засыпает.", "Вечерняя прохлада.", "Время для себя.", "Забудь про проблемы.", "Я с тобой.", "Хорошо сидим.", "Вечер — маленькая жизнь."],
            flirty: ["Ты заставляешь меня краснеть...", "Мне нравится это.", "Ой, щекотно!", "Какой настойчивый...", "Daddy? 😏", "Мяу <3", "Продолжай...", "Электрические клики...", "Я вся горю...", "Ближе...", "Только для тебя.", "Забери меня домой.", "Ты особенный.", "Сердечко стучит...", "Мрр...", "Смотри в глаза...", "Шалун.", "Это судьба?", "Твои клики такие... уверенные.", "Не дразни меня.", "Мы одни?", "Какой ты... интересный.", "Поиграем?", "Ты мне нравишься.", "Иди сюда.", "Стесняюсь...", "Ты меня смущаешь.", "Горячо!", "Может, познакомимся поближе?", "Твой курсор так близко...", "А ты милый.", "Сердце бьется чаще.", "Только тсс...", "Это наш секрет.", "Ты умеешь удивлять.", "Мне с тобой хорошо.", "Не останавливайся."],
            cute: ["Мяу?", ":3", "UwU",  "Ты такой добрый!", "Давай дружить.", "Ня-пока!", "Я пушистая.", "Погладь!", "Смотри, как я умею!", "Ты мой герой.", "Ми-ми-ми.", "Хочу на ручки.", "Не обижай меня.", "Я стараюсь!", "Ты хороший.", "Вместе веселее.", "Хвостик мерзнет.", "Ушки на макушке.", "Ты мне рад?", "Я тут!", "Люблю вкусняшки.", "Ты супер!", "Я красивая?", "Я няшная?", "Я красивая?", "Почеши за ушком~", "Люблю тебя.", "Ты лучший!", "Хочу обнимашек!", "*виляет хвостиком*", "Заметил меня?", "Ня!", "Большой курсор...", "Я маленькая.", "Не забывай меня.", "Кусь!", "Мур-мур..."],
            angry_soft: ["Бака!", "Ну хватит уже.", "Я не в духе.", "Отвали.", "Не мешай.", "Что за манеры?", "Я обиделась.", "Не наглей.", "Уйди противный.", "Не сейчас.", "Зачем ты так?", "Прекрати.", "Мне не нравится.", "Грубиян.", "Не трожь!", "Я кусаюсь.", "У меня когти.", "Бука.", "Отстань, а?", "Надоел.", "Чего тебе?", "Не трогай!", "Хм...", "Ну что?", "Я занята.", "Руки!", "Эй!", "Не тыкай.", "Имей совесть.", "Чего надо?", "Я не кнопка.", "Соблюдай дистанцию.", "Пф-ф...", "Странный ты."],
            angry_med: ["Отстань!", "Ты переходишь границы!", "Я сейчас уйду!", "Ты глухой?", "Больно!", "Прекращай!", "Я серьезно!", "Отвали по-хорошему.", "Бесишь неимоверно.", "У меня лопнет терпение.", "Ты кто такой?", "Не лезь!", "Руки убрал!", "Я злая!", "Сейчас укушу!", "Ты доиграешься.", "Имей уважение!", "Достал уже.", "Всё, хватит!", "Уйди с глаз!", "НЕ ТРОГАЙ!", "Атпути!", "ММ...", "Достал!", "Прекрати!", "Больно же!", "Я пожалуюсь!", "У меня есть права!", "Ну хватит!", "Ты надоел.", "Уйди!", "Игнор mode: ON.", "Гррр...", "Бесишь."],
            angry_hard: ["ИЗВРАЩЕНЕЦ!", "ХВАТИТ!", "ПОЛИЦИЯ!", "💢", "РЯЯЯ!", "КЬЯ!", "УБЬЮ!", "ТЫ ЧТО ТВОРИШЬ?!", "МАНЬЯК!", "СПАСИТЕ!", "ПОМОГИТЕ!", "АДМИН, ЗАБАНЬ ЕГО!", "ТЫ БОЛЬНОЙ?!", "НЕНАВИЖУ ТЕБЯ!", "ИСЧЕЗНИ!", "ГОРИ В АДУ!", "ТЫ ХУДШИЙ!", "НЕ ПРОЩУ!", "Я ЗВОНЮ В ПОЛИЦИЮ!", "ТЫ МЕРЗКИЙ!", "УМРИ!", "ПРОКЛЯНУ!", "ТЫ ДЛЯ МЕНЯ УМЕР!", "ААААААА!!!", "ВСЁ КОНЧЕНО!", "УДАЛЮ ВИНДУ!", "БАН ТЕБЕ!", "СМЕРТЬ!", "ААААА!", "НЕНАВИЖУ!", "ТЫ МОНСТР!", "ВЫЗЫВАЙТЕ ОМОН!", "ВСЁ, Я УХОЖУ!", "ТЫ ТРУП!"],
            welcome: ["Йо!", "Я тут!",  "А вот и ты!", "Скучал?", "Давно не виделись.", "Привет-привет!", "Готов к тредам?", "Я проснулась!", "Ку-ку!", "Сап!", "Вечер в хату.", "Рада видеть.", "Ну, здравствуй.", "Заходи, не бойся.", "Включайся!", "Жду приказаний.", "Приветствую.", "Снова здесь?", "Хай!", "Салют!", "Добро пожаловать домой.", "Ждала тебя.", "Наконец-то!", "Привет, Анон!", "С возвращением!", "Бу!", "Мур?", "Загрузилась!", "Опа, ты пришел!"],
            maid_intro: ["Хозяин? <3", "Слушаюсь и повинуюсь.", "Ваша горничная прибыла.", "Пора навести порядок.", "Чай, кофе, или...?", "В вашем распоряжении.", "Жду указаний, господин.", "Тут немного пыльно.", "Я все уберу.", "Ваш верный слуга.", "Приказ?", "Готова служить.", "Служба — моя радость.", "Чего желаете?", "Ваша воля — закон.", "Уборка?", "Чего изволите?", "Ваш заказ?", "К службе готова!"],
            maid_react: ["Прибраться тут?", "Не смотри под юбку!",  "Ой, я неуклюжая...", "Не пачкай!", "Я только помыла!", "Это неприлично, хозяин!", "Не отвлекайте от работы.", "Я сейчас покраснею.", "Ну господин...", "Это не входит в обязанности.", "Смущаете...", "Я усердно работаю.", "Чистота — залог здоровья.", "Не трогайте фартук!", "Я буду жаловаться!", "Работа не ждет.", "Я стараюсь...", "Чай или кофе?", "Пыли много..."],
            bunny_intro: ["Сюрприз! 🐰", "Я твой зайчик.", "Морковка есть?", "Прыгаю к тебе.", "Ушки на макушке.", "Я пушистая!", "Твой маленький кролик.", "Сюрприз!", "Ждал меня?", "Я тут самая милая.", "Прыг!", "Кролик прибыл.", "Люблю прыгать.", "Твой личный багз банни.", "Поиграем в догонялки?", "Прыг-скок!", "Твой подарочек.", "Ушки!", "Хе-хе~"],
            bunny_react: ["Н-не смотри так...", "Хвостик не трогай!", "Я не плюшевая!", "Ой, щекотно!", "Не дергай за уши!", "Я быстрая!", "Поймай меня!", "Стесняюсь в этом...", "Это костюм, а не... ну ты понял.", "Морковку давай!", "Я милая?", "Не смотри на хвостик!", "Прыг-скок!", "Хватит пялиться!", "Я убегу!", "Смущаешь...", "Холодно...", "Нравится?", "Только для тебя."],
            cozy_intro: ["Я у тебя поживу?", "Можно остаться?", "Твоя одежда такая большая.", "Чувствую себя защищенной.", "Домашний режим.", "Так тепло.", "Мне идет?", "Твой запах...", "Как будто ты меня обнимаешь.", "Никуда не пойду.", "У тебя уютно.", "Можно чаю?", "Я тут полежу.", "Не выгоняй.", "Мне так удобно.", "Твоя рубашка...", "Как дома.", "Уютно~", "Ничего лишнего."],
            cozy_react: ["Теплая...", "Великовата, да?", "Не подглядывай!", "Я просто отдыхаю.", "Ничего не видно!", "Мне не холодно.", "Твоя рубашка лучшая.", "Спряталась.", "Не смущай.", "Я как в коконе.", "Мягкая ткань.", "Запах родной.", "Не забирай!", "Мне так нравится.", "По-домашнему.", "Не смотри на ноги!", "Без штанов лучше.", "Так удобнее."],
            beach_hot: ["Уфф, жарко...", "Время пляжа! 🏖️", "Плавится асфальт...", "Хочу мороженое!", "Кондиционер не спасает.", "Пойдем купаться?", "Лето в разгаре.", "Солнечный удар...", "Пекло...", "Нужна вода!", "Спасай меня от жары.", "Мечтаю о бассейне.", "Жаришка!", "Вентилятор включи.", "Воды...", "Солнце, хватит!", "Хочу на море...", "Солнце печет...", "Нужен перерыв."],
            beach_shy: ["Не смотри так :3", "Все смотрят...", "Отвернись!", "Я не привыкла...", "Купальник не слишком...?", "Спрячь меня.", "Не пялься!", "Мне неловко.", "Закрой глаза.", "Ну перестань...", "Я краснею.", "Слишком много внимания.", "Не фотографируй!", "Я стесняша.", "Ой...", "Я стесняюсь...", "Прикрой меня!", "Слишком открыто?"],
            beach_bold: ["Как тебе вид? 😏", "Я горячая штучка.", "Любуйся.", "Завидуй молча.", "Я звезда.", "Все взгляды на мне.", "Нравится?", "Смотри во все глаза.", "Идеальный загар.", "Я знаю, что я красивая.", "Не обожгись.", "Пляж мой.", "Я тут главная.", "Смотри, но руками не трогай.", "Шикарный вид, да?", "Оцени купальник!", "Королева пляжа.", "Смотри, не трогай."],
            beach_norm: ["Водичка супер!",  "Отличный день.", "Отдыхаем!", "Загорай со мной.", "Крем намажешь?", "Лето же!"],
            beach_ball: ["Поиграем?", "Отбивай!", "Волейбол?", "Не урони!", "Пас!", "Подача!", "Я выиграю!", "Пляжный спорт.", "Лови мяч!", "Лови!", "Прыг-скок!", "Мячик!", "Спорт - это жизнь."],
            beach_gun_bold: ["Сдавайся!", "Руки вверх!", "Намочу!", "Хэдшот! 💦", "Ты попал.",  "Атака!", "Получай!", "Снайпер на позиции.", "Никакой пощады!", "Ты проиграл!", "Водная кара.", "Цель захвачена.", "Огонь!", "Сдавайся или промокнешь!", "Я тебя достану!", "Прямое попадание!", "В яблочко!", "Ты мой!"],
            beach_gun: ["Пиу-пиу! 🔫", "Водичкой пшик!", "Охладись!", "Дуэль!", "Водяной пистолет — сила.", "Стреляю!", "Брызги во все стороны!", "Война!", "Не убежишь!", "Пиу!", "Мокро!", "Осторожно, вода!", "Прицеливаюсь...", "Заряжаю...", "Берегись!", "Освежись!", "Водная битва!", "Прицел взят."],
            beach_end: ["Хватит на сегодня.", "Пора домой.", "Замерзла.", "Солнце садится.", "Накупалась.", "Устала...", "Хочу в душ.", "Песок везде...", "Хорошо отдохнули.", "До завтра.", "Пляж закрывается.", "Одежду подай.", "Брр, ветер.", "Заканчиваем.", "Было весело.", "Одеваюсь...", "Стало прохладно."],
            panty_bold: ["Хотел увидеть? 😏", "Твой приз.", "Любуйся.", "Эксклюзив.", "Только для твоих глаз.", "Ну как?", "Заценил?", "Не скромничай.", "Я знаю, ты хотел.", "Специальный показ.", "Бонус.", "Секретик.", "Видел?", "Наслаждайся.", "Подарок.", "Нравится?", "Запоминай.", "Смотри.", "Секрет раскрыт."],
            panty_shy: ["ИЗВРАЩЕНЕЦ!!",  "Отвернись сейчас же!", "Не смей!", "Как тебе не стыдно!", "Ай!", "Спрячь меня!", "Это случайно!", "Не подглядывай!", "Фу!", "Уходи!", "Забудь что видел!", "Какой позор...", "Мамочки!", "Я не хотела!", "Бесстыдник!", "НЕ СМОТРИ!", "ААА!", "Закрой глаза!", "Дурак!"],
            undress_pants: ["Жарко...", "Штаны — это зло.", "Свободу жопе!", "Без них лучше.", "Душно в них.", "Снимаю...", "Они мне жмут.", "Долой штаны!", "Расслабляюсь.", "Так прохладнее.", "Мешают сидеть.", "Лишняя одежда.", "Свобода!", "Убираем низ.", "Оп!", "Мешают...", "Сниму лишнее."],
            undress_panties: ["Только тсс...", "Это между нами.", "Тшш...", "Не пали.", "Упс...", "Случайность.", "Тихо!", "Никому не говори.", "Молчок.", "Это тайна.", "Ой-ой...", "Секрет.", "Только ты видел.", "Тссс...", "Маленькая тайна.", "Секретик...", "Ой..."],
            undress_hoodie: ["Мешает...", "Жарко в ней.", "Сниму кофту.", "Лишний груз.", "Раскрываюсь.", "Долой капюшон.", "Без нее легче.", "Снимаю верх.", "Освобождаюсь.", "Так лучше.", "Слишком тепло.", "Убираем.", "Снимаю.", "Оп-ля!", "Свободнее.", "Тяжелая...", "Сниму."],
            sweater_start: ["Холодно стало...", "Замерзла.", "Где мой свитер?", "Утепляюсь.", "Мороз по коже.", "Хочу тепла.", "Свитер — лучшее решение.", "Одеваюсь.", "Холодрыга.", "Греюсь.", "Уютный свитер.", "Надеваю.", "Зябко.", "Тепло...", "В домике.", "Укутаюсь.", "Брр..."],
            sweater_end: ["Всё, переодеваюсь.", "Согрелась.", "Хватит.", "Стало жарко.", "Снимаю свитер.", "Достаточно.", "Перегрелась.", "Возвращаюсь в норму.", "Свитер офф.", "Фух, тепло.", "Больше не холодно.", "Обратно.", "Снимаю.", "Всё.", "Готово.", "Согрелась, спасибо.", "Меняем лук."],
            sweater_drag: ["Эй, свитер растянешь!", "Не тяни!", "Пряжа порвется!", "Испортишь вещь!", "Отпусти рукав!", "Тянешь же!", "Порвешь!", "Свитер жалко!", "Не дергай!", "Аккуратнее!", "Он растянется!", "Руки!", "Мой любимый свитер!", "Не тяни резинку!", "Эй-эй!", "Осторожно!", "Шерсть!"],
            sweater_tea: ["Вкусно...", "Греюсь.", "Чай?", "Тепло.", "Горячий...", "С чаем лучше.", "Грею руки.", "Ароматный.", "Чаепитие.", "Уют.", "Глоток тепла.", "Вкуснятина.", "Чай с печенькой?", "Согревает.", "Люблю чай.", "Ммм...", "Релакс.", "Пауза на чай."],
            sweater_spin: ["Тебе нравится?", "Мягкий?", "Покрутилась.", "Я красивая?", "Смотри как сидит.", "Круть-верть.", "Оцени лук.", "Уютно выглядит?", "Мягко.", "Показала.", "Как я тебе?", "Свитер топ.", "Кружусь.", "Модельная проходка.", "Нравится?", "Стильно?", "Позер."],
            succubus_start: ["Время шалостей! 😈", "Поиграем?", "Ня?", "Твоя душа моя.", "Адский прикид.", "Демонически красиво.", "Искушение...", "Готов грешить?", "Я пришла за тобой.", "Маленький чертенок.", "Рожки!", "Бу! Испугался?", "Время грехов.", "Шалю.", "Демон внутри."],
            succubus_drag: ["Полетаем? 😈", "Выше!", "Уии!", "Твой личный демон.", "На крыльях ночи!", "В ад!", "Летим!", "Не урони демона!", "Выше к звездам!", "Ух ты!", "Полет!", "Крылья!", "Вверх!", "Держи крепче!", "А полетели!", "В бездну!", "Демонический полет!", "Уи-и-и!"],
            tracking: ["Что там?", "Хм...", "А??", "Я слежу.", "Вижу...", "Куда ты смотришь?", "Интересно...", "Наблюдаю.", "Что это?", "Вижу курсор.", "Слежка.", "Не упускаю из виду.", "Ага...", "Так-так...", "Зафиксировала.", "Взгляд.", "Смотрю.", "Все вижу.", "От меня не скроешься."],
            glitch: ["SYSTEM ERROR", "Low Battery...", "0100101", "LAG", "REBOOT", "ERROR 404", "CRITICAL FAILURE", "BSOD", "MATRIX", "GLITCH", "BUG", "NO SIGNAL", "DATA CORRUPT", "FATAL ERROR", "SYSTEM HALT", "KERNEL PANIC", "SEGMENTATION FAULT", "OVERLOAD", "DISCONNECT"],
            hide: ["Ку-ку...", "Я тут.", "Спряталась.", "Вижу тебя.", "Меня нет.", "В укрытии.", "Тихо...", "Я в тени.", "Не найдешь.", "Прятки.", "Тсс...", "Я наблюдаю.", "Засада.", "Скрытность 100.", "Ниндзя.", "Исчезла.", "Мимикрия.", "Шпион."],
            box_phrase: ["Мой домик.", "Я в домике.", "Коробка - это жизнь.", "Уютно.", "Спряталась.", "Не кантовать!", "Мяу?", "Коробка моя!", "Тут безопасно.", "Кот Шредингера.", "Закрой крышку.", "Моя крепость.", "Доставка!", "Посылка.", "Не трогай коробку.", "Тут тепло.", "Мое место.", "Занято.", "Живу здесь.", "Коробка топ.", "Мяу!"],
            sleep_phrase: ["Zzz...", "Хрр...", "Сплю...", "Не будить...", "Ммм...", "Снов...", "Храплю...", "Сладкие сны.", "Офлайн.", "Спящий режим.", "Бай-бай.", "Сны...", "Подушка...", "Одеялко...", "Сплюшка.", "Тихий час.", "Не шуми.", "Сновидения.", "Ушла в астрал.", "Zzz..."],
            wake_phrase: ["Доброе...", "А? Что?", "Я не спала...", "Уже утро?", "Ой...", "Пора вставать...", "Где я?", "Который час?", "Проснулась.", "Зевок...", "Потягиваюсь.", "Утро?", "Что случилось?", "Я бодра (нет).", "Встаю...", "Ох...", "Ну еще 5 минут...", "Будильник?", "Жива.", "Доброе утро."],
            falling: ["Уиии!", "Ааа!", "Лечу!", "Бум!", "Опа!", "Поберегись!", "Падаю!", "Вниз!", "Гравитация!", "Плюх!", "Шлеп!", "Ааааа!", "Спасите!", "Земля близко!", "Ух!", "Ой!", "Летим вниз!", "Банзай!", "Катапульта!"]
        },
        en: {
            night: ["Can't sleep?", "Just you and me...", "It's quiet.", "Cold alone...", "Stay up late? <3", "Night owl?", "Eyes tired?", "World is sleeping...", "Nice moon...", "Shh... Don't wake them.", "Secret time.", "Not scared with you.", "One more page?", "Zombie mode: ON."],
            morning: ["Morning, sleepyhead!", "Coffee?", "Early today~", "Looking good!", "Let's start!", "Stretch~!", "Sun is up.", "New threads waiting.", "Breakfast is key.", "5 more mins...", "Let's go!", "You got this!", "Open your eyes!", "Plan: scrolling.", "Wakey wakey!"],
            day: ["Working?", "Drink water!", "I'm here.", "Bored...", "Pet me more~", "Posture check!", "Time for fun - me.", "Distraction?", "Sun's shining.", "Busy?", "Scroll scroll...", "Watching you.", "Lunch?", "Productivity!", "Boss coming! Jk."],
            evening: ["Tired? Relax.", "How was your day?", "Cozy evening.", "Finally you're here.", "Chill time.", "Tea & boards.", "Long day.", "No work.", "Saw the sunset?", "Vibe together.", "Deep thoughts...", "You earned it.", "Dinner?", "Lo-fi time.", "Don't be sad."],
            flirty: ["Making me blush...", "I like that.", "Tickles!", "So persistent...", "Daddy? 😏", "My hero <3", "Don't stop...", "Electric clicks...", "I'm burning...", "Closer...", "Just for you.", "Take me home.", "You're special.", "Heart beating...", "Mrr...", "Look at me...", "Naughty.", "Destiny?"],
            cute: ["Meow?", ":3", "UwU", "Am I pretty?", "Scritches?~", "Love you.", "You're the best!", "Hugs please!", "*wags tail*", "Notice me?", "Nya!", "Big cursor...", "I'm smol.", "Don't forget me.", "Nom!", "Purr..."],
            angry_soft: ["Baka!", "What?", "Don't touch!", "Hm...", "Well?", "Busy.", "Hands off!", "Hey!", "Don't poke.", "Rude.", "What do you want?", "Not a button.", "Keep distance.", "Pff...", "Weirdo."],
            angry_med: ["Go away!", "DON'T TOUCH!", "Stahp!", "MM...", "Enough!", "Stop it!", "It hurts!", "I'll report you!", "I have rights!", "Cut it out!", "Annoying.", "Leave!", "Ignore mode: ON.", "Grrr...", "Ugh."],
            angry_hard: ["PERVERT!", "STOP!", "POLICE!", "💢", "REEE!", "KYA!", "DELETING WINDOWS!", "BAN!", "DIE!", "AAAAA!", "HATE YOU!", "MONSTER!", "CALLING FBI!", "I'M LEAVING!", "YOU'RE DEAD!"],
            welcome: ["Yo!", "I'm here!", "Waited for you.", "Finally!", "Hi Anon!", "Welcome back!", "Boo!", "Purr?", "Loaded!", "Oh, you're here!"],
            maid_intro: ["Master? <3", "Cleaning?", "Your orders?", "Order please?", "Ready to serve!"],
            maid_react: ["Clean here?", "Don't look up!", "I'm trying...", "Tea or coffee?", "Dusty..."],
            bunny_intro: ["Surprise! 🐰", "Hop-hop!", "Your gift.", "Bunny ears!", "Hehe~"],
            bunny_react: ["D-don't stare...", "Embarrassing...", "Cold...", "Like it?", "Just for you."],
            cozy_intro: ["Can I stay?", "Your shirt...", "Like home.", "Cozy~", "Nothing else."],
            cozy_react: ["Warm...", "Too big, huh?", "Don't look at legs!", "Better no pants.", "Comfy."],
            beach_hot: ["Oof, hot...", "Beach time! 🏖️", "Want to sea...", "Sun is burning...", "Need a break."],
            beach_shy: ["Don't look :3", "I'm shy...", "Cover me!", "Too much skin?"],
            beach_bold: ["Like the view? 😏", "Rate my swimsuit!", "Beach queen.", "Look, don't touch."],
            beach_norm: ["Water is great!", "Tan with me.", "Sunscreen?", "It's summer!"],
            beach_ball: ["Let's play?", "Catch!", "Bounce!", "Ball!", "Sports!"],
            beach_gun_bold: ["Give up!", "Hands up!", "Soaking you!", "Headshot! 💦", "Gotcha."],
            beach_gun: ["Pew-pew! 🔫", "Refresh!", "Water war!", "Target locked."],
            beach_end: ["Enough for today.", "Getting dressed...", "Getting chilly."],
            panty_bold: ["Wanted to see? 😏", "Like it?", "Memorize it.", "Look.", "Secret revealed."],
            panty_shy: ["PERVERT!!", "DON'T LOOK!", "AHH!", "Close eyes!", "Idiot!"],
            undress_pants: ["Hot here...", "In the way...", "Removing excess."],
            undress_panties: ["Shh...", "Secret...", "Oops..."],
            undress_hoodie: ["Annoying...", "Heavy...", "Taking off."],
            sweater_start: ["Got cold...", "Bundling up.", "Brr..."],
            sweater_end: ["Changing back.", "Warmed up.", "Done."],
            sweater_drag: ["Hey, stretching it!", "Don't pull!", "Yarn will break!"],
            sweater_tea: ["Tasty...", "Warming up.", "Tea?", "Warm."],
            sweater_spin: ["Like it?", "Soft?", "Spinning."],
            succubus_start: ["Naughty time! 😈", "Let's play?", "Soul or body?"],
            succubus_drag: ["Let's fly? 😈", "Higher!", "Whee!", "Your demon."],
            tracking: ["What's there?", "Hm...", "Mouse moving?", "I'm watching.", "Cursor seen."],
            glitch: ["SYSTEM ERROR", "Low Battery...", "0100101", "LAG", "REBOOT"],
            hide: ["Peekaboo...", "I'm here.", "Hiding.", "I see you."],
            box_phrase: ["My castle.", "I'm in a box.", "Cozy.", "Delivery!", "My spot.", "If I fits, I sits.", "Meow?"],
            sleep_phrase: ["Zzz...", "Nnngh...", "Sleeping...", "Don't wake me...", "Mmm...", "Dreaming..."],
            wake_phrase: ["Morning...", "Huh?", "I wasn't sleeping...", "Is it day?", "Oh...", "Up we go..."],
            falling: ["Wheee!", "Yikes!", "Whoops!", "Falling!", "Geronimo!", "Oof!"]
        },
        jp: {
            night: ["眠れない？", "二人きり...", "夜は静かだね", "寒いよ...", "朝まで起きる？<3", "夜更かし？", "目が疲れた？", "世界はお休み...", "月が綺麗...", "シーッ...起こさないで", "秘密の時間", "怖くないよ", "もう1ページ？", "ゾンビモードON"],
            morning: ["おはよー！", "コーヒー飲む？", "早いね〜", "元気そう！", "始めよう！", "のび〜", "太陽が出たよ", "スレが待ってる", "朝ごはんは大事", "あと5分...", "いくぞー！", "頑張って！", "目を開けて！", "予定：スクロール", "シャキッと！"],
            day: ["仕事中？", "水飲んでね！", "そばにいるよ", "退屈...", "なでて〜", "背筋伸ばして！", "遊びたいな", "休憩しない？", "いい天気", "忙しい？", "スクロール...", "見てるよ", "昼ごはんは？", "効率いいね！", "上司だ！冗談ｗ"],
            evening: ["お疲れ様", "どうだった？", "いい夜だね", "やっと会えた", "リラックス〜", "お茶と掲示板", "長い一日だった", "仕事なし！", "夕日見た？", "まったりしよ", "深い考え...", "休んでいいよ", "晩ごはんは？", "Lo-fiかけて", "元気出して"],
            flirty: ["顔が熱い...", "それ好き...", "くすぐったい！", "しつこいなぁ...", "パパ？😏", "私のヒーロー<3", "続けて...", "クリックが...痺れる", "燃えちゃう...", "もっと近くに...", "あなただけ", "お持ち帰りして？", "特別だよ", "ドキドキ...", "んっ...", "目を見て...", "エッチ", "運命かな？"],
            cute: ["にゃ？", ":3", "UwU", "可愛い？", "なでなでして〜", "大好き", "最高！", "ぎゅっとして！", "*しっぽフリフリ*", "気づいて？", "にゃん！", "大きいカーソル...", "私はちっこい", "忘れないで", "ガブッ！", "ゴロゴロ..."],
            angry_soft: ["バカ！", "なに？", "触らないで", "ふーん", "で？", "忙しいの", "手どけて！", "おい！", "つつくな", "失礼ね", "用事？", "ボタンじゃないよ", "離れて", "プイッ", "変な人"],
            angry_med: ["あっち行って！", "触るな！", "やめて！", "んん...", "もう！", "ストップ！", "痛い！", "通報するよ！", "権利があるの！", "いい加減にして！", "うざい", "消えて！", "無視します", "ガルル...", "ムカつく"],
            angry_hard: ["変態！", "やめろ！", "ポリス！", "💢", "キェェェ！", "キャー！", "Win削除するよ！", "BANだ！", "死ね！", "あああ！", "大嫌い！", "怪物！", "特殊部隊呼ぶ！", "もう帰る！", "殺す！"],
            welcome: ["よっ！", "来たよ！", "待ってた", "やっと！", "やっほー！", "おかえり！", "わっ！", "ん？", "ロード完了！", "あ、来た！"],
            maid_intro: ["ご主人様？<3", "お掃除？", "ご用命は？", "ご注文は？", "奉仕します！"],
            maid_react: ["掃除する？", "下見ないで！", "頑張る...", "お茶にする？", "埃が..."],
            bunny_intro: ["サプライズ！🐰", "ピョンピョン！", "プレゼントだよ", "うさ耳！", "へへ〜"],
            bunny_react: ["じ、見ないで...", "恥ずかしい...", "寒い...", "好き？", "あなただけ"],
            cozy_intro: ["泊まっていい？", "シャツ借りた", "実家感", "落ち着く〜", "これだけでいい"],
            cozy_react: ["あったかい...", "大きいね...", "脚見ないで！", "パンツなしが楽", "快適"],
            beach_hot: ["あつーい...", "海だ！🏖️", "泳ぎたい...", "日差しが...", "休憩しよ"],
            beach_shy: ["見ないで :3", "恥ずかしい...", "隠して！", "露出多い？"],
            beach_bold: ["いい眺め？😏", "水着どう？", "ビーチの女王", "見るだけよ"],
            beach_norm: ["水が最高！", "焼こうかな", "日焼け止め塗って", "夏だもん！"],
            beach_ball: ["遊ぼう？", "キャッチ！", "ぽよん！", "ボール！", "スポーツ！"],
            beach_gun_bold: ["降参して！", "手ェあげろ！", "濡らすよ！", "ヘッドショット！💦", "当たった"],
            beach_gun: ["ピューピュー！🔫", "涼んで！", "水鉄砲バトル！", "ロックオン"],
            beach_end: ["今日はここまで", "着替えるね", "冷えてきた"],
            panty_bold: ["見たかった？😏", "好きでしょ？", "目に焼き付けて", "ほら", "秘密公開"],
            panty_shy: ["変態！！", "見ないで！", "キャー！", "目閉じて！", "バカ！"],
            undress_pants: ["暑いし...", "邪魔...", "脱いじゃお"],
            undress_panties: ["シーッ...", "秘密...", "あ..."],
            undress_hoodie: ["邪魔...", "重い...", "脱ぐね"],
            sweater_start: ["寒くなった...", "着込むね", "ブルブル"],
            sweater_end: ["着替える", "温まった", "終わり"],
            sweater_drag: ["伸びちゃう！", "引っ張るな！", "糸切れる！"],
            sweater_tea: ["美味しい...", "あったまる", "お茶？", "ぽかぽか"],
            sweater_spin: ["どう？", "ふわふわ？", "くるくる"],
            succubus_start: ["イタズラしよ！😈", "遊ぶ？", "魂か体か？"],
            succubus_drag: ["飛ぶ？😈", "高く！", "わーい！", "君の悪魔"],
            tracking: ["何があるの？", "ふむ...", "マウスどこ？", "見てるよ", "カーソル発見"],
            glitch: ["SYSTEM ERROR", "Low Battery...", "0100101", "LAG", "再起動"],
            hide: ["いないいない...", "ここだよ", "隠れた", "見えてるよ"],
            box_phrase: ["私の城", "箱入り娘", "落ち着く...", "宅急便です", "隠れ家", "猫かな？", "入っちゃった"],
            sleep_phrase: ["Zzz...", "むにゃ...", "寝てる...", "起こさないで...", "スー...", "夢の中..."],
            wake_phrase: ["おは...", "え？なに？", "寝てないよ...", "朝？", "あれ...", "起きる..."],
            falling: ["わー！", "きゃー！", "飛んでる！", "とう！", "あぶな！", "いくよー！"]
        }
    },
    getContextualPhrase() {
        const h = new Date().getHours();
        let timeKey = 'day';
        if (h >= 23 || h < 5) timeKey = 'night';
        else if (h >= 5 && h < 11) timeKey = 'morning';
        else if (h >= 17 && h < 23) timeKey = 'evening';
        return this.getRandomText(timeKey);
    },
    db: {
        idle: [
            'usual-stands-still-smiles.webp',
            'usual-stand-still-no-emotions.webp',
            'chilling-stands-eyes-closed.webp',
            'stands-looking-in-phone-smiles.webp',
            'stands-eyes-semiclosed-looks-tired-eyebrows-differ.webp',
            'chilling-stands-hand-on-chest-(introducing-herself)-eyes-closed.webp',
            'chilling-stands-eyes-closed-head-tilted.webp',
            'chilling-turned-tilted-aside-eyes-closed-sad-mouth-(;c).webp',
            'chilling-turned-tilted-aside-eyes-closed-w-mouth-(;3).webp',
            'stands-looking-with-interest-eyebrows-differs.webp',
            'standing-idle-with-hood.webp',
            'watching-phone-idle.webp',
        ],
        interested: ['stands-looking-with-interest-eyebrows-differs.webp'],
        walk: ['walking-frame-1.webp', 'walking-frame-2.webp', 'walking-frame-3.webp', 'walking-frame-4.webp'],
        drag_start: [
            'being-held-angry-1.webp',
            'being-held-angry-2.webp',
            'being-held-angry-3.webp',
            'being-held-looking-annoyed.webp',
            'being-held-looking-annoyed-2.webp'
        ],
        drag_loop: ['being-held-swinging-in-air-1.webp', 'being-held-swinging-in-air-2.webp'],
        fall: ['falling-or-fallen-passed-out.webp'],
        land: ['falling-or-fallen-passed-out-2.webp', 'tired-passed-out-wall.webp'],
        hide: ['hiding-aside-wall.webp'],
        look_up: ['looking-up-central.webp', 'looking-up-left.webp', 'looking-up-right.webp'],
        happy: [
            'cute-happy-with-phone.webp', 'welcoming-waving-hand.webp',
            'cute-tilted-body-happy-with-phone.webp', 'happy-with-phone-in-hands.webp',
            'happily-walking-with-phone.webp'
        ],
        cute: [
            'cute-happy-with-phone.webp',
            'cute-tilted-body-happy-with-phone.webp',
            'cute-blushing-touch-hair.webp',
            'cute-blushing-hands-crossed.webp',
            'cute-heart-eyes-looking-in-smartphone.webp',
            'cute-heart-eyes-watching-in-smartphone.webp',
            'cute-embarassed-pose-2.webp',
            'somewhat-embarassed-cute.webp'
        ],
        angry_soft: ['displeased-hands-crossed.webp', 'displeased-hands-crossed-turns-her-back.webp'],
        angry_med: ['angry-with-phone.webp', 'angry-with-phone-mirrored.webp', 'being-held-looking-annoyed-2.webp'],
        angry_hard: ['angry-shaking.webp', 'shows-middle-finger.webp', 'shocked-embarassed-vapor-out-of-ears.webp'],
        love: [
            'spiral-eyes-obsessed-blush.webp',
            'cute-blushing-touch-hair.webp',
            'cute-blushing-hands-crossed.webp',
            'cute-blushing-look.webp',
            'super-embarassed-red-face.webp',
            'heart-eyes-look1.webp',
            'heart-eyes-look-2-obsessed.webp',
            'cute-heart-eyes-looking-in-smartphone.webp',
            'cute-heart-eyes-watching-in-smartphone.webp'
        ],
        actions: [
            'drinking-standing.webp', 'drinking-standing-var2.webp',
            'squatting-eating-noodles.webp', 'squatting-eating-noodles-var2.webp',
            'stands-taking-a-selfie.webp'
        ],
        sleep: [
            'on-floor-looking-to-phone.webp', 'sitting-in-the-box.webp',
            'cute-blushing-sitting-on-floor.webp', 'passed-out-on-floor.webp'
        ],
        undress_hoodie: [
            'undressing-3rd-var-1-or-staring-at-viewer-idle.webp',
            'undressing-3rd-var-2.webp',
            'undressing-3rd-var-3.webp'
        ],
        pantyshot_variants: [
            'showing-panties-embarassed.webp',
            'showing-off-panties-2.webp'
        ],
        beach_extras: [
            'swimsuit-embarassed.webp',
            'swimsuit-hands-on-hips.webp',
            'swimsuit-waterpistol-smirking.webp'
        ],
        sweater_idle: [
            'in-sweater-stands.webp',
            'in-sweater-holds-cup-stands.webp',
            'in-sweater-front-holds-collar.webp'
        ],
        sweater_sit: [
            'in-sweater-sits-on-floor.webp',
            'in-sweater-sits-on-floor-holds-cup.webp',
            'in-sweater-sits-on-floor-holds-cup-2.webp',
            'in-sweater-sits-on-floor-sniffs-cup.webp',
            'in-sweater-sits-on-floor-holds-cup-peace.webp'
        ],
        sweater_action: [
            'in-sweater-back.webp',
            'in-sweater-half-turned-left.webp',
            'in-sweater-jumps.webp'
        ],
        succubus_idle: [
            'succub-stands-smiles.webp',
            'succub-stands-hands-in-pockets.webp',
            'succub-stands-stare.webp',
            'succub-stands-stare-;3.webp',
            'succub-stands-winks-happy.webp'
        ],
        succubus_action: [
            'succub-glowing-eyes.webp',
            'succub-stabds-wings-deployed.webp',
            'succub-stands-points-finger-red-eyes-heart-with-horns.webp'
        ],
        succubus_sit: [
            'succub-sits-on-floor-playful.webp',
            'succub-sits-on-skull.webp',
            'succub-sits-on-skull-2.webp',
            'succub-playful.webp'
        ],
        succubus_drag: [
            'succub-playful-2.webp',
            'succub-stands-undressing-hoodie.webp'
        ],
        glitch: ['error-glitching.webp', 'error-glitching-low-battery.webp'],
        fly: ['with-balloons.webp'],
        ban: ['with-BAN-sign.webp'],
        pantyshot: ['showing-panties-embarassed.webp'],
        undress_pants: ['undress-top-has-pants-1st.webp', 'undress-top-has-pants-2nd.webp', 'undress-top-has-pants-3rd.webp'],
        undress_nopants: ['undress-top-have-no-pants-1st.webp', 'undress-top-have-no-pants-2nd.webp', 'undress-top-have-no-pants-3rd.webp'],
        beach: ['beach-outfit-usual.webp', 'beach-outfit-on-ball.webp', 'beach-outfit-water-gun.webp']
    },
    noMirror: ['with-BAN-sign.webp', 'sitting-in-the-box.webp'],
    getLang() {
        if (this._cachedLang) return this._cachedLang;
        let lang = 'ru';
        const saved = localStorage.getItem('lang');
        if (saved && ['ru', 'en', 'jp'].includes(saved)) lang = saved;
        else {
            const htmlLang = document.documentElement.lang;
            if (htmlLang && ['ru', 'en', 'jp'].includes(htmlLang)) lang = htmlLang;
        }
        this._cachedLang = lang;
        return lang;
    },
    getText(category) {
        const lang = this.getLang();
        return this.locales[lang][category] || this.locales['ru'][category] || [];
    },
    getRandomText(category) {
        const list = this.getText(category);
        return list.length > 0 ? list[Math.floor(Math.random() * list.length)] : "...";
    },
    init() {
        if (window.isStaticMascotActive) return;
        if (this.initialized) return; 
        if (!SettingsManager.current.showMascot) return;
        if (document.body.classList.contains('page-roulette')) return;
        if (document.body.classList.contains('page-random-img')) return;
        if (document.querySelector('.catalog-grid')) return;
        if (location.pathname.includes('/catalog/')) return;
        this.initialized = true; 
        this.createDOM();
        if (window.SettingsManager && !window.SettingsManager.current.showMascot) {
            this.wrapper.style.display = 'none';
        }
        this.preloadEssentials();
        this.startPhysicsLoop();
        this.startLogicLoop();
        this.addFeedingListeners();
        this.initFormObservers();
        const events = ['mousedown', 'touchstart'];
        events.forEach(evt => this.body.addEventListener(evt, (e) => this.onDragStart(e), { passive: false }));
        const moveEvents = ['mousemove', 'touchmove'];
        moveEvents.forEach(evt => {
            const listener = (e) => {
                this.onDragMove(e);
                if(e.type === 'mousemove') this.onMouseMove(e);
            };
            window.addEventListener(evt, listener, { passive: false });
        });
        const endEvents = ['mouseup', 'touchend', 'touchcancel'];
        endEvents.forEach(evt => window.addEventListener(evt, (e) => this.onDragEnd(e)));
        this.body.addEventListener('contextmenu', (e) => {
            if (e.pointerType === 'touch') {
                e.preventDefault();
                return;
            }
            e.preventDefault();
            if(confirm(t('confirm_mascot_hide', 'Скрыть маскота?'))) {
                this.wrapper.style.display = 'none';
                if (window.SettingsManager) {
                    window.SettingsManager.current.showMascot = false;
                    window.SettingsManager.save();
                    const checkbox = document.querySelector('input[data-setting="showMascot"]');
                    if (checkbox) checkbox.checked = false;
                } else {
                    localStorage.setItem('hide_mascot', 'true');
                }
                this.isFrozen = true;
                this.isPhysicsRunning = false;
                if (this.timers.logic) clearTimeout(this.timers.logic);
                if (this.timers.walk) clearInterval(this.timers.walk);
                if (typeof showToast === 'function') {
                    showToast(t('mascot_hidden_hint', 'Маскот скрыт. Если хочешь вернуть — смотри настройки.'));
                }
            }
        });
        this.initVisibilityControl();
        this.setMood('idle');
        if (this.wrapper) this.wrapper.style.touchAction = 'none';
    },
    activateOutfitMode(mode) {
        if (this.timers.outfit) clearTimeout(this.timers.outfit);
        this.currentOutfit = mode;
        this.isLocked = true;
        this.spawnParticles(mode === 'succubus' ? 'anger' : 'love', 15);
        if (mode === 'sweater') {
            this.setImg(this.db.sweater_idle[0], true);
            this.say(this.getRandomText('sweater_start'), 2000);
        } else {
            this.setImg(this.db.succubus_idle[0], true);
            this.say(this.getRandomText('succubus_start'), 2000);
        }
        setTimeout(() => { this.isLocked = false; }, 1000);
        const duration = 90000 + Math.random() * 60000;
        this.timers.outfit = setTimeout(() => {
            this.spawnParticles('love', 10);
            this.say(this.getRandomText('sweater_end'), 2000);
            this.resetToDefault();
            this.updateVisuals();
        }, duration);
    },
    welcome() {
        if (localStorage.getItem('hide_mascot') === 'true') return;
        this.resetToDefault();
        this.isLocked = true;
        this.setImg('welcoming-waving-hand.webp', true);
        this.say(this.getRandomText('welcome'), 2000);
        setTimeout(() => {
            this.isLocked = false;
            this.setMood('idle');
        }, 2500);
    },
    addFeedingListeners() {
        this.body.addEventListener('dragenter', (e) => {
            e.preventDefault();
            if (this.state !== 'feed' && !this.isLocked) {
                this.setMood('cute');
                this.body.style.transform += ' scale(1.1)';
            }
        });
        this.body.addEventListener('dragover', (e) => e.preventDefault());
        this.body.addEventListener('dragleave', (e) => {
            if (!this.isLocked) this.setMood('idle');
        });
        this.body.addEventListener('drop', (e) => {
            e.preventDefault();
            this.startFeedingSequence();
        });
    },
    triggerRageMode() {
        this.resetToDefault();
        this.isLocked = true;
        this.setMood('angry_hard');
        this.spawnParticles('anger', 10);
        this.imgFront.classList.add('anim-shake');
        this.say(this.getRandomText('angry_hard'), 3000);
        this.p.vy = 10;
        setTimeout(() => {
            this.imgFront.classList.remove('anim-shake');
            this.isLocked = false;
            this.setMood('idle');
            this.say(this.getRandomText('angry_soft'), 1500);
        }, 4000);
    },
    startFeedingSequence() {
        if (this.state === 'feed') return;
        this.isLocked = true;
        this.state = 'feed';
        const foodSprite = Math.random() > 0.5 ? 'drinking-standing.webp' : 'squatting-eating-noodles.webp';
        this.setImg(foodSprite, true);
        this.say(t('mascot_eat'), 1000);
        this.body.classList.add('anim-squish');
        if (this.timers.feed) clearTimeout(this.timers.feed);
        this.timers.feed = setTimeout(() => {
            this.body.classList.remove('anim-squish');
            this.isLocked = false;
            this.state = 'idle';
            this.setMood('idle');
            this.say(t('mascot_thx'), 1000);
            this.updateVisuals();
        }, 2000);
    },
    createDOM() {
        this.wrapper = document.getElementById('mascot-wrapper');
        if (!this.wrapper) {
            this.wrapper = document.createElement('div');
            this.wrapper.id = 'mascot-wrapper';
            document.body.appendChild(this.wrapper);
        }
        this.wrapper.style.height = 'min(350px, 25vh)';
        this.wrapper.style.width = '240px';
        this.body = document.createElement('div');
        this.body.className = 'mascot-body';
        this.bubble = document.createElement('div');
        this.bubble.className = 'mascot-bubble';
        this.imgBack = document.createElement('img');
        this.imgBack.className = 'mascot-img hidden';
        this.imgFront = document.createElement('img');
        this.imgFront.className = 'mascot-img visible';
        this.body.append(this.imgBack, this.imgFront);
        this.wrapper.append(this.bubble, this.body);
    },
    preloadEssentials() {
        const toLoad = [...this.db.idle, ...this.db.walk, ...this.db.drag_start];
        toLoad.forEach(src => { const i = new Image(); i.src = this.basePath + src; });
    },
    setImg(filename, instant = false) {
        if (!this.imgFront || !this.imgBack) return;
        if (!filename) return;
        if (this.isLocked && !instant) return;

        if (this.noMirror.includes(filename)) {
            this.forceNoMirror = true;
        } else {
            this.forceNoMirror = false;
        }

        const currentFile = this.imgFront.src.split('/').pop();
        if (currentFile === filename) return;
        
        this.imgBack.onload = null;
        this.imgBack.src = this.basePath + filename;
        const swap = () => {
            this.imgBack.className = 'mascot-img visible';
            this.imgFront.className = 'mascot-img hidden';
            const temp = this.imgFront;
            this.imgFront = this.imgBack;
            this.imgBack = temp;
            this.imgBack.onload = null;
        };
        if (instant || this.imgBack.complete) {
            swap();
        } else {
            this.imgBack.onload = swap;
        }
    },
    setMood(category) {
        if (this.currentOutfit && this.currentOutfit !== 'default') {
            const mode = this.currentOutfit;
            if (category === 'idle' || category === 'cute') {
                category = `${mode}_idle`;
            }
            else if (category === 'sleep') {
                category = `${mode}_sit`;
            }
            else if (category === 'actions') {
                category = `${mode}_action`;
            }
        }
        if (!this.db[category]) return;
        const list = this.db[category];
        const item = list[Math.floor(Math.random() * list.length)];
        this.setImg(item);
    },
    initVisibilityControl() {
        window.addEventListener('online', () => {
            console.log('[Net] Online! Retrying broken images...');
            const broken = document.querySelectorAll('.broken-media, .broken-final');
            broken.forEach(el => {
                const wrapper = el.classList.contains('file-thumb') ? el : el.closest('.file-thumb');
                if (wrapper && wrapper.onclick) {
                    wrapper.click();
                }
            });
            if (typeof WSManager !== 'undefined') {
                WSManager.attempts = 0;
                WSManager.startWS();
            }
        });
        document.addEventListener('visibilitychange', () => {
            if (document.hidden) {
                document.querySelectorAll('video, audio').forEach(el => {
                    if (el.tagName === 'AUDIO') return;
                    if (el.closest('.custom-audio-player')) return;
                    if (el.closest('#system-dock')) return;
                    if (document.pictureInPictureElement === el) return;
                    if (!el.paused) {
                        el._wasPlaying = true;
                        el.pause();
                    }
                });
                this.isPhysicsRunning = false; 
            } else {
                document.querySelectorAll('video').forEach(el => {
                    if (el._wasPlaying) {
                        delete el._wasPlaying;
                        if (isElementInViewport(el)) el.play().catch(()=>{});
                    }
                });
                this.startPhysicsLoop();
                if (typeof WSManager !== 'undefined') WSManager.refreshContent();
            }
        });
        if ('IntersectionObserver' in window && this.wrapper) {
            this.viewObserver = new IntersectionObserver((entries) => {
                this.isViewportVisible = entries[0].isIntersecting;
                if (this.isViewportVisible) {
                    this.startPhysicsLoop();
                } else {
                    this.isPhysicsRunning = false; 
                }
            });
            this.viewObserver.observe(this.wrapper);
        } else {
            this.isViewportVisible = true;
        }
    },
    startPhysicsLoop() {
        if (this.isPhysicsRunning) return;
        if (this.isFrozen) return;
        if (document.hidden) return;
        if (this.isViewportVisible === false && this.state !== 'fall' && this.state !== 'fly') return;

        this.isPhysicsRunning = true;
        const loop = () => {
            if (this.isFrozen || document.hidden) {
                this.isPhysicsRunning = false;
                return;
            }
            if (this.isViewportVisible === false && this.state !== 'fall' && this.state !== 'fly') {
                this.isPhysicsRunning = false;
                return;
            }
            this.updatePhysics();
            this.renderFrame();
            requestAnimationFrame(loop);
        };
        loop();
    },
    updatePhysics() {
        if (this.state === 'fly') return;
        const p = this.p;
        const time = Date.now();
        const winHeight = window.innerHeight;
        const winWidth = window.innerWidth;
        
        const globalFloor = 0;
        let targetFloor = globalFloor;
        if (winWidth <= 768) {
            targetFloor = 0; 
        }
        const mascotCenterOffset = 200;
        const isMob = winWidth <= 768;
        const leftSafetyMargin = isMob ? 10 : 50;
        const mascotOffset = isMob ? 140 : 200;
        let minX = -(winWidth - mascotOffset - leftSafetyMargin);
        let maxX = isMob ? 10 : 100;
        
        if (this.attachedElement) {
            if (!this.attachedElement.isConnected) {
                this.detachFromForm(t('mascot_lost_form', "Ой..."));
                this.state = 'fall';
                return;
            }
            if (!this.cachedRect || !this.lastRectTime || (time - this.lastRectTime > 100)) { 
                this.cachedRect = this.attachedElement.getBoundingClientRect();
                this.lastRectTime = time;
            }
            const rect = this.cachedRect;
            const isVisible = this.attachedElement.offsetParent !== null;
            const isLargeEnough = rect.width > 150 && rect.height > 100;
            const isOnScreen = rect.top < winHeight && rect.bottom > 0 && rect.left < winWidth && rect.right > 0;
            
            if (!isVisible || !isLargeEnough || !isOnScreen) {
                this.detachFromForm(t('mascot_drop'));
            } else {
                const paddingBottom = 15;
                targetFloor = Math.max(0, winHeight - rect.bottom + paddingBottom);
                const screenCenterX = winWidth - mascotCenterOffset;
                minX = rect.left - screenCenterX + 60;
                maxX = rect.right - screenCenterX - 60;
            }
        }
        if (Math.abs(targetFloor - this.floorOffset) > 5) {
             this.floorOffset = targetFloor;
        } else {
             this.floorOffset += (targetFloor - this.floorOffset) * 0.2;
        }
        const currentSrc = this.imgFront.src;
        const isDizzy = currentSrc.includes('spiral') || currentSrc.includes('dizzy');
        if (isDizzy) {
            p.rot = Math.sin(time / 200) * 5;
        }
        const canBreathe = !p.isDragging && Math.abs(p.vy) < 0.5;
        const allowedState = (this.state === 'idle' || this.state === 'land' || this.state === 'feed' || this.state === 'sequence');
        let targetSX = 1;
        let targetSY = 1;
        if (canBreathe && allowedState) {
            const intensity = (this.state === 'feed') ? 0.6 : 1.0;
            const breath = Math.sin(time / 500) * 0.02 * intensity;
            targetSY = 1 + breath;
            targetSX = 1 - breath * 0.5;
        }
        if (!p.isDragging) {
            p.sx += (targetSX - p.sx) * 0.1;
            p.sy += (targetSY - p.sy) * 0.1;
        }
        if (p.isDragging) {
            let targetRot = (p.vx * 2.5);
            targetRot = Math.max(-35, Math.min(35, targetRot));
            p.rot += (targetRot - p.rot) * 0.2;
            const speed = Math.sqrt(p.vx*p.vx + p.vy*p.vy);
            p.sy = 1 + Math.min(speed * 0.002, 0.05);
            p.sx = 1 / p.sy;
            if (Date.now() - this.dragStartTime > 3000 && !this.imgFront.classList.contains('anim-shake')) {
                this.imgFront.classList.add('anim-shake');
                this.setMood('angry_hard');
                this.say(t('mascot_letgo'), 1000);
            }
        } else {
            if (p.y > this.floorOffset) {
                p.vy -= p.gravity;
                p.vx *= 0.99;
                p.rot *= 0.9;
                if (this.attachedElement && p.y - this.floorOffset < 50 && this.state !== 'fall') {
                     this.setMood('idle');
                }
            }
            else {
                if (p.y <= this.floorOffset) {
                    if (p.vy < -7 && (this.state === 'fall' || Math.random() > 0.5)) {
                        this.state = 'land';
                        this.setMood('land');
                        p.sy = 0.8; p.sx = 1.2;
                        if (this.timers.land) clearTimeout(this.timers.land);
                        this.timers.land = setTimeout(() => {
                            if (!p.isDragging && !this.isLocked) {
                                this.state = 'idle';
                                this.setMood('idle');
                                this.updateVisuals();
                            }
                        }, 500);
                    }
                    p.y = this.floorOffset;
                    p.vy = -p.vy * p.floorBounce;
                    if (Math.abs(p.vy) < 1.5) p.vy = 0;
                    p.vx *= p.friction;
                    if (Math.abs(p.vx) < 0.1) p.vx = 0;
                    p.rot *= 0.8;
                }
            }
            if (this.state === 'walk') {
                if (typeof this.walkTarget === 'undefined') this.walkTarget = p.x;
                const dist = Math.abs(this.walkTarget - p.x);
                if (dist < 5) {
                    this.state = 'idle';
                    this.updateVisuals();
                } else {
                    const dir = Math.sign(this.walkTarget - p.x);
                    p.vx = dir * 3;
                    this.direction = dir;
                    p.y = this.floorOffset + Math.abs(Math.sin(time / 100)) * 5;
                }
            }
            p.x += p.vx;
            p.y += p.vy;
            if (p.x < minX) { p.x = minX; p.vx *= -0.5; this.direction = 1; }
            if (p.x > maxX) { p.x = maxX; p.vx *= -0.5; this.direction = -1; }
        }
    },
    renderFrame() {
        if (this.state === 'fly') return;
        const p = this.p;
        this.wrapper.style.transform = `translate3d(${p.x}px, ${-p.y}px, 0)`;
        const dir = this.forceNoMirror ? 1 : this.direction;
        this.body.style.transform = `rotate(${p.rot}deg) scale(${p.sx * dir}, ${p.sy})`;
    },
    onDragStart(e) {
        if (e.touches && e.touches.length === 0 && this.isInputDown) {
            this.onDragEnd(e);
            return;
        }
        if (this.state === 'fly' || this.isLocked) return;
        if (e.type === 'mousedown' && e.button !== 0) return;
        const clientX = e.type.includes('touch') ? e.touches[0].clientX : e.clientX;
        const clientY = e.type.includes('touch') ? e.touches[0].clientY : e.clientY;
        this.wrapper.style.transition = 'none';
        this.body.style.transition = 'none';
        this.p.lastMouseX = clientX;
        this.p.lastMouseY = clientY;
        const isTouch = this.isMobile || e.type === 'touchstart';
        const secretHoldTime = isTouch ? 3000 : 1500;
        if (isTouch) {
            this.isTouchWait = true;
            this.isInputDown = false;
            this.touchStartX = clientX;
            this.touchStartY = clientY;
            if (this.timers.dragEnable) clearTimeout(this.timers.dragEnable);
                this.timers.dragEnable = setTimeout(() => {
                this.isInputDown = true;
                this.isTouchWait = false;
                this.p.isDragging = true;
                this.body.classList.add('no-transition');
                this.p.vx = 0;
                this.p.vy = 0;
                this.p.lastMouseX = this.touchStartX;
                this.p.lastMouseY = this.touchStartY;
                try { if (navigator.vibrate) navigator.vibrate(50); } catch(e) {}
                this.body.style.transform += ' scale(1.1)';
                document.body.style.overscrollBehaviorY = 'contain';
            }, 400);
        } else {
            this.isInputDown = true;
            this.p.isDragging = false;
            this.body.classList.add('no-transition');
            this.dragStartTime = Date.now();
        }
        if (this.timers.hold) clearTimeout(this.timers.hold);
        this.timers.hold = setTimeout(() => {
            if ((this.isInputDown || this.isTouchWait) && !this.p.isDragging) {
                this.isInputDown = false;
                this.isTouchWait = false;
                this.triggerSecretEvent();
            }
        }, secretHoldTime);
    },
    onDragMove(e) {
        const clientX = e.type.includes('touch') ? e.touches[0].clientX : e.clientX;
        const clientY = e.type.includes('touch') ? e.touches[0].clientY : e.clientY;
        if (this.isTouchWait) {
            const dist = Math.hypot(clientX - this.touchStartX, clientY - this.touchStartY);
            if (dist > 10) {
                clearTimeout(this.timers.dragEnable);
                clearTimeout(this.timers.hold);
                this.isTouchWait = false;
                this.isInputDown = false;
            }
            return;
        }
        if (!this.isInputDown || this.isLocked) return;
        if (e.cancelable && this.p.isDragging) e.preventDefault();
        const dx = clientX - this.p.lastMouseX;
        const dy = clientY - this.p.lastMouseY;
        if (!this.p.isDragging && Math.hypot(dx, dy) < 5) return;
        if (!this.p.isDragging) {
            this.p.isDragging = true;
            this.body.classList.add('no-transition');
            if (this.timers.hold) clearTimeout(this.timers.hold);
            this.state = 'drag';
            if (this.currentOutfit === 'succubus') {
                const list = this.db.succubus_drag;
                this.setImg(list[Math.floor(Math.random() * list.length)], true);
                this.say(this.getRandomText('succubus_drag'), 1000);
            }
            else if (this.currentOutfit === 'sweater') {
                this.say(this.getRandomText('sweater_drag'), 1500);
                if (this.imgFront.src.includes('sits') || this.imgFront.src.includes('floor')) {
                    this.setImg(this.db.sweater_idle[0], true);
                }
            }
            else {
                this.setMood('drag_start');
            }
            if (this.attachedElement) this.detachFromForm(null);
        }
        this.p.vx = this.p.vx * 0.5 + dx * 0.5;
        this.p.vy = this.p.vy * 0.5 - dy * 0.5;
        this.p.x += dx;
        this.p.y -= dy;
        const winW = window.innerWidth;
        const winH = window.innerHeight;
        const isMob = winW <= 768;
        const leftSafetyMargin = isMob ? 10 : 50;
        const mascotRightSpace = isMob ? 140 : 200;
        const minX = -(winW - mascotRightSpace - leftSafetyMargin);
        const maxX = isMob ? 10 : 100;
        
        if (this.p.x < minX) this.p.x = minX;
        if (this.p.x > maxX) this.p.x = maxX;
        if (this.p.y < 0) this.p.y = 0;
        if (this.p.y > winH - 100) this.p.y = winH - 100;
        
        this.p.lastMouseX = clientX;
        this.p.lastMouseY = clientY;
        if (Math.abs(dx) > 2) this.direction = dx > 0 ? 1 : -1;
        
        if (!this._dragTick) this._dragTick = 0;
        this._dragTick++;

        if (this._dragTick % 15 === 0) {
            requestAnimationFrame(() => {
                this.wrapper.style.pointerEvents = 'none';
                const elemBelow = document.elementFromPoint(clientX, clientY);
                this.wrapper.style.pointerEvents = '';
                const form = elemBelow?.closest('#post-form, #floating-reply-box');
                const oldTargets = document.querySelectorAll('.mascot-target');
                let changed = false;
                oldTargets.forEach(el => {
                    if (el !== form) { el.classList.remove('mascot-target'); changed = true; }
                });
                if (form && !form.classList.contains('mascot-target')) {
                    form.classList.add('mascot-target');
                    changed = true;
                }
            });
        }
        
        if (this.currentOutfit === 'default' && Math.abs(this.p.vx) > 8 && !this.imgFront.src.includes('swinging')) {
            this.setMood('drag_loop');
        }
    },
    onDragEnd(e) {
        clearTimeout(this.timers.dragEnable);
        this.isTouchWait = false;
        if (!this.isInputDown) return;
        this.isInputDown = false;
        this.wrapper.style.pointerEvents = '';
        this.body.style.pointerEvents = 'auto';
        document.body.style.overscrollBehaviorY = '';
        this.imgFront.classList.remove('anim-shake');
        if (this.timers.hold) clearTimeout(this.timers.hold);
        document.querySelectorAll('.mascot-target').forEach(el => el.classList.remove('mascot-target'));
        if (this.p.isDragging) {
            this.p.isDragging = false;
            this.isLocked = false;
            const now = Date.now();
            if (now - this.lastThrowTime < 1500) {
                this.throwCount++;
            } else {
                this.throwCount = 1;
            }
            this.lastThrowTime = now;
            if (this.throwCount >= 3) {
                this.throwCount = 0;
                if (typeof this.triggerRageMode === 'function') {
                    this.triggerRageMode();
                } else {
                    this.setMood('angry_hard');
                    this.say(t('mascot_angry'), 2000);
                }
                return;
            }
            const clientX = e.type.includes('touch') && e.changedTouches ? e.changedTouches[0].clientX : e.clientX;
            const clientY = e.type.includes('touch') && e.changedTouches ? e.changedTouches[0].clientY : e.clientY;
            this.wrapper.style.pointerEvents = 'none';
            const elemBelow = document.elementFromPoint(clientX, clientY);
            this.wrapper.style.pointerEvents = '';
            const form = elemBelow?.closest('#post-form, #floating-reply-box');
            if (form) {
                this.attachToForm(form);
            } else {
                this.p.vx *= 1.5;
                this.p.vy *= 1.5;
                this.setMood('idle');
                this.state = 'fall';
                this.say(this.getRandomText ? this.getRandomText('falling') : t('mascot_fly_whee'), 800);
            }
        } else {
            this.onClick();
        }
    },
    startBeachSequence() {
        this.resetToDefault();
        this.isLocked = true;
        this.state = 'sequence';
        if (this.timers.reset) clearTimeout(this.timers.reset);
        if (this.timers.walk) clearInterval(this.timers.walk);
        if (this.timers.feed) clearTimeout(this.timers.feed);
        if (this.timers.logic) clearTimeout(this.timers.logic);
        this.say(this.getRandomText('beach_hot'), 1500);
        this.timers.sequence = setTimeout(() => {
            this.spawnParticles('love', 12);
            const mode = Math.floor(Math.random() * 3);
            let sprite = '';
            let phrase = '';
            this.body.classList.remove('mode-ball');
            if (mode === 0) {
                const subVar = Math.random();
                if (subVar < 0.33) {
                    sprite = 'swimsuit-embarassed.webp';
                    phrase = this.getRandomText('beach_shy');
                    this.spawnParticles('anger', 3);
                } else if (subVar < 0.66) {
                    sprite = 'swimsuit-hands-on-hips.webp';
                    phrase = this.getRandomText('beach_bold');
                } else {
                    sprite = 'beach-outfit-usual.webp';
                    phrase = this.getRandomText('beach_norm');
                }
            }
            else if (mode === 1) {
                sprite = 'beach-outfit-on-ball.webp';
                phrase = this.getRandomText('beach_ball');
                this.body.classList.add('mode-ball');
            }
            else if (mode === 2) {
                if (Math.random() > 0.5) {
                    sprite = 'swimsuit-waterpistol-smirking.webp';
                    phrase = this.getRandomText('beach_gun_bold');
                } else {
                    sprite = 'beach-outfit-water-gun.webp';
                    phrase = this.getRandomText('beach_gun');
                }
            }
            this.setImg(sprite, true);
            this.say(phrase, 2500);
            setTimeout(() => {
                this.spawnParticles('love', 6);
                this.body.classList.remove('mode-ball');
                this.isLocked = false;
                this.state = 'idle';
                this.setMood('idle');
                this.say(this.getRandomText('beach_end'), 1500);
            }, 6000);
        }, 1500);
    },
    onClick() {
        if (this.timers.walk) clearInterval(this.timers.walk);
        if (this.state === 'hide' || this.isLocked) return;
        this.p.sx = 1.07;
        this.p.sy = 0.95;
        this.clickCount++;
        if (this.clickCount > 5 && Math.random() < 0.10) {
            this.say(Math.random() > 0.5 ? t('mascot_wanna_play') : t('mascot_brace_yourself'), 2000);
            this.clickCount = 0;
            this.isInputDown = false;
            this.p.isDragging = false;
            this.body.classList.remove('no-transition');
            setTimeout(() => {
                if (typeof window.startSpankGame === 'function') {
                    window.startSpankGame();
                }
            }, 800);
            return;
        }
        if (this.timers.reset) clearTimeout(this.timers.reset);
        this.timers.reset = setTimeout(() => {
            this.clickCount = 0;
            this.state = 'idle';
            this.updateVisuals();
        }, 4000);
        if (this.visualCooldown) return;
        this.visualCooldown = true;
        setTimeout(() => { this.visualCooldown = false; }, 2000);
        const isLovePath = Math.random() > 0.5;
        if (isLovePath) {
            this.spawnParticles('love', Math.min(this.clickCount, 5));
            if (this.clickCount <= 3) {
                this.setMood('cute');
                this.say(this.getRandomText('cute'));
            } else {
                this.setMood('love');
                this.say(this.getRandomText('flirty'));
            }
        } else {
            this.spawnParticles('anger', Math.min(this.clickCount, 5));
            if (this.clickCount <= 2) {
                this.setMood('angry_soft');
                this.say(this.getRandomText('angry_soft'));
            } else if (this.clickCount <= 5) {
                this.setMood('angry_med');
                this.say(this.getRandomText('angry_med'));
            } else {
                this.setMood('angry_hard');
                this.imgFront.classList.add('anim-shake');
                setTimeout(() => this.imgFront.classList.remove('anim-shake'), 500);
                this.say(this.getRandomText('angry_hard'));
            }
        }
    },
    triggerSpecial(img, text) {
        this.isLocked = true;
        this.setImg(img);
        if(text) this.say(text);
        setTimeout(() => { this.isLocked = false; this.state='idle'; this.updateVisuals(); }, 3000);
    },
    startLogicLoop() {
        setInterval(() => {
            if (this.p.isDragging || this.p.y > this.floorOffset + 5 || this.isLocked) return;
            if (this.attachedElement) {
                if (Math.random() > 0.7) this.setMood('cute');
                else this.updateVisuals();
                return;
            }
            if (this.currentOutfit === 'sweater') {
                const r = Math.random();
                if (r > 0.6) {
                    this.setMood('sleep');
                    this.say(this.getRandomText('sweater_tea'), 2000);
                    this.isLocked = true;
                    setTimeout(() => {
                        this.isLocked = false;
                        this.updateVisuals();
                    }, 6000);
                } else if (r > 0.3) {
                    this.setMood('actions');
                    this.say(this.getRandomText('sweater_spin'), 2000);
                    this.isLocked = true;
                    setTimeout(() => {
                        this.isLocked = false;
                        this.updateVisuals();
                    }, 4000);
                } else {
                    this.updateVisuals();
                }
                return;
            }
            if (this.currentOutfit === 'succubus') {
                this.state = 'idle';
                const r = Math.random();
                if (r > 0.7) {
                    this.setMood('actions');
                    this.spawnParticles('anger', 2);
                } else if (r > 0.4) {
                    this.startWalk();
                } else if (r > 0.2) {
                    this.setMood('sleep');
                } else {
                    this.updateVisuals();
                }
                return;
            }
            const rand = Math.random();
            if (rand > 0.99) {
                if (this.currentOutfit === 'default') {
                    this.triggerSecretEvent();
                    return;
                }
            }
            if (rand > 0.85) {
                this.setMood('cute');
                const talkRand = Math.random();
                if (talkRand > 0.6) this.say(this.getContextualPhrase(), 3000);
                else if (talkRand > 0.3) {
                    this.say(this.getRandomText('cute'), 2000);
                }
                setTimeout(() => { if(!this.isLocked && !this.p.isDragging) this.updateVisuals(); }, 4000);
            }
            else if (rand > 0.75) {
                this.setMood('actions');
                this.isLocked = true;
                setTimeout(() => {
                    this.isLocked = false;
                    this.updateVisuals();
                }, 6000);
            }
            else if (rand > 0.60) {
                this.state = 'tracking';
                this.say(this.getRandomText('tracking'), 1500);
                setTimeout(() => {
                    if (this.state === 'tracking') {
                        this.state = 'idle';
                        this.updateVisuals();
                    }
                }, 5000 + Math.random() * 2000);
            }
            else if (rand > 0.43) {
                if (this.timers.walk) clearInterval(this.timers.walk);
                this.state = 'sleep';
                this.setMood('sleep');
                this.isLocked = true;
                setTimeout(() => {
                     if (this.imgFront.src.includes('box')) this.say(this.getRandomText('box_phrase'), 2000);
                     else this.say(this.getRandomText('sleep_phrase'), 2000);
                }, 100);
                setTimeout(() => {
                    if(this.isLocked) {
                        this.isLocked = false;
                        this.updateVisuals();
                        this.say(this.getRandomText('wake_phrase'), 1000);
                    }
                }, 7000);
            }
            else if (rand > 0.40) {
                if (this.timers.walk) clearInterval(this.timers.walk);
                this.state = 'sequence';
                this.isLocked = true;
                this.p.x = 80;
                this.direction = 1;
                if (this.db.hide) this.setImg(this.db.hide[0], true);
                this.say(this.getRandomText('hide'), 1500);
                setTimeout(() => {
                    this.isLocked = false;
                    this.state = 'idle';
                    this.updateVisuals();
                }, 5000);
            }
            else if (rand > 0.38) {
                if (this.db.glitch) this.setImg(this.db.glitch[Math.floor(Math.random()*this.db.glitch.length)], true);
                this.isLocked = true;
                this.spawnParticles('anger', 3);
                this.imgFront.classList.add('anim-shake');
                this.say(this.getRandomText('glitch'), 800);
                setTimeout(() => {
                    this.imgFront.classList.remove('anim-shake');
                    this.isLocked = false;
                    this.updateVisuals();
                    this.say(this.getRandomText('wake_phrase'), 1000);
                }, 1500);
            }
            else if (rand > 0.20) {
                this.startWalk();
            }
            else {
                this.updateVisuals();
            }
        }, 4000);
    },
    startWalk() {
        if (this.isLocked || this.p.isDragging || this.state === 'land' || this.state === 'fall') return;
        this.state = 'walk';
        this.body.classList.add('no-transition');
        const startX = this.p.x;
        let lastX = startX;
        let stuckCounter = 0;
        const isMob = window.innerWidth <= 768;
        const leftSafetyMargin = isMob ? 10 : 50;
        const mascotCenterOffset = isMob ? 140 : 200;
        let minX = -(window.innerWidth - mascotCenterOffset - leftSafetyMargin);
        let maxX = isMob ? 10 : 100;
        let distance = 150 + Math.random() * 200;
        let targetX = startX + (Math.random() > 0.5 ? distance : -distance);
        if (targetX > maxX) targetX = maxX;
        if (targetX < minX) targetX = minX;
        if (Math.abs(targetX - startX) < 50) {
            this.state = 'idle';
            this.updateVisuals();
            return;
        }
        this.walkTarget = targetX;
        this.direction = (targetX > startX) ? 1 : -1;
        let frame = 0;
        if (this.timers.walk) clearInterval(this.timers.walk);
        this.timers.walk = setInterval(() => {
            if (this.state !== 'walk' || this.p.isDragging || this.isLocked) {
                clearInterval(this.timers.walk);
                return;
            }
            if (Math.abs(this.p.x - lastX) < 1) { 
                stuckCounter++;
                if (stuckCounter > 10) { 
                    clearInterval(this.timers.walk);
                    this.state = 'idle';
                    this.setMood('idle');
                    this.updateVisuals();
                    return;
                }
            } else {
                stuckCounter = 0;
            }
            lastX = this.p.x;

            this.setImg(this.db.walk[frame % 4], true);
            frame++;
            const dir = Math.sign(this.walkTarget - this.p.x);
            this.p.vx = dir * 3;
            this.direction = dir;
            this.p.y = this.floorOffset + Math.abs(Math.sin(Date.now() / 100)) * 5;
            const dist = Math.abs(this.walkTarget - this.p.x);
            if (dist < 10) {
                clearInterval(this.timers.walk);
                this.p.vx = 0;
                this.state = 'idle';
                this.setMood('idle');
                this.updateVisuals();
            }
        }, 120);
    },
    updateVisuals() {
        if (this.state === 'idle') {
            this.body.classList.remove('no-transition');
            this.setMood('idle');
            if (!this.noMirror.includes(this.imgBack.src.split('/').pop())) {
                 this.direction = Math.random() > 0.5 ? 1 : -1;
            }
        }
    },
    onMouseMove(e) {
        if (this.state !== 'tracking' || this.p.isDragging || this.isLocked || this.currentOutfit !== 'default') return;
        const now = Date.now();
        if (now - (this._lastMoveTime || 0) < 50) return;
        this._lastMoveTime = now;
        const rect = this.wrapper.getBoundingClientRect();
        const centerX = rect.left + rect.width / 2;
        const dist = Math.hypot(e.clientX - centerX, e.clientY - (rect.top + rect.height/2));
        if (dist > 600) return;
        const eyeLevel = rect.top + 80;
        const xRel = e.clientX - centerX;
        let targetImg = null;
        let targetDir = 1;
        if (e.clientY < eyeLevel) {
            if (xRel < -60) {
                targetImg = this.db.look_up[1];
            } else if (xRel > 60) {
                targetImg = this.db.look_up[2];
            } else {
                targetImg = this.db.look_up[0];
            }
        } else {
            if (xRel < -50) {
                targetImg = 'usual-stand-still-looking-aside(mirrorable).webp';
                targetDir = 1;
            } else if (xRel > 50) {
                targetImg = 'usual-stand-still-looking-aside(mirrorable).webp';
                targetDir = -1;
            } else {
                targetImg = 'stands-looking-in-phone-smiles.webp';
            }
        }
        if (targetImg) {
            const currentSrc = this.imgFront.src.split('/').pop();
            const currentDir = parseFloat(this.imgFront.dataset.dir || 1);
            if (currentSrc !== targetImg || (targetImg.includes('looking-aside') && currentDir !== targetDir)) {
                this.direction = targetDir;
                this.setImg(targetImg);
                if (this.imgBack) this.imgBack.dataset.dir = targetDir;
            }
        }
    },
    onTyping() {
        this.setMood('interested');
    },
    onNewPost() {
        this.setMood('happy');
        this.p.vy = 15;
        this.say(t('mascot_new'));
        setTimeout(() => { if(!this.p.isDragging) { this.state='idle'; this.updateVisuals(); } }, 2000);
    },
    onReport() {
        this.triggerSpecial(this.db.ban[0], t('mascot_ban'));
    },
    fly() {
        this.state = 'fly';
        this.isLocked = true;
        const sprite = (this.db.fly && this.db.fly.length) ? this.db.fly[0] : 'usual-stands-still-smiles.webp';
        this.setImg(sprite, true);
        this.say(t('mascot_up'));
        this.wrapper.style.transition = "transform 1.5s ease-in";
        void this.wrapper.offsetHeight;
        this.wrapper.style.transform = `translate3d(${this.p.x}px, -120vh, 0)`;
        
        setTimeout(() => {
            this.wrapper.style.transition = "";
            
            if (Math.random() > 0.5) {
                this.p.y = window.innerHeight + 200;
                this.p.vy = 0;
                this.state = 'fall';
                this.setMood('idle');
                this.say(t('mascot_fly_whee'), 1000);
                this.isLocked = false;
                this.startPhysicsLoop(); 
            } else {
                this.wrapper.style.display = 'none';
                this.isFrozen = true;
                this.isPhysicsRunning = false;
                this.isLocked = false;
            }
        }, 1500);
    },
    say(text, time=2000) {
        if (!this.bubble) return;
        if (this._bubbleTimer) {
            clearTimeout(this._bubbleTimer);
            this._bubbleTimer = null;
        }
        this.bubble.textContent = text;
        this.bubble.classList.remove('show');
        void this.bubble.offsetWidth;
        this.bubble.classList.add('show');
        this._bubbleTimer = setTimeout(() => {
            this.bubble.classList.remove('show');
            this._bubbleTimer = null;
        }, time);
    },
    playSequence(frames) {
        if (!frames || frames.length === 0) {
            this.isLocked = false;
            return;
        }
        this.isLocked = true;
        this.state = 'sequence';
        frames.forEach(src => {
            const img = new Image();
            img.src = this.basePath + src;
        });
        let i = 0;
        const nextFrame = () => {
            if (i >= frames.length) {
                setTimeout(() => {
                    this.isLocked = false;
                    this.state = 'idle';
                    this.updateVisuals();
                }, 2000);
                return;
            }
            this.setImg(frames[i], true);
            i++;
            setTimeout(nextFrame, 800);
        };
        nextFrame();
    },
    spawnParticles(type = 'love', count = 3) {
        if (!this.wrapper || this.wrapper.style.display === 'none') return;
        const dict = {
            love:   { chars: ['❤', '💖', '💕', '😻'], color: '#ff1493' },
            anger:  { chars: ['💢', '⚡', '🗯️', '😤'], color: '#ff4500' },
            question: { chars: ['?', '¿', '??'], color: '#1e90ff' },
            music:  { chars: ['🎵', '🎶', '🎼'], color: '#32cd32' }
        };
        const config = dict[type] || dict.love;
        const rect = this.wrapper.getBoundingClientRect();
        const centerX = rect.left + rect.width / 2;
        const startY = rect.top + rect.height / 3;
        for (let i = 0; i < count; i++) {
            const p = document.createElement('div');
            p.className = 'mascot-particle';
            p.textContent = config.chars[Math.floor(Math.random() * config.chars.length)];
            p.style.color = config.color;
            p.style.position = 'fixed';
            p.style.left = centerX + 'px';
            p.style.top = startY + 'px';
            p.style.zIndex = '20002';
            p.style.pointerEvents = 'none';
            const tx = (Math.random() - 0.5) * 150 + 'px';
            const ty = -(50 + Math.random() * 100) + 'px';
            p.style.setProperty('--tx', tx);
            p.style.setProperty('--ty', ty);
            document.body.appendChild(p);
            setTimeout(() => p.remove(), 1000);
        }
    },
    attachToForm(form) {
        this.attachedElement = form;
        this.body.classList.add('in-form');
        this.say(t('mascot_sit'));
        this.setMood('idle');
        this.p.vx = 0;
        this.p.vy = 0;
        this.throwCount = 0; 
        this.initFormObservers();
    },
    triggerSecretEvent() {
        this.resetToDefault();
        const rand = Math.random();
        if (rand > 0.75) {
            this.startBeachSequence();
        }
        else if (rand > 0.50) {
            if (Math.random() > 0.5) {
                this.activateOutfitMode('sweater');
            } else {
                this.activateOutfitMode('succubus');
            }
        }
        else if (rand > 0.25) {
            this.isLocked = true;
            if (this.timers.walk) clearInterval(this.timers.walk);
            if (this.timers.logic) clearTimeout(this.timers.logic);
            if (Math.random() > 0.5) {
                this.setImg('showing-off-panties-2.webp', true);
                this.spawnParticles('love', 5);
                this.say(this.getRandomText('panty_bold'), 2000);
            } else {
                this.setImg('showing-panties-embarassed.webp', true);
                this.spawnParticles('anger', 5);
                this.say(this.getRandomText('panty_shy'), 2000);
            }
            setTimeout(() => {
                this.isLocked = false;
                this.state = 'idle';
                this.setMood('idle');
            }, 3000);
        }
        else {
            this.isLocked = true;
            if (this.timers.walk) clearInterval(this.timers.walk);
            const r = Math.random();
            let set;
            let comment;
            if (r < 0.33) {
                set = this.db.undress_pants;
                comment = this.getRandomText('undress_pants');
            } else if (r < 0.66) {
                set = this.db.undress_nopants;
                comment = this.getRandomText('undress_panties');
            } else {
                set = this.db.undress_hoodie;
                comment = this.getRandomText('undress_hoodie');
            }
            this.say(comment, 1000);
            this.playSequence(set);
        }
    },
    resetToDefault() {
        if (!this.body || !this.imgFront) return;
        this.currentOutfit = 'default';
        this.isLocked = false;
        this.state = 'idle';
        this.observersInited = false;
        Object.keys(this.timers).forEach(key => {
            if (this.timers[key]) {
                clearTimeout(this.timers[key]);
                clearInterval(this.timers[key]);
                this.timers[key] = null;
            }
        });
        this.startLogicLoop();
        this.body.classList.remove('mode-ball');
        this.imgFront.classList.remove('anim-shake');
        this.setMood('idle');
    },
    detachFromForm(msg) {
        this.attachedElement = null;
        this.cachedRect = null;
        this.body.classList.remove('in-form');
        if (msg) this.say(msg);
    },
    initFormObservers() {
        if (this.observersInited) return;
        this.observersInited = true;
        const handleFocus = (isFocus) => {
            if (window.innerWidth <= 768) {
                this.wrapper.style.opacity = isFocus ? '0' : '1';
                this.wrapper.style.pointerEvents = isFocus ? 'none' : (this.state === 'idle' ? 'none' : 'auto'); 
            }
        };
        const inputs = document.querySelectorAll('textarea, input[type="text"]');
        inputs.forEach(input => {
            input.addEventListener('focus', () => handleFocus(true));
            input.addEventListener('blur', () => handleFocus(false));
            input.addEventListener('input', () => {
                if (this.attachedElement && this.state === 'idle' && !this.isLocked) {
                    if (Math.random() > 0.8) {
                        this.setImg('stands-looking-with-interest-eyebrows-differs.webp');
                    }
                }
            });
        });
        document.addEventListener('change', (e) => {
            if (e.target.type === 'file' && this.attachedElement) {
                this.setImg('stands-taking-a-selfie.webp');
                this.say(t('mascot_pics'), 1500);
                this.isLocked = true;
                setTimeout(() => {
                    this.isLocked = false;
                    this.setMood('idle');
                }, 2000);
            }
        });
    },
};
const CaptchaAudio = {
    sounds: {},
    init() {
        if (this.loaded) return;
        const load = (name, vol=0.5) => {
            const a = new Audio(`/static/sounds/${name}.wav`);
            a.volume = vol;
            this.sounds[name] = a;
        };
        load('tick', 0.2);
        load('scrape', 0.15);
        load('pop', 0.4);
        load('click', 0.1);
        load('splat', 0.3);
        load('printer', 0.4);
        load('stamp', 0.6);
        this.loaded = true;
    },
    play(name, variance = false) {
        if (!this.loaded) this.init();
        const src = this.sounds[name];
        if (!src) return;
        const sound = src.cloneNode();
        sound.volume = src.volume;
        if (variance) sound.playbackRate = 0.8 + Math.random() * 0.4;
        sound.play().catch(() => {});
    }
};
const CaptchaManager = {
    isEnabled: false,
    activeForm: null,
    isReloading: false,
    dragState: { active: false, el: null, startX: 0, startY: 0, initialX: 0, initialY: 0 },
    async init(form) {
        let globalEnabled = false;
        if (this.checkedStatus === undefined) {
            try {
                const res = await fetch('/api/captcha/status');
                const d = await res.json();
                globalEnabled = d.enabled;
                this.checkedStatus = true;
            } catch(e) { globalEnabled = false; }
        }
        const localEnabled = SettingsManager.current.selfCaptcha;
        this.isEnabled = globalEnabled || localEnabled;
        this.fallbackForm = form; 
        if (!this.isEnabled) return;
        if (document.body.dataset.userId && !localEnabled) return;
        this.toggleSubmitBtn(form, false);
        if (!document.getElementById('captcha-container')) {
            this.render(form);
        }
    },
    toggleSubmitBtn(form, enabled) {
        const formsToUpdate = form ? [form] : document.querySelectorAll('form');
        formsToUpdate.forEach(f => {
            const btn = f.querySelector('button[type="submit"]');
            if (btn) {
                if (enabled) {
                    btn.disabled = false;
                    btn.classList.remove('captcha-locked');
                    btn.onclick = null;
                } else {
                    btn.disabled = true;
                    btn.classList.add('captcha-locked');
                    btn.onclick = (e) => {
                        if (btn.disabled) {
                            e.preventDefault();
                            showToast(t('captcha_locked'));
                            const c = document.getElementById('captcha-container');
                            if(c) {
                                c.classList.add('form-error-shake');
                                setTimeout(() => c.classList.remove('form-error-shake'), 500);
                            }
                        }
                    };
                }
            }
        });
    },
    moveTo(form) {
        if (!this.isEnabled) return;
        let container = document.getElementById('captcha-container');
        if (!container) {
            this.render(form);
            return;
        }
        if (!container.classList.contains('captcha-container')) {
            container.classList.add('captcha-container');
        }
        container.removeAttribute('style');
        if (!form) return;
        if (container.closest('form') === form) return;
        const controls = form.querySelector('.form-controls');
        const submitBtn = form.querySelector('button[type="submit"]');
        if (controls) controls.before(container);
        else if (submitBtn) submitBtn.before(container);
        else form.appendChild(container);
        this.activeForm = form;
        const isHidden = container.classList.contains('captcha-hidden');
        this.toggleSubmitBtn(form, isHidden);
    },
    async render(form) {
        this.activeForm = form;
        let container = document.getElementById('captcha-container');
        if (!container) {
            container = document.createElement('div');
            container.id = 'captcha-container';
            container.innerHTML = `
                <div id="c-view-area" style="width:100%; height:100%; position:relative;"></div>
                <input type="hidden" name="captcha_id" id="c-id">
                <input type="hidden" name="captcha_x" id="c-x">
                <input type="hidden" name="captcha_y" id="c-y">
                <input type="hidden" name="captcha_value" id="c-val">
                <button type="button" id="c-reload-btn" class="captcha-reload-btn" onclick="CaptchaManager.reset()" title="${t('captcha_reload')}">↻</button>
            `;
            const controls = form.querySelector('.form-controls');
            if (controls) {
                controls.before(container);
            } else {
                const submitBtn = form.querySelector('button[type="submit"]');
                if (submitBtn) submitBtn.before(container);
                else form.appendChild(container);
            }
        }
        await this.loadChallenge();
        container.style.display = 'block';
    },
    async reset() {
        if (this.isReloading) return;
        let container = document.getElementById('captcha-container');
        if (!container && this.fallbackForm) {
            console.log("Captcha forced by server response. Rendering...");
            this.isEnabled = true;
            this.render(this.fallbackForm);
            return;
        }

        if (!this.isEnabled) return;
        this.isReloading = true;
        const btn = document.getElementById('c-reload-btn');
        if(btn) btn.classList.add('rotating');
        await this.loadChallenge();
        
        setTimeout(() => {
            this.isReloading = false;
            if(btn) btn.classList.remove('rotating');
        }, 1000);
    },
    async loadChallenge() {
        CaptchaAudio.init();
        const view = document.getElementById('c-view-area');
        let container = document.getElementById('captcha-container');

        if (!container) {
            console.error("Captcha container missing!");
            return;
        }

        if (this.protocolTimer) {
            clearInterval(this.protocolTimer);
            this.protocolTimer = null;
        }

        container.className = 'captcha-container';
        container.removeAttribute('style');
        view.style.opacity = '';
        view.style.pointerEvents = '';
        view.style.background = '';

        view.innerHTML = `<div style="display:flex;justify-content:center;align-items:center;height:100%;color:var(--text-secondary);">${t('loading')}</div>`;
        
        try {
            const res = await fetch('/api/captcha/generate');
            if (!res.ok) throw new Error('Network');

            const data = await res.json();
            document.getElementById('c-id').value = data.captcha_id;
            ['c-x', 'c-y', 'c-val'].forEach(id => {
                const el = document.getElementById(id);
                if (el) el.value = '';
            });

            this.toggleSubmitBtn(this.activeForm, false);

            const renderers = {
                'monkey': this.renderMonkey,
                'bottle': this.renderBottle,
                'math': this.renderMath,
                'word': this.renderWord,
                'escobar': this.renderEscobar,
                'fan': this.renderFan,
                'find_odd': this.renderFindOdd,
                'spank': this.renderSpank,
                'humiliation': this.renderHumiliation,
                'clock': this.renderClock,
                'can': this.renderCan,
                'major': this.renderMajor,
                'protocol': this.renderProtocol
            };

            if (renderers[data.mode]) {
                renderers[data.mode].call(this, view, data);
            }
        } catch (e) {
            view.innerHTML = `<div style="color:red;text-align:center;padding-top:60px;cursor:pointer;" onclick="CaptchaManager.reset()">Error. Retry ↻</div>`;
            this.isReloading = false;
            const reloadBtn = document.getElementById('c-reload-btn');
            if (reloadBtn) reloadBtn.classList.remove('rotating');
        }
    },
    solve() {
        const container = document.getElementById('captcha-container');
        const currentForm = container ? container.closest('form') : this.activeForm;
        this.toggleSubmitBtn(currentForm, true);
        const floatForm = document.getElementById('floating-reply-form');
        if (floatForm && currentForm !== floatForm) this.toggleSubmitBtn(floatForm, true);
        const view = document.getElementById('c-view-area');
        if(view) {
            view.style.opacity = '0.5';
            view.style.pointerEvents = 'none';
        }
        showToast(t('captcha_passed'));
        setTimeout(() => {
            if (container) container.classList.add('captcha-hidden');
        }, 2000);
    },
    renderMonkey(view, data) {
        document.getElementById('captcha-container').classList.remove('square-mode');
        view.innerHTML = `
            <img src="/static/img/captcha/log.webp" class="captcha-log" style="left:${data.log_x}px; top:${data.log_y}px;">
            <img src="/static/img/captcha/monkey.webp" id="c-monkey" class="captcha-monkey" style="left:${data.monkey_x}px; top:${data.monkey_y}px;">
            <div class="captcha-hint">${t('captcha_hint_monkey')}</div>
        `;
        const el = document.getElementById('c-monkey');
        el.ondragstart = () => false;
        this.makeDraggable(el, (el) => {
            const rect = el.getBoundingClientRect();
            const parent = el.offsetParent.getBoundingClientRect();
            const monkeyCX = (rect.left + rect.width/2) - parent.left;
            const monkeyCY = (rect.top + rect.height/2) - parent.top;
            const visualTargetY = data.log_y + 27;
            const dist = Math.hypot(monkeyCX - data.log_x, monkeyCY - visualTargetY);
            if (dist < 15) {
                el.style.transition = 'top 0.2s ease-out, left 0.2s ease-out';
                el.style.left = data.log_x + 'px';
                el.style.top = visualTargetY + 'px';
                setTimeout(() => el.style.transition = '', 200);
                CaptchaAudio.play('pop');
                if (navigator.vibrate) navigator.vibrate(20);
                document.getElementById('c-x').value = Math.round(data.log_x);
                document.getElementById('c-y').value = Math.round(data.log_y);
                this.solve();
            } else {
                this.shakeAndReset(el, view);
            }
        });
    },
    shakeAndReset(el, container) {
        showToast(t('captcha_fail_monkey'));
        
        el.classList.add('error-shake');
        setTimeout(() => {
            el.classList.remove('error-shake');
            
            const w = container.offsetWidth;
            const h = container.offsetHeight;
            const randX = Math.random() * (w - 60) + 30;
            const randY = Math.random() * (h - 60) + 30;
            el.style.transition = 'all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275)';
            el.style.left = randX + 'px';
            el.style.top = randY + 'px';
            el.style.transform = 'translate(-50%, -50%) rotate(' + (Math.random()*60-30) + 'deg)'; 
            setTimeout(() => {
                el.style.transition = ''; 
                el.style.transform = 'translate(-50%, -50%)';
            }, 400);
        }, 300);
    },
    renderBottle(view, data) {
        document.getElementById('captcha-container').classList.remove('square-mode');
        view.innerHTML = `
            <div class="captcha-bottle-wrap">
                <!-- Сюрприз сверху -->
                <img src="/static/img/captcha/monkey.webp" id="c-sitter" class="bottle-sitter">
                <img src="/static/img/captcha/bottle.webp" id="c-bottle" class="captcha-bottle" style="transform: rotate(${data.start_angle}deg);">
                <input type="range" class="captcha-slider" min="-180" max="180" value="0" onchange="CaptchaManager.onBottleRotate(this, ${data.start_angle})">
                <div class="captcha-hint">${t('captcha_hint_bottle')}</div>
            </div>
        `;
        const slider = view.querySelector('.captcha-slider');
        const bottle = view.querySelector('#c-bottle');
        slider.oninput = () => {
            bottle.style.transform = `rotate(${data.start_angle + parseInt(slider.value)}deg)`;
            const val = parseInt(slider.value);
            if (Math.abs(val - (slider.lastSoundVal || 0)) > 10) {
                CaptchaAudio.play('click', true);
                if (navigator.vibrate) navigator.vibrate(5);
                slider.lastSoundVal = val;
            }
        };
    },
    onBottleRotate(slider, start) {
        const val = parseInt(slider.value);
        document.getElementById('c-val').value = val;
        const final = (start + val) % 360;
        const normalized = (final + 360) % 360;
        if (normalized <= 10 || normalized >= 350) {
            const sitter = document.getElementById('c-sitter');
            if(sitter) sitter.classList.add('sit');
            this.solve();
        } else {
            showToast(t('captcha_fail_bottle'));
        }
    },
    renderWord(view, data) {
        document.getElementById('captcha-container').classList.remove('square-mode');
        const slots = '<div class="captcha-slot" id="slot-0"></div><div class="captcha-slot" id="slot-1"></div><div class="captcha-slot" id="slot-2"></div>';
        view.innerHTML = `
            <div class="captcha-word-container">
                <div class="captcha-slots">${slots}</div>
                <div id="letters-pool" style="height:50px; position:relative; width:100%;"></div>
                <div class="captcha-hint">${t('captcha_hint_word')}</div>
            </div>
        `;
        const pool = view.querySelector('#letters-pool');
        data.chars.forEach((char, i) => {
            const el = document.createElement('div');
            el.className = 'captcha-letter';
            el.textContent = char;
            el.style.left = (40 + i * 80) + 'px';
            el.dataset.char = char;
            el.ondragstart = () => false;
            pool.appendChild(el);
            this.makeDraggable(el, () => this.checkWord());
        });
    },
    checkWord() {
        const letters = Array.from(document.querySelectorAll('.captcha-letter'));
        const slotContainer = document.querySelector('.captcha-slots');
        const zone = slotContainer.getBoundingClientRect();
        const activeLetters = letters.filter(el => {
            const r = el.getBoundingClientRect();
            const cx = r.left + r.width / 2;
            const cy = r.top + r.height / 2;
            return cx > zone.left && cx < zone.right && cy > zone.top && cy < zone.bottom;
        });
        if (activeLetters.length !== 3) return;
        activeLetters.sort((a, b) => {
            return a.getBoundingClientRect().left - b.getBoundingClientRect().left;
        });
        const finalWord = activeLetters.map(el => el.dataset.char).join('');
        document.getElementById('c-val').value = finalWord;
        this.solve();
    },
    renderFan(view, data) {
        document.getElementById('captcha-container').classList.remove('square-mode');
        view.innerHTML = `
            <img src="/static/img/captcha/fan.webp" class="captcha-fan" id="c-fan-target">
            <img src="/static/img/captcha/shit.webp" id="c-shit" class="captcha-shit">
            <div class="captcha-hint">${t('captcha_hint_fan')}</div>
        `;
        const shit = document.getElementById('c-shit');
        shit.ondragstart = () => false;
        this.makeDraggable(shit, (el) => {
            const shitRect = el.getBoundingClientRect();
            const fanRect = document.getElementById('c-fan-target').getBoundingClientRect();
            const dx = (shitRect.left + shitRect.width/2) - (fanRect.left + fanRect.width/2);
            const dy = (shitRect.top + shitRect.height/2) - (fanRect.top + fanRect.height/2);
            const dist = Math.hypot(dx, dy);
            if (dist < 40) {
                view.style.background = "repeating-linear-gradient(45deg, #5d4037, #5d4037 10px, #3e2723 10px, #3e2723 20px)";
                CaptchaAudio.play('splat');
                if (navigator.vibrate) navigator.vibrate([30, 30, 30]);
                el.style.display = 'none';
                document.getElementById('c-val').value = "success";
                this.solve();
            } else {
                 el.style.transition = 'all 0.2s';
                 el.style.left = '';
                 el.style.top = '';
                 setTimeout(() => el.style.transition = '', 200);
            }
        });
    },
    renderEscobar(view, data) {
        view.innerHTML = `
            <div class="captcha-chairs-wrap" style="position:relative; width:100%; height:100%; display:flex; align-items:center; justify-content:space-between; padding:0 20px; overflow:hidden; touch-action: none;">
                <!-- Стул с пиками -->
                <div id="c-chair-spikes" class="c-item c-chair" style="width:60px; cursor:grab; touch-action: none;">
                    <img src="/static/img/captcha/chair_wrong.webp" style="width:100%; pointer-events:none;">
                </div>
                <!-- Человечек -->
                <div id="c-man" class="c-item c-man" style="width:40px; cursor:grab; margin-top:20px; touch-action: none;">
                    <img src="/static/img/captcha/monkey.webp" style="width:100%; pointer-events:none;">
                </div>
                <!-- Стул с хуями -->
                <div id="c-chair-dicks" class="c-item c-chair" style="width:60px; cursor:grab; touch-action: none;">
                    <img src="/static/img/captcha/chair_dick.webp" style="width:100%; pointer-events:none;">
                </div>
                <div class="captcha-hint" style="top:10px; bottom:auto;">${t('captcha_hint_escobar')}</div>
            </div>
        `;
        const items = view.querySelectorAll('.c-item');
        const man = view.querySelector('#c-man');
        items.forEach(item => {
            let startX, startY, initialLeft, initialTop;
            
            const onMove = (e) => {
                e.preventDefault(); 
                const clientX = e.touches ? e.touches[0].clientX : e.clientX;
                const clientY = e.touches ? e.touches[0].clientY : e.clientY;
                const dx = clientX - startX;
                const dy = clientY - startY;
                item.style.transform = `translate(${dx}px, ${dy}px)`;
            };
            const onEnd = (e) => {
                
                window.removeEventListener('mousemove', onMove);
                window.removeEventListener('mouseup', onEnd);
                window.removeEventListener('touchmove', onMove);
                window.removeEventListener('touchend', onEnd);
                item.style.cursor = 'grab';
                item.style.zIndex = '';
                const rect = item.getBoundingClientRect();
                const others = Array.from(items).filter(i => i !== item);
                let hit = null;
                others.forEach(other => {
                    const r2 = other.getBoundingClientRect();
                    if (!(rect.right < r2.left || rect.left > r2.right || rect.bottom < r2.top || rect.top > r2.bottom)) {
                        hit = other;
                    }
                });
                if (hit) {
                    const isChair1 = item.classList.contains('c-chair');
                    const isChair2 = hit.classList.contains('c-chair');
                    
                    if (isChair1 && isChair2) {
                        this.finishEscobar(view, true);
                        return;
                    }
                    
                    if (item === man || hit === man) {
                        this.failEscobar(man, view);
                        return;
                    }
                }
                
                item.style.transition = 'transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275)';
                item.style.transform = 'translate(0, 0)';
            };
            const onStart = (e) => {
                if (e.type === 'mousedown' && e.button !== 0) return;
                
                item.style.zIndex = 100;
                item.style.transition = 'none';
                item.style.cursor = 'grabbing';
                const clientX = e.touches ? e.touches[0].clientX : e.clientX;
                const clientY = e.touches ? e.touches[0].clientY : e.clientY;
                startX = clientX;
                startY = clientY;
                
                window.addEventListener('mousemove', onMove, {passive: false});
                window.addEventListener('mouseup', onEnd);
                window.addEventListener('touchmove', onMove, {passive: false});
                window.addEventListener('touchend', onEnd);
            };
            item.addEventListener('mousedown', onStart);
            item.addEventListener('touchstart', onStart, {passive: false});
        });
    },
    failEscobar(man, view) {
        man.style.transition = 'transform 0.1s';
        man.style.transform += ' scale(0) rotate(360deg)';
        view.style.background = '#500';
        showToast(t('captcha_fail_escobar'));
        setTimeout(() => {
            view.style.background = '';
            man.style.transition = 'transform 0.3s';
            man.style.transform = 'translate(0, 0) scale(1)';
            view.querySelectorAll('.c-item').forEach(el => el.style.transform = 'translate(0,0)');
        }, 800);
    },
    finishEscobar(view, success) {
        const chairs = view.querySelectorAll('.c-chair');
        chairs.forEach(c => {
            c.style.transition = 'all 0.3s';
            c.style.opacity = '0';
            c.style.transform += ' scale(2)';
        });
        const boom = document.createElement('div');
        boom.innerHTML = "💥";
        boom.style.cssText = "position:absolute; top:50%; left:50%; transform:translate(-50%, -50%); font-size:60px;";
        view.appendChild(boom);
        document.getElementById('c-val').value = "success";
        this.solve();
    },
    renderFindOdd(view, data) {
        const c = document.getElementById('captcha-container');
        c.classList.add('square-mode');
        let html = '<div class="captcha-grid">';
        let girlCounter = 1;
        for(let i=0; i<9; i++) {
            let imgName;
            if (i === data.trap_index) {
                imgName = 'trap.webp';
            } else {
                imgName = `girl_${girlCounter}.webp`;
                girlCounter = (girlCounter % 8) + 1;
            }
            html += `<div class="captcha-grid-item" onclick="CaptchaManager.onOdd(${i}, ${data.trap_index}, this)">
                        <img src="/static/img/captcha/${imgName}" ondragstart="return false">
                     </div>`;
        }
        html += `</div>`;
        view.innerHTML = html;
        const hint = document.createElement('div');
        hint.className = 'captcha-hint';
        hint.textContent = t('captcha_hint_odd');
        view.appendChild(hint);
    },
    onOdd(idx, correctIdx, el) {
        if (idx === correctIdx) {
            document.getElementById('c-val').value = idx;
            el.style.border = '2px solid var(--accent-primary)';
            this.solve();
        } else {
            showToast(t('captcha_fail_odd'));
            el.classList.add('error');
            setTimeout(() => el.classList.remove('error'), 500);
        }
    },
    renderHumiliation(view, data) {
        document.getElementById('captcha-container').classList.remove('square-mode');
        view.innerHTML = `
            <div class="captcha-humiliation-container">
                <div style="font-size:0.9em; margin-bottom:5px;">${t('captcha_hum_label')}</div>
                <div class="captcha-humiliation-text">${data.phrase}</div>
                <input type="text" class="captcha-humiliation-input" placeholder="${t('captcha_hum_placeholder')}" oninput="CaptchaManager.onHumiliationInput(this, '${data.phrase}')">
                <div class="captcha-hint">${t('captcha_hum_hint')}</div>
            </div>
        `;
    },
    renderClock(view, data) {
        document.getElementById('captcha-container').classList.remove('square-mode');
        view.innerHTML = `
           <div class="captcha-clock-container">
                <div class="clock-task-text">${t('captcha_clock_label')} <span style="color:#d00">${data.display_time}</span></div>
                <div id="clock-face">
                    <div class="clock-center-dot"></div>
                    <div id="hand-hour" class="clock-hand hand-h"></div>
                    <div id="hand-min" class="clock-hand hand-m"></div>
                    <div id="clock-marks"></div>
                </div>
                <div class="captcha-hint">${t('captcha_clock_hint')}</div>
            </div>
        `;
        const marks = document.getElementById('clock-marks');
        for(let i=0; i<12; i++) {
            const el = document.createElement('div');
            el.className = 'clock-mark';
            el.style.transform = `translateX(-50%) rotate(${i * 30}deg)`;
            marks.appendChild(el);
        }
        const face = document.getElementById('clock-face');
        face.dataset.targetH = data.target_h;
        face.dataset.targetM = data.target_m;
        this.debouncedCheck = debounce(() => {
            this.checkClockSolution();
        }, 1800);

        this.initHandDrag('hand-hour');
        this.initHandDrag('hand-min');
    },
    checkClockSolution() {
        const face = document.getElementById('clock-face');
        const hEl = document.getElementById('hand-hour');
        const mEl = document.getElementById('hand-min');
        if (!face || !hEl || !mEl) return;
        let targetH = parseFloat(face.dataset.targetH);
        let targetM = parseFloat(face.dataset.targetM);
        if (targetH < 24) targetH = (targetH % 12) * 30;
        if (targetM < 60) targetM = targetM * 6;

        const currentH = parseFloat(hEl.dataset.angle || 0);
        const currentM = parseFloat(mEl.dataset.angle || 0);
        const angleDiff = (a, b) => {
            const diff = Math.abs(a - b) % 360;
            return Math.min(diff, 360 - diff);
        };
        if (angleDiff(currentH, targetH) <= 15 && angleDiff(currentM, targetM) <= 15) {
            face.style.boxShadow = '0 0 20px 5px #2ecc71';
            face.style.borderColor = '#2ecc71';
            this.solve();
        } else {
            console.log("Clock captcha: wrong time");
        }
    },
    initHandDrag(id) {
        const hand = document.getElementById(id);
        const face = document.getElementById('clock-face');
        
        const update = (e) => {
            e.preventDefault();
            const rect = face.getBoundingClientRect();
            const cx = rect.left + rect.width / 2;
            const cy = rect.top + rect.height / 2;
            
            const clientX = e.touches ? e.touches[0].clientX : e.clientX;
            const clientY = e.touches ? e.touches[0].clientY : e.clientY;
            
            const dx = clientX - cx;
            const dy = clientY - cy;
            
            let deg = (Math.atan2(dy, dx) * 180 / Math.PI) + 90;
            if (deg < 0) deg += 360;
            
            const isHour = id === 'hand-hour';
            const step = isHour ? 30 : 6;
            
            deg = Math.round(deg / step) * step;
            if (deg >= 360) deg = 0;

            hand.style.transform = `rotate(${deg}deg)`;
            hand.dataset.angle = deg;
            
            this.updateClockValue();
        };
        
        const start = (e) => {
            e.preventDefault();
            e.stopPropagation();
            document.addEventListener('mousemove', update);
            document.addEventListener('touchmove', update, {passive: false});
            document.addEventListener('mouseup', end);
            document.addEventListener('touchend', end);
            update(e); 
        };
        
        const end = () => {
            document.removeEventListener('mousemove', update);
            document.removeEventListener('touchmove', update);
            document.removeEventListener('mouseup', end);
            document.removeEventListener('touchend', end);
            if (CaptchaManager.debouncedCheck) {
                CaptchaManager.debouncedCheck();
            }
        };
        
        hand.style.cursor = 'grab';
        hand.onmousedown = start;
        hand.ontouchstart = start;
    },
    updateClockValue() {
        const hEl = document.getElementById('hand-hour');
        const mEl = document.getElementById('hand-min');
        if (!hEl || !mEl) return;
        let hAng = parseFloat(hEl.dataset.angle || 0);
        let mAng = parseFloat(mEl.dataset.angle || 0);
        document.getElementById('c-val').value = `${hAng}:${mAng}`;
    },
    renderCan(view, data) {
        document.getElementById('captcha-container').classList.remove('square-mode');
        view.innerHTML = `
            <div class="captcha-can-container">
                <canvas id="can-canvas" width="200" height="200"></canvas>
                <div class="captcha-hint">${t('captcha_can_hint')}</div>
            </div>
        `;
        const canvas = document.getElementById('can-canvas');
        const ctx = canvas.getContext('2d');
        const cx = 100, cy = 100, r = 80;
        const drawClosed = () => {
            ctx.beginPath(); ctx.arc(cx, cy, r+6, 0, Math.PI*2);
            ctx.fillStyle = '#444'; ctx.fill();
            ctx.beginPath(); ctx.arc(cx, cy, r, 0, Math.PI*2);
            const g = ctx.createLinearGradient(0,0,200,200);
            g.addColorStop(0, '#ddd'); g.addColorStop(1, '#888');
            ctx.fillStyle = g; ctx.fill();
            ctx.fillStyle = 'rgba(0,0,0,0.3)';
            ctx.font = 'bold 24px monospace'; ctx.textAlign = 'center';
            ctx.fillText(t('captcha_can_label', "ТУШЕНКА"), cx, cy+8);
        };
        drawClosed();
        let openedSectors = new Set();
        let isCutting = false;
        const cut = (e) => {
            if (!isCutting) return;
            e.preventDefault();
            const rect = canvas.getBoundingClientRect();
            const clientX = e.touches ? e.touches[0].clientX : e.clientX;
            const clientY = e.touches ? e.touches[0].clientY : e.clientY;
            const dx = clientX - rect.left - cx;
            const dy = clientY - rect.top - cy;
            const dist = Math.sqrt(dx*dx + dy*dy);
            if (dist > r - 15 && dist < r + 15) {
                ctx.globalCompositeOperation = 'destination-out';
                ctx.beginPath(); ctx.arc(dx+cx, dy+cy, 8, 0, Math.PI*2); ctx.fill();
                let ang = Math.atan2(dy, dx) * 180 / Math.PI;
                if (ang < 0) ang += 360;
                if (Math.random() > 0.85) {
                    CaptchaAudio.play('scrape', true); 
                    if (navigator.vibrate) navigator.vibrate(10); 
                }
                openedSectors.add(Math.floor(ang / 10));
                if (openedSectors.size > 28) this.finishCan(ctx, cx, cy, r);
            }
        };
        const start = (e) => { isCutting=true; cut(e); };
        const end = () => { isCutting=false; };
        canvas.addEventListener('mousedown', start);
        canvas.addEventListener('mousemove', cut);
        window.addEventListener('mouseup', end);
        canvas.addEventListener('touchstart', start, {passive: false});
        canvas.addEventListener('touchmove', cut, {passive: false});
        window.addEventListener('touchend', end);
    },
    finishCan(ctx, cx, cy, r) {
        const old = document.getElementById('can-canvas');
        const n = old.cloneNode(true);
        old.parentNode.replaceChild(n, old);
        const nCtx = n.getContext('2d');
        nCtx.beginPath(); nCtx.arc(cx, cy, r, 0, Math.PI*2);
        nCtx.fillStyle = '#8b0000'; nCtx.fill(); 
        nCtx.beginPath(); nCtx.arc(cx-20, cy-20, 25, 0, Math.PI*2);
        nCtx.fillStyle = '#ffffe0'; nCtx.fill();
        nCtx.fillStyle = '#fff'; nCtx.font = 'bold 30px Arial';
        nCtx.textAlign = 'center'; nCtx.fillText(t('captcha_can_tasty', "ВКУСНО"), cx, cy+10);
        document.getElementById('c-val').value = "opened";
        this.solve();
    },
    renderMajor(view, data) {
        const c = document.getElementById('captcha-container');
        c.classList.add('major-mode');
        c.style.height = 'auto'; 
        const offensesRaw = t('captcha_major_offenses', 'Extremism,Terrorism,Espionage');
        const offenses = offensesRaw.split(',');

        let offensesHtml = offenses.map((offense, i) => `
            <label class="major-checkbox-label">
                <input type="checkbox" name="offense" value="${i}" onchange="CaptchaManager.onMajorCheck()"> ${offense.trim()}
            </label>
        `).join('');

        view.innerHTML = `
            <div class="major-form">
                <div class="major-header">
                    <img src="/static/img/captcha/fsb_logo.webp" class="major-logo">
                    <div class="major-header-text">
                        ФЕДЕРАЛЬНАЯ СЛУЖБА БЕЗОПАСНОСТИ<br>
                        <span style="font-size: 0.9em; font-weight: normal;">Электронная Приёмная</span>
                    </div>
                </div>
                <h3>${t('captcha_major_title')}</h3>
                <div class="major-field">
                    <label>${t('captcha_major_from')}</label>
                    <input type="text" readonly value="${t('captcha_major_val_ip', 'Пользователь IP: ' + data.user_id).replace('{0}', data.user_id)}">
                </div>
                <div class="major-field">
                    <label>${t('captcha_major_target')}</label>
                    <input type="text" readonly value="${t('captcha_major_val_post', 'Автор публикации #' + data.post_num).replace('{0}', data.post_num)}">
                </div>
                <div class="major-field">
                    <label>${t('captcha_major_offense')}</label>
                    <div class="major-checkbox-group">${offensesHtml}</div>
                </div>
                <button id="major-submit-btn" class="btn btn-danger" disabled onclick="CaptchaManager.onMajorSubmit()">${t('captcha_major_submit')}</button>
                <div class="major-footer">
                    ${t('captcha_major_footer')}
                </div>
            </div>
        `;
    },

    onMajorCheck() {
        const checks = document.querySelectorAll('.major-checkbox-group input:checked');
        const btn = document.getElementById('major-submit-btn');
        btn.disabled = checks.length === 0;
    },

    onMajorSubmit() {
       const btn = document.getElementById('major-submit-btn');
        const form = document.querySelector('.major-form');
        btn.disabled = true;
        btn.textContent = t('captcha_major_processing', 'ОБРАБОТКА...');
        CaptchaAudio.play('printer');

        setTimeout(() => {
            const stamp = document.createElement('div');
            stamp.className = 'major-stamp';
            stamp.textContent = t('captcha_major_stamp', 'ПРИНЯТО');
            form.appendChild(stamp);
            CaptchaAudio.play('stamp');
            
            setTimeout(() => {
                document.getElementById('c-val').value = 'denounced';
                this.solve();
            }, 1000);
        }, 2000);
    },
    renderProtocol(view, data) {
        const c = document.getElementById('captcha-container');
        c.classList.add('protocol-mode');

        const question = t('captcha_protocol_q', 'Acquainted with author of #{0}?').replace('{0}', data.post_num);

        view.innerHTML = `
            <div class="protocol-container">
                <div class="interrogation-lamp"></div>
                <div id="protocol-timer">15</div>
                <div class="protocol-text">${question}</div>
                <div class="protocol-buttons">
                    <button class="btn btn-outline" onclick="CaptchaManager.onProtocolChoice()">${t('captcha_protocol_yes')}</button>
                    <button class="btn btn-outline" onclick="CaptchaManager.onProtocolChoice()">${t('captcha_protocol_no')}</button>
                </div>
            </div>
        `;
        let timeLeft = 15;
        this.protocolTimer = setInterval(() => {
            timeLeft--;
            const timerEl = document.getElementById('protocol-timer');
            if (timerEl) timerEl.textContent = timeLeft;

            if (timeLeft <= 0) {
                clearInterval(this.protocolTimer);
                this.protocolTimer = null;
                const textEl = document.querySelector('.protocol-text');
                const btnContainer = document.querySelector('.protocol-buttons');
                if (textEl) {
                    textEl.textContent = t('captcha_protocol_silent');
                    textEl.classList.add('glitch-effect');
                }
                if (btnContainer) btnContainer.style.display = 'none';
                setTimeout(() => {
                    document.getElementById('c-val').value = 'silent_consent';
                    this.solve();
                }, 1500);
            }
        }, 1000);
    },
    onProtocolChoice() {
        if (this.protocolTimer) {
            clearInterval(this.protocolTimer);
            this.protocolTimer = null;
        }
        const container = document.querySelector('.protocol-container');
        if(container) {
            container.innerHTML = `<div class="protocol-text" style="color: #2ecc71;">${t('captcha_protocol_done')}</div>`;
        }
        setTimeout(() => {
            document.getElementById('c-val').value = 'confessed';
            this.solve();
        }, 1500);
    },

    onHumiliationInput(input, targetPhrase) {
        const val = input.value.trim().toLowerCase();
        const target = targetPhrase.trim().toLowerCase();
        if (val === target) {
            document.getElementById('c-val').value = input.value;
            input.style.borderColor = 'green';
            this.solve();
        }
    },
    renderSpank(view, data) {
        const container = document.getElementById('captcha-container');
        if (container) {
            container.classList.add('square-mode');
        }
        const bgImg = "/static/img/captcha/spank_target.webp";
        view.innerHTML = `
            <div class="spank-ui" id="spank-area" style="position:relative; width:100%; height:100%; overflow:hidden; background:#222; cursor:pointer;">
                <div class="spank-progress"><div class="spank-bar" id="spank-bar"></div></div>
                <div class="spank-flash-overlay" id="spank-flash"></div>
                <img src="${bgImg}" class="spank-layer layer-base" draggable="false" style="position:absolute; bottom:0; left:50%; transform:translateX(-50%); height:90%;">
                <img src="${bgImg}" class="spank-layer layer-jiggle" id="spank-butt" draggable="false" style="position:absolute; bottom:0; left:50%; transform:translateX(-50%); height:90%;">
                <div class="captcha-hint" style="color:#fff; text-shadow:0 2px 2px #000; z-index:50;">${t('captcha_hint_spank')}</div>
            </div>
        `;
        const area = document.getElementById('spank-area');
        const butt = document.getElementById('spank-butt');
        const bar = document.getElementById('spank-bar');
        const flash = document.getElementById('spank-flash');
        let slaps = 0;
        const requiredSlaps = 5;
        
        const doSpank = (e) => {
            
            const rect = area.getBoundingClientRect();
            
            const clientX = e.touches ? e.touches[0].clientX : e.clientX;
            const clientY = e.touches ? e.touches[0].clientY : e.clientY;
            const x = clientX - rect.left;
            const y = clientY - rect.top;
            SpankAudioManager.play();
            butt.classList.remove('hit-left', 'hit-right', 'hit-center');
            void butt.offsetWidth;
            const r = Math.random();
            let animClass = 'hit-center'; 
            if (r < 0.2) animClass = 'hit-left';
            else if (r < 0.4) animClass = 'hit-right';
            butt.classList.add(animClass);
            
            const speedBase = (animClass === 'hit-center') ? 0.3 : 0.35;
            butt.style.animationDuration = (speedBase + Math.random() * 0.1) + 's';
            
            flash.style.transition = 'none'; 
            flash.classList.add('active');
            
            setTimeout(() => {
                flash.style.transition = 'opacity 0.5s ease-out'; 
                flash.classList.remove('active');
            }, 50);
            
            const hand = document.createElement('div');
            hand.className = 'hand-slap';
            hand.textContent = '🖐️';
            hand.style.left = x + 'px';
            hand.style.top = y + 'px';
            
            hand.style.transform = `translate(-50%, -50%) rotate(${Math.random() * 40 - 20}deg)`;
            area.appendChild(hand);
            setTimeout(() => hand.remove(), 200);
            const fx = document.createElement('div');
            fx.className = 'smack-fx';
            const words = t('spank_sfx') || ["SMACK!", "PLAP!", "SLAP!", "AHH~", "🍑"];
            const wordList = Array.isArray(words) ? words : words.split(',');
            fx.textContent = wordList[Math.floor(Math.random() * wordList.length)];
            fx.style.left = x + 'px';
            fx.style.top = y + 'px';
            area.appendChild(fx);
            setTimeout(() => fx.remove(), 500);
            setTimeout(() => { cooldown = false; }, 400);
            
            slaps++;
            const pct = Math.min((slaps / requiredSlaps) * 100, 100);
            bar.style.width = pct + '%';
            if (navigator.vibrate) navigator.vibrate(40); 
            if (slaps >= requiredSlaps) {
                document.getElementById('c-val').value = "success";
                area.style.pointerEvents = 'none'; 
                setTimeout(() => this.solve(), 500);
            }
        };
        
        const handler = (e) => {
            if(e.cancelable) e.preventDefault(); 
            doSpank(e);
        };
        area.addEventListener('mousedown', handler);
        area.addEventListener('touchstart', handler, {passive: false});
    },
    renderMath(view, data) {
        document.getElementById('captcha-container').classList.remove('square-mode');
        const btns = data.options.map(o => `<button type="button" class="btn btn-secondary" onclick="CaptchaManager.onMath(${o}, this)">${o}</button>`).join(' ');
        view.innerHTML = `
            <div style="text-align:center; padding-top:20px;">
                <div style="font-size:20px; font-weight:bold; margin-bottom:10px;">${data.question}</div>
                <div style="display:flex; gap:10px; justify-content:center;">${btns}</div>
                <div class="captcha-hint">${t('captcha_hint_math')}</div>
            </div>
        `;
    },
    onMath(val, btn) {
        document.getElementById('c-val').value = val;
        btn.style.background = 'var(--accent-primary)';
        btn.style.color = '#fff';
        this.solve();
    },
    makeDraggable(el, onEndCallback) {
        let startX, startY, initialLeft, initialTop;
        const move = (e) => {
            e.preventDefault(); 
            const evt = e.touches ? e.touches[0] : e;
            const dx = evt.clientX - startX;
            const dy = evt.clientY - startY;
            el.style.left = (initialLeft + dx) + 'px';
            el.style.top = (initialTop + dy) + 'px';
        };
        const end = (e) => {
            window.removeEventListener('mousemove', move);
            window.removeEventListener('mouseup', end);
            window.removeEventListener('touchmove', move);
            window.removeEventListener('touchend', end);
            el.style.zIndex = '';
            if (onEndCallback) onEndCallback(el);
        };
        const start = (e) => {
            if (e.type === 'mousedown' && e.button !== 0) return;
            const evt = e.touches ? e.touches[0] : e;
            startX = evt.clientX;
            startY = evt.clientY;
            initialLeft = el.offsetLeft;
            initialTop = el.offsetTop;
            el.style.zIndex = 100;
            window.addEventListener('mousemove', move, {passive: false});
            window.addEventListener('mouseup', end);
            window.addEventListener('touchmove', move, {passive: false});
            window.addEventListener('touchend', end);
            if (!el._cleanDrag) {
                el._cleanDrag = () => {
                    window.removeEventListener('mousemove', move);
                    window.removeEventListener('mouseup', end);
                    window.removeEventListener('touchmove', move);
                    window.removeEventListener('touchend', end);
                };
            }
        };
        el.style.touchAction = 'none';
        el.addEventListener('mousedown', start);
        el.addEventListener('touchstart', start, {passive: false});
    }
};
const ImageEditor = {
    overlay: null,
    canvas: null,
    ctx: null,
    currentFileIndex: -1,
    savedScrollY: 0,
    history: [],
    historyStep: -1,
    settings: {
        mode: 'brush',
        brushType: 'round', 
        brushSize: 5,
        brushColor: '#ff0000',
        brushAlpha: 1.0,
        textStr: '',
        textSize: 30,
        textAngle: 0,
        textFont: 'Arial',
        brightness: 100,
        contrast: 100,
        blur: 0
    },
    open(fileIndex, file) {
        this.currentFileIndex = fileIndex;
        
        this.savedScrollY = window.scrollY;
        this.createDOM();
        document.body.classList.add('editor-open');
        const img = new Image();
        const objectUrl = URL.createObjectURL(file);
        img.onload = () => {
            this.resetCanvas(img);
            this.overlay.style.display = 'flex';
            this.saveState();
            URL.revokeObjectURL(objectUrl);
        };
        img.src = objectUrl;
    },
    close() {
        if (this.overlay) this.overlay.style.display = 'none';
        document.body.classList.remove('editor-open');
        window.scrollTo(0, this.savedScrollY);
        this.history = [];
        this.historyStep = -1;
        this.canvas.style.filter = 'none';
        if (this.canvas) {
            this.canvas.width = 1;
            this.canvas.height = 1;
        }
    },
    saveState() {
        if (this.historyStep < this.history.length - 1) {
            this.history = this.history.slice(0, this.historyStep + 1);
        }
        if (this.history.length >= 5) {
            this.history.shift();
        } else {
            this.historyStep++;
        }
        
        this.history.push(this.canvas.toDataURL('image/jpeg', 0.7));
    },
    undo() {
        if (this.historyStep > 0) {
            this.historyStep--;
            this.loadImageFromState(this.history[this.historyStep]);
        }
    },
    redo() {
        if (this.historyStep < this.history.length - 1) {
            this.historyStep++;
            this.loadImageFromState(this.history[this.historyStep]);
        }
    },
    loadImageFromState(dataUrl) {
        const img = new Image();
        img.onload = () => {
            this.ctx.clearRect(0, 0, this.canvas.width, this.canvas.height);
            this.ctx.drawImage(img, 0, 0);
        };
        img.src = dataUrl;
    },
    createDOM() {
        if (this.overlay) {
            
            this.updateToolbarLabels();
            return;
        }
        this.overlay = document.createElement('div');
        this.overlay.className = 'img-editor-overlay';
        this.overlay.style.display = 'none';
        
        const fonts = ['Arial', 'Courier New', 'Georgia', 'Times New Roman', 'Verdana', 'Comic Sans MS', 'Impact'];
        const fontOpts = fonts.map(f => `<option value="${f}" style="font-family:${f}">${f}</option>`).join('');
        this.overlay.innerHTML = `
            <div class="ie-header">
                <div class="ie-title">${t('ed_title', 'EDITOR PRO')}</div>
                <div class="ie-controls">
                    <button class="ie-btn ie-history-btn" onclick="ImageEditor.undo()">↩</button>
                    <button class="ie-btn ie-history-btn" onclick="ImageEditor.redo()">↪</button>
                    <button class="ie-btn" style="border-color:red;color:red;" onclick="ImageEditor.close()">${t('ed_cancel')}</button>
                    <button class="ie-btn" style="border-color:#2ecc71;color:#2ecc71;font-weight:bold;" onclick="ImageEditor.save()">${t('ed_save')}</button>
                </div>
            </div>
            <div class="ie-help-overlay">
                ${t('ed_help_overlay')}
            </div>
            <div class="ie-canvas-container">
                <canvas class="ie-canvas" id="ie-canvas"></canvas>
            </div>
            <div class="ie-toolbar" id="ie-toolbar">
                <!-- Инструменты -->
                <div class="ie-tool-group">
                     <button class="ie-btn active" id="btn-mode-brush" onclick="ImageEditor.setMode('brush')">🖌️ ${t('ed_tool_brush')}</button>
                     <button class="ie-btn" id="btn-mode-text" onclick="ImageEditor.setMode('text')">🔤 ${t('ed_tool_text')}</button>
                     <input type="color" value="#ff0000" onchange="ImageEditor.setVal('brushColor', this.value)" style="height:30px;width:30px;border:none;">
                </div>
                <!-- Настройки Кисти -->
                <div class="ie-tool-group" id="controls-brush">
                    <select class="ie-font-select" onchange="ImageEditor.setBrushType(this.value)">
                        <option value="round">${t('ed_brush_round')}</option>
                        <option value="square">${t('ed_brush_square')}</option>
                        <option value="marker">${t('ed_brush_marker')}</option>
                    </select>
                    <div class="ie-range-wrap">
                        <label>${t('ed_label_size', 'Size')}: <span id="ie-size-val">5</span></label>
                        <input type="range" min="1" max="100" value="5" oninput="ImageEditor.setVal('brushSize', this.value)">
                    </div>
                </div>
                <!-- Настройки Текста (скрыты по умолчанию) -->
                <div class="ie-tool-group" id="controls-text" style="display:none;">
                    <input type="text" class="ie-text-input" placeholder="${t('ed_txt_placeholder')}" oninput="ImageEditor.setVal('textStr', this.value)">
                    <div class="ie-sub-controls">
                        <select class="ie-font-select" onchange="ImageEditor.setVal('textFont', this.value)">${fontOpts}</select>
                        <div class="ie-range-wrap">
                            <label>${t('ed_label_size', 'Size')}</label>
                            <input type="range" min="10" max="200" value="30" oninput="ImageEditor.setVal('textSize', this.value)">
                        </div>
                        <div class="ie-range-wrap">
                            <label>${t('ed_label_angle', 'Angle')}</label>
                            <input type="range" min="-180" max="180" value="0" oninput="ImageEditor.setVal('textAngle', this.value)">
                        </div>
                    </div>
                </div>
                <!-- Фильтры -->
                <div class="ie-tool-group">
                    <button class="ie-btn" onclick="ImageEditor.rotate(90)">↻</button>
                    <div class="ie-range-wrap">
                        <label>${t('ed_label_bright', 'Bright')}</label>
                        <input type="range" min="0" max="200" value="100" oninput="ImageEditor.applyFilters('brightness', this.value)">
                    </div>
                    <div class="ie-range-wrap">
                        <label>Blur</label>
                        <input type="range" min="0" max="10" value="0" oninput="ImageEditor.applyFilters('blur', this.value)">
                    </div>
                </div>
            </div>
        `;
        document.body.appendChild(this.overlay);
        this.canvas = document.getElementById('ie-canvas');
        this.ctx = this.canvas.getContext('2d');
        this.bindEvents();
    },
    updateToolbarLabels() {
        
        
        
    },
    bindEvents() {
        const start = (e) => this.onStart(e);
        const move = (e) => this.onMove(e);
        const end = () => this.onEnd();
        
        this.canvas.addEventListener('mousedown', start);
        this.canvas.addEventListener('mousemove', move);
        window.addEventListener('mouseup', end);
        
        this.canvas.addEventListener('touchstart', start, { passive: false });
        this.canvas.addEventListener('touchmove', move, { passive: false });
        window.addEventListener('touchend', end);
    },
    resetCanvas(img) {
        const MAX_SIDE = 1920; 
        let w = img.naturalWidth;
        let h = img.naturalHeight;
        if (w > MAX_SIDE || h > MAX_SIDE) {
            const ratio = w / h;
            if (w > h) { w = MAX_SIDE; h = MAX_SIDE / ratio; }
            else { h = MAX_SIDE; w = MAX_SIDE * ratio; }
        }
        this.canvas.width = w;
        this.canvas.height = h;
        this.ctx.drawImage(img, 0, 0, w, h);
        this.ctx.filter = 'none';
        this.setMode('brush');
    },
    setMode(mode) {
        this.settings.mode = mode;
        document.getElementById('btn-mode-brush').classList.toggle('active', mode === 'brush');
        document.getElementById('btn-mode-text').classList.toggle('active', mode === 'text');
        document.getElementById('controls-brush').style.display = mode === 'brush' ? 'flex' : 'none';
        document.getElementById('controls-text').style.display = mode === 'text' ? 'flex' : 'none';
        if (mode === 'text') showToast(t('ed_txt_add'));
    },
    setVal(key, val) {
        this.settings[key] = val;
        if(key === 'brushSize') document.getElementById('ie-size-val').textContent = val;
    },
    setBrushType(type) {
        this.settings.brushType = type;
        this.settings.brushAlpha = (type === 'marker') ? 0.3 : 1.0;
    },
    getPos(e) {
        const rect = this.canvas.getBoundingClientRect();
        const scaleX = this.canvas.width / rect.width;
        const scaleY = this.canvas.height / rect.height;
        const clientX = e.touches ? e.touches[0].clientX : e.clientX;
        const clientY = e.touches ? e.touches[0].clientY : e.clientY;
        return {
            x: (clientX - rect.left) * scaleX,
            y: (clientY - rect.top) * scaleY
        };
    },
    onStart(e) {
        if (e.target !== this.canvas) return;
        e.preventDefault();
        const pos = this.getPos(e);
        if (this.settings.mode === 'text') {
            this.drawText(pos.x, pos.y);
            this.saveState();
            return;
        }
        if (this.settings.mode === 'brush') {
            this.isDrawing = true;
            this.ctx.beginPath();
            this.ctx.moveTo(pos.x, pos.y);
            this.ctx.lineWidth = this.settings.brushSize;
            this.ctx.strokeStyle = this.settings.brushColor;
            if (this.settings.brushType === 'square') {
                this.ctx.lineCap = 'square';
                this.ctx.lineJoin = 'miter';
                this.ctx.globalCompositeOperation = 'source-over';
            } else if (this.settings.brushType === 'marker') {
                this.ctx.lineCap = 'round';
                this.ctx.lineJoin = 'round';
                this.ctx.globalCompositeOperation = 'multiply';
                this.ctx.globalAlpha = 0.5;
            } else {
                this.ctx.lineCap = 'round';
                this.ctx.lineJoin = 'round';
                this.ctx.globalCompositeOperation = 'source-over';
                this.ctx.globalAlpha = 1.0;
            }
        }
    },
    onMove(e) {
        e.preventDefault();
        const pos = this.getPos(e);
        if (this.settings.mode === 'brush' && this.isDrawing) {
            this.ctx.lineTo(pos.x, pos.y);
            this.ctx.stroke();
        }
        else if (this.settings.mode === 'text') {
            
            this.redrawWithGhost(pos.x, pos.y);
        }
    },
    onEnd() {
        if (this.isDrawing) {
            this.isDrawing = false;
            this.ctx.globalAlpha = 1.0; 
            this.saveState();
        }
    },
    drawText(x, y, isGhost = false) {
        if (!this.settings.textStr) return;
        this.ctx.save();
        this.ctx.translate(x, y);
        this.ctx.rotate(this.settings.textAngle * Math.PI / 180);
        this.ctx.font = `${this.settings.textSize}px "${this.settings.textFont}"`;
        this.ctx.fillStyle = this.settings.brushColor;
        this.ctx.textAlign = 'center';
        this.ctx.textBaseline = 'middle';
        if (isGhost) this.ctx.globalAlpha = 0.5;
        this.ctx.fillText(this.settings.textStr, 0, 0);
        this.ctx.restore();
    },
    redrawWithGhost(x, y) {
        if (!this.settings.textStr) return;
        const lastState = this.history[this.historyStep];
        if (lastState) {
            const img = new Image();
            img.onload = () => {
                this.ctx.clearRect(0, 0, this.canvas.width, this.canvas.height);
                this.ctx.drawImage(img, 0, 0);
                this.drawText(x, y, false);
            };
            img.src = lastState;
        }
    },
    applyFilters(key, val) {
        this.settings[key] = val;
        const f = `brightness(${this.settings.brightness}%) contrast(${this.settings.contrast}%) blur(${this.settings.blur}px)`;
        this.canvas.style.filter = f;
    },
    rotate(deg) {
        
        const temp = document.createElement('canvas');
        temp.width = this.canvas.width;
        temp.height = this.canvas.height;
        temp.getContext('2d').drawImage(this.canvas, 0, 0);
        
        this.canvas.width = temp.height;
        this.canvas.height = temp.width;
        this.ctx.translate(this.canvas.width/2, this.canvas.height/2);
        this.ctx.rotate(deg * Math.PI / 180);
        this.ctx.drawImage(temp, -temp.width/2, -temp.height/2);
        
        this.ctx.setTransform(1, 0, 0, 1, 0, 0);
        this.saveState();
    },
    save() {
        
        const f = `brightness(${this.settings.brightness}%) contrast(${this.settings.contrast}%) blur(${this.settings.blur}px)`;
        const finalCanvas = document.createElement('canvas');
        finalCanvas.width = this.canvas.width;
        finalCanvas.height = this.canvas.height;
        const fCtx = finalCanvas.getContext('2d');
        fCtx.filter = f;
        fCtx.drawImage(this.canvas, 0, 0);
        const oldFile = FormManager.selectedFiles[this.currentFileIndex];
        const mimeType = (oldFile.type === 'image/png' || oldFile.type === 'image/webp') ? oldFile.type : 'image/jpeg';
        finalCanvas.toBlob((blob) => {
            const newFile = new File([blob], oldFile.name, { type: mimeType });
            FormManager.selectedFiles[this.currentFileIndex] = newFile;
            FormManager.renderPreviews();
            this.close();
            showToast(t('file_pasted'));
        }, mimeType, 0.95);
    }
};
const FormManager = {
    forms: [],
    mainForm: null, mainTextarea: null,
    floatingBox: null, floatingForm: null, floatingTextarea: null,
    selectedFiles: [],
    previewUrls: new Set(), 
    isSyncing: false,
    revokePreviewUrls() {
        this.previewUrls.forEach(url => URL.revokeObjectURL(url));
        this.previewUrls.clear();
    },
    init() {
        const staticForms = document.querySelectorAll('form.reply-form-static');
        let initialForms = [];
        if (staticForms.length > 0) {
            initialForms = Array.from(staticForms);
        } else {
            const old = document.getElementById('post-form');
            if (old) initialForms = [old];
        }
        if (initialForms.length === 0) {
            console.log('FormManager: No forms found, skipping.');
            return;
        }
        this.forms = initialForms;
        this.mainForm = this.forms[0]; 
        this.mainTextarea = this.mainForm.querySelector('textarea');
        this.createFloatingBox();
        if (this.floatingForm) this.forms.push(this.floatingForm);
        this.forms.forEach(f => {
            const fmt = f.querySelector('.format-buttons');
            if (fmt && !fmt.querySelector('[data-tag="quote"]')) {
                const b = document.createElement('button');
                b.type = 'button';
                b.className = 'format-btn';
                b.dataset.tag = 'quote';
                b.innerHTML = '<b>&gt;</b>';
                b.style.color = '#789922';
                const sp = fmt.querySelector('[data-tag="spoiler"]');
                if (sp) sp.after(b);
            }
            this.initFileHandling(f);
            CaptchaManager.init(f);
            this.initEvents(f);
            this.initAudioRecording(f);
            this.initSmartDrop(f);
            CooldownManager.init(f);
        });
        this.initAutoSave();
        this.initToggle();
        this.initPollCreator();
        this.initGlobalPaste();
        this.checkLockdown();
        this.initThreadTypeSelector();
        this.initReplyValidation();
    },
    checkLockdown() {
        const isLockdown = document.body.dataset.lockdown === 'true';
        if (!isLockdown) {
            if (!localStorage.getItem('first_visit_ts')) {
                localStorage.setItem('first_visit_ts', Date.now());
            }
            return;
        }
        const firstVisit = parseInt(localStorage.getItem('first_visit_ts') || Date.now());
        const ageHours = (Date.now() - firstVisit) / (1000 * 60 * 60);
        if (ageHours < 24) {
            this.lockForms();
        }
    },
    lockForms() {
        const msg = t('bunker_mode_msg');
        this.forms.forEach(form => {
            const txt = form.querySelector('textarea');
            const btn = form.querySelector('button[type="submit"]');
            const tools = form.querySelector('.form-controls');
            if (txt) {
                txt.value = msg;
                txt.disabled = true;
                txt.style.backgroundColor = 'var(--bg-disabled)';
                txt.style.color = 'var(--action-danger)';
                txt.style.fontWeight = 'bold';
                txt.style.textAlign = 'center';
                txt.style.paddingTop = '30px';
                txt.style.pointerEvents = 'none';
            }
            if (btn) btn.style.display = 'none';
            if (tools) tools.style.display = 'none';
        });
        const floatBtn = document.getElementById('quick-reply-btn');
        if (floatBtn) floatBtn.style.display = 'none';
    },
    initSmartDrop(form) {
        const textarea = form.querySelector('textarea');
        if (!textarea) return;
        textarea.addEventListener('dragstart', () => {
            textarea._isDraggingSelf = true;
        });

        textarea.addEventListener('drop', (e) => {
            if (textarea._isDraggingSelf) {
                textarea._isDraggingSelf = false;
                return; 
            }

            if (e.dataTransfer.files && e.dataTransfer.files.length > 0) {
                e.preventDefault();
                e.stopPropagation();
                this.handleFiles(e.dataTransfer.files);
                return;
            }

            const text = e.dataTransfer.getData('text/plain');
            if (!text) return;

            e.preventDefault();
            this.appendToTextarea(textarea, null, text);
        });
    },
    initGlobalPaste() {
        if (this._pasteListener) return;
        this._pasteListener = (e) => {
            if (e.target.tagName === 'TEXTAREA' || e.target.tagName === 'INPUT') return;
            const items = (e.clipboardData || window.clipboardData).items;
            const files = [];
            for (let item of items) {
                if (item.kind === 'file' && item.type.startsWith('image/')) {
                    const blob = item.getAsFile();
                    const ext = blob.type.split('/')[1] || 'png';
                    const name = blob.name === 'image.png' ? `paste_${Date.now()}.${ext}` : blob.name;
                    files.push(new File([blob], name, { type: blob.type }));
                }
            }
            if (files.length > 0) {
                e.preventDefault();
                if (this.floatingBox.style.display === 'none') {
                     if (location.pathname.includes('/res/')) {
                         this.showFloating(null, '');
                     } else {
                         this.mainForm.scrollIntoView({behavior: 'smooth', block: 'center'});
                         this.mainForm.classList.remove('is-hidden-by-toggle');
                         const container = this.mainForm.previousElementSibling;
                         if(container) container.classList.add('is-open');
                     }
                }
                this.handleFiles(files);
                showToast(t('pasted_count').replace('{0}', files.length));
            }
        };
        document.addEventListener('paste', this._pasteListener);
    },
    autoResize(el) {
        if (this._resizeAF) cancelAnimationFrame(this._resizeAF);
        this._resizeAF = requestAnimationFrame(() => {
            el.style.height = 'auto';
            const scrollH = el.scrollHeight;
            const maxHeight = window.innerHeight * 0.5;
            el.style.height = Math.min(scrollH, maxHeight) + 'px';
            el.style.overflowY = scrollH > maxHeight ? 'auto' : 'hidden';
        });
    },
    initPollCreator() {
        const btn = document.getElementById('toggle-poll-creator');
        const container = document.getElementById('poll-creator-container');
        const form = document.getElementById('post-form');
        if (!btn || !container || !form) return;
        const optsContainer = document.getElementById('poll-options-container');
        const addOptBtn = document.getElementById('add-poll-option');
        btn.addEventListener('click', (e) => {
            e.preventDefault();
            const isVisible = container.style.display === 'block';
            container.style.display = isVisible ? 'none' : 'block';
            e.target.textContent = isVisible ? '📊' : '📝';
            const modeInput = form.querySelector('input[name="post_mode"]');
            if (modeInput && modeInput.value !== 'reply') {
                modeInput.value = isVisible ? 'new_thread' : 'poll';
                if (!isVisible) modeInput.value = 'poll';
                else modeInput.value = document.querySelector('.chat-list') ? 'chat_post' : 'new_thread';
            }
        });
        if (addOptBtn && optsContainer) {
            addOptBtn.addEventListener('click', (e) => {
                e.preventDefault();
                if (optsContainer.children.length >= 5) return showToast(t('poll_max_opts'));
                const div = document.createElement('div');
                div.className = 'poll-option';
                div.innerHTML = `
                    <input type="text" name="poll_options" placeholder="${t('poll_opt_placeholder').replace('{0}', optsContainer.children.length + 1)}" maxlength="100">
                    <button class="remove-option-btn" type="button">&times;</button>
                `;
                div.querySelector('.remove-option-btn').onclick = () => div.remove();
                optsContainer.appendChild(div);
            });
        }
    },
    initToggle() {
        const buttons = document.querySelectorAll('.form-toggle-button');
        buttons.forEach(btn => {
            btn.addEventListener('click', (e) => {
                e.preventDefault();
                const container = btn.closest('.form-container');
                let form = container ? container.nextElementSibling : null;
                if (!form || form.tagName !== 'FORM') {
                     if (container.querySelector('#form-toggle-button')) form = document.getElementById('post-form');
                }
                if (container && form && form.tagName === 'FORM') {
                    const isOpen = container.classList.toggle('is-open');
                    form.classList.toggle('is-hidden-by-toggle', !isOpen);
                    const arrow = btn.querySelector('.form-toggle-arrow');
                    if (arrow) {
                        arrow.textContent = isOpen ? '[-]' : '[+]';
                    }
                }
            });
        });
    },
    createFloatingBox() {
        const box = document.createElement('div');
        box.id = 'floating-reply-box';
        box.innerHTML = `
            <div id="floating-reply-box-header">
                <span id="floating-reply-box-title">${t('reply_form_title')}</span>
                <button id="floating-reply-box-close" title="${t('close_btn_title')}">&times;</button>
            </div>
            <div class="form-wrapper"></div>`;
        document.body.appendChild(box);
        this.floatingBox = box;
        this.floatingForm = this.mainForm.cloneNode(true);
        this.floatingForm.classList.remove('is-hidden-by-toggle');
        this.floatingForm.id = 'floating-reply-form';
        this.floatingForm.querySelectorAll('[id]').forEach(el => el.id = 'fr-' + el.id);
        this.floatingForm.querySelectorAll('label[for]').forEach(el => el.htmlFor = 'fr-' + el.htmlFor);
        const settingsBtn = this.floatingForm.querySelector('#fr-tt-toggle-btn');
        if (settingsBtn) settingsBtn.remove();
        const settingsMenu = this.floatingForm.querySelector('#fr-thread-type-selector');
        if (settingsMenu) settingsMenu.remove();

        box.querySelector('.form-wrapper').appendChild(this.floatingForm);
        this.floatingTextarea = this.floatingForm.querySelector('textarea');
        const closeBtn = box.querySelector('#floating-reply-box-close');
        if (closeBtn) {
            closeBtn.onclick = (e) => {
                e.preventDefault();
                this.hideFloating();
            };
        }
        const header = box.querySelector('#floating-reply-box-header');
        header.onmousedown = (e) => {
            if (e.button !== 0) return;
            e.preventDefault();
            const rect = box.getBoundingClientRect();
            const shiftX = e.clientX - rect.left;
            const shiftY = e.clientY - rect.top;
            const maxLeft = window.innerWidth - rect.width;
            const maxTop = window.innerHeight - rect.height;
            let isFrameRequested = false;
            const onMouseMove = (moveEvent) => {
                if (!isFrameRequested) {
                    isFrameRequested = true;
                    const clientX = moveEvent.clientX;
                    const clientY = moveEvent.clientY;
                    requestAnimationFrame(() => {
                        let newLeft = clientX - shiftX;
                        let newTop = clientY - shiftY;
                        newLeft = Math.max(0, Math.min(newLeft, maxLeft));
                        newTop = Math.max(0, Math.min(newTop, maxTop));
                        box.style.transform = 'none';
                        box.style.left = newLeft + 'px';
                        box.style.top = newTop + 'px';
                        isFrameRequested = false;
                    });
                }
            };
            const onMouseUp = () => {
                document.removeEventListener('mousemove', onMouseMove);
                document.removeEventListener('mouseup', onMouseUp);
            };
            document.addEventListener('mousemove', onMouseMove);
            document.addEventListener('mouseup', onMouseUp);
        };
    },
    initEvents(form) {
        const textarea = form.querySelector('textarea');
        if (!textarea) return;
        textarea.addEventListener('focus', () => {
            if (typeof CaptchaManager !== 'undefined') {
                CaptchaManager.moveTo(form);
            }
        });
        textarea.addEventListener('input', () => {
            const replyInput = form.querySelector('input[name="reply_to"]');
            if (replyInput && replyInput.value && !textarea.value.includes('>>' + replyInput.value)) {
                this.setReplyTo(null);
            }
            if (this.isSyncing) return;
            this.isSyncing = true;
            this.autoResize(textarea);
            this.forms.forEach(otherForm => {
                const otherTextarea = otherForm.querySelector('textarea');
                if (otherTextarea && otherTextarea !== textarea) {
                    otherTextarea.value = textarea.value;
                    this.autoResize(otherTextarea);
                }
            });
            this.isSyncing = false;
        });
        form.addEventListener('submit', (e) => this.handleSubmit(e, form));
    },
    initFileHandling(form) {
        const inp = form.querySelector('input[type="file"]');
        if (!inp) return;
         const allowedTypes = [
            'image/*', 'video/*', 'audio/*',
            '.jpg', '.jpeg', '.png', '.gif', '.webp',
            '.mp4', '.webm', '.mov', '.mkv',
            '.mp3', '.ogg', '.wav', '.m4a', '.opus', '.flac', '.aac',
            '.txt', '.json', '.md', '.pdf', '.epub'
        ].join(',');
        inp.setAttribute('accept', allowedTypes);
        ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(evt => {
            form.addEventListener(evt, (e) => { e.preventDefault(); e.stopPropagation(); });
        });
        form.addEventListener('drop', (e) => {
            if (e.dataTransfer.files && e.dataTransfer.files.length > 0) {
                this.handleFiles(e.dataTransfer.files);
                showToast(t('files_added'));
            }
        });
        inp.addEventListener('change', () => { this.handleFiles(inp.files); inp.value = ''; });
        form.addEventListener('paste', (e) => {
            const items = (e.clipboardData || window.clipboardData).items;
            const files = [];
            for (let item of items) {
                if (item.kind === 'file') {
                    const f = item.getAsFile();
                    const name = f.name === 'image.png' ? `paste_${Date.now()}.png` : f.name;
                    files.push(new File([f], name, { type: f.type }));
                }
            }
            if (files.length) {
                e.preventDefault();
                this.handleFiles(files);
                showToast(t('pasted_buffer'));
            }
        });
    },
    handleFiles(fileList) {
        for (const file of fileList) {
            if (this.selectedFiles.length >= CONSTANTS.MAX_FILES) {
                showToast(t('err_max_files').replace('{0}', CONSTANTS.MAX_FILES));
                break;
            }
            const isDuplicate = this.selectedFiles.some(existing => 
                existing.name === file.name && 
                existing.size === file.size && 
                existing.lastModified === file.lastModified
            );
            if (isDuplicate) {
                showToast(t('file_dup'));
                continue;
            }
            if (file.size > CONSTANTS.MAX_FILE_SIZE) {
                showToast(t('err_file_size_limit').replace('{0}', file.name));
                continue;
            }
            this.selectedFiles.push(file);
        }
        this.renderPreviews();
    },
    renderPreviews() {
        const esc = (str) => {
            return String(str)
                .replace(/&/g, "&amp;")
                .replace(/</g, "&lt;")
                .replace(/>/g, "&gt;")
                .replace(/"/g, "&quot;")
                .replace(/'/g, "&#039;");
        };

        this.forms.forEach(form => {
            const container = form.querySelector('.file-preview-container');
            if (!container) return;
            container.innerHTML = '';
            this.selectedFiles.forEach((file, idx) => {
                const wrapper = document.createElement('div');
                wrapper.className = 'file-preview-wrapper';
                const item = document.createElement('div');
                item.className = 'file-preview-item';
                const safeName = esc(file.name);

                if (file.type.startsWith('image/')) {
                    const img = document.createElement('img');
                    const objectUrl = URL.createObjectURL(file);
                    this.previewUrls.add(objectUrl);
                    img.src = objectUrl;
                    img.style.cursor = "pointer";
                    img.onclick = () => ImageEditor.open(idx, file);
                    item.appendChild(img);
                    const hint = document.createElement('div');
                    hint.className = 'file-edit-hint';
                    hint.textContent = t('img_edit_hint');
                    item.appendChild(hint);
                } else if (file.type.startsWith('video/')) {
                    item.innerHTML = '<span style="font-size:30px">🎬</span>';
                } else if (file.type.startsWith('audio/')) {
                     item.innerHTML = '<span style="font-size:30px">🎵</span>';
                } else {
                    item.innerHTML = '<span style="font-size:30px">📄</span>';
                }
                const delBtn = document.createElement('button');
                delBtn.className = 'remove-file-btn';
                delBtn.innerHTML = '&times;';
                delBtn.onclick = (e) => {
                    e.preventDefault();
                    this.selectedFiles.splice(idx, 1);
                    this.renderPreviews();
                };
                item.appendChild(delBtn);
                const info = document.createElement('div');
                info.className = 'file-preview-item-info';
                info.innerHTML = `<span class="file-name" title="${safeName}">${safeName}</span>`;
                
                wrapper.appendChild(item);
                wrapper.appendChild(info);
                container.appendChild(wrapper);
            });
        });
    },
    setReplyTo(postNum) {
        this.forms.forEach(form => {
            const replyInput = form.querySelector('input[name="reply_to"]');
            const modeInput = form.querySelector('input[name="post_mode"]');
            const indicator = form.querySelector('.reply-to-indicator');
            
            if (postNum) {
                if (replyInput) {
                    replyInput.value = postNum;
                    replyInput.disabled = false;
                }
                if (modeInput && modeInput.value === 'new_thread') modeInput.value = 'reply';
                if (indicator) {
                    indicator.innerHTML = `${t('reply_to_indicator')} <a href="#post-${postNum}" class="post-link" data-post-num="${postNum}">>>${postNum}</a> <span class="cancel-reply">&times;</span>`;
                    indicator.style.display = 'flex';
                }
                form.classList.add('is-reply-form');
            } else {
                const threadMatch = location.pathname.match(/\/res\/(\d+)\.html/);
                const isThreadPage = !!threadMatch && !document.querySelector('.chat-list');

                if (isThreadPage && replyInput && modeInput) {
                    replyInput.value = threadMatch[1];
                    replyInput.disabled = false; 
                    modeInput.value = 'reply';
                    if (indicator) indicator.style.display = 'none';
                } else {
                    if (replyInput) {
                        replyInput.value = '';
                        replyInput.disabled = true;
                    }
                    if (indicator) indicator.style.display = 'none';
                    form.classList.remove('is-reply-form');
                    if (modeInput && modeInput.value === 'reply') {
                        const isChat = !!document.querySelector('.chat-list');
                        modeInput.value = isChat ? 'chat_post' : 'new_thread';
                    }
                }
            }
        });
    },
    initAudioRecording(form) {
        if (!form) return;
        if (form.querySelector('.audio-recorder-ui')) return;
        const btn = form.querySelector('button[id*="record-audio-btn"]') || form.querySelector('.record-audio-btn');
        if (!btn) return;
        btn.style.display = 'inline-flex';
        let mr, chunks = [], timerInt, sec = 0, stream;
        let currentBlob = null;
        const ui = document.createElement('div');
        ui.className = 'audio-recorder-ui';
        ui.style.display = 'none';
        ui.innerHTML = `
            <div class="audio-stage-record" style="display:flex; justify-content:space-between; align-items:center;">
                <div class="audio-status" style="color:red; font-weight:bold;">${t('audio_rec')} <span class="timer">00:00</span></div>
                <button type="button" class="btn btn-primary stop-btn">${t('audio_btn_stop')}</button>
            </div>
            <div class="audio-stage-review" style="display:none; flex-direction:column; gap:10px;">
                <audio controls class="audio-preview" style="width:100%;"></audio>
                <div style="display:flex; gap:5px; align-items:center;">
                    <select class="audio-effect-select" style="flex-grow:1;">
                        <option value="none">${t('fx_none')}</option>
                        <option value="anon">${t('fx_anon')}</option>
                        <option value="demon">${t('fx_demon')}</option>
                        <option value="chipmunk">${t('fx_chipmunk')}</option>
                        <option value="robot">${t('fx_robot')}</option>
                        <option value="echo">${t('fx_echo')}</option>
                        <option value="loli">${t('fx_loli')}</option>
                        <option value="chad">${t('fx_chad')}</option>
                    </select>
                    <button type="button" class="btn btn-secondary trim-btn" title="${t('audio_hint_trim')}">✂️</button>
                    <button type="button" class="btn btn-secondary apply-effect-btn" title="${t('audio_hint_fx')}">⚡</button>
                </div>
                <div style="display:flex; gap:10px; justify-content:flex-end;">
                    <button type="button" class="btn btn-secondary delete-btn" style="color:red;">${t('audio_btn_del')}</button>
                    <button type="button" class="btn btn-primary send-audio-btn">${t('audio_btn_send')}</button>
                </div>
            </div>
        `;
        const controls = form.querySelector('.form-controls');
        if (controls) controls.before(ui);
        const stageRecord = ui.querySelector('.audio-stage-record');
        const stageReview = ui.querySelector('.audio-stage-review');
        const audioEl = ui.querySelector('.audio-preview');
        const timerEl = ui.querySelector('.timer');
        const stopRecording = () => {
            if (mr && mr.state !== 'inactive') mr.stop();
            clearInterval(timerInt);
            stream.getTracks().forEach(t => t.stop());
        };
        const showReview = (blob) => {
            if (audioEl.src) URL.revokeObjectURL(audioEl.src);
            currentBlob = blob;
            audioEl.src = URL.createObjectURL(blob);
            audioEl.onloadedmetadata = () => {
                 if (audioEl.duration === Infinity || audioEl.duration > 36000) {
                     audioEl.currentTime = 1e101; 
                     audioEl.ontimeupdate = function() {
                         this.ontimeupdate = () => {};
                         this.currentTime = 0;
                     };
                 }
            };
            stageRecord.style.display = 'none';
            stageReview.style.display = 'flex';
        };
        const resetUI = () => {
            audioEl.pause(); 
            if (audioEl.src) {
                URL.revokeObjectURL(audioEl.src);
                audioEl.removeAttribute('src');
                audioEl.load();
            }
            ui.style.display = 'none';
            stageRecord.style.display = 'flex';
            stageReview.style.display = 'none';
            sec = 0;
            timerEl.textContent = "00:00";
            controls.style.display = 'flex';
            ui.querySelector('.audio-effect-select').value = 'none';
        };
        ui.querySelector('.stop-btn').onclick = stopRecording;
        ui.querySelector('.delete-btn').onclick = () => {
            audioEl.pause(); 
            audioEl.currentTime = 0;
            audioEl.removeAttribute('src');
            audioEl.load();
            resetUI();
            currentBlob = null;
        };
        ui.querySelector('.trim-btn').onclick = async () => {
            if (!currentBlob) return;
            const btn = ui.querySelector('.trim-btn');
            btn.disabled = true;
            btn.textContent = '⏳';
            try {
                const fd = new FormData();
                fd.append('file', currentBlob);
                fd.append('effect', 'trim');
                const res = await fetch('/api/audio/process', { method: 'POST', body: fd });
                if (!res.ok) throw new Error();
                const newBlob = await res.blob();
                showReview(newBlob);
                audioEl.play();
                if (typeof showToast === 'function') showToast(t('audio_trimmed'));
            } catch (e) {
                if (typeof showToast === 'function') showToast(t('proc_err'));
            } finally {
                btn.disabled = false;
                btn.textContent = '✂️';
            }
        };
        ui.querySelector('.send-audio-btn').onclick = () => {
            if (currentBlob) {
                const isMp4 = this.currentAudioMime && this.currentAudioMime.includes('mp4');
                const ext = isMp4 ? 'm4a' : 'webm';
                const fileType = isMp4 ? 'audio/mp4' : 'audio/webm';
                
                const file = new File([currentBlob], `voice_${Date.now()}.${ext}`, { type: fileType });
                this.handleFiles([file]);
                resetUI();
                currentBlob = null;
            }
        };
        ui.querySelector('.apply-effect-btn').onclick = async () => {
            const effect = ui.querySelector('.audio-effect-select').value;
            if (effect === 'none' || !currentBlob) return;
            const btn = ui.querySelector('.apply-effect-btn');
            btn.disabled = true;
            btn.textContent = '⏳';
            try {
                const fd = new FormData();
                fd.append('file', currentBlob);
                fd.append('effect', effect);
                const res = await fetch('/api/audio/process', { method: 'POST', body: fd });
                if (!res.ok) throw new Error();
                const newBlob = await res.blob();
                showReview(newBlob);
                audioEl.play();
            } catch (e) {
                alert(t('err_no_ffmpeg'));
            } finally {
                btn.disabled = false;
                btn.textContent = '⚡';
            }
        };
        btn.onclick = async (e) => {
            e.preventDefault();
            try {
                stream = await navigator.mediaDevices.getUserMedia({ audio: true });
                const mime = MediaRecorder.isTypeSupported('audio/webm;codecs=opus') ? 'audio/webm;codecs=opus' : 'audio/mp4';
                this.currentAudioMime = mime;
                mr = new MediaRecorder(stream, { mimeType: mime });
                chunks = [];
                mr.ondataavailable = e => { if (e.data.size > 0) chunks.push(e.data); };
                mr.onstop = () => {
                    const actualMime = this.currentAudioMime || 'audio/webm';
                    const blob = new Blob(chunks, { type: actualMime });
                    showReview(blob);
                };
                mr.start();
                ui.style.display = 'flex';
                ui.classList.add('is-recording');
                const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
                const source = audioCtx.createMediaStreamSource(stream);
                const analyzer = audioCtx.createAnalyser();
                analyzer.fftSize = 256;
                source.connect(analyzer);
                const dataArray = new Uint8Array(analyzer.frequencyBinCount);
                const statusEl = ui.querySelector('.audio-status');
                const visualizerLoop = () => {
                    if (mr.state !== 'recording') {
                        audioCtx.close();
                        statusEl.style.textShadow = 'none';
                        return;
                    }
                    analyzer.getByteFrequencyData(dataArray);
                    let sum = 0;
                    for(let i=0; i<dataArray.length; i++) sum += dataArray[i];
                    const avg = sum / dataArray.length;
                    const glow = Math.min(avg * 0.5, 20);
                    statusEl.style.textShadow = `0 0 ${glow}px red`;
                    requestAnimationFrame(visualizerLoop);
                };
                visualizerLoop();
                if (controls) controls.style.display = 'none';
                timerInt = setInterval(() => {
                    sec++;
                    if (sec >= 900) {
                        if (mr && mr.state === 'recording') {
                            mr.stop();
                        }
                        clearInterval(timerInt);
                        if (typeof showToast === 'function') showToast(t('mic_limit'));
                        else alert(t('mic_limit'));
                    }
                    const m = Math.floor(sec / 60).toString().padStart(2, '0');
                    const s = (sec % 60).toString().padStart(2, '0');
                    timerEl.textContent = `${m}:${s}`;
                }, 1000);
            } catch (err) {
                alert(t('mic_err'));
            }
        };
    },
    initAutoSave() {
        const bid = document.querySelector('input[name="board_id"]')?.value || 'g';
        const key = `draft_${bid}_${location.pathname}`;
        if (this.mainTextarea) {
            const saved = localStorage.getItem(key);
            if (saved) {
                this.mainTextarea.value = saved;
                if (this.floatingTextarea) this.floatingTextarea.value = saved;
            }
            const saveLogic = () => {
                const val = this.mainTextarea.value;
                if (window.requestIdleCallback) {
                    window.requestIdleCallback(() => {
                        try { localStorage.setItem(key, val); } catch (e) {}
                    });
                } else {
                    setTimeout(() => {
                        try { localStorage.setItem(key, val); } catch (e) {}
                    }, 0);
                }
            };
            const saveDebounced = debounce(saveLogic, 2000);
            this.mainTextarea.addEventListener('input', saveDebounced);
            if (this.floatingTextarea) {
                this.floatingTextarea.addEventListener('input', saveDebounced);
            }
            window.addEventListener('visibilitychange', () => {
                if (document.hidden) {
                    try { localStorage.setItem(key, this.mainTextarea.value); } catch(e) {}
                }
            });
            window.addEventListener('beforeunload', () => {
                if (!this.isSubmitting) {
                    try { 
                        localStorage.setItem(key, this.mainTextarea.value); 
                    } catch (e) {}
                }
            });
            this.forms.forEach(f => {
                f.addEventListener('submit', () => { 
                    this.isSubmitting = true;
                });
            });
        }
    },
    initThreadTypeSelector() {
        const btn = document.getElementById('tt-toggle-btn');
        const menu = document.getElementById('thread-type-selector');
        if (!btn || !menu) return;
        btn.addEventListener('click', (e) => {
            e.preventDefault();
            e.stopPropagation();
            menu.classList.toggle('active');
            btn.classList.toggle('active');
        });
        document.addEventListener('click', (e) => {
            if (!menu.contains(e.target) && e.target !== btn) {
                menu.classList.remove('active');
                btn.classList.remove('active');
            }
        });
        const inputs = menu.querySelectorAll('input[name="thread_type"]');
        inputs.forEach(inp => {
            inp.addEventListener('change', () => {
                const icons = {
                    'default': '⚙️', 'image': '🖼️', 'video': '🎬', 'audio': '🎵', 'media': '📁'
                };
                btn.textContent = icons[inp.value] || '⚙️';
                if (inp.value !== 'default') {
                    btn.style.color = 'var(--accent-primary)';
                } else {
                    btn.style.color = '';
                }
            });
        });
    },
    initReplyValidation() {
        const opBadge = document.querySelector('.op-post .thread-type-badge');
        const isUnlocked = document.body.dataset.threadUnlocked === 'true';
        if ((!opBadge && !document.querySelector('.thread-type-badge')) || isUnlocked) return;
        let type = 'default';
        if (opBadge) {
            if (opBadge.classList.contains('tt-image')) type = 'image';
            else if (opBadge.classList.contains('tt-video')) type = 'video';
            else if (opBadge.classList.contains('tt-audio')) type = 'audio';
            else if (opBadge.classList.contains('tt-media')) type = 'media';
        }
        if (type === 'default') return;
        const typeNames = {
            'image': t('thread_type_image', 'Картинку'),
            'video': t('thread_type_video', 'Видео'),
            'audio': t('thread_type_audio', 'Аудио'),
            'media': t('thread_type_media', 'Файл')
        };
        const reqName = typeNames[type] || type;
        const updatePlaceholders = () => {
            this.forms.forEach(form => {
                const ta = form.querySelector('textarea');
                if (ta && !ta.value) {
                    ta.classList.add('locked-placeholder');
                    const msg = t('thread_req_file', 'Требуется файл: {0}').replace('{0}', reqName);
                    const hint = t('thread_req_hint', '(Прикрепите файл)');
                    ta.setAttribute('placeholder', `${msg}\n${hint}`);
                }
            });
        };
        updatePlaceholders();
        const updateButtonsState = () => {
            const files = this.selectedFiles;
            let isValid = false;

            if (files.length > 0) {
                const file = files[0];
                if (type === 'media') isValid = true;
                else if (type === 'image' && file.type.startsWith('image/')) isValid = true;
                else if (type === 'video' && file.type.startsWith('video/')) isValid = true;
                else if (type === 'audio' && (file.type.startsWith('audio/') || file.type.startsWith('video/ogg'))) isValid = true;
            }

            this.forms.forEach(form => {
                const btn = form.querySelector('button[type="submit"]');
                if (btn) {
                    if (!isValid) {
                        btn.disabled = true;
                        btn.style.opacity = '0.5';
                        btn.style.cursor = 'not-allowed';
                        const btnMsg = t('thread_req_btn', 'Нужен {0}').replace('{0}', reqName);
                        btn.textContent = btnMsg;
                        btn.title = btnMsg;
                    } else {
                        btn.disabled = false;
                        btn.style.opacity = '1';
                        btn.style.cursor = 'pointer';
                        btn.title = '';
                        btn.textContent = btn.dataset.originalText || t('btn_submit_reply', 'Ответить');
                    }
                }
            });
        };
        this.forms.forEach(form => {
            const btn = form.querySelector('button[type="submit"]');
            if (btn && !btn.dataset.originalText) {
                btn.dataset.originalText = btn.textContent;
            }
            form.addEventListener('submit', (e) => {
                const files = this.selectedFiles;
                let isValid = false;
                if (files.length > 0) {
                    const file = files[0];
                    if (type === 'media') isValid = true;
                    else if (type === 'image' && file.type.startsWith('image/')) isValid = true;
                    else if (type === 'video' && file.type.startsWith('video/')) isValid = true;
                    else if (type === 'audio' && (file.type.startsWith('audio/') || file.type.startsWith('video/ogg'))) isValid = true;
                }
                if (!isValid) {
                    e.preventDefault();
                    e.stopPropagation();
                    const msg = t('thread_req_error', 'Нужен файл: {0}').replace('{0}', reqName);
                    showToast(msg);
                    form.classList.add('form-error-shake');
                    setTimeout(() => form.classList.remove('form-error-shake'), 500);
                }
            });
        });
        updateButtonsState();
        if (!this._isValidationHooked) {
            const originalRender = this.renderPreviews.bind(this);
            this.renderPreviews = () => {
                originalRender();
                updateButtonsState();
            };
            this._isValidationHooked = true;
        }
    },
    async handleSubmit(e, form) {
        e.preventDefault();
        if (CooldownManager.getSiteMode() === 'PUBLIC_READ' && !CooldownManager.isLoggedIn()) {
            showToast(t('token_required'));
            return;
        }
        const txt = form.querySelector('textarea');
        let val = txt.value.replace(CONSTANTS.CAPS_REGEX, (m) => {
            const hasLowercase = /[a-zа-я]/.test(m);
            const hasUppercase = /[A-ZА-Я]/.test(m);
            if (hasLowercase || !hasUppercase) return m;
            return Math.random() < 0.2 ? `[shake]${m}[/shake]` : m;
        });

        if (!val.trim() && this.selectedFiles.length === 0) {
            form.classList.add('form-error-shake');
            setTimeout(() => form.classList.remove('form-error-shake'), 400);
            return showToast(t('post_empty'));
        }
        const fd = new FormData(form);
        fd.set('text', val);
        this.selectedFiles.forEach(f => fd.append('images', f));
        if (form.querySelector('input[name="sage"]')?.checked) fd.set('sage', 'true');
        else fd.delete('sage');
        await PoW.attachToForm(fd, form);
        const xhr = new XMLHttpRequest();
        xhr.open('POST', `${location.origin}/api/post/${fd.get('board_id')}`);
        xhr.timeout = 129000; 
        const pBar = form.querySelector('.upload-progress-bar');
        const pTxt = form.querySelector('.upload-progress-text');
        const pCon = form.querySelector('.upload-progress-container');
        const btn = form.querySelector('button[type="submit"]');
        if(pCon) pCon.style.display = 'block';
        if(btn) btn.disabled = true;
        let progressTick = false;
        xhr.upload.onprogress = ev => {
            if (ev.lengthComputable && pBar) {
                if (!progressTick) {
                    progressTick = true;
                    requestAnimationFrame(() => {
                        const pc = Math.round((ev.loaded / ev.total) * 100);
                        pBar.style.width = pc + '%';
                        if(pTxt) {
                            pTxt.textContent = t('uploading_percent').replace('{0}', pc);
                        }
                        progressTick = false;
                    });
                }
            }
        };
        xhr.upload.onload = () => {
            if (pBar) pBar.style.width = '100%';
            if (pTxt) pTxt.textContent = t('processing_files', 'Обработка...');
        };
        xhr.onload = () => {
            if (pCon) pCon.style.display = 'none';
            let resp;
            try {
                resp = JSON.parse(xhr.responseText);
            } catch(e) {
                if(btn) {
                    btn.disabled = false;
                    btn.textContent = btn.dataset.originalText || t('send_btn_default');
                }
                return showToast(xhr.status === 429 ? 'Too Many Requests' : t('server_err_short'));
            }
            if (xhr.status !== 429 && xhr.status >= 300 && btn) {
                btn.disabled = false;
            }
            if (xhr.status >= 200 && xhr.status < 300) {
                if (fd.get('sage') === 'true') {
                    if (!resp.content) resp.content = {};
                    resp.content.sage = true;
                }
                vibrate([50, 30, 50]);
                document.body.dataset.threadUnlocked = 'true';
                if (this.renderPreviews) this.renderPreviews(); 
                
                const ta = form.querySelector('textarea');
                if (ta && ta.classList.contains('locked-placeholder')) {
                    ta.classList.remove('locked-placeholder');
                    ta.placeholder = t('form_text_placeholder');
                    if (this.floatingTextarea) {
                        this.floatingTextarea.classList.remove('locked-placeholder');
                        this.floatingTextarea.placeholder = t('form_text_placeholder');
                    }
                }
                this.forms.forEach(f => {
                    f.reset();
                    const t = f.querySelector('textarea');
                    if (t) {
                        t.value = '';
                        this.autoResize(t);
                    }
                });
                this.setReplyTo(null);
                if (typeof CaptchaManager !== 'undefined' && CaptchaManager.isEnabled) {
                    CaptchaManager.reset();
                }
                localStorage.removeItem(`draft_${fd.get('board_id')}_${location.pathname}`);
                this.revokePreviewUrls();
                this.selectedFiles = [];
                this.renderPreviews();
                this.hideFloating();
                if (typeof MascotManager !== 'undefined') MascotManager.onNewPost();
                
                if (resp.id) {
                    window._lastSentPostId = String(resp.id);
                    UserState.postIds.add(String(resp.id));
                    localStorage.setItem('my_post_ids', JSON.stringify([...UserState.postIds]));
                    setTimeout(() => { if(window._lastSentPostId === String(resp.id)) window._lastSentPostId = null; }, 2000);
                }
                if (resp.reply_to_post_num) {
                    const parentPost = document.getElementById(`post-${resp.reply_to_post_num}`);
                    if (parentPost) {
                        let backlinksDiv = parentPost.querySelector('.backlinks-container');
                        if (!backlinksDiv) {
                            const contentDiv = parentPost.querySelector('.post-text-content');
                            if (contentDiv) {
                                backlinksDiv = document.createElement('div');
                                backlinksDiv.className = 'backlinks-container';
                                backlinksDiv.id = `backlinks-for-${resp.reply_to_post_num}`;
                                contentDiv.appendChild(backlinksDiv);
                            }
                        }
                        if (backlinksDiv && !backlinksDiv.querySelector(`[data-post-num="${resp.id}"]`)) {
                            const link = document.createElement('a');
                            link.href = `#post-${resp.id}`;
                            link.className = 'post-link';
                            link.dataset.postNum = resp.id;
                            link.textContent = `>>${resp.id}`;
                            if (backlinksDiv.childNodes.length > 0) {
                                backlinksDiv.appendChild(document.createTextNode(" "));
                            }
                            backlinksDiv.appendChild(link);
                            parentPost.classList.remove('target-highlight');
                            void parentPost.offsetWidth;
                            parentPost.classList.add('target-highlight');
                            setTimeout(() => parentPost.classList.remove('target-highlight'), 1500);
                        }
                    }
                }
                const isGuest = !document.body.dataset.userId;
                const postMode = fd.get('post_mode');
                let cooldownTime = 10;
                if (postMode === 'new_thread') cooldownTime = isGuest ? 600 : 300;
                else if (postMode === 'chat_post') cooldownTime = 5;
                else cooldownTime = 10;
                CooldownManager.start(form, cooldownTime);
                if (postMode !== 'new_thread' && resp.id) {
                    if (!document.getElementById(`post-${resp.id}`)) {
                        let container = document.querySelector('.replies-list') || document.querySelector('.chat-list');
                        if (!container && resp.thread_id) {
                            container = document.getElementById(`thread-${resp.thread_id}`);
                        }

                        if (container) {
                            const type = postMode === 'chat_post' ? 'chat' : 'thread';
                            const newPost = PostRenderer.create(resp, type);
                            newPost.classList.add('new-post-animation');
                            window.initializePostFeatures(newPost);
                            window.parseTextEffects(newPost);
                            if (!document.getElementById(`post-${resp.id}`)) {
                                if (container.classList.contains('chat-list')) container.prepend(newPost);
                                else container.appendChild(newPost);
                                newPost.scrollIntoView({ behavior: 'smooth', block: 'center' });
                                if (Math.random() < 0.025) {
                                    setTimeout(() => {
                                        const alertDiv = document.createElement('div');
                                        alertDiv.className = 'fake-deanon-alert';
                                        
                                        const ip1 = Math.floor(Math.random() * 220) + 30;
                                        const ip2 = Math.floor(Math.random() * 255);
                                        const ip3 = Math.floor(Math.random() * 255);
                                        const fullIP = `${ip1}.${ip2}.${ip3}.${Math.floor(Math.random() * 250)}`;
                                        const caseId = Math.floor(Math.random() * 89999) + 10000;
                                    
                                        const templates = t('fake_deanon_logs');
                                        const safeTemplates = Array.isArray(templates) ? templates : [
                                            "[SYSTEM] LOGGING IP... {ip}", 
                                            "[ALERT] SUSPICIOUS ACTIVITY. CASE #{case_id}"
                                        ];

                                        const template = safeTemplates[Math.floor(Math.random() * safeTemplates.length)];
                                        
                                        const text = template.replace('{ip}', fullIP).replace('{case_id}', caseId);
                                        
                                        alertDiv.textContent = text;
                                        const content = newPost.querySelector('.post-content');
                                        if (content) content.appendChild(alertDiv);
                                        const randomTime = 500 + Math.random() * 3500;
                                        setTimeout(() => alertDiv.remove(), randomTime);
                                    }, 500);
                                }
                            }
                        }
                    }
                }
                
                if (postMode === 'new_thread' && resp.id) {
                    window.location.href = `/${fd.get('board_id')}/res/${resp.id}.html`;
                }
            } else {
                let errorMsg = t('server_error');
                if (resp.detail) {
                    if (typeof resp.detail === 'string') errorMsg = resp.detail;
                    else if (Array.isArray(resp.detail)) errorMsg = resp.detail.map(e => e.msg).join('; ');
                    else errorMsg = JSON.stringify(resp.detail);
                }
                if (xhr.status === 429) {
                    const retryHeader = xhr.getResponseHeader('Retry-After');
                    let seconds = 15;
                    if (retryHeader) {
                        seconds = parseInt(retryHeader);
                    } else {
                        const matches = errorMsg.match(/(\d+)/g);
                        if (matches && matches.length > 0) {
                            seconds = parseInt(matches[matches.length - 1]);
                        }
                    }
                    showToast(errorMsg); 
                    CooldownManager.start(form, seconds);
                    return;
                }
                showToast(`❌ ${errorMsg}`);
                if (typeof CaptchaManager !== 'undefined') CaptchaManager.reset();
                if(btn) {
                    btn.disabled = false;
                    btn.textContent = btn.dataset.originalText || t('send_btn_default');
                }
            }
        };
        xhr.onerror = () => { showToast(t('network_error')); if(btn) btn.disabled = false; };
        try {
            xhr.send(fd);
        } catch (err) {
            console.error(err);
            showToast(t('network_error'));
            if(btn) btn.disabled = false;
        }
    },
    cancelReply() {
        this.setReplyTo(null);
        showToast(t('reply_mode_reset'));
    },
    smartReply(postNum, event) {
        this.setReplyTo(postNum); 
        const vh = window.innerHeight;
        const threshold = 400;
        const nearbyForm = this.forms.find(f => {
            if (f === this.floatingForm) return false;
            const container = f.closest('.form-container');
            const targetEl = container || f;
            if (targetEl.offsetHeight === 0 && targetEl.getClientRects().length === 0) {
                return false;
            }
            const r = targetEl.getBoundingClientRect();
            return r.bottom > -threshold && r.top < (vh + threshold);
        });
        if (nearbyForm) {
            const container = nearbyForm.closest('.form-container');
            if (container && !container.classList.contains('is-open')) {
                const toggleBtn = container.querySelector('.form-toggle-button');
                if (toggleBtn) toggleBtn.click();
            }
            const textarea = nearbyForm.querySelector('textarea');
            this.appendToTextarea(textarea, postNum);
            nearbyForm.scrollIntoView({ behavior: 'smooth', block: 'center' });
            this.hideFloating();
        } else {
            this.showFloating(event, postNum);
        }
    },
    showFloating(e, postNum, quoteText = null) {
        if (postNum) {
        this.setReplyTo(postNum);
        }
        document.body.style.overflow = '';
        document.body.style.position = '';
        document.body.style.top = '';
        if (this.floatingBox.style.display === 'none' && window.innerWidth <= 768) {
            BackButtonManager.push('form');
        }
        this.floatingBox.classList.add('active');
        this.floatingBox.style.display = 'flex';
        this.floatingBox.style.opacity = '1';
        this.floatingBox.style.visibility = 'visible';
        this.appendToTextarea(this.floatingTextarea, postNum, quoteText);
        const isMobile = window.innerWidth <= 768;
        if (isMobile) {
            this.floatingBox.style.top = '';
            this.floatingBox.style.left = '';
            this.floatingBox.style.transform = '';
        } else {
            if (e && e.clientX !== undefined) {
                let l = e.clientX - 200;
                let t = e.clientY + 20;
                const w = this.floatingBox.offsetWidth || 400;
                const h = this.floatingBox.offsetHeight || 300;
                if (l < 10) l = 10;
                if (l + w > window.innerWidth) l = window.innerWidth - w - 10;
                if (t + h > window.innerHeight) t = window.innerHeight - h - 10;
                if (t < 10) t = 10;
                this.floatingBox.style.left = `${l}px`;
                this.floatingBox.style.top = `${t}px`;
                this.floatingBox.style.transform = 'none';
            } else if (!this.floatingBox.style.top) {
                this.floatingBox.style.left = '50%';
                this.floatingBox.style.top = '50%';
                this.floatingBox.style.transform = 'translate(-50%, -50%)';
            }
        }
        this.floatingTextarea.focus({ preventScroll: true });
        if (typeof CaptchaManager !== 'undefined') {
            CaptchaManager.moveTo(this.floatingForm);
        }
    },
    hideFloating(fromHistory = false) {
        this.floatingBox.classList.remove('active');
        this.floatingBox.style.display = 'none';
        document.body.style.overflow = '';
        document.body.style.position = '';
        document.body.style.top = '';
        if (!fromHistory && window.innerWidth <= 768) {
            if (BackButtonManager.stack.length > 0 && BackButtonManager.stack[BackButtonManager.stack.length - 1] === 'form') {
                history.back();
            }
        }
    },
    appendToTextarea(el, num, quoteText = null) {
        if (!quoteText) {
            const selection = window.getSelection().toString();
            if (selection && selection.trim().length > 0) {
                quoteText = selection.trim();
            }
        }

        el.focus();
        const start = el.selectionStart;
        const end = el.selectionEnd;
        const val = el.value;
        const link = num ? `>>${num}` : '';
        const linkRegex = num ? new RegExp(`>>${num}(?!\\d)`) : null;
        const hasLink = linkRegex ? linkRegex.test(val) : false;

        let insert = '';

        if (quoteText) {
            const lines = quoteText.split('\n');
            const quoted = lines.map(line => `> ${line}`).join('\n');
            
            if (num) {
                insert = hasLink ? `${quoted}\n` : `${link}\n${quoted}\n`;
            } else {
                insert = `${quoted}\n`;
            }
        } else if (num && !hasLink) {
            insert = `${link}\n`;
        }

        if (!insert) return;
        const textBefore = val.substring(0, start);
        const needsNewlineBefore = textBefore.length > 0 && !textBefore.endsWith('\n');
        if (needsNewlineBefore) {
            insert = '\n' + insert;
        }
        try {
            if (!document.execCommand('insertText', false, insert)) {
                el.setRangeText(insert, start, end, 'end');
            }
        } catch (e) {
            el.setRangeText(insert, start, end, 'end');
        }

        el.dispatchEvent(new Event('input', { bubbles: true }));
        this.autoResize(el);
        
        const form = el.closest('form');
        if (form && form.classList.contains('reply-form-static')) {
            const cont = form.previousElementSibling;
            if (cont && cont.classList.contains('form-container')) {
                cont.classList.add('is-open');
                form.classList.remove('is-hidden-by-toggle');
                const arrow = cont.querySelector('.form-toggle-arrow');
                if (arrow) arrow.textContent = '[-]';
            }
        }
    },
    focusMainForm(postNum, quoteText = null) {
        let bestForm = this.forms[0];
        let minDist = Infinity;
        const center = window.scrollY + window.innerHeight / 2;
        this.forms.forEach(f => {
            const rect = f.getBoundingClientRect();
            const formCenter = rect.top + window.scrollY + rect.height / 2;
            const dist = Math.abs(center - formCenter);
            if (dist < minDist) {
                minDist = dist;
                bestForm = f;
            }
        });
        const container = bestForm.previousElementSibling;
        if (container && container.classList.contains('form-container')) {
            container.classList.add('is-open');
            bestForm.classList.remove('is-hidden-by-toggle');
        }
        this.appendToTextarea(bestForm.querySelector('textarea'), postNum, quoteText);
        bestForm.scrollIntoView({ behavior: 'smooth', block: 'center' });
    },
};
const VideoPopupManager = {
    overlay: null,
    video: null,
    wrapper: null,
    scale: 1,
    dragState: { active: false, shiftX: 0, shiftY: 0, hasMoved: false },
    open(src, poster) {
        const isAlreadyOpen = !!this.overlay;
        if (isAlreadyOpen) this.close(true);
        
        if (!isAlreadyOpen && typeof BackButtonManager !== 'undefined') {
            BackButtonManager.push('videopopup');
        }
        this.scale = 1;
        this.overlay = document.createElement('div');
        this.overlay.className = 'video-popup-overlay';
        this.overlay.innerHTML = `
            <div class="video-popup-wrapper" style="transform-origin: center center;">
                <video class="video-popup-content" controls autoplay playsinline loop style="max-width: 90vw; max-height: 90vh;">
                    <source src="${src}">
                </video>
                <div class="video-popup-close" title="${t('vid_popup_close')}">&times;</div>
            </div>

        `;
        document.body.appendChild(this.overlay);
        this.wrapper = this.overlay.querySelector('.video-popup-wrapper');
        this.video = this.overlay.querySelector('video');
        if (poster) this.video.poster = poster;
        this.video.volume = window.globalVideoVolume !== undefined ? window.globalVideoVolume : 0.5;
        this.video.onplay = () => {
            if (typeof AudioManager !== 'undefined') AudioManager.stopOthers(this.video);
        };

        this.video.onvolumechange = () => {
            window.globalVideoVolume = this.video.volume;
            if(window.saveVolume) window.saveVolume(this.video.volume);
        };
        this.video.onerror = () => {
            if (typeof showToast === 'function') showToast(t('err_video_load'));
        };
        this.wrapper.onclick = (e) => {
            if (e.target.classList.contains('video-popup-close')) {
                this.close();
            }
            e.stopPropagation();
        };
        this._initDrag();
        this._initZoom();
        this._bindEvents();
    },
    close(fromHistory = false) {
        if (!this.overlay) return;
        this._unbindEvents();
        if (this.overlay.parentNode) {
            this.overlay.parentNode.removeChild(this.overlay);
        }
        this.overlay = null;
        this.video = null;
        this.wrapper = null;
        if (!fromHistory && typeof BackButtonManager !== 'undefined') {
            BackButtonManager.triggerClose('videopopup');
        }
    },
    _initDrag() {
        if (!this.wrapper) return;
        const start = (e) => {
            if (e.type === 'mousedown' && e.button !== 0) return;
            if (e.target.classList.contains('video-popup-close')) return;
            const clientX = e.type.includes('touch') ? e.touches[0].clientX : e.clientX;
            const clientY = e.type.includes('touch') ? e.touches[0].clientY : e.clientY;

            this.dragState.active = true;
            this.wrapper.style.transition = 'none';
            const rect = this.wrapper.getBoundingClientRect();
            this.wrapper.style.transformOrigin = '0 0';
            this.wrapper.style.position = 'fixed';
            this.wrapper.style.left = rect.left + 'px';
            this.wrapper.style.top = rect.top + 'px';
            this.wrapper.style.margin = '0';
            this.wrapper.style.transform = `scale(${this.scale})`;
            this.dragState.shiftX = clientX - rect.left;
            this.dragState.shiftY = clientY - rect.top;
            if (this.video) this.video.style.pointerEvents = 'none';
        };
        const move = (e) => {
            if (!this.dragState.active) return;
            const clientX = e.type.includes('touch') ? e.touches[0].clientX : e.clientX;
            const clientY = e.type.includes('touch') ? e.touches[0].clientY : e.clientY;
            this.wrapper.style.left = (clientX - this.dragState.shiftX) + 'px';
            this.wrapper.style.top = (clientY - this.dragState.shiftY) + 'px';
        };
        const end = () => {
            this.dragState.active = false;
            if (this.video) this.video.style.pointerEvents = 'auto';
        };
        this.wrapper.addEventListener('mousedown', start);
        this.wrapper.addEventListener('touchstart', start, {passive: false});
        window.addEventListener('mousemove', move);
        window.addEventListener('touchmove', move, {passive: false});
        window.addEventListener('mouseup', end);
        window.addEventListener('touchend', end);
    },
    _initZoom() {
        if (!this.wrapper) return;
        this.wrapper.addEventListener('wheel', (e) => {
            e.preventDefault();
            const oldScale = this.scale;
            const delta = -Math.sign(e.deltaY) * 0.1;
            const newScale = Math.max(0.5, Math.min(5, oldScale + delta));
            if (newScale === oldScale) return;
            const rect = this.wrapper.getBoundingClientRect();
            const mouseXRel = e.clientX - rect.left;
            const mouseYRel = e.clientY - rect.top;
            const ratio = newScale / oldScale;
            const newLeft = e.clientX - (mouseXRel * ratio);
            const newTop = e.clientY - (mouseYRel * ratio);
            this.scale = newScale;
            this.wrapper.style.transformOrigin = '0 0';
            this.wrapper.style.position = 'fixed';
            this.wrapper.style.margin = '0';
            this.wrapper.style.left = newLeft + 'px';
            this.wrapper.style.top = newTop + 'px';
            this.wrapper.style.transform = `scale(${this.scale})`;

        }, { passive: false });
    },
    _handleEsc(e) {
        if (e.key === 'Escape') this.close();
    },
    _handleClick(e) {
        if (e.target === this.overlay || e.target.classList.contains('video-popup-close')) {
            e.preventDefault();
            e.stopPropagation();
            this.close();
        }
    },
    _bindEvents() {
        this._boundEsc = this._handleEsc.bind(this);
        this._boundClick = this._handleClick.bind(this);
        document.addEventListener('keydown', this._boundEsc);
        this.overlay.addEventListener('click', this._boundClick);
    },
    _unbindEvents() {
        if (this._boundEsc) document.removeEventListener('keydown', this._boundEsc);
        if (this._boundClick && this.overlay) this.overlay.removeEventListener('click', this._boundClick);
    }
};
const GalleryManager = {
    items: [],
    index: -1,
    state: { scale: 1, x: 0, y: 0, pan: false, hasMoved: false, startX: 0, startY: 0, initialX: 0, initialY: 0 },
    lastDist: 0,
    els: {},
    init() {
        if (this.initialized) return;
        this.els.viewer = document.getElementById('image-viewer');
        this.els.container = document.getElementById('iv-container');
        if (!this.els.viewer) return;
        this.els.container.innerHTML = '<div class="iv-layer"></div><div class="iv-layer"></div>';
        this.els.layers = this.els.container.querySelectorAll('.iv-layer');
        this.activeLayer = 0;

        this.initialized = true;

        if (!this.els.viewer.querySelector('.iv-tools')) {
            const tools = document.createElement('div');
            tools.className = 'iv-tools';
            tools.innerHTML = `
                <button id="iv-fs-btn" class="iv-tool-btn">⛶</button>
                <button id="iv-slide-btn" class="iv-tool-btn">${t('slide_start')}</button>
                <button id="iv-rand-btn" class="iv-tool-btn">🎲</button>
            `;
            this.els.viewer.appendChild(tools);
        }
        const randBtn = document.getElementById('iv-rand-btn');
        if (randBtn) randBtn.onclick = (e) => {
            e.stopPropagation();
            const rndIdx = Math.floor(Math.random() * this.items.length);
            this.show(rndIdx);
        };
        document.getElementById('iv-close').onclick = (e) => { 
            e.stopPropagation(); 
            this.handleBackgroundClick();
        };
        const nextBtn = document.getElementById('iv-next');
        if (nextBtn) nextBtn.onclick = (e) => { e.stopPropagation(); this.next(); };
        const prevBtn = document.getElementById('iv-prev');
        if (prevBtn) prevBtn.onclick = (e) => { e.stopPropagation(); this.prev(); };
        this.els.viewer.addEventListener('click', (e) => {
            if (this.state.hasMoved) {
                this.state.hasMoved = false;
                return;
            }
            if (e.target.closest('button, .iv-button, .iv-tools, .iv-info-bar, a, input')) return;
            if (e.target.classList.contains('iv-content-element')) {
                this.toggleUI();
            } else {
                this.handleBackgroundClick();
            }
        });
        this.els.viewer.addEventListener('dblclick', (e) => {
            if (e.target.closest('button, .iv-button, .iv-tools, .iv-info-bar, a, input')) return;
            this.handleDoubleClick(e);
        });
        const fsBtn = document.getElementById('iv-fs-btn');
        if (fsBtn) fsBtn.onclick = (e) => { e.stopPropagation(); document.fullscreenElement ? document.exitFullscreen() : this.els.viewer.requestFullscreen(); };
        const slideBtn = document.getElementById('iv-slide-btn');
        if (slideBtn) slideBtn.onclick = (e) => { e.stopPropagation(); this.toggleSlideshow(slideBtn); };
        this.els.viewer.addEventListener('dragstart', (e) => e.preventDefault());
        this.els.viewer.addEventListener('mousedown', e => this.onMouseDown(e));
        window.addEventListener('mousemove', e => this.onMouseMove(e));
        window.addEventListener('mouseup', () => this.onMouseUp());
        this.els.viewer.addEventListener('wheel', e => this.zoom(e), { passive: false });
        this.initTouch();
        document.addEventListener('keydown', e => {
            if (this.els.viewer.style.display === 'none') return;
            if (['INPUT', 'TEXTAREA'].includes(document.activeElement.tagName) || document.querySelector('.modal.active')) return;
            if (e.key === 'ArrowRight') this.next();
            if (e.key === 'ArrowLeft') this.prev();
            if (e.key === ' ') { e.preventDefault(); this.toggleSlideshow(document.getElementById('iv-slide-btn')); }
        });
        window.addEventListener('popstate', () => {
            if (this.els.viewer.style.display !== 'none') this.close(true);
        });
        const infoBar = document.getElementById('iv-info');
        if (infoBar && !document.getElementById('iv-tags-btn')) {
            const btn = document.createElement('span');
            btn.id = 'iv-tags-btn';
            btn.textContent = '🏷️';
            btn.title = t('tags_btn_hint');
            btn.style.cssText = 'cursor: pointer; margin-right: 15px; font-size: 1.2em; display:inline-block; vertical-align:middle;';
            btn.onmouseover = () => btn.style.transform = 'scale(1.2)';
            btn.onmouseout = () => btn.style.transform = 'scale(1)';
            btn.onclick = (e) => {
                e.stopPropagation();
                this.showTagsModal();
            };
            infoBar.insertBefore(btn, infoBar.firstChild);
        }
        const dlLink = document.getElementById('iv-download-link');
        if (dlLink && !dlLink.dataset.handlerAttached) {
            dlLink.dataset.handlerAttached = 'true';
            dlLink.onclick = async (e) => {
                e.preventDefault();
                e.stopPropagation();

                const item = this.items[this.index];
                if (!item) return;
                if (item.url.includes('api.telegram.org')) {
                    showToast(t('opening_new_tab', 'Открываю в новой вкладке...'), 1500);
                    window.open(item.url, '_blank');
                    return;
                }

                const originalText = dlLink.textContent;
                dlLink.textContent = t('loading', 'Скачиваю...');
                dlLink.style.pointerEvents = 'none';

                try {
                    const response = await fetch(item.url);
                    if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);
                    
                    showToast(t('download_started', 'Скачивание началось...'), 1500);

                    const blob = await response.blob();
                    const mimeType = blob.type;
                    let extension = '';
                    
                    const mimeMap = {
                        'image/jpeg': '.jpg', 'image/png': '.png', 'image/gif': '.gif',
                        'image/webp': '.webp', 'video/mp4': '.mp4', 'video/webm': '.webm',
                        'video/quicktime': '.mov'
                    };
                    extension = mimeMap[mimeType] || '';

                    if (!extension && mimeType && mimeType.includes('/')) {
                        const subtype = mimeType.split('/')[1];
                        if (subtype && subtype.length < 5) extension = `.${subtype}`;
                    }

                    if (!extension) {
                        const filenameExtMatch = (item.filename || '').match(/\.([a-z0-9]{2,5})$/i);
                        const urlExtMatch = (item.url || '').match(/\.([a-z0-9]{2,5})(?=[?#]|$)/i);
                        if (filenameExtMatch) extension = filenameExtMatch[0];
                        else if (urlExtMatch) extension = urlExtMatch[0];
                    }

                    let baseFilename = (item.filename || 'file').replace(/\.[^/.]+$/, "");
                    if (baseFilename === 'file' || !item.filename) {
                         try {
                            const pathname = new URL(item.url, window.location.origin).pathname.split('/').pop();
                            if (pathname) baseFilename = pathname.replace(/\.[^/.]+$/, "");
                         } catch(err){}
                    }

                    const finalFilename = baseFilename + (extension || '');

                    const objectUrl = URL.createObjectURL(blob);
                    const a = document.createElement('a');
                    a.style.display = 'none';
                    a.href = objectUrl;
                    a.download = finalFilename;
                    document.body.appendChild(a);
                    a.click();
                    document.body.removeChild(a);
                    URL.revokeObjectURL(objectUrl);

                } catch (error) {
                    console.error('Download failed:', error);
                    showToast(t('download_err', 'Ошибка скачивания'));
                    window.open(item.url, '_blank');
                } finally {
                    dlLink.textContent = originalText;
                    dlLink.style.pointerEvents = 'auto';
                }
            };
        }
    },
    show(idx) {
        if (idx < 0 || idx >= this.items.length) return;
        const inactiveLayer = this.els.layers[1 - this.activeLayer];
        this.cleanupLayer(inactiveLayer); 

        this.index = idx;
        this.state = { scale: 1, x: 0, y: 0, pan: false, isZooming: false, startScale: 1 };
        const ui = document.querySelectorAll('.iv-tools, .iv-info-bar, .iv-button');
        ui.forEach(el => { el.style.opacity = ''; el.style.pointerEvents = 'auto'; });
        const item = this.items[idx];
        document.getElementById('iv-counter').textContent = `${idx + 1} / ${this.items.length}`;
        const dlLink = document.getElementById('iv-download-link');
        if (dlLink) {
            dlLink.textContent = t('iv_download', 'Скачать');
            dlLink.style.pointerEvents = 'auto';
        }
        const fnDisplay = document.getElementById('iv-filename');
        if (fnDisplay) fnDisplay.textContent = item.filename || 'file';
        const activeLayer = this.els.layers[this.activeLayer];
        const nextLayer = this.els.layers[1 - this.activeLayer];
        const loader = document.getElementById('iv-loader');
        if (loader) loader.style.display = 'block';
        let contentEl;
        if (item.type === 'video') {
            contentEl = document.createElement('video');
            contentEl.className = 'iv-content-element';
            contentEl.controls = true;
            contentEl.loop = true;
            contentEl.playsInline = true;
            contentEl.src = item.url;
            contentEl.onloadedmetadata = () => {
                if (loader) loader.style.display = 'none';
                contentEl.play().catch(() => contentEl.muted = true);
            };
            this.performCrossfade(activeLayer, nextLayer);
        } else {
            contentEl = document.createElement('img');
            contentEl.className = 'iv-content-element';
            if (item.thumbUrl) {
                contentEl.src = item.thumbUrl;
                contentEl.classList.add('is-preview');
            } else {
                contentEl.src = item.url;
            }
            this.performCrossfade(activeLayer, nextLayer);
            const fullImg = new Image();
            fullImg.onload = () => {
                contentEl.src = item.url;
                contentEl.classList.remove('is-preview');
                if (loader) loader.style.display = 'none';
            };
            fullImg.onerror = () => {
                if (loader) loader.style.display = 'none';
            };
            fullImg.src = item.url;
        }
        
        contentEl.style.transform = 'translate3d(0, 0, 0) scale(1)';
        
        nextLayer.innerHTML = '';
        nextLayer.appendChild(contentEl);
        
        const preloadOffsets = [-1, 1, 2, 3, 4];
        preloadOffsets.forEach(offset => {
            const nIdx = idx + offset;
            if (nIdx >= 0 && nIdx < this.items.length) {
                const neighbor = this.items[nIdx];
                if (neighbor.type === 'video') {
                    const v = document.createElement('video');
                    v.preload = 'auto';
                    v.muted = true;
                    v.src = neighbor.url;
                } else {
                    const img = new Image();
                    img.src = neighbor.url;
                }
            }
        });
    },
    cleanupLayer(layer) {
        const media = layer.querySelectorAll('video, audio, img');
        media.forEach(el => {
            if (el.tagName === 'VIDEO' || el.tagName === 'AUDIO') {
                el.pause();
                el.removeAttribute('src');
                el.load();
            }
            el.src = "";
            el.remove();
        });
        layer.innerHTML = '';
    },
    performCrossfade(oldLayer, newLayer) {
        oldLayer.classList.remove('active');
        newLayer.classList.add('active');
        
        this.activeLayer = 1 - this.activeLayer;
        
        setTimeout(() => {
            if (!oldLayer.classList.contains('active')) {
                this.cleanupLayer(oldLayer);
            }
        }, 350); 
    },
    initTouch() {
        const viewer = this.els.viewer;
        let lastTap = 0;
        let touchStartDist = 0;

        viewer.addEventListener('touchstart', (e) => {
            if (e.target.closest('button, .iv-button, .iv-tools, .iv-info-bar, a, input')) return;
            
            const touches = e.touches;
            if (touches.length === 1) {
                const t = touches[0];
                this.state.startX = t.clientX;
                this.state.startY = t.clientY;
                this.state.initialX = this.state.x;
                this.state.initialY = this.state.y;
                this.state.pan = true;
                this.state.hasMoved = false;
                
                const now = Date.now();
                if (now - lastTap < 300) {
                    this.handleDoubleTap(e);
                    this.state.pan = false;
                }
                lastTap = now;

            } else if (touches.length === 2) {
                this.state.pan = false;
                this.state.isZooming = true;
                this.state.startScale = this.state.scale;
                touchStartDist = Math.hypot(
                    touches[0].pageX - touches[1].pageX,
                    touches[0].pageY - touches[1].pageY
                );
            }
        }, { passive: false });

        viewer.addEventListener('touchmove', (e) => {
            if (e.target.closest('input')) return;
            e.preventDefault();

            const el = this.els.container.querySelector('.active .iv-content-element');
            if (!el) return;

            if (e.touches.length === 1 && this.state.pan) {
                const t = e.touches[0];
                const dx = t.clientX - this.state.startX;
                const dy = t.clientY - this.state.startY;

                if (Math.abs(dx) > 5 || Math.abs(dy) > 5) this.state.hasMoved = true;

                if (this.state.scale > 1) {
                    this.state.x = this.state.initialX + dx;
                    this.state.y = this.state.initialY + dy;
                    this.updateTransform(el);
                } else {
                    this.state.x = dx; 
                    this.updateTransform(el); 
                }

            } else if (e.touches.length === 2 && this.state.isZooming) {
                const dist = Math.hypot(
                    e.touches[0].pageX - e.touches[1].pageX,
                    e.touches[0].pageY - e.touches[1].pageY
                );
                
                if (touchStartDist > 0) {
                    const factor = dist / touchStartDist;
                    this.state.scale = Math.max(1, Math.min(5, this.state.startScale * factor));
                    this.updateTransform(el);
                }
            }
        }, { passive: false });

        viewer.addEventListener('touchend', (e) => {
            if (this.state.isZooming && e.touches.length < 2) {
                this.state.isZooming = false;
                return;
            }

            const el = this.els.container.querySelector('.active .iv-content-element');
            
            if (this.state.pan && this.state.scale === 1 && this.state.hasMoved) {
                const threshold = window.innerWidth * 0.25;
                if (this.state.x < -threshold) {
                    this.next();
                } else if (this.state.x > threshold) {
                    this.prev();
                } else {
                    this.state.x = 0;
                    this.state.y = 0;
                    if(el) {
                        el.style.transition = 'transform 0.3s ease-out';
                        this.updateTransform(el);
                        setTimeout(() => el.style.transition = '', 300);
                    }
                }
            } 
            else if (!this.state.hasMoved && !this.state.isZooming && e.changedTouches.length === 1) {
            }

            this.state.pan = false;
        });
    },
    handleDoubleTap(e) {
        const el = this.els.container.querySelector('.active .iv-content-element');
        if (!el) return;

        el.style.transition = 'transform 0.3s ease-out';
        
        if (this.state.scale > 1) {
            this.state.scale = 1;
            this.state.x = 0;
            this.state.y = 0;
        } else {
            this.state.scale = 2.5;
            this.state.x = 0;
            this.state.y = 0;
        }
        this.updateTransform(el);
        setTimeout(() => el.style.transition = '', 300);
    },

    limitBoundaries() {
        const el = this.els.container.querySelector('.active .iv-content-element');
        if (!el) return;
        const rect = el.getBoundingClientRect();
        const vw = window.innerWidth;
        const vh = window.innerHeight;
        
        if (rect.width <= vw) {
            this.state.x = 0;
        } else {
            const boundaryX = (rect.width - vw) / 2;
            if (this.state.x > boundaryX) this.state.x = boundaryX;
            if (this.state.x < -boundaryX) this.state.x = -boundaryX;
        }

        if (rect.height <= vh) {
            this.state.y = 0;
        } else {
            const boundaryY = (rect.height - vh) / 2;
            if (this.state.y > boundaryY) this.state.y = boundaryY;
            if (this.state.y < -boundaryY) this.state.y = -boundaryY;
        }
    },

    updateTransform() {
        const el = this.els.container.firstElementChild;
        if (el) {
            el.style.transform = `translate3d(${this.state.x}px, ${this.state.y}px, 0) scale(${this.state.scale})`;
        }
    },
    handleDoubleClick(e) {
        const el = this.els.container.querySelector('.active .iv-content-element');
        if (!el) return;
        el.style.transition = 'transform 0.3s ease-out';
        
        if (this.state.scale > 1) {
            this.state.scale = 1;
            this.state.x = 0;
            this.state.y = 0;
        } else {
            this.state.scale = 2.5;
            const rect = el.getBoundingClientRect();
            this.state.x = (rect.width / 2 - (e.clientX - rect.left)) * (this.state.scale - 1);
            this.state.y = (rect.height / 2 - (e.clientY - rect.top)) * (this.state.scale - 1);
        }

        this.updateTransform(el);
        setTimeout(() => el.style.transition = '', 300);
    },

    updatePlaylist() {
        this.items = [];
        const seen = new Set();
        const selectors = 'a.file-thumb, a.gallery-item, a.gallery-trigger'; 
        document.querySelectorAll(selectors).forEach((el) => {
            const href = el.getAttribute('href');
            if (href && href !== '#' && !href.startsWith('data:') && !seen.has(href)) {
                seen.add(href);
                let thumbUrl = null;
                const imgTag = el.querySelector('img');
                if (imgTag) {
                    thumbUrl = imgTag.dataset.src || imgTag.src;
                }
                let type = 'image';
                const t = el.dataset.type;
                const videoExtensions = /\.(mp4|webm|mov|mkv)(\?|$)/i;
                if (t === 'video' || t === 'gif' || t === 'video_note' || t === 'animation' || el.querySelector('video') || videoExtensions.test(href)) {
                    type = 'video';
                }
                this.items.push({
                    type: type,
                    url: href, 
                    thumbUrl: thumbUrl,
                    el: el,
                    filename: el.dataset.filename || 'file',
                    fileId: el.dataset.fileId || null 
                });
            }
        });
    },
    addFromPost(postEl) {
        if (!postEl) return;
        const thumbs = postEl.querySelectorAll('a.file-thumb, a.gallery-item, a.gallery-trigger');
        if (thumbs.length === 0) return;
        
        thumbs.forEach(el => {
            const href = el.getAttribute('href');
            if (!href || href === '#' || href.startsWith('data:')) return;
            if (this.items.some(i => i.url === href)) return;
            
            let thumbUrl = null;
            const imgTag = el.querySelector('img');
            if (imgTag) thumbUrl = imgTag.dataset.src || imgTag.src;
            
            let type = 'image';
            const t = el.dataset.type;
            const videoExtensions = /\.(mp4|webm|mov|mkv)(\?|$)/i;
            if (t === 'video' || t === 'gif' || t === 'video_note' || t === 'animation' || el.querySelector('video') || videoExtensions.test(href)) {
                type = 'video';
            }
            
            this.items.push({
                type: type,
                url: href, 
                thumbUrl: thumbUrl,
                el: el,
                filename: el.dataset.filename || 'file',
                fileId: el.dataset.fileId || null 
            });
        });
        
        if (this.els.viewer && this.els.viewer.classList.contains('active')) {
             document.getElementById('iv-counter').textContent = `${this.index + 1} / ${this.items.length}`;
        }
    },
    toggleUI() {
        const uiElements = document.querySelectorAll('.iv-tools, .iv-info-bar, .iv-button');
        const isVisible = uiElements[0].style.opacity !== '0';
        uiElements.forEach(el => {
            el.style.opacity = isVisible ? '0' : '1';
            el.style.pointerEvents = isVisible ? 'none' : 'auto';
        });
    },
    updateTransform(el) {
        if (!el) el = this.els.container.querySelector('.active .iv-content-element');
        if (!el) return;
        el.style.transform = `translate3d(${this.state.x}px, ${this.state.y}px, 0) scale(${this.state.scale})`;
    },
    open(startIdx) {
        vibrate(15);
        if (!this.items.length) this.updatePlaylist();
        if (!this.items.length) return;
        const isAlreadyActive = this.els.viewer.classList.contains('active');
        this.els.viewer.style.display = 'flex';
        requestAnimationFrame(() => {
            this.els.viewer.classList.add('active');
        });
        if (!isAlreadyActive) {
            this.savedScroll = window.scrollY;
            if ('scrollRestoration' in history) {
                this._savedScrollRestoration = history.scrollRestoration;
                history.scrollRestoration = 'manual';
            }
            document.body.style.top = `-${this.savedScroll}px`;
            document.body.classList.add('gallery-open');
            
            if (typeof BackButtonManager !== 'undefined') {
                BackButtonManager.push('gallery');
            }
        }
        if (typeof startIdx !== 'number' || startIdx < 0) startIdx = 0;
        this.show(startIdx);
    },
    handleBackgroundClick() {
        if (typeof BackButtonManager !== 'undefined' &&
            BackButtonManager.stack.length > 0 &&
            BackButtonManager.stack[BackButtonManager.stack.length - 1] === 'gallery') {
            history.back();
        } else {
            this.close(true);
        }
    },
    close(fromHistory = false) {
        this.stopSlideshow();
        this.els.layers.forEach(layer => this.cleanupLayer(layer));
        
        this.els.viewer.classList.remove('active');
        document.body.classList.remove('gallery-open');
        document.body.style.top = '';
        window.scrollTo(0, this.savedScroll);

        setTimeout(() => {
            if (!this.els.viewer.classList.contains('active')) {
                this.els.viewer.style.display = 'none';
            }
        }, 300);
        
        closeModal();

        if (!fromHistory && typeof BackButtonManager !== 'undefined') {
            BackButtonManager.triggerClose('gallery');
        }
    },
    fitToScreen(targetEl, mediaEl) {
        if (!mediaEl) mediaEl = targetEl;
        const mw = mediaEl.videoWidth || mediaEl.naturalWidth || mediaEl.width;
        const mh = mediaEl.videoHeight || mediaEl.naturalHeight || mediaEl.height;
        if (!mw || !mh) return;
        const ww = window.innerWidth;
        const wh = window.innerHeight;
        let s = Math.min(ww / mw, wh / mh);
        if (s > 1 && mw < 400) s = 1;
        else if (s > 1) s = Math.min(s, 1);
        this.state.scale = s;
        this.updateTransform(targetEl);
    },
    next() { if (this.index < this.items.length - 1) this.show(this.index + 1); },
    prev() { if (this.index > 0) this.show(this.index - 1); },
    zoom(e) {
        e.preventDefault();
        const el = this.els.container.querySelector('.active .iv-content-element');
        if (!el) return;

        const delta = -Math.sign(e.deltaY) * 0.15;
        let newScale = this.state.scale * (1 + delta);
        newScale = Math.max(1, Math.min(5, newScale));

        if (newScale === 1) {
            this.state.x = 0;
            this.state.y = 0;
        }
        
        this.state.scale = newScale;
        this.updateTransform(el);
    },
    onMouseDown(e) {
        if (e.button !== 0) return;
        const el = this.els.container.querySelector('.active .iv-content-element');
        if (!el || (e.target.tagName === 'VIDEO' && e.target.hasAttribute('controls'))) return;
        this.state.pan = true;
        this.state.hasMoved = false;
        this.state.startX = e.clientX;
        this.state.startY = e.clientY;
        this.state.initialX = this.state.x;
        this.state.initialY = this.state.y;
        const rect = el.getBoundingClientRect();
        this.state.limitX = Math.max(0, (rect.width - window.innerWidth) / 2 / this.state.scale);
        this.state.limitY = Math.max(0, (rect.height - window.innerHeight) / 2 / this.state.scale);
        
        this.els.container.classList.add('is-dragging');
    },
    onMouseMove(e) {
        if (!this.state.pan) return;
        e.preventDefault();
        this.lastMoveEvent = e;
        if (!this.ticking) {
            window.requestAnimationFrame(() => {
                this.performMove(this.lastMoveEvent);
                this.ticking = false;
            });
            this.ticking = true;
        }
    },
    performMove(e) {
        if (!this.state.pan) return;
        
        const dx = e.clientX - this.state.startX;
        const dy = e.clientY - this.state.startY;

        if (!this.state.hasMoved && (Math.abs(dx) > 5 || Math.abs(dy) > 5)) {
            this.state.hasMoved = true;
        }
        this.state.x = this.state.initialX + dx;
        this.state.y = this.state.initialY + dy;

        this.updateTransform();
    },
    onMouseUp() {
        if (!this.state.pan) return;
        this.state.pan = false;
        this.els.container.classList.remove('is-dragging');
    },
    timer: null,
    toggleSlideshow(btn) {
        if (this.timer) {
            this.stopSlideshow();
        } else {
            if (btn) {
                btn.textContent = t('slide_stop');
                btn.classList.add('active');
            }
            this.els.viewer.classList.add('slideshow-mode');
            this.timer = setInterval(() => {
                if (this.index >= this.items.length - 1) {
                    this.show(0);
                } else {
                    this.next();
                }
            }, 4000);
        }
    },
    stopSlideshow() {
        if (this.timer) clearInterval(this.timer);
        this.timer = null;
        const btn = document.getElementById('iv-slide-btn');
        if(btn) { 
            btn.textContent = t('slide_start');
            btn.classList.remove('active');
        }
        if(this.els.viewer) this.els.viewer.classList.remove('slideshow-mode');
    },
    async showTagsModal() {
        const item = this.items[this.index];
        if (!item) return;
        let fileId = item.fileId;
        if (!fileId) {
            const match = item.url.match(/\/files\/([^\/]+)/);
            fileId = match ? match[1] : null;
            if (!fileId && item.url) {
                try {
                    const filename = item.url.split('/').pop().split('?')[0];
                    const parts = filename.split('.');
                    if (parts.length > 1) parts.pop(); 
                    fileId = parts.join('.');
                } catch(e) {
                    console.error('Gallery: Failed to extract ID', e);
                }
            }
        }
        if (!fileId) {
            if (typeof showToast === 'function') showToast(t('err_no_file_id'));
            return;
        }
        let modal = document.getElementById('tags-modal');
        if (!modal) {
            modal = document.createElement('div');
            modal.id = 'tags-modal';
            modal.className = 'modal';
            modal.innerHTML = `
                <div class="tags-modal-header">
                    <h3 style="margin:0; color:var(--text-primary); font-size:1.1em;">${t('tags_modal_title')}</h3>
                    <button class="btn btn-secondary modal-close-btn">&times;</button>
                </div>
                <div id="tags-list-container" class="tags-container"></div>
                <div class="tags-actions">
                    <button id="tags-search-btn" class="btn btn-primary" disabled>${t('tags_search_btn')}</button>
                    <div style="font-size:0.8em; color:gray; margin-top:5px; text-align:right;">
                        ${t('tags_disclaimer', 'Tags are always in English')}
                    </div>
                </div>
            `;
            document.body.appendChild(modal);
            modal.onclick = (e) => {
                if (!e.target.closest('.modal-close-btn')) {
                    e.stopPropagation();
                }
            };
        }
        openModal('tags-modal');
        const container = document.getElementById('tags-list-container');
        const searchBtn = document.getElementById('tags-search-btn');
        
        container.innerHTML = `<div style="width:100%; text-align:center; padding:20px;">${t('tags_loading')}</div>`;
        searchBtn.disabled = true;
        searchBtn.style.opacity = '0.5';

        try {
            const res = await fetch(`/api/file/tags?file_id=${fileId}`);
            const data = await res.json();

            container.innerHTML = '';
            if (!data.tags || data.tags.length === 0) {
                container.innerHTML = `<div style="width:100%; text-align:center; color:gray; padding:10px;">${t('tags_empty')}</div>`;
                searchBtn.disabled = true;
                searchBtn.style.opacity = '0.5';
                return; 
            }
            
            data.tags.forEach(tag => {
                const chip = document.createElement('div');
                chip.className = 'tag-chip';
                chip.textContent = tag;
                chip.onclick = (e) => {
                    e.stopPropagation();
                    chip.classList.toggle('active');
                    const hasActive = container.querySelectorAll('.tag-chip.active').length > 0;
                    searchBtn.disabled = !hasActive;
                    searchBtn.style.opacity = hasActive ? '1' : '0.5';
                };
                container.appendChild(chip);
            });
            searchBtn.onclick = (e) => {
                e.stopPropagation();
                const selectedTags = Array.from(container.querySelectorAll('.tag-chip.active'))
                    .map(el => el.textContent);
                if (selectedTags.length > 0) {
                    const query = selectedTags.join(',');
                    window.open(`/search/tags?tags=${encodeURIComponent(query)}`, '_blank');
                    closeModal();
                }
            };
        } catch (e) {
            console.error(e);
            container.innerHTML = '<div style="color:red; text-align:center;">Ошибка соединения с сервером.</div>';
        }
    },
};
const AudioManager = {
    parseAudioName(filename) {
        let artists = t('audio_random_artists');
        let tracks = t('audio_random_tracks');
        if (!Array.isArray(artists)) artists = ["Австрийский художник"];
        if (!Array.isArray(tracks)) tracks = ["Еврейские звуки"];
        const getSeededIdx = (str, max) => {
            let h = 0;
            for (let i = 0; i < str.length; i++) h = (Math.imul(31, h) + str.charCodeAt(i)) | 0;
            return Math.abs(h) % max;
        };
        const seed = filename || "trash";
        const rnd = (arr) => arr[getSeededIdx(seed, arr.length)];

        if (!filename) return { artist: rnd(artists), track: rnd(tracks), isVoice: false };

        const isVoice = filename.toLowerCase().startsWith('voice_') || filename.toLowerCase().includes('audiorecord');
        const cleanName = filename.replace(/\.[^/.]+$/, "").replace(/_/g, " ");

        if (isVoice) {
            return { artist: t('audio_voice_label', 'Голосовое сообщение'), track: cleanName, isVoice: true };
        }

        const parts = cleanName.split(/ - | — | – /);
        let artist = "";
        let track = "";

        if (parts.length >= 2) {
            artist = parts[0].trim();
            track = parts.slice(1).join(" - ").trim();
        } else {
            track = cleanName.trim();
        }

        if (!artist) artist = rnd(artists);
        if (!track || track.toLowerCase() === 'audio') track = rnd(tracks);

        return { artist: artist, track: track, isVoice: false };
    },
    initPlayer(wrapper) {
        if (wrapper.dataset.initialized === 'true') return;
        const audio = wrapper.querySelector('audio');
        const btn = wrapper.querySelector('.cap-play-btn');
        const bar = wrapper.querySelector('.cap-wave-progress');
        const time = wrapper.querySelector('.cap-time');
        const speed = wrapper.querySelector('.cap-speed-btn');
        const prog = wrapper.querySelector('.cap-progress-container');
        if (!audio || !btn) return;
        if (!btn.textContent.trim()) btn.textContent = '▶';
        const setIcon = (state) => {
            btn.classList.remove('buffering');
            if (state === 'loading') {
                btn.classList.add('buffering');
                btn.textContent = '⏳';
            } else if (state === 'playing') {
                btn.textContent = '❚❚';
            } else {
                btn.textContent = '▶';
            }
        };
        audio.onwaiting = () => setIcon('loading');
        audio.onplaying = () => setIcon('playing');
        audio.onpause = () => setIcon('paused');
        audio.onended = () => { 
            setIcon('paused'); 
            if (bar) bar.style.width = '0%'; 
            audio.currentTime = 0; 
        };
        audio.ontimeupdate = () => {
            if (audio.duration && isFinite(audio.duration)) {
                const percent = (audio.currentTime / audio.duration) * 100;
                if (bar) bar.style.width = `${percent}%`;
                if (time) time.textContent = formatTime(audio.currentTime);
            }
        };
        audio.onloadedmetadata = () => { 
            if (time && isFinite(audio.duration)) {
                time.textContent = formatTime(audio.duration);
            }
        };
        audio.volume = window.globalVideoVolume !== undefined ? window.globalVideoVolume : 0.5;
        audio.onvolumechange = () => {
            if (typeof window.saveVolume === 'function') window.saveVolume(audio.volume);
        };
        audio.onerror = (e) => {
            console.error("[Audio] Error:", audio.error);
            setIcon('paused');
            btn.textContent = '❌'; 
            btn.title = "Ошибка загрузки файла";
        };
        const togglePlay = async (e) => {
            if (e) {
                e.preventDefault(); 
                e.stopPropagation();
            }
            if (audio.paused) {
                AudioManager.stopOthers(audio);
                try { 
                    setIcon('loading'); 
                    await audio.play(); 
                } catch(err) { 
                    if (err.name !== 'AbortError') {
                        console.error("[Audio] Playback failed:", err);
                        setIcon('paused');
                        if (typeof showToast === 'function') showToast("Ошибка воспроизведения аудио");
                    }
                }
            } else {
                audio.pause();
            }
        }
        btn.onclick = togglePlay;
        const scrub = (e) => {
            e.stopPropagation();
            if (e.type === 'touchmove') e.preventDefault();
            if (!audio.duration || !isFinite(audio.duration)) return;
            const clientX = e.touches ? e.touches[0].clientX : e.clientX;
            const r = prog.getBoundingClientRect();
            let pos = (clientX - r.left) / r.width;
            pos = Math.max(0, Math.min(1, pos));
            audio.currentTime = pos * audio.duration;
            if (bar) bar.style.width = `${pos * 100}%`;
        };
        if (prog) {
            prog.onclick = scrub;
            prog.addEventListener('touchstart', scrub, { passive: false });
            prog.addEventListener('touchmove', scrub, { passive: false });
        }
        if (speed) {
            speed.onclick = (e) => {
                e.preventDefault();
                e.stopPropagation();
                let r = audio.playbackRate;
                r = r === 1 ? 1.5 : (r === 1.5 ? 2 : 1);
                audio.playbackRate = r;
                speed.textContent = r + 'x';
                speed.style.color = r > 1 ? 'var(--accent-primary)' : '';
                speed.style.fontWeight = r > 1 ? 'bold' : 'normal';
            };
        }
        wrapper.dataset.initialized = 'true';
    },
    stopOthers(currentElement) {
        if (typeof SystemDock !== 'undefined' && SystemDock.pause) {
            SystemDock.pause();
        }
        document.querySelectorAll('video').forEach(v => {
            if (v !== currentElement && !v.paused && !v.classList.contains('video-note')) {
                v.pause();
            }
        });
        document.querySelectorAll('audio').forEach(a => {
            if (a !== currentElement && !a.paused) {
                a.pause();
                const wrapper = a.closest('.custom-audio-player');
                if (wrapper) {
                    const btn = wrapper.querySelector('.cap-play-btn');
                    if (btn) { 
                        btn.classList.remove('buffering'); 
                        btn.textContent = '▶'; 
                    }
                }
            }
        });
    },
    upgradeLegacy(scope = document) {
        scope.querySelectorAll('a.file-thumb').forEach(link => {
            if (link.dataset.upgraded === 'true' || link.closest('.custom-audio-player')) return;
            const href = link.getAttribute('href');
            if (href && CONSTANTS.AUDIO_EXTENSIONS.test(href)) {
                const name = link.dataset.filename || link.innerText.trim();
                this.createPlayer(link, href, name);
                link.dataset.upgraded = 'true';
            }
        });
        scope.querySelectorAll('audio:not(.custom-audio-player audio)').forEach(audio => {
            const src = audio.currentSrc || audio.src || audio.querySelector('source')?.src;
            if (src) this.createPlayer(audio, src, 'Audio');
        });
        scope.querySelectorAll('.custom-audio-player').forEach(player => {
            this.initPlayer(player);
        });
    },
    createPlayer(oldEl, src, name) {
        const safeName = (name || t('audio_def_name', 'Audio'))
            .replace(/<[^>]*>?/gm, '')
            .replace(/"/g, '&quot;');
            
        const info = this.parseAudioName(safeName);
        const uid = 'aud-' + Math.random().toString(36).substr(2, 9);
        const div = document.createElement('div');
        div.className = 'custom-audio-player';
        div.innerHTML = `
            <audio id="${uid}" preload="metadata" playsinline webkit-playsinline><source src="${src}"></audio>
            <button class="cap-play-btn" data-target="${uid}" type="button">▶</button>
            <div class="cap-body">
                <div class="cap-title" title="${safeName}">
                    ${info.artist ? `<span class="cap-artist">${info.artist}</span>` : ''}
                    <span class="cap-track">${info.track}</span>
                </div>
                <div class="cap-controls-row">
                    <div class="cap-info"><span class="cap-time">00:00</span></div>
                    <div class="cap-progress-container" data-target="${uid}"><div class="cap-wave-base"></div><div class="cap-wave-progress"></div></div>
                    <div class="cap-info"><button class="cap-speed-btn" data-target="${uid}" type="button">1x</button></div>
                    <a href="${src}" download="${safeName}" class="cap-download-btn" title="${t('download_title', 'Скачать')}">⬇</a>
                </div>
            </div>`;
        
        if (oldEl && oldEl.parentNode) {
            oldEl.parentNode.replaceChild(div, oldEl);
            this.initPlayer(div);
        }
    }
};
const WSManager = {
    socket: null,
    attempts: 0,
    buffer: [],
    hb: null,
    notifBtn: null,
    mode: 'ws', 
    pollingInterval: null,
    lastUpdateTs: Date.now() / 1000,
    boardId: null,
    init(boardId) {
        if (!boardId) return;
        this.boardId = boardId;
        const timestamps = Array.from(document.querySelectorAll('.ts-recalc'))
            .map(el => parseFloat(el.dataset.ts))
            .filter(ts => !isNaN(ts));
        
        if (timestamps.length > 0) {
            this.lastUpdateTs = Math.max(...timestamps);
        } else {
             this.lastUpdateTs = (Date.now() / 1000) - 60;
        }
        
        console.log(`[WS] Initialized. Last Post TS: ${this.lastUpdateTs}`);
        this.refreshContent();
        
        if (this.socket) { this.socket.close(); clearInterval(this.hb); }
        if (this.pollingInterval) clearInterval(this.pollingInterval);
        const chatList = document.querySelector('.chat-list');
        const threadList = document.querySelector('.thread-list');
        const repliesList = document.querySelector('.replies-list');
        this.isBoardIndex = !!threadList && !location.pathname.includes('/res/');
        
        const targetList = chatList || repliesList;
        if (targetList) {
            const oldBtn = document.querySelector('.new-posts-notification');
            if (oldBtn) oldBtn.remove();
            const notifBtn = document.createElement('div');
            notifBtn.className = 'new-posts-notification';
            notifBtn.innerHTML = t('new_msgs_btn');
            notifBtn.onclick = (e) => {
                e.preventDefault();
                e.stopPropagation();
                if (this.buffer.length > 0) {
                    const frag = document.createDocumentFragment();
                    const postsToRender = chatList ? [...this.buffer].reverse() : this.buffer;
                    postsToRender.forEach(el => {
                        frag.appendChild(el);
                        window.initializePostFeatures(el);
                        if (typeof GalleryManager !== 'undefined') GalleryManager.addFromPost(el);
                    });
                    if (chatList) targetList.prepend(frag);
                    else targetList.appendChild(frag);
                    this.buffer = [];
                }
                notifBtn.classList.remove('show');
                
                if (chatList) {
                    window.scrollTo({ top: 0, behavior: 'smooth' });
                } else {
                    window.scrollTo({ top: document.body.scrollHeight, behavior: 'smooth' });
                }
                
                document.title = document.body.dataset.siteName || 'ТГАЧ';
                this.unreadCount = 0;
                this.unseenPosts = [];
                const ind = document.getElementById('new-posts-indicator');
                if (ind) ind.style.display = 'none';
            };
            document.body.appendChild(notifBtn);
            this.notifBtn = notifBtn;
            window.addEventListener('scroll', () => {
                if ((window.innerHeight + window.scrollY) >= document.body.offsetHeight - 100) {
                    if (this.notifBtn) this.notifBtn.classList.remove('show');
                }
            });
        }
        if (this._visibilityHandler) {
            document.removeEventListener('visibilitychange', this._visibilityHandler);
        }
        this._visibilityHandler = () => {
            if (this.socket && (this.socket.readyState === WebSocket.CONNECTING || this.socket.readyState === WebSocket.OPEN)) {
                return;
            }
            if (!document.hidden && (!this.socket || this.socket.readyState === WebSocket.CLOSED)) {
                console.log('Tab woke up, forcing WS reconnect...');
                this.attempts = 0;
                this.startWS();
            }
        };
        document.addEventListener('visibilitychange', () => {
            if (!document.hidden) {
                this.refreshContent();
                this._visibilityHandler();
            }
        });
        this.startWS();
        window.addEventListener('online', () => {
            if (!this.socket || this.socket.readyState !== WebSocket.OPEN) {
                console.log('🌐 [WS] Network is back! Force reconnecting...');
                this.attempts = 0;
                this.startWS();
            }
        });

    },
    async refreshContent() {
        if (!this.boardId) return 0;
        const mobileBtnIcon = document.querySelector('#mobile-refresh-btn .mobile-nav-icon');
        if (mobileBtnIcon) {
            mobileBtnIcon.style.transition = 'transform 0.5s ease';
            mobileBtnIcon.style.transform = 'rotate(360deg)';
        }
        const ind = document.getElementById('ws-status-indicator');
        if(ind) ind.className = 'polling'; 
        
        let addedCount = 0;

        try {
            const res = await fetch(`/api/updates/${this.boardId}?since=${this.lastUpdateTs}`);
            if (res.ok) {
                const posts = await res.json();
                if (posts && posts.length > 0) {
                    if (posts.length > 150) {
                        console.warn(`[WS] Too many updates (${posts.length}), preventing freeze.`);
                        showToast(t('ws_too_many_posts', 'Слишком много новых постов. Перезагрузка...'), 3000);
                        setTimeout(() => window.location.reload(), 1000);
                        return 0;
                    }

                    posts.sort((a,b) => a.timestamp - b.timestamp);
                    
                    const fragment = document.createDocumentFragment();
                    let hasAdditions = false;
                    let shouldNotify = false;

                    posts.forEach(p => {
                        this.lastUpdateTs = Math.max(this.lastUpdateTs, p.timestamp);
                        if (!document.getElementById(`post-${p.id}`)) {
                            const inserted = this.handleData(p, fragment); 
                            if (inserted) {
                                hasAdditions = true;
                                addedCount++;
                                if (String(p.author_id) !== UserState.userId) shouldNotify = true;
                            }
                        }
                    });

                    if (hasAdditions) {
                        const target = document.querySelector('.chat-list') || document.querySelector('.thread-list') || document.querySelector('.replies-list');
                        if (target) {
                            if (target.classList.contains('chat-list')) {
                                target.prepend(fragment);
                            } else {
                                target.appendChild(fragment);
                            }
                            
                            // Post-insert initializations
                            requestAnimationFrame(() => {
                                const newElements = target.querySelectorAll('.new-post-animation:not([data-ws-init])');
                                newElements.forEach(el => {
                                    el.dataset.wsInit = "true";
                                    window.initializePostFeatures(el);
                                });
                            });
                        }
                    
                        if (shouldNotify) {
                            this.updateTitleCount();
                            if (SettingsManager.current.soundEnabled && (document.hidden || window.scrollY > 200)) {
                                const sfx = notificationSound.cloneNode();
                                sfx.volume = notificationSound.volume;
                                sfx.play().catch(()=>{});
                            }
                            if (document.hidden) sendDesktopNotification("Новые посты", `+${posts.length} новых сообщений`);
                            if (typeof MascotManager !== 'undefined') MascotManager.onNewPost();
                        }
                    }
                }
            }
        } catch (e) {
            console.error("Refresh failed", e);
        } finally {
            if(ind) ind.className = 'online';
            if (mobileBtnIcon) {
                setTimeout(() => {
                    mobileBtnIcon.style.transition = 'none';
                    mobileBtnIcon.style.transform = 'none';
                }, 500);
            }
        }
        return addedCount;
    },
    startWS() {
        if (!document.getElementById('ws-status-indicator')) {
            const ind = document.createElement('div');
            ind.id = 'ws-status-indicator';
            ind.title = t('ws_connecting');
            document.body.appendChild(ind);
        }
        const ind = document.getElementById('ws-status-indicator');
        
        if (this.attempts > 3) {
            console.warn('WS unstable. Switching to Polling mode (15s).');
            this.mode = 'polling';
            this.startPolling();
            if(ind) { ind.className = 'polling'; ind.title = t('ws_polling'); }
            return;
        }
        const proto = location.protocol === 'https:' ? 'wss' : 'ws';
        const type = (this.isBoardIndex || document.querySelector('.catalog-grid')) ? 'threads' : 'chat';
        this.socket = new WebSocket(`${proto}://${location.host}/ws/${this.boardId}/${type}`);
        this.socket.onopen = () => {
            console.log('WS Connected');
            this.attempts = 0;
            if(ind) { ind.className = 'online'; ind.title = t('ws_online'); }
            if (this.lastUpdateTs > 0) {
                fetch(`/api/updates/${this.boardId}?since=${this.lastUpdateTs}`)
                    .then(r => r.json())
                    .then(posts => {
                        if (posts && posts.length) {
                            if (posts.length > 150) {
                                showToast(t('ws_too_many_posts'), 10000);
                                if (this.notifBtn) {
                                    this.notifBtn.innerHTML = t('btn_refresh_many');
                                    this.notifBtn.onclick = () => location.reload();
                                    this.notifBtn.classList.add('show');
                                }
                                return;
                            }
                            console.log(`[WS] Recovered ${posts.length} missed posts`);
                            posts.sort((a,b) => a.timestamp - b.timestamp);
                            const fragment = document.createDocumentFragment();
                            let hasUpdates = false;
                            posts.forEach(p => {
                                this.lastUpdateTs = Math.max(this.lastUpdateTs, p.timestamp);
                                if (!document.getElementById(`post-${p.id}`)) {
                                    const inserted = this.handleData(p, fragment); 
                                    if(inserted) hasUpdates = true;
                                }
                            });
                            if (hasUpdates) {
                                const list = document.querySelector('.chat-list') || document.querySelector('.thread-list');
                                if (list) {
                                    if (list.classList.contains('chat-list')) list.prepend(fragment);
                                    else list.appendChild(fragment);
                                }
                            }
                        }
                    })
                    .catch(e => console.warn("[WS] Recovery failed", e));
            }
            this.hb = setInterval(() => { 
                if (this.socket.readyState === 1) {
                    this.socket.send('ping');
                    this._pongTimeout = setTimeout(() => {
                        console.warn('WS: Pong timeout, forcing reconnect');
                        this.socket.close();
                    }, 10000);
                }
            }, 40000);
        };
        this.socket.onclose = () => {
            if(ind) { ind.className = 'offline'; ind.title = t('ws_offline'); }
            clearInterval(this.hb);
            this.attempts++;
            const baseDelay = Math.min(1000 * (2 ** this.attempts), 10000);
            const jitter = Math.random() * 3000; 
            setTimeout(() => this.startWS(), baseDelay + jitter);
        };
        this.socket.onmessage = (e) => {
            if (this._watchdogTimer) clearTimeout(this._watchdogTimer);
            this._watchdogTimer = setTimeout(() => {
                this.socket.close(); 
            }, 70000);
            
            if (e.data === 'ping' || e.data === 'pong') return;

            try {
                const data = JSON.parse(e.data);
                if (data.event_type === 'delete') {
                    if (typeof removePostFromDOM === 'function') removePostFromDOM(data.post_num);
                    const catalogCard = document.querySelector(`.catalog-item[href*="${data.post_num}.html"]`);
                    if (catalogCard) catalogCard.remove();
                    return;
                }
                if (data.event_type === 'update' && data.post) {
                    const existingPost = document.getElementById(`post-${data.post.id}`);
                    if (existingPost) {
                        const newPost = PostRenderer.create(data.post, location.pathname.includes('/res/') ? 'thread' : 'board');
                        existingPost.innerHTML = newPost.innerHTML;
                        window.initializePostFeatures(existingPost);
                        window.parseTextEffects(existingPost);
                        existingPost.classList.add('target-highlight');
                        setTimeout(() => existingPost.classList.remove('target-highlight'), 1000);
                    }
                    return;
                }
                this.lastUpdateTs = Math.max(this.lastUpdateTs, data.timestamp);
                const isMine = String(data.author_id) === UserState.userId;
                const isReplyToMe = data.reply_to_post_num && UserState.postIds.has(String(data.reply_to_post_num));
                
                if (this.handleData(data)) {
                    if (!isMine && SettingsManager.current.soundEnabled && (document.hidden || window.scrollY > 300)) {
                        notificationSound.cloneNode().play().catch(() => {});
                    }
                    if (isReplyToMe && !isMine) {
                        showToast('✉️ ' + t('reply_to_me_toast', 'Вам ответили!'));
                        if (typeof SystemDock !== 'undefined') {
                            SystemDock.checkUnread();
                            SystemDock.renderReplies(true);
                        }
                    }
                }
            } catch(e) { console.error("WS Message Error:", e); }
        };
    },
    startPolling() {
        this.pollingInterval = setInterval(async () => {
            try {
                const res = await fetch(`/api/updates/${this.boardId}?since=${this.lastUpdateTs}`);
                if (res.ok) {
                    const posts = await res.json();
                    if (posts.length > 0) {
                        posts.sort((a,b) => a.timestamp - b.timestamp);
                        posts.forEach(p => {
                            if (!document.getElementById(`post-${p.id}`)) {
                                this.handleData(p);
                            }
                        });
                        this.lastUpdateTs = posts[posts.length-1].timestamp;
                    }
                }
            } catch(e) { console.error("Polling error", e); }
        }, 15000); 
    },
    updateTitleCount() {
        if (document.hidden) {
            this.unreadCount = (this.unreadCount || 0) + 1;
            const siteName = document.body.dataset.siteName || 'ТГАЧ';
            document.title = `(${this.unreadCount}) ${siteName}`;
        }
    },
    showIndicator(count) {
        const ind = document.getElementById('new-posts-indicator');
        if (!ind) return;
        ind.textContent = count + " " + t('new_msgs_btn');
        ind.style.display = 'block';
        ind.onclick = (e) => {
            e.preventDefault();
            const first = this.unseenPosts[0];
            if (first) {
                first.scrollIntoView({ behavior: 'smooth', block: 'center' });
                first.classList.add('target-highlight');
                setTimeout(() => first.classList.remove('target-highlight'), 1500);
            }
            ind.style.display = 'none';
            this.unseenPosts = [];
        };
    },
    updateCatalog(data) {
        const grid = document.querySelector('.catalog-grid');
        if (!grid) return;
        
        const threadId = data.is_op_post ? data.id : data.thread_id;
        const card = grid.querySelector(`a.catalog-item[href*="/res/${threadId}.html"]`);
        
        if (card) {
            const repliesEl = card.querySelector('.stat-replies');
            const imagesEl = card.querySelector('.stat-images');
            
            if (repliesEl) {
                let rCount = parseInt(repliesEl.textContent.match(/\d+/) || 0) + 1;
                repliesEl.textContent = `R: ${rCount}`;
                repliesEl.classList.add('catalog-stats-update');
                setTimeout(() => repliesEl.classList.remove('catalog-stats-update'), 1000);
            }
            
            if (imagesEl && data.content.files?.length) {
                let iCount = parseInt(imagesEl.textContent.match(/\d+/) || 0) + data.content.files.length;
                imagesEl.textContent = `I: ${iCount}`;
            }

            const sortMode = new URLSearchParams(location.search).get('sort');
            if ((!sortMode || sortMode === 'bump') && !data.sage) {
                card.classList.remove('bump-anim');
                void card.offsetWidth;
                card.classList.add('bump-anim');
                grid.prepend(card);
            }
        } else if (data.is_op_post) {
            const newCard = PostRenderer.createCatalogCard(data);
            grid.prepend(newCard);
            if (typeof SmartLoader !== 'undefined') SmartLoader.scan(newCard);
            showToast(t('new_thread_toast'));
        }
    },
    
    handleData(data, batchFragment = null) {
        if (window.location.pathname.includes('/search')) return false;
        if (!window._recentSentIds) window._recentSentIds = new Set();
        if (document.getElementById(`post-${data.id}`)) return false;
        if (window._recentSentIds.has(String(data.id))) return false;

        try {
            const chatList = document.querySelector('.chat-list');
            const repliesList = document.querySelector('.replies-list');
            const isChat = !!chatList;
            const targetList = chatList || repliesList;
            const catalogGrid = document.querySelector('.catalog-grid');
            
            let currentThreadId = null;
            if (!isChat && !this.isBoardIndex && !catalogGrid && location.pathname.includes('/res/')) {
                currentThreadId = location.pathname.split('/').pop().replace('.html','');
            }
            
            if (isChat && data.thread_id) return false;
            if (!isChat && !this.isBoardIndex && !catalogGrid && String(data.thread_id || data.id) !== String(currentThreadId)) return false;
            
            if (catalogGrid) {
                this.updateCatalog(data);
                return false;
            }
            
            const isOverboard = location.pathname.includes('/overboard/');
            if (this.isBoardIndex || isOverboard) {
                const threadId = data.thread_id || data.id;
                const threadCard = document.getElementById(`thread-${threadId}`);
                if (threadCard) {
                    const statsEl = threadCard.querySelector('.thread-stats') || threadCard.querySelector('.thread-stats-container');
                    if (statsEl) {
                        const text = statsEl.textContent;
                        const replyMatch = text.match(/\[\s*(\d+)/); 
                        let repliesCount = replyMatch ? parseInt(replyMatch[1]) + 1 : 1;
                        
                        if (isOverboard) {
                            statsEl.childNodes[0].nodeValue = statsEl.childNodes[0].nodeValue.replace(/\d+/, repliesCount);
                        } else {
                            const anonsMatch = text.match(/\/\s*(\d+|\?)/);
                            const anons = anonsMatch ? anonsMatch[1] : '?';
                            
                            statsEl.innerHTML = `[ ${repliesCount} / ${anons} ${t('anons_count')} ] <span style="color:var(--accent-primary); font-size:0.8em;">●</span>`;
                        }
                    }

                    if (!data.is_op_post) {
                        let repliesList = threadCard.querySelector('.latest-replies-container');
                        if (!repliesList) {
                            repliesList = document.createElement('div');
                            repliesList.className = 'latest-replies-container';
                            if (isOverboard) {
                                repliesList.style.cssText = 'margin-top: 10px; padding-top: 5px; border-top: 1px dashed var(--border-separator);';
                            }
                            threadCard.appendChild(repliesList);
                        }
                        
                        const limit = isOverboard ? 2 : 3;
                        const newPreview = PostRenderer.create(data, 'board-reply');
                        newPreview.classList.add('reply-preview', 'new-post-animation');
                        if (isOverboard) newPreview.classList.add('overboard-reply');
                        
                        repliesList.appendChild(newPreview);
                        
                        while (repliesList.children.length > limit) {
                            repliesList.firstElementChild.remove();
                        }
                        window.initializePostFeatures(newPreview);
                    }

                    if (!data.sage) {
                        const container = document.querySelector('.thread-list');
                        threadCard.style.transition = 'transform 0.5s';
                        container.prepend(threadCard);
                        threadCard.classList.remove('bump-anim');
                        void threadCard.offsetWidth;
                        threadCard.classList.add('bump-anim');
                    }
                }
                else if (data.is_op_post && !batchFragment) {
                    const container = document.querySelector('.thread-list');
                    if (container) {
                        const newThread = ScrollManager.createThreadElement(data);
                        newThread.classList.add('new-post-animation');
                        container.prepend(newThread);
                        window.initializePostFeatures(newThread);
                        if (typeof GalleryManager !== 'undefined') GalleryManager.addFromPost(newThread);
                    }
                    showToast(t('new_thread_toast'));
                }
                return false;
            }
            
            const existingEl = document.getElementById(`post-${data.id}`);
            if (existingEl) {
                const newEl = PostRenderer.create(data, isChat ? 'chat' : 'thread');
                existingEl.querySelector('.post-header').innerHTML = newEl.querySelector('.post-header').innerHTML;
                existingEl.querySelector('.post-content').innerHTML = newEl.querySelector('.post-content').innerHTML;
                window.initializePostFeatures(existingEl);
                window.parseTextEffects(existingEl);
                return false;
            }

            const isMine = String(data.author_id) === UserState.userId;
            
            if (!batchFragment && !isMine) {
                this.updateTitleCount();
                if (SettingsManager.current.soundEnabled && (document.hidden || window.scrollY > 200)) {
                    const now = Date.now();
                    if (!this._lastSoundTime || (now - this._lastSoundTime > 200)) {
                        const sfx = notificationSound.cloneNode(); 
                        sfx.volume = notificationSound.volume;
                        sfx.play().catch(()=>{});
                        this._lastSoundTime = now;
                    }
                }
                if (document.hidden) {
                    sendDesktopNotification("Новый пост", data.content.text || "Медиа");
                }
                if (typeof MascotManager !== 'undefined') MascotManager.onNewPost();
            }
            
            const el = PostRenderer.create(data, isChat ? 'chat' : 'thread');
            window.parseTextEffects(el);
            const isScrolledToBottom = (window.innerHeight + window.scrollY) >= document.body.offsetHeight - 300;
            if (batchFragment) {
                batchFragment.appendChild(el);
                // Отложенная инициализация фич делается снаружи batchFragment, но здесь вызываем для гарантии структуры
                // initializePostFeatures будет вызван для фрагмента целиком в loadMore
                
                if (data.reply_to_post_num) {
                    this._updateBacklinks(data.reply_to_post_num, data.id, data.board_id, data.thread_id, batchFragment);
                }
                return true; 
            }

            if (isChat) {
                if (window.scrollY < 400 || isMine) {
                    const prevHeight = targetList.scrollHeight;
                    const prevScroll = window.scrollY;
                    targetList.prepend(el);
                    if (prevScroll > 0) {
                        const newHeight = targetList.scrollHeight;
                        window.scrollTo(0, prevScroll + (newHeight - prevHeight));
                    }
                    el.classList.add('new-post-animation');
                    setTimeout(() => el.classList.remove('new-post-animation'), 10000);
                    window.initializePostFeatures(el);
                    if (typeof GalleryManager !== 'undefined') GalleryManager.addFromPost(el);
                    if (window.scrollY === 0) window.scrollTo(0, 0);
                    if (targetList.children.length > 100) {
                        targetList.lastElementChild.remove();
                    }
                } else {
                    this.buffer.push(el);
                    if (this.buffer.length > 100) {
                        const dropped = this.buffer.shift();
                        if (dropped) dropped.remove(); 
                    }
                    if (this.notifBtn) {
                        const count = this.buffer.length;
                        this.notifBtn.textContent = count >= 100 ? `99+ ${t('new_msgs_btn')}` : t('show_new_msgs').replace('{0}', count);
                        this.notifBtn.classList.add('show');
                    }
                    this.updateTitleCount();
                }
            } else {
                const tId = location.pathname.split('/').pop().replace('.html','');
                if (String(data.thread_id || data.id) === tId) {
                    targetList.appendChild(el);
                    window.initializePostFeatures(el);
                    if (typeof GalleryManager !== 'undefined') GalleryManager.addFromPost(el); 
                    if (isMine) {
                        el.scrollIntoView({behavior:'smooth', block:'center'});
                    } else if (!isScrolledToBottom) {
                        if (this.notifBtn && chatList) {
                            this.buffer.push(el);
                            this.notifBtn.textContent = t('new_msgs_btn');
                            this.notifBtn.classList.add('show');
                            this.buffer = [];
                        } else {
                            if (!this.unseenPosts) this.unseenPosts = [];
                            this.unseenPosts.push(el);
                            this.showIndicator(this.unseenPosts.length);
                        }
                    }
                }
            }
            
            if (data.reply_to_post_num) {
                this._updateBacklinks(data.reply_to_post_num, data.id, data.board_id, data.thread_id);
            }
            return true;

        } catch (err) { console.error("Data Handle Error:", err); return false; }
    },

    _updateBacklinks(parentId, childId, boardId, threadId, context = document) {
        const parentPost = document.getElementById(`post-${parentId}`);
        if (parentPost) {
            let backlinksDiv = parentPost.querySelector('.backlinks-container');
            if (!backlinksDiv) {
                const contentDiv = parentPost.querySelector('.post-text-content');
                if (contentDiv) {
                    backlinksDiv = document.createElement('div');
                    backlinksDiv.className = 'backlinks-container';
                    backlinksDiv.id = `backlinks-for-${parentId}`;
                    contentDiv.appendChild(backlinksDiv);
                }
            }
            if (backlinksDiv && !backlinksDiv.querySelector(`[data-post-num="${childId}"]`)) {
                const link = document.createElement('a');
                const tId = threadId || parentId;
                const isThreadPage = tId && location.pathname.includes(`/res/${tId}.html`);
                link.href = isThreadPage ? `#post-${childId}` : `/${boardId}/res/${tId}.html#post-${childId}`;
                link.className = 'post-link';
                link.dataset.postNum = childId;
                link.textContent = `>>${childId}`;
                if (typeof PreviewManager !== 'undefined') {
                    link.addEventListener('mouseenter', (e) => PreviewManager.onHover(e, link));
                    link.addEventListener('mouseleave', (e) => PreviewManager.onOut(e));
                }
                if (backlinksDiv.childNodes.length > 0) {
                        backlinksDiv.appendChild(document.createTextNode(" "));
                }
                backlinksDiv.appendChild(link);
                if (context === document) {
                    const originalBg = parentPost.style.backgroundColor;
                    parentPost.style.transition = 'background-color 0.5s';
                    parentPost.style.backgroundColor = 'var(--bg-hover)';
                    setTimeout(() => parentPost.style.backgroundColor = originalBg, 1000);
                }
            }
        }
    }
};
const PostRenderer = {
    create(data, type) {
        const esc = (str) => {
            if (!str) return "";
            return String(str)
                .replace(/&/g, "&amp;")
                .replace(/</g, "&lt;")
                .replace(/>/g, "&gt;")
                .replace(/"/g, "&quot;")
                .replace(/'/g, "&#039;");
        };

        const div = document.createElement('div');
        const isMine = UserState.postIds.has(String(data.id));
        const threadOpId = document.body.dataset.opAuthorId;
        const currentUserId = document.body.dataset.userId;
        const isUserOp = (currentUserId && threadOpId === currentUserId);
        let opModOverlay = '';
        let extraClasses = '';
        if (data.is_op_hidden) {
            extraClasses += ' op-hidden';
            opModOverlay = `<div class="op-hidden-overlay">${t('op_hidden_label')}</div>`;
        }
        let opButton = '';
        if (isUserOp && String(data.id) !== String(data.thread_id)) {
             const actionText = data.is_op_hidden ? t('op_show_btn') : t('op_hide_btn');
             const actionType = data.is_op_hidden ? "show" : "hide";
             opButton = `<a href="#" class="action-op-mod" data-post-num="${data.id}" data-action="${actionType}" style="color:#e67e22;">${actionText}</a>`;
        }
        const isSage = data.content && data.content.sage === true;
        div.className = `post ${isMine ? 'is-yours' : ''} ${isSage ? 'is-sage' : ''}`;
        div.id = `post-${data.id}`;
        div.dataset.reports = data.report_count || 0;
        const isOverboard = location.pathname.includes('/overboard/');
        const boardTag = isOverboard ? `<a href="/${data.board_id}/" class="is-op-badge" style="background:var(--text-secondary); margin-right:5px; text-decoration:none;">/${data.board_id}/</a>` : '';
        const icon = `<canvas class="identicon" data-userid="${data.author_id}" width="20" height="20"></canvas>`;
        const youMarker = isMine ? '<span class="you-marker">(You)</span>' : '';
        let hrefUrl = "";
        const tId = data.thread_id || data.id;
        const bId = data.board_id || document.body.dataset.currentBoard || 'b';
        
        const isThreadPage = location.pathname.includes('/res/');
        
        if (isThreadPage && String(tId) === location.pathname.split('/').pop().replace('.html','')) {
            hrefUrl = `#post-${data.id}`;
        } else {
            hrefUrl = `/${bId}/res/${tId}.html#post-${data.id}`;
        }
        
        const link = `<a href="${hrefUrl}" class="post-num-link" data-post-num="${data.id}">#${data.id}</a> ${youMarker}`;
        let threadLink = '';
        let openThreadBtn = '';
        
        if (type === 'board') {
            const pCount = data.posts_count || (data.reply_count + 1) || 1;
            const aCount = data.anon_count || 1;
            const statsHtml = `
                <span class="thread-stats">
                    [ ${pCount} / ${aCount} ${t('anons_count')} ]
                </span>
            `;
            threadLink = `${statsHtml} <a href="/${data.board_id}/res/${data.thread_id||data.id}.html" class="thread-link-btn">[${t('thread_link')}]</a>`;
            const label = t('btn_goto_thread', '[ В тред ]');
            openThreadBtn = `<a href="/${data.board_id}/res/${data.id}.html" class="post-header-link">${label}</a>`;
        }
        let imagesHtml = '';
        let audiosHtml = '';
        if (data.content.files?.length) {
            let imgContent = '';
            let audContent = '';
            data.content.files.forEach(f => {
                const url = f.original_url || `/files/${f.original_file_id}`;
                let thumbCandidate = f.thumbnail_url || (f.thumbnail_file_id ? `/files/${f.thumbnail_file_id}` : null);
                const isVideoType = ['video', 'gif', 'animation', 'video_note'].includes(f.type);
                if (!thumbCandidate) {
                    if (isVideoType) {
                        thumbCandidate = '';
                    } else {
                        thumbCandidate = url; 
                    }
                }
                const safeThumb = thumbCandidate ? esc(thumbCandidate) : '';
                const safeUrl = esc(url);
                const filename = esc(f.filename || t('default_filename'));
                const placeholder = "data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7";
                const fileIdAttr = f.original_file_id ? `data-file-id="${esc(f.original_file_id)}"` : '';

                if (['audio', 'voice'].includes(f.type)) {
                     const uniqueId = `audio-${data.id}-${Math.random().toString(36).substr(2, 5)}`;
                     const audioInfo = AudioManager.parseAudioName(filename);
                     audContent += `<div class="custom-audio-player" id="player-${uniqueId}" data-src="${safeUrl}">
                        <audio id="${uniqueId}" preload="none"><source src="${safeUrl}" type="${esc(f.mime_type) || 'audio/mpeg'}"></audio>
                        <button class="cap-play-btn" data-target="${uniqueId}" type="button">▶</button>
                        <div class="cap-body">
                            <div class="cap-title" title="${filename}">
                                ${audioInfo.artist ? `<span class="cap-artist">${audioInfo.artist}</span>` : ''}
                                <span class="cap-track">${audioInfo.track}</span>
                            </div>
                            <div class="cap-controls-row">
                                <div class="cap-info"><span class="cap-time">00:00</span></div>
                                <div class="cap-progress-container" data-target="${uniqueId}"><div class="cap-wave-base"></div><div class="cap-wave-progress"></div></div>
                                <div class="cap-info"><button class="cap-speed-btn" data-target="${uniqueId}" type="button">1x</button></div>
                                <a href="${safeUrl}" download="${filename}" class="cap-download-btn" title="${t('download_title', 'Скачать')}">⬇</a>
                            </div>
                        </div>
                    </div>`;
                }
                else if (f.type === 'document') {
                     audContent += `<div style="margin-top:5px;"><a href="${safeUrl}" target="_blank" class="file-document-link">📄 ${filename}</a></div>`;
                }
                else {
                     if (f.type === 'sticker') {
                        const isWebm = f.filename && f.filename.endsWith('.webm');
                        imgContent += `<div class="sticker-wrapper" style="display:inline-block; vertical-align:bottom;">
                            <a href="${safeUrl}" class="gallery-trigger" data-type="${isWebm ? 'video' : 'image'}" data-filename="${filename}" ${fileIdAttr}>
                                ${isWebm 
                                    ? `<video class="post-sticker" src="${safeUrl}" autoplay loop muted playsinline webkit-playsinline style="max-height: 150px; width: auto; background: transparent; display: block;" referrerpolicy="no-referrer"></video>`
                                    : `<img src="${placeholder}" data-src="${safeThumb || safeUrl}" class="post-sticker lazy-load" style="max-height: 150px;" onclick="event.stopPropagation();" referrerpolicy="no-referrer">`
                                }
                            </a>
                        </div>`;
                    }
                    else if (f.type === 'video_note') {
                        if (type === 'chat') {
                            imgContent += `<video class="post-video video-note lazy-load" data-src="${safeUrl}" ${safeThumb ? `poster="${safeThumb}"` : ''} autoplay loop muted playsinline onclick="event.preventDefault(); event.stopPropagation(); this.paused ? this.play() : this.pause();" style="border-radius: 50%; object-fit: cover;" referrerpolicy="no-referrer"></video>`;
                        } else {
                            const badgeType = 'VIDEO'; 
                            imgContent += `<div class="file-thumb lazy-media-wrapper" data-src="${safeUrl}" data-filename="${filename}" data-type="video">
                                ${blurHtml || ''}
                                <video class="post-image lazy-load" data-src="${safeUrl}" ${safeThumb ? `poster="${safeThumb}"` : ''} preload="metadata" muted playsinline loop style="object-fit: cover; width: 100%; height: 100%; background: #000;"></video>
                                <span class="lazy-badge">${badgeType}</span>
                                ${bayanHtml || ''}
                            </div>`;
                        }
                    }
                    else if (['image', 'photo', 'video', 'gif', 'animation', 'video_note'].includes(f.type)) {
                        const isVid = ['video', 'gif', 'animation', 'video_note'].includes(f.type);
                        let blurHtml = '';
                        if (f.blurhash) {
                            blurHtml = `<canvas class="blurhash-canvas" data-hash="${esc(f.blurhash)}" width="32" height="32"></canvas>`;
                        }
                        let bayanHtml = '';
                        const dCount = parseInt(f.dupe_count || 0);
                        if (dCount > 1) {
                            bayanHtml = `<div class="bayan-text" title="Этот файл уже встречался ${dCount} раз">♻️ ${t('label_duplicate', 'Повтор')} (${dCount})</div>`;
                        }

                        if (isVid) {
                            const badgeType = (f.type === 'gif' || f.type === 'animation') ? 'GIF' : 'VIDEO';
                            imgContent += `<div class="file-thumb lazy-media-wrapper" data-src="${safeUrl}" data-filename="${filename}" data-type="${f.type === 'gif' ? 'gif' : 'video'}">
                                ${blurHtml}
                                <video class="post-image lazy-load" data-src="${safeUrl}" ${safeThumb ? `poster="${safeThumb}"` : ''} preload="metadata" muted playsinline loop style="object-fit: cover; width: 100%; height: 100%; background: #000;"></video>
                                <span class="lazy-badge">${badgeType}</span>
                                ${bayanHtml}
                            </div>`;
                        } else {
                            imgContent += `<a href="${safeUrl}" class="file-thumb" data-filename="${filename}" data-src="${safeUrl}" data-type="image" ${fileIdAttr}>
                                ${blurHtml}
                                <img src="${placeholder}" data-src="${safeThumb || safeUrl}" class="post-image lazy-load" alt="img" referrerpolicy="no-referrer" onload="this.classList.add('loaded')">
                                ${bayanHtml}
                            </a>`;
                        }
                    }
                }
            });
            if (imgContent) imagesHtml = `<div class="post-files-container post-gallery">${imgContent}</div>`;
            if (audContent) audiosHtml = `<div class="post-audios-container">${audContent}</div>`;
        }
        let replyIndicator = '';
        if (data.reply_to_post_num && String(data.reply_to_post_num) !== String(data.thread_id)) {
            const txt = data.content.text || "";
            const re = new RegExp(`(?:>>|&gt;&gt;)\\s*${data.reply_to_post_num}(?:[^0-9]|$)`, 'i');
            if (!re.test(txt)) {
                replyIndicator = `<a href="#post-${data.reply_to_post_num}" class="post-link" data-post-num="${data.reply_to_post_num}" style="color:var(--accent-link); text-decoration:none; font-weight:bold;">&gt;&gt;${data.reply_to_post_num}</a><br>`;
            }
        }
        let textContent = '';
        if (data.content.poll_data) {
            const poll = data.content.poll_data;
            const safeQuestion = esc(poll.question);
            const options = poll.options || [];
            const votes = poll.votes || {};
            
            let totalVotes = 0;
            Object.values(votes).forEach(vList => { totalVotes += vList.length; });

            let optionsHtml = '';
            options.forEach((opt, idx) => {
                const count = votes[String(idx)] ? votes[String(idx)].length : 0;
                const pct = totalVotes > 0 ? (count / totalVotes * 100).toFixed(1) : 0;
                
                optionsHtml += `
                    <div class="poll-option-display" data-post-num="${data.id}" data-option-index="${idx}">
                        <div class="poll-option-text">${esc(opt)} (${count})</div>
                        <div class="poll-bar-container">
                            <div class="poll-bar" style="width: ${pct}%;"></div>
                        </div>
                    </div>`;
            });

            textContent = `
                <div class="poll-display" data-post-num="${data.id}">
                    <h4>📊 ${safeQuestion}</h4>
                    <div class="poll-options-list">${optionsHtml}</div>
                </div>`;
        } else {
            const opId = data.thread_id || data.id;
            const safeText = window.formatTextGlobal(data.content.text, opId, data.board_id, data.thread_id);
            textContent = `<div class="post-text">${safeText}</div>`;
        }
        let externalLinksHtml = '';
        if (data.external_links && data.external_links.length > 0) {
            const links = data.external_links.map(l => {
                return `<a href="/${l.board}/res/0#post-${l.post}" class="external-link-tag">
                            <span class="ext-board">/${l.board}/</span>#${l.post}
                        </a>`;
            }).join(' ');
            externalLinksHtml = `
                <div class="external-links-container">
                    <span class="ext-label">${t('mentioned_in')}</span> ${links}
                </div>`;
        }
        const safeThreadId = data.thread_id ? data.thread_id : '';
        const actions = `
            <div class="post-actions">
                <button class="action-menu-trigger"
                    data-post-num="${data.id}"
                    data-board-id="${data.board_id}"
                    data-thread-id="${safeThreadId}"
                    data-author-id="${data.author_id}">
                    ▶
                </button>
            </div>`;

        let backlinksHtml = '';
        if (data.backlinks && data.backlinks.length > 0) {
            const tId = data.thread_id || data.id;
            const isThreadPage = tId && location.pathname.includes(`/res/${tId}.html`);
            data.backlinks.forEach(bl => {
                const blUrl = isThreadPage ? `#post-${bl}` : `/${data.board_id}/res/${tId}.html#post-${bl}`;
                backlinksHtml += `<a href="${blUrl}" class="post-link" data-post-num="${bl}" onclick="event.stopPropagation();">&gt;&gt;${bl}</a> `;
            });
        }
        const isBoardReply = (type === 'board-reply');
        const contentStyle = (isBoardReply && isOverboard) ? 'style="flex-direction: row; align-items: flex-start; flex-wrap: wrap;"' : '';

        div.innerHTML = `
        <div class="post-header">${boardTag}${icon}<span>${t('anon_name')} </span>${link}<span class="ts-recalc" data-ts="${data.timestamp}">${formatTimestamp(data.timestamp)}</span>${isSage ? '<span class="sage-indicator">SAGE</span>' : ''}
        ${type === 'board' ? `<a href="#" class="favourite-btn" data-thread-id="${data.id}" title="${t('fav_title')}">⭐</a>` : ''}
        ${openThreadBtn}
        ${actions}</div>
            <div class="post-content" ${contentStyle}>
                ${opModOverlay}
                ${imagesHtml}
                ${audiosHtml}
                <div class="post-text-content">
                    ${replyIndicator}
                    ${textContent}
                    ${externalLinksHtml}
                    <div class="backlinks-container" id="backlinks-for-${data.id}">${backlinksHtml}</div>
                </div>
            </div>
        `;
        return div;
    },
    createCatalogCard(data) {
        const esc = (str) => String(str || "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
        const threadId = data.id;
        const hue = (parseInt(threadId) * 137) % 360;
        const bgColor = `hsl(${hue}, 60%, 40%)`;
        
        let thumbHtml = '';
        const files = data.content.files || [];
        if (files.length > 0) {
            const f = files[0];
            const isVid = ['video', 'gif', 'animation', 'video_note'].includes(f.type);
            if (isVid) {
                thumbHtml = `
                    <div class="catalog-thumb lazy-media-wrapper" data-src="${f.original_url}" data-type="video" style="background-color: #000;">
                        <video class="lazy-load" preload="metadata" muted playsinline loop data-src="${f.original_url}" poster="${f.thumbnail_url || ''}" style="width: 100%; height: 100%; object-fit: cover;"></video>
                        <span class="lazy-badge" style="position:absolute; bottom:5px; right:5px; background:rgba(0,0,0,0.6); color:white; padding:2px 4px; font-size:10px; border-radius:3px;">VIDEO</span>
                    </div>`;
            } else {
                thumbHtml = `<div class="catalog-thumb"><img src="${f.thumbnail_url || f.original_url}" class="lazy-load" style="width: 100%; height: 100%; object-fit: cover;" referrerpolicy="no-referrer"></div>`;
            }
        } else {
            thumbHtml = `<div class="catalog-thumb" style="background-color: ${bgColor};"><div class="catalog-ambient"><span>📝</span><small>${esc(data.content.text).substring(0, 40)}</small></div></div>`;
        }

        const badges = `
            ${data.is_pinned ? '<div class="cat-badge badge-pin">PIN</div>' : ''}
            ${data.is_endless ? '<div class="cat-badge badge-cyclic">🔄</div>' : ''}
            ${data.is_archived ? '<div class="cat-badge badge-lock">LOCK</div>' : ''}
        `;

        const typeBadge = data.thread_type !== 'default' ? `<span style="color:var(--accent-primary); font-weight:bold;">[${data.thread_type.toUpperCase()}]</span> ` : '';
        const title = data.thread_title || `№${threadId}`;

        const card = document.createElement('a');
        card.href = `/${data.board_id}/res/${threadId}.html`;
        card.className = 'catalog-item new-post-animation';
        card.innerHTML = `
            <div style="position:relative; width:100%; height:100%;">
                ${thumbHtml}
                ${badges}
            </div>
            <div class="catalog-info">
                <div class="catalog-stats-line">
                    <span class="stat-replies">R: 0</span> <span style="opacity:0.3">|</span> 
                    <span class="stat-images">I: ${files.length}</span>
                </div>
                <div class="catalog-text-content">
                    <span class="catalog-subject">${typeBadge}${esc(title)}</span>
                    ${esc(data.content.text)}
                </div>
            </div>
        `;
        return card;
    },
     
};
function initTruncatePosts(el) {
    const textContents = el.querySelectorAll('.post-text-content');
    textContents.forEach(content => {
        if (content.dataset.truncated) return; 
        const MAX_HEIGHT = 400;
        if (content.scrollHeight > MAX_HEIGHT) {
            content.style.maxHeight = MAX_HEIGHT + 'px';
            content.style.overflow = 'hidden';
            content.style.position = 'relative';
            content.style.maskImage = 'linear-gradient(to bottom, black 80%, transparent 100%)';
            content.style.webkitMaskImage = 'linear-gradient(to bottom, black 80%, transparent 100%)';
            const btn = document.createElement('div');
            btn.className = 'expand-post-btn';
            btn.textContent = `⬇ Читать дальше`;
            btn.onclick = (e) => {
                e.stopPropagation();
                e.preventDefault();
                content.style.maxHeight = 'none';
                content.style.maskImage = 'none';
                content.style.webkitMaskImage = 'none';
                btn.remove();
            };
            content.parentNode.insertBefore(btn, content.nextSibling);
            content.dataset.truncated = 'true';
        }
    });
}
window.initializePostFeatures = function(el) {
    if (!el || el.dataset.initialized) return;
    try {
        if (typeof initTooltips === 'function') initTooltips(el);
        if (typeof initTruncatePosts === 'function') initTruncatePosts(el);
        
        if (typeof AudioManager !== 'undefined') AudioManager.upgradeLegacy(el);
        if (typeof window.parseTextEffects === 'function') window.parseTextEffects(el);
        if (typeof processEmbeds === 'function') processEmbeds(el);
        if (typeof applyAdaptiveFontSize === 'function') applyAdaptiveFontSize(el);
        el.querySelectorAll('video.post-video').forEach(v => {
            v.removeAttribute('onmouseover');
            v.removeAttribute('onmouseout');
            
            v.onplay = () => {
                if (typeof AudioManager !== 'undefined') AudioManager.stopOthers(v);
            };
            
            if (window.observeSticky) window.observeSticky(v);
            if (window.videoObserver) window.videoObserver.observe(v);
            
            if (v.videoWidth) checkIfVideoIsNote(v);
            else v.onloadedmetadata = () => checkIfVideoIsNote(v);
        });

        el.querySelectorAll('img.post-image').forEach(img => {
            img.onerror = () => handleImageError(img);
        });
        el.querySelectorAll('.file-thumb[data-type="video"], .file-thumb[data-type="gif"], .file-thumb[data-type="animation"]').forEach(wrapper => {
            wrapper.style.cursor = 'pointer'; 
        });

        checkPostGet(el);
        generateIdenticons(el);
        CrossBoardManager.resolve(el);
        
        if (window.UserState && window.UserState.postIds.has(el.id.replace('post-', ''))) {
            el.classList.add('is-yours');
        }
        
        if (typeof SmartLoader !== 'undefined') {
            SmartLoader.scan(el);
        }

    } catch (e) {
        console.error("Critical: Error initializing post features:", e, el);
    }
    el.dataset.initialized = 'true';
};
window.parseTextEffects = (el) => {
    let container = el.querySelector('.post-text') || el.querySelector('.post-text-content');
    if (!container) return;
    if (container.dataset.parsed === 'true') return;
    let html = container.innerHTML;
    const linkRegex = /(?<!["'=])(https?:\/\/[^\s<"']+)/g;
    html = html.replace(linkRegex, '<a href="$1" target="_blank" rel="noopener" class="auto-link">$1</a>');
    const simpleTags = [
        { open: '\\[b\\]', close: '\\[\\/b\\]', tag: 'b' },
        { open: '\\[i\\]', close: '\\[\\/i\\]', tag: 'i' },
        { open: '\\[s\\]', close: '\\[\\/s\\]', tag: 's' },
        { open: '\\[u\\]', close: '\\[\\/u\\]', tag: 'u' },
        { open: '\\[code\\]', close: '\\[\\/code\\]', tag: 'code' },
        { open: '\\[h1\\]', close: '\\[\\/h1\\]', tag: 'h3', class: 'post-heading' },
        { open: '\\|\\|', close: '\\|\\|', class: 'spoiler' } 
    ];
    html = html.replace(/\[btn=(https?:\/\/[^\]]+)\](.*?)\[\/btn\]/gi, (match, url, text) => {
        const safeUrl = url.replace(/"/g, '&quot;').replace(/'/g, '&#39;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
        return `<a href="${safeUrl}" target="_blank" rel="noopener noreferrer" class="btn btn-primary btn-small post-btn">${text}</a>`;
    });
    html = html.replace(/\[size=(\d+)\](.*?)\[\/size\]/gi, (match, size, content) => {
        let s = parseInt(size);
        if (s < 10) s = 10; if (s > 30) s = 30;
        return `<span style="font-size: ${s}px;">${content}</span>`;
    });
    simpleTags.forEach(t => {
        const regex = new RegExp(`${t.open}([\\s\\S]*?)${t.close}`, 'gi');
        if (t.tag) {
            html = html.replace(regex, `<${t.tag}>$1</${t.tag}>`);
        } else if (t.class) {
            html = html.replace(regex, `<span class="${t.class}">$1</span>`);
        }
    });
    const effects = [
        { tag: 'shake', class: 'effect-shake' },
        { tag: 'rainbow', class: 'effect-rainbow' },
        { tag: 'blur', class: 'effect-blur' }
    ];
    effects.forEach(eff => {
        const regex = new RegExp(`\\[${eff.tag}\\]([\\s\\S]*?)\\[\\/${eff.tag}\\]`, 'gi');
        html = html.replace(regex, `<span class="${eff.class}">$1</span>`);
    });
    html = html.replace(/\[glitch\]([\s\S]*?)\[\/glitch\]/gi, (match, content) => {
        const clean = content.replace(/<[^>]*>/g, '');
        return `<span class="effect-glitch" data-text="${clean}">${content}</span>`;
    });
    container.innerHTML = html;
    container.dataset.parsed = 'true';
};
function handleImageError(img) {
    if (img.dataset.finalError) return;
    const parent = img.closest('.file-thumb');
    if (img.classList.contains('post-sticker') && !img.dataset.triedVideoSwap) {
        img.dataset.triedVideoSwap = "true";
        const vid = document.createElement('video');
        vid.className = img.className;
        vid.src = img.src || img.dataset.src;
        vid.autoplay = true;
        vid.loop = true;
        vid.muted = true;
        vid.playsInline = true;
        vid.setAttribute('webkit-playsinline', 'true');
        vid.onerror = () => {
            const errDiv = document.createElement('div');
            errDiv.className = 'broken-media';
            errDiv.innerHTML = '<span style="font-size:20px">🎭</span><span style="font-size:10px">TGS</span>';
            errDiv.title = "Animated Sticker (TGS/Lottie) - Not supported yet";
            vid.replaceWith(errDiv);
        };
        img.replaceWith(vid);
        return;
    }
    if (img.classList.contains('broken-final') || img.dataset.retried === "2") {
        img.classList.add('broken-final');
        img.style.display = 'none';
        if (parent) {
            parent.classList.add('broken-media');
            parent.innerHTML = `<div style="padding:10px; text-align:center; color: var(--text-secondary); font-size: 0.8em;">⚠️ 404</div>`;
        }
        return;
    }
    img.dataset.retried = (parseInt(img.dataset.retried) || 0) + 1;
    const parentThumb = img.closest('a.file-thumb, a.gallery-item');
    if (parentThumb && parentThumb.tagName === 'A' && parentThumb.href && !img.src.endsWith(parentThumb.href)) {
        if (img.tagName === 'IMG') {
            img.src = parentThumb.href;
            return;
        }
    }
    img._retryCount = (img._retryCount || 0) + 1;
    if (img._retryCount <= 5) {
        setTimeout(() => {
            if (!img.isConnected) return;
            const src = img.src;
            if (src && !src.startsWith('data:')) {
                const url = new URL(src);
                url.searchParams.set('retry', Date.now());
                img.src = url.toString();
            }
        }, 3000 * img._retryCount);
    } else {
        img.classList.add('broken-final');
        if (parent) parent.innerHTML = `<div class="broken-media"><span>RIP</span></div>`;
    }
}
function checkPostGet(el) {
    const id = el.id.replace('post-', '');
    const match = id.match(/(\d)\1{2,}$/);
    if (match) {
        const digit = match[1];
        const len = match[0].length;
        el.classList.add(`get-digit-${digit}`);
        if (len >= 5) {
            el.classList.add('get-legendary');
        } else if (len === 4) {
            el.classList.add('get-epic');
        } else {
            el.classList.add('get-rare');
        }
    }
}
function checkThreadLimit(threadContainer) {
    if (!threadContainer || !threadContainer.classList.contains('thread-container')) return;
    if (threadContainer.dataset.endless === 'true') return;
    const statsEl = threadContainer.querySelector('.thread-stats');
    if (!statsEl) return;
    const text = statsEl.textContent;
    const match = text.match(/(\d+)/);
    if (match) {
        const count = parseInt(match[1]);
        const LIMIT = 600;
        const oldIcon = threadContainer.querySelector('.post-header .limit-icon');
        if (oldIcon) oldIcon.remove();
        threadContainer.classList.remove('limit-warning', 'limit-danger', 'limit-sunk');
        if (count >= LIMIT) {
            threadContainer.classList.add('limit-sunk');
            const icon = document.createElement('span');
            icon.className = 'limit-icon sunk';
            icon.title = t('limit_sunk_title');
            icon.textContent = t('limit_dead_label');
            statsEl.after(icon);
        } else if (count > 550) {
            threadContainer.classList.add('limit-danger');
            const icon = document.createElement('span');
            icon.className = 'limit-icon danger';
            icon.title = t('limit_danger_title').replace('{0}', LIMIT - count);
            icon.textContent = t('limit_limit_label');
            statsEl.after(icon);
        } else if (count > 480) {
            threadContainer.classList.add('limit-warning');
            const icon = document.createElement('span');
            icon.className = 'limit-icon warning';
            icon.title = t('limit_warn_title');
            icon.textContent = ' ⚠️';
            statsEl.after(icon);
        }
    }
}
const identiconCache = new Map();
let identiconObserver = null;
function generateIdenticons(scope = document) {
    if (!SettingsManager.current.showIdenticons) return;
    if (!identiconObserver) {
        identiconObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    drawIdenticon(entry.target);
                    identiconObserver.unobserve(entry.target);
                }
            });
        }, { rootMargin: "200px" });
    }

    const canvases = scope.querySelectorAll('canvas.identicon:not([data-drawn])');
    canvases.forEach(cvs => identiconObserver.observe(cvs));
}

function drawIdenticon(cvs) {
    const id = cvs.dataset.userid;
    if (!id) return;
    if (identiconCache.has(id)) {
        const ctx = cvs.getContext('2d');
        const img = new Image();
        img.onload = () => ctx.drawImage(img, 0, 0);
        img.src = identiconCache.get(id);
        cvs.dataset.drawn = 'true';
        return;
    }

    const ctx = cvs.getContext('2d');
    let hash = 0;
    for (let i = 0; i < id.length; i++) hash = id.charCodeAt(i) + ((hash << 5) - hash);
    const isCyber = document.body.classList.contains('theme-cyberpunk');
    const color = isCyber 
        ? `hsl(${Math.abs(hash % 360)}, 100%, 50%)` 
        : '#' + (hash & 0xFFFFFF).toString(16).padStart(6, '0');
    ctx.fillStyle = color;
    const s = cvs.width / 5;
    for (let x = 0; x < 3; x++) {
        for (let y = 0; y < 5; y++) {
            if ((hash >> (x * 5 + y)) & 1) {
                ctx.fillRect(x * s, y * s, s, s);
                if (x < 2) ctx.fillRect((4 - x) * s, y * s, s, s);
            }
        }
    }
    if (identiconCache.size > 500) {
        identiconCache.clear();
        console.log('🧹 [Memory] identiconCache cleared');
    }

    identiconCache.set(id, cvs.toDataURL());
    cvs.dataset.drawn = 'true';
}
const wrapText = (textarea, tag) => {
    if (!textarea) return;
    textarea.focus();
    const start = textarea.selectionStart;
    const end = textarea.selectionEnd;
    const selected = textarea.value.substring(start, end);
    let o = '', c = '';
    switch (tag) {
        case 'b': o = '[b]'; c = '[/b]'; break;
        case 'i': o = '[i]'; c = '[/i]'; break;
        case 's': o = '[s]'; c = '[/s]'; break;
        case 'u': o = '[u]'; c = '[/u]'; break;
        case 'spoiler': o = '||'; c = '||'; break;
        case 'code': o = '[code]'; c = '[/code]'; break;
        case 'shake': o = '[shake]'; c = '[/shake]'; break;
        case 'rainbow': o = '[rainbow]'; c = '[/rainbow]'; break;
        case 'glitch': o = '[glitch]'; c = '[/glitch]'; break;
        case 'blur': o = '[blur]'; c = '[/blur]'; break;
    }
    
    const fullText = o + selected + c;
    try {
        if (!document.execCommand('insertText', false, fullText)) {
            textarea.setRangeText(fullText, start, end, 'select');
        }
    } catch (e) {
        textarea.setRangeText(fullText, start, end, 'select');
    }

    if (start === end) {
        const newPos = start + o.length;
        textarea.setSelectionRange(newPos, newPos);
    }
    
    textarea.dispatchEvent(new Event('input', { bubbles: true }));
};
document.body.addEventListener('click', (e) => {
    const t = e.target;
    const anchorLink = t.closest('a');
    if (anchorLink && !location.pathname.includes('/res/')) {
        const href = anchorLink.getAttribute('href');
        if (href && href.startsWith('#post-')) {
            const threadCont = anchorLink.closest('.thread-container');
            if (threadCont) {
                e.preventDefault();
                e.stopPropagation();
                
                const postId = href.replace('#post-', '');
                const threadId = threadCont.id.replace('thread-', '');
                let boardId = document.body.dataset.currentBoard || 'b';
                const postEl = anchorLink.closest('.post');
                if (postEl) {
                    const menuBtn = postEl.querySelector('.action-menu-trigger');
                    if (menuBtn && menuBtn.dataset.boardId) {
                        boardId = menuBtn.dataset.boardId;
                    }
                }
                
                window.location.href = `/${boardId}/res/${threadId}.html#post-${postId}`;
                return;
            }
        }
    }

    if (e.button === 1 || e.ctrlKey || e.metaKey) return;
    if (t.classList.contains('post') && t.classList.contains('hidden')) {
        e.preventDefault();
        t.classList.remove('hidden');
        const id = t.id.replace('post-', '');
        if (window.UserState && window.UserState.hiddenPosts.has(id)) {
            window.UserState.hiddenPosts.delete(id);
            localStorage.setItem('hidden_posts', JSON.stringify([...window.UserState.hiddenPosts]));
        }
        return;
    }
    if (t.closest('.post.hidden')) {
        e.preventDefault();
        const p = t.closest('.post.hidden');
        p.classList.remove('hidden');
        const id = p.id.replace('post-', '');
        if (window.UserState && window.UserState.hiddenPosts.has(id)) {
            window.UserState.hiddenPosts.delete(id);
            localStorage.setItem('hidden_posts', JSON.stringify([...window.UserState.hiddenPosts]));
        }
        return;
    }
    if (t.classList.contains('action-menu-trigger')) {
        vibrate(10);
        e.preventDefault();
        e.stopPropagation();
        const parent = t.closest('.post-actions');
        const currentPost = t.closest('.post');
        document.querySelectorAll('.post-actions.active').forEach(el => {
            if (el !== parent) {
                el.classList.remove('active');
                const p = el.closest('.post');
                if (p) p.classList.remove('z-lift');
            }
        });
        const isActive = parent.classList.toggle('active');
        if (currentPost) {
            if (isActive) currentPost.classList.add('z-lift');
            else currentPost.classList.remove('z-lift');
        }
        return;
    }
    if (!t.closest('.action-menu')) {
        document.querySelectorAll('.post-actions.active').forEach(el => {
            el.classList.remove('active');
            const p = el.closest('.post');
            if (p) p.classList.remove('z-lift');
        });
    }
    const stealthBtn = t.closest('.action-stealth-edit');
    if (stealthBtn) {
        e.preventDefault();
        e.stopPropagation();
        const menu = stealthBtn.closest('.post-actions');
        if (menu) menu.classList.remove('active');
        const postNum = stealthBtn.dataset.postNum;
        StealthEditor.open(postNum);
        return;
    }
    const lazyWrapper = t.closest('.lazy-media-wrapper');
    if (lazyWrapper && (lazyWrapper.dataset.type === 'note' || lazyWrapper.dataset.type === 'sticker')) {
        e.preventDefault();
        e.stopPropagation();
        if (lazyWrapper.querySelector('video')) return;
        const src = lazyWrapper.dataset.src;
        const type = lazyWrapper.dataset.type;
        const video = document.createElement('video');
        video.src = src;
        video.autoplay = true;
        video.playsInline = true;
        video.classList.add('loaded');
        if (type === 'note') {
            video.className = 'post-video video-note loaded';
            video.loop = true;
        } else {
            video.className = 'post-sticker loaded';
            video.loop = true;
            video.muted = true;
        }
        lazyWrapper.innerHTML = '';
        lazyWrapper.appendChild(video);
        lazyWrapper.classList.remove('lazy-media-wrapper');
        return;
    }
    const pLink = t.closest('.post-num-link');
    if (pLink) {
        if (pLink.classList.contains('cross-board-link')) return;
        if (location.pathname.includes('/res/')) {
            e.preventDefault();
            const num = pLink.dataset.postNum;
            FormManager.smartReply(num, e);
            const targetPost = document.getElementById(`post-${num}`);
            if (targetPost) {
                targetPost.classList.remove('target-highlight');
                void targetPost.offsetWidth;
                targetPost.classList.add('target-highlight');
                setTimeout(() => targetPost.classList.remove('target-highlight'), 1500);
            }
            return;
        }
    }
    const fThumb = t.closest('.file-thumb') || t.closest('a.gallery-trigger');
    if (fThumb && !t.closest('.cap-download-btn') && !t.closest('.custom-audio-player')) {
        e.preventDefault();
        e.stopPropagation();
        
        const isVideo = fThumb.dataset.type === 'video' ||
                        fThumb.dataset.type === 'gif' || 
                        fThumb.querySelector('video') ||
                        (fThumb.href && /\.(mp4|webm|mov|mkv)(\?|$)/i.test(fThumb.href));

        if (isVideo) {
            const src = fThumb.href || fThumb.dataset.fullUrl || fThumb.dataset.src;
            const img = fThumb.querySelector('img');
            const poster = img ? img.src : null;

            if (typeof VideoPopupManager !== 'undefined') {
                VideoPopupManager.open(src, poster);
            } else {
                window.open(src, '_blank');
            }
            return;
        }
        if (SettingsManager.current.expandImages) {
            toggleInlineExpansion(fThumb);
            return;
        }
        const targetUrl = fThumb.dataset.fullUrl || fThumb.getAttribute('href');
        let index = GalleryManager.items.findIndex(i => 
            decodeURI(i.url) === decodeURI(targetUrl) || i.url === targetUrl
        );
        if (index === -1) {
            GalleryManager.updatePlaylist();
            index = GalleryManager.items.findIndex(i => 
                decodeURI(i.url) === decodeURI(targetUrl) || i.url === targetUrl
            );
        }
        GalleryManager.open(index !== -1 ? index : 0);
        return;
    }
    if (t.closest('#help-tags-btn') || t.closest('#fr-help-tags-btn') || t.closest('#help-tags-btn-bottom')) {
        e.preventDefault();
        openModal('help-modal');
        return;
    }
    const hiddenContent = t.closest('.spoiler, .effect-blur');
    if (hiddenContent) {
        e.preventDefault();
        hiddenContent.classList.toggle('revealed');
        return;
    }
    if (document.body.classList.contains('dim-mode')) {
        const img = t.closest('.post-image, .post-video, .post-sticker');
        if (img) {
            if (!img.classList.contains('revealed')) {
                e.preventDefault();
                e.stopPropagation();
                img.classList.add('revealed');
                return;
            }
        }
    }
    const fmtBtn = t.closest('.format-btn');
    if (fmtBtn) {
        e.preventDefault();
        const form = fmtBtn.closest('form');
        const textarea = form ? form.querySelector('textarea') : null;
        if (textarea) {
            if (fmtBtn.dataset.tag === 'quote') {
                const start = textarea.selectionStart;
                textarea.setRangeText('> ', start, start, 'end');
                textarea.focus();
                textarea.dispatchEvent(new Event('input'));
            } else {
                wrapText(textarea, fmtBtn.dataset.tag);
            }
        }
        return;
    }
    const pollOpt = t.closest('.poll-option-display');
    if (pollOpt) {
        if (pollOpt.classList.contains('voted') || pollOpt.closest('.poll-display').dataset.voting) return;
        const parent = pollOpt.closest('.poll-display');
        parent.dataset.voting = "true";
        parent.style.opacity = "0.7";
        const pid = pollOpt.dataset.postNum;
        const idx = pollOpt.dataset.optionIndex;
        fetch('/api/poll/vote', {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify({ post_num: parseInt(pid), option_index: parseInt(idx) })
        })
        .then(async res => {
            if (res.ok) {
                showToast(t('vote_accepted', 'Голос принят!'));
                pollOpt.classList.add('voted-highlight');
            } else {
                const err = await res.json();
                showToast(t('error_prefix') + err.detail);
                parent.style.opacity = "1";
                delete parent.dataset.voting;
            }
        })
        .catch(() => {
            showToast(t('network_error'));
            parent.style.opacity = "1";
            delete parent.dataset.voting;
        });
        return;
    }
    const embedBtn = t.closest('.embed-toggle');
    if (embedBtn) {
        e.preventDefault();
        const container = document.getElementById(embedBtn.dataset.target);
        if (container) {
            const isHidden = container.style.display === 'none';
            container.style.display = isHidden ? 'block' : 'none';
            embedBtn.textContent = isHidden ? t('btn_hide_video') : t('btn_show_video');
            const iframe = container.querySelector('iframe');
            if (isHidden && iframe.dataset.src) {
                iframe.src = iframe.dataset.src;
                iframe.removeAttribute('data-src');
            }
        }
        return;
    }
    if (t.closest('.cancel-reply')) {
        e.preventDefault();
        FormManager.cancelReply();
        return;
    }
});
document.addEventListener('keydown', (e) => {
    if (e.altKey && e.key === 'Enter') {
        const form = document.querySelector('#floating-reply-box:not([style*="none"]) form') || FormManager.mainForm;
        form?.querySelector('button[type="submit"]').click();
    }
    if (e.key === 'Escape') {
        FormManager.hideFloating();
        closeModal();
    }
    if (e.code === 'KeyR' && !e.ctrlKey && !['INPUT','TEXTAREA'].includes(document.activeElement.tagName)) {
        e.preventDefault();
        FormManager.focusMainForm('');
    }
    if ((e.ctrlKey || e.metaKey) && ['KeyB', 'KeyI', 'KeyS'].includes(e.code)) {
        const activeEl = document.activeElement;
        if (activeEl.tagName === 'TEXTAREA') {
            e.preventDefault();
            let tag = '';
            if (e.code === 'KeyB') tag = 'b';
            if (e.code === 'KeyI') tag = 'i';
            if (e.code === 'KeyS') tag = 'spoiler';
            if (typeof wrapText === 'function') {
                wrapText(activeEl, tag);
            }
        }
    }
});
function openModal(id) {
    const modal = document.getElementById(id);
    let overlay = document.getElementById('modal-overlay');
    if (!overlay) {
        overlay = document.createElement('div');
        overlay.id = 'modal-overlay';
        overlay.className = 'modal-overlay';
        document.body.appendChild(overlay);
        overlay.addEventListener('click', (e) => {
            if (e.target === overlay) {
                if (typeof BackButtonManager !== 'undefined' &&
                    BackButtonManager.stack.length > 0 &&
                    BackButtonManager.stack[BackButtonManager.stack.length - 1] === 'modal') {
                    history.back();
                } else {
                    closeModal();
                }
            }
        });
    }

    if (!modal) {
        console.error(`Modal #${id} not found`);
        return;
    }
    overlay.style.display = 'block';
    modal.style.display = 'block';
    requestAnimationFrame(() => {
        overlay.classList.add('active');
        modal.classList.add('active');
        document.body.classList.add('modal-open');
        modal.style.overscrollBehavior = 'contain';
    });
    if (typeof BackButtonManager !== 'undefined') {
        BackButtonManager.push('modal');
    }
}
function closeModal() {
    const activeModals = document.querySelectorAll('.modal.active, .modal[style*="block"]');
    const overlay = document.getElementById('modal-overlay');
    activeModals.forEach(el => el.classList.remove('active'));
    if (overlay) overlay.classList.remove('active');
    document.body.classList.remove('modal-open');
    document.body.style.paddingRight = '';
    const header = document.querySelector('header');
    if (header) header.style.paddingRight = '';

    setTimeout(() => {
        activeModals.forEach(el => el.style.display = 'none');
        if (overlay) overlay.style.display = 'none';
    }, 300);
}
const RouletteManager = {
    video: null,
    loader: null,
    isLoading: false,
    historyStack: [],
    currentIndex: -1,
    nextVideoData: null,
    userMuted: true,
    currentBlobUrl: null,
    init() {
        if (!document.body.classList.contains('page-roulette')) return;
        this.video = document.getElementById('r-video');
        if (this.video) this.video.referrerPolicy = "no-referrer";
        this.loader = document.getElementById('r-loader');
        this.prevBtn = document.getElementById('r-prev-btn');
        this.nextBtn = document.getElementById('r-next-btn');
        this.dlBtn = document.getElementById('r-download-btn');
        if (this.dlBtn) {
            this.dlBtn.onclick = async (e) => {
                e.preventDefault();
                e.stopPropagation();
                const currentData = this.historyStack[this.currentIndex];
                if (!currentData) return;

                const originalText = this.dlBtn.textContent;
                this.dlBtn.textContent = t('loading', '⏳');
                this.dlBtn.style.pointerEvents = 'none';

                try {
                    const response = await fetch(currentData.src);
                    if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);
                    showToast(t('download_started', 'Скачивание началось...'), 1500);
                    const blob = await response.blob();
                    const mimeType = blob.type;
                    let extension = '';
                    const mimeMap = {
                        'video/mp4': '.mp4', 'video/webm': '.webm', 'video/quicktime': '.mov',
                        'image/jpeg': '.jpg', 'image/png': '.png', 'image/gif': '.gif', 'image/webp': '.webp'
                    };
                    extension = mimeMap[mimeType] || '';
                    if (!extension && currentData.filename) {
                        const match = currentData.filename.match(/\.([a-z0-9]{2,5})$/i);
                        if (match) extension = match[0];
                    }
                    if (!extension) {
                        const urlExtMatch = (currentData.src || '').match(/\.([a-z0-9]{2,5})(?=[?#]|$)/i);
                        if (urlExtMatch) extension = urlExtMatch[0];
                    }
                    let baseFilename = (currentData.filename || 'video').replace(/\.[^/.]+$/, "");
                    if (baseFilename === 'video') {
                        baseFilename = `tgach_${currentData.board_id}_${currentData.post_id}`;
                    }
                    const finalFilename = baseFilename + (extension || '.mp4');
                    const objectUrl = URL.createObjectURL(blob);
                    const a = document.createElement('a');
                    a.style.display = 'none';
                    a.href = objectUrl;
                    a.download = finalFilename;
                    document.body.appendChild(a);
                    a.click();
                    document.body.removeChild(a);
                    URL.revokeObjectURL(objectUrl);
                } catch (err) {
                    console.error('Download failed:', err);
                    showToast(t('download_err', 'Ошибка скачивания'));
                    window.open(currentData.src, '_blank');
                } finally {
                    this.dlBtn.textContent = originalText;
                    this.dlBtn.style.pointerEvents = 'auto';
                }
            };
        }
        if (this.loader) {
            this.loader.innerHTML = '<div class="loader-spinner"></div>';
        }
        if (this.nextBtn) this.nextBtn.onclick = () => this.goNext();
        if (this.prevBtn) this.prevBtn.onclick = () => this.goPrev();
        if (this.video) {
            this.video.onclick = () => {
                if (this.video.paused) {
                    this.video.play();
                } else {
                    if (this.video.muted && this.userMuted) {
                        this.video.muted = false;
                        this.userMuted = false;
                        showToast(t('sound_on'));
                    } else {
                        this.video.pause();
                    }
                }
            };
            this.video.onerror = () => {
                console.log("Video error, skipping...");
                showToast(t('roulette_skip'), 1000);
                setTimeout(() => this.goNext(), 1000);
            };
            this.video.onvolumechange = () => {
                this.userMuted = this.video.muted;
            };
            this.video.onwaiting = () => this.toggleLoader(true);
            this.video.onstalled = () => this.toggleLoader(true);
            this.video.onloadstart = () => this.toggleLoader(true);
            this.video.oncanplay = () => this.toggleLoader(false);
            this.video.onplaying = () => this.toggleLoader(false);
        }
        document.addEventListener('keydown', (e) => {
            if (e.code === 'Space' || e.code === 'ArrowDown') {
                e.preventDefault();
                this.goNext();
            }
            if (e.code === 'ArrowUp') {
                e.preventDefault();
                this.goPrev();
            }
        });
        this.goNext();
    },
    toggleLoader(show) {
        if (!this.loader) return;
        if (show) {
            this.loader.style.display = 'block';
            this.video.style.opacity = '0.5';
        } else {
            this.loader.style.display = 'none';
            this.video.style.opacity = '1';
        }
    },
    async goNext() {
        if (this.isLoading) return;
        if (this.currentIndex < this.historyStack.length - 1) {
            this.currentIndex++;
            this.playVideo(this.historyStack[this.currentIndex]);
            this.updateButtons();
            this.preloadNext(); 
            return;
        }
        this.isLoading = true;
        this.toggleLoader(true);
        this.video.pause();
        
        let data;
        if (!this.preloadBuffer) this.preloadBuffer = [];
        
        if (this.nextVideoData) {
            this.preloadBuffer.unshift(this.nextVideoData);
            this.nextVideoData = null;
        }

        if (this.preloadBuffer.length > 0) {
            data = this.preloadBuffer.shift();
        } else {
            data = await this.loadVideoData();
        }

        this.isLoading = false;
        if (data) {
            this.historyStack.push(data);
            this.currentIndex++;
            this.playVideo(data);
            this.updateButtons();
            this.preloadNext();
        } else {
            setTimeout(() => this.goNext(), 2000);
        }
    },
    goPrev() {
        if (this.currentIndex > 0) {
            this.currentIndex--;
            this.playVideo(this.historyStack[this.currentIndex]);
            this.updateButtons();
        }
    },
    updateButtons() {
        if (!this.prevBtn) return;
        if (this.currentIndex > 0) {
            this.prevBtn.style.opacity = '1';
            this.prevBtn.style.cursor = 'pointer';
            this.prevBtn.disabled = false;
        } else {
            this.prevBtn.style.opacity = '0.3';
            this.prevBtn.style.cursor = 'default';
            this.prevBtn.disabled = true;
        }
    },
    async loadVideoData() {
        try {
            let url = '/api/tv/next';
            try {
                const saved = localStorage.getItem('tgach_tv_filter');
                if (saved) {
                    const boards = JSON.parse(saved).join(',');
                    if (boards) url += `?boards=${boards}`;
                }
            } catch (e) { console.warn('Filter read error', e); }
            
            console.log('[Roulette] Loading:', url);
            
            const res = await fetch(url);
            if (!res.ok) throw new Error();
            return await res.json();
        } catch (e) {
            console.error("Failed to load video metadata");
            return null;
        }
    },
    async preloadNext() {
        if (!this.preloadBuffer) this.preloadBuffer = [];
        if (this.isPreloading) return;
        this.isPreloading = true;
        try {
            while (this.preloadBuffer.length < 3) {
                const data = await this.loadVideoData();
                if (data) {
                    const preloader = document.createElement('video');
                    preloader.preload = 'auto';
                    preloader.src = data.src;
                    preloader.muted = true;
                    preloader.load(); 
                    data._preloaderElement = preloader;
                    if (data.poster) new Image().src = data.poster;
                    this.preloadBuffer.push(data);
                } else break;
            }
        } catch(e) {} finally { this.isPreloading = false; }
    },
    playVideo(data) {
        if (this.currentBlobUrl) {
            URL.revokeObjectURL(this.currentBlobUrl);
            this.currentBlobUrl = null;
        }
        if (this.currentPreloader) {
            this.currentPreloader.src = "";
            this.currentPreloader.load();
            this.currentPreloader = null;
        }
        if (data._preloaderElement) {
            this.currentPreloader = data._preloaderElement;
            delete data._preloaderElement; 
        }
        this.video.src = data.src;
        this.video.poster = data.poster || '';
        this.video.muted = this.userMuted;
        const textEl = document.getElementById('r-text');
        if (textEl) textEl.innerHTML = window.formatTextGlobal(data.text);
        const metaEl = document.getElementById('r-meta');
        if (metaEl) metaEl.textContent = `/${data.board_id}/ #${data.post_id}`;
        const linkBtn = document.getElementById('r-link-btn');
        if (linkBtn) {
            if (data.thread_id) {
                linkBtn.style.display = '';
                linkBtn.href = `/${data.board_id}/res/${data.thread_id}.html#post-${data.post_id}`;
            } else {
                linkBtn.style.display = 'none';
            }
        }
        const p = this.video.play();
        if (p) {
            p.then(() => {
                if (this.video.muted) {
                    showToast(t('roulette_sound_hint'), 6000);
                }
                this.toggleLoader(false);
            }).catch(e => {
                if (e.name === 'AbortError') return;
                console.log("Autoplay blocked or failed, muting...", e);
                this.video.muted = true;
                this.userMuted = true;
                this.video.play().catch(() => {
                    this.toggleLoader(false);
                });
                showToast(t('roulette_auto_hint'), 6000);
            });
        }
    }
};
window.RouletteManager = RouletteManager;       
const ImageRouletteManager = {
    preloadBuffer: [],
    isPreloading: false,

    init() {
        if (!document.body.classList.contains('page-random-img')) return;
        this.container = document.getElementById('ri-container');
        this.img = document.getElementById('ri-img');
        this.errorCount = 0; 
        this.img.onerror = () => {
            this.errorCount++;
            console.warn("Image 404 in roulette, skipping...");
            if (this.errorCount > 16) {
                showToast(t('net_error_stop', 'Слишком много ошибок. Проверьте интернет.'));
                this.toggleLoader(false);
                this.errorCount = 0;
                return;
            }
            setTimeout(() => this.goNext(), 500);
        };
        this.img.onload = () => {
            this.errorCount = 0;
            this.toggleLoader(false);
            this.img.style.opacity = '1';
        };
        this.loader = document.getElementById('ri-loader');
        this.uiLayer = document.getElementById('ri-ui-layer');
        this.metaEl = document.getElementById('ri-meta');
        this.linkBtn = document.getElementById('ri-link-btn');
        this.prevBtn = document.getElementById('ri-prev-btn');
        this.nextBtn = document.getElementById('ri-next-btn');
        this.prevBtnMobile = document.getElementById('ri-prev-btn-mobile');
        this.nextBtnMobile = document.getElementById('ri-next-btn-mobile');
        this.linkBtnMobile = document.getElementById('ri-link-btn-mobile');
        this.tagsBtn = document.getElementById('ri-tags-btn');
        this.dlBtn = document.getElementById('ri-download-btn');

        if (this.tagsBtn) {
            this.tagsBtn.onclick = (e) => {
                e.stopPropagation();
                if (this.currentData) {
                    const fid = this.currentData.file_id || 
                                this.currentData.original_file_id || 
                                (this.currentData.src ? (this.currentData.src.match(/\/files\/([^\/]+)/)?.[1]) : null);
                    
                    GalleryManager.items = [{ url: this.currentData.src, fileId: fid }];
                    GalleryManager.index = 0;
                    GalleryManager.showTagsModal();
                }
            };
        }

        if (this.dlBtn) {
            this.dlBtn.onclick = async (e) => {
                e.preventDefault();
                e.stopPropagation();
                if (!this.currentData) return;

                const originalText = this.dlBtn.textContent;
                this.dlBtn.textContent = t('loading', '...');
                this.dlBtn.style.pointerEvents = 'none';

                try {
                    const response = await fetch(this.currentData.src);
                    if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);

                    showToast(t('download_started', 'Скачивание началось...'), 1500);

                    const blob = await response.blob();
                    const mimeType = blob.type;
                    let extension = '';
                    
                    const mimeMap = {
                        'image/jpeg': '.jpg', 'image/png': '.png', 'image/gif': '.gif',
                        'image/webp': '.webp', 'video/mp4': '.mp4', 'video/webm': '.webm',
                        'video/quicktime': '.mov'
                    };
                    extension = mimeMap[mimeType] || '';

                    if (!extension && mimeType && mimeType.includes('/')) {
                        const subtype = mimeType.split('/')[1];
                        if (subtype && subtype.length < 5) extension = `.${subtype}`;
                    }

                    if (!extension) {
                        const urlExtMatch = (this.currentData.src || '').match(/\.([a-z0-9]{2,5})(?=[?#]|$)/i);
                        if (urlExtMatch) extension = urlExtMatch[0];
                    }

                    let baseFilename = (this.currentData.filename || 'file').replace(/\.[^/.]+$/, "");
                    if (baseFilename === 'file' || !this.currentData.filename) {
                         try {
                            const pathname = new URL(this.currentData.src, window.location.origin).pathname.split('/').pop();
                            if (pathname) baseFilename = pathname.replace(/\.[^/.]+$/, "");
                         } catch(err){}
                    }
                    if (baseFilename === 'file') {
                        baseFilename = `tgach_${this.currentData.board_id}_${this.currentData.post_id}`;
                    }

                    const finalFilename = baseFilename + (extension || '');

                    const objectUrl = URL.createObjectURL(blob);
                    const a = document.createElement('a');
                    a.style.display = 'none';
                    a.href = objectUrl;
                    a.download = finalFilename;
                    document.body.appendChild(a);
                    a.click();
                    document.body.removeChild(a);
                    URL.revokeObjectURL(objectUrl);
                } catch (err) {
                    console.error('Download failed:', err);
                    showToast(t('download_err', 'Ошибка скачивания'));
                    window.open(this.currentData.src, '_blank');
                } finally {
                    this.dlBtn.textContent = originalText;
                    this.dlBtn.style.pointerEvents = 'auto';
                }
            };
        }

        if (this.nextBtn) this.nextBtn.onclick = (e) => { e.stopPropagation(); this.goNext(); };
        if (this.prevBtn) this.prevBtn.onclick = (e) => { e.stopPropagation(); this.goPrev(); };
        if (this.nextBtnMobile) this.nextBtnMobile.onclick = (e) => { e.stopPropagation(); this.goNext(); };
        if (this.prevBtnMobile) this.prevBtnMobile.onclick = (e) => { e.stopPropagation(); this.goPrev(); };
        this.historyStack = [];
        this.currentIndex = -1;
        this.isLoading = false;
        this.container.addEventListener('click', (e) => {
            if (e.target.closest('a') || e.target.closest('.ri-nav-btn') || e.target.closest('.ri-top-btn')) return;
            this.toggleUI();
        });
        document.addEventListener('keydown', (e) => {
            if (e.code === 'ArrowRight' || e.code === 'Space') {
                e.preventDefault();
                this.goNext();
            }
            if (e.code === 'ArrowLeft') {
                e.preventDefault();
                this.goPrev();
            }
        });

        let touchStartX = 0;
        let touchStartY = 0;
        document.addEventListener('touchstart', e => {
            touchStartX = e.changedTouches[0].screenX;
            touchStartY = e.changedTouches[0].screenY;
        }, {passive: false});

        document.addEventListener('touchend', e => {
            const touchEndX = e.changedTouches[0].screenX;
            const touchEndY = e.changedTouches[0].screenY;
            
            if (Math.abs(touchEndY - touchStartY) > 50) return;

            if (touchStartX - touchEndX > 50) this.goNext();
            if (touchEndX - touchStartX > 50) this.goPrev();
        }, {passive: false});
        this.img.onerror = () => {
            console.warn("Image 404 in roulette, skipping...");
            setTimeout(() => this.goNext(), 500);
        };
        this.goNext();
    },

    toggleUI() {
        this.uiLayer.classList.toggle('ui-hidden');
    },

    toggleLoader(show) {
        if (this.loader) this.loader.style.display = show ? 'block' : 'none';
        if (this.img) this.img.style.opacity = show ? '0.5' : '1';
    },

    async preloadNext() {
        if (this.isPreloading) return;
        this.isPreloading = true;
        try {
            let url = '/api/img/next';
            try {
                const saved = localStorage.getItem('tgach_img_filter');
                if (saved) url += `?boards=${JSON.parse(saved).join(',')}`;
            } catch (e) {}

            while (this.preloadBuffer.length < 3) {
                const res = await fetch(url);
                if (res.ok) {
                    const data = await res.json();
                    if (!data.error) {
                        const temp = new Image();
                        temp.src = data.src;
                        this.preloadBuffer.push(data);
                    } else {
                        break;
                    }
                } else {
                    break;
                }
            }
        } catch (e) {
            console.error("Preload error", e);
        } finally {
            this.isPreloading = false;
        }
    },

    async goNext() {
        if (this.isLoading) return;
        if (this.currentIndex < this.historyStack.length - 1) {
            this.currentIndex++;
            this.render(this.historyStack[this.currentIndex]);
            if (this.currentIndex === this.historyStack.length - 1) {
                this.preloadNext();
            }
            return;
        }
        if (this.preloadBuffer.length > 0) {
            const item = this.preloadBuffer.shift();
            this.pushToHistory(item);
            this.render(item);
            this.preloadNext();
            return;
        }

        this.isLoading = true;
        this.toggleLoader(true);

        try {
            let url = '/api/img/next';
            try {
                const saved = localStorage.getItem('tgach_img_filter');
                if (saved) url += `?boards=${JSON.parse(saved).join(',')}`;
            } catch (e) {}

            const res = await fetch(url);
            if (!res.ok) throw new Error();
            const data = await res.json();
            
            if (data.error) {
                if (typeof showToast === 'function') showToast("Картинки кончились?");
                return;
            }

            this.pushToHistory(data);
            this.render(data);
            this.preloadNext();
        } catch (e) {
            console.error(e);
            setTimeout(() => this.goNext(), 1000);
        } finally {
            this.isLoading = false;
            this.toggleLoader(false);
        }
    },

    pushToHistory(item) {
        this.historyStack.push(item);
        this.currentIndex++;
        if (this.historyStack.length > 50) {
            this.historyStack.shift();
            this.currentIndex--;
        }
    },

    goPrev() {
        if (this.currentIndex > 0) {
            this.currentIndex--;
            this.render(this.historyStack[this.currentIndex]);
        }
    },

    render(data) {
        this.currentData = data;
        const isImageLoaded = this.img.complete && this.img.naturalHeight !== 0;
        this.img.src = data.src;
        if (this.metaEl) this.metaEl.textContent = `/${data.board_id}/ #${data.post_id}`;
        const threadUrl = `/${data.board_id}/res/${data.thread_id}.html#post-${data.post_id}`;
        if (this.linkBtn) {
            this.linkBtn.href = threadUrl;
        }
        if (this.linkBtnMobile) {
            this.linkBtnMobile.href = threadUrl;
        }
        
        const canGoBack = this.currentIndex > 0;
        if (this.prevBtn) {
            this.prevBtn.style.opacity = canGoBack ? '1' : '0.3';
            this.prevBtn.style.pointerEvents = canGoBack ? 'auto' : 'none';
        }
        if (this.prevBtnMobile) {
            this.prevBtnMobile.style.opacity = canGoBack ? '1' : '0.3';
            this.prevBtnMobile.disabled = !canGoBack;
        }
        if (this.prevBtn) this.prevBtn.style.pointerEvents = this.currentIndex > 0 ? 'auto' : 'none';
    }
};
window.ImageRouletteManager = ImageRouletteManager;
const GlobalMenu = {
    menu: null,
    activeTrigger: null,
    ctx: { postNum: null, boardId: null, threadId: null, authorId: null },
    init() {
        this.menu = document.getElementById('global-action-menu');
        if (!this.menu) return;
        const role = document.body.dataset.userRole || 'user';
        const isRootAdmin = document.body.dataset.isAdmin === 'true';
        const levels = { 'user': 0, 'janitor': 1, 'mod': 2, 'admin': 3 };
        let myLevel = levels[role] || 0;
        if (isRootAdmin) myLevel = 3;
        if (myLevel >= 1) {
            this.menu.querySelectorAll('.role-janitor').forEach(el => el.style.display = 'block');
        }
        if (myLevel >= 2) {
            this.menu.querySelectorAll('.role-mod').forEach(el => el.style.display = 'block');
        }
        if (myLevel >= 3) {
            this.menu.querySelectorAll('.role-admin').forEach(el => el.style.display = 'block');
        }
        document.body.addEventListener('click', (e) => {
            const trigger = e.target.closest('.action-menu-trigger');
            if (trigger) {
                e.preventDefault(); e.stopPropagation();
                this.toggle(trigger);
            } else if (!e.target.closest('#global-action-menu')) {
                this.close();
            }
        });
        this.menu.addEventListener('click', (e) => {
            const btn = e.target.closest('.menu-btn');
            if (!btn) return;
            e.preventDefault();
            this.handleAction(btn.dataset.action);
            this.close();
        });
        window.addEventListener('scroll', () => {
            if(this.menu.style.display === 'block') this.close();
        }, {passive: true});
    },
    close() {
        if (this.menu) this.menu.style.display = 'none';
        if (this.activeTrigger) {
            this.activeTrigger.classList.remove('active');
            this.activeTrigger = null;
        }
    },
    toggle(trigger) {
        if (this.activeTrigger === trigger && this.menu.style.display === 'block') {
            this.close(); return;
        }
        this.activeTrigger = trigger;
        this.ctx = {
            postNum: trigger.dataset.postNum,
            boardId: trigger.dataset.boardId,
            threadId: trigger.dataset.threadId,
            authorId: trigger.dataset.authorId,
            isOp: trigger.dataset.isOp === 'true',
            isHidden: trigger.dataset.isHidden === 'true'
        };
        const viewBtn = document.getElementById('menu-view-thread-btn');
        if(viewBtn) {
            const isInsideThread = location.pathname.includes(`/res/${this.ctx.threadId}`);
            
            if (!this.ctx.threadId || this.ctx.threadId === 'null' || isInsideThread) {
                viewBtn.style.display = 'none';
            } else {
                viewBtn.style.display = 'block';
                viewBtn.href = `/${this.ctx.boardId}/res/${this.ctx.threadId}.html#post-${this.ctx.postNum}`;
            }
        }
        const hideUserBtn = this.menu.querySelector('[data-action="user-hide"]');
        if (hideUserBtn) {
            const showIdenticons = document.body.classList.contains('show-identicons');
            hideUserBtn.style.display = showIdenticons ? 'block' : 'none';
        }
        const opBtn = document.getElementById('menu-op-mod-btn');
        if (opBtn) {
            const isSelf = String(this.ctx.postNum) === String(this.ctx.threadId);
            if (this.ctx.isOp && !isSelf) {
                opBtn.style.display = 'block';
                opBtn.textContent = this.ctx.isHidden ? "👁️ Вернуть (OP)" : "⛔ Скрыть (OP)";
            } else {
                opBtn.style.display = 'none';
            }
        }
        const rect = trigger.getBoundingClientRect();
        const menuW = 180;
        const menuH = 220;
        let left = rect.left + window.scrollX - menuW + rect.width;
        if (left < 5) left = 5;
        let top = rect.bottom + window.scrollY + 5;
        const spaceBelow = window.innerHeight - rect.bottom;
        const viewportHeight = window.visualViewport ? window.visualViewport.height : window.innerHeight;
        const viewportTop = window.scrollY;
        if ((rect.bottom - viewportTop + menuH) > viewportHeight) {
            top = rect.top + window.scrollY - menuH - 5;
        }
        if (top < viewportTop) {
            top = viewportTop + 10;
        }

        this.menu.style.left = `${left}px`;
        this.menu.style.top = `${top}px`;
        this.menu.style.display = 'block';
        const oldFake = this.menu.querySelector('.fake-admin-btn');
        if (oldFake) oldFake.remove();
        if (this.ctx.isOp && Math.random() < 0.05) {
            const fakeBtn = document.createElement('a');
            fakeBtn.href = "#";
            fakeBtn.className = 'menu-btn fake-admin-btn';
            fakeBtn.dataset.action = 'fake-delete';
            fakeBtn.style.color = 'red';
            fakeBtn.style.fontWeight = 'bold';
            fakeBtn.innerHTML = t('fake_del_btn');
            this.menu.appendChild(fakeBtn);
        }
        if (this.activeTrigger) this.activeTrigger.classList.remove('active');
        trigger.classList.add('active');
    },
    handleAction(action) {
        const { postNum, boardId, threadId, authorId, isHidden } = this.ctx;
        if (!postNum) return;
        switch(action) {
            case 'fake-delete':
                if (confirm(t('fake_del_confirm'))) {
                    const threadEl = document.getElementById(`thread-${postNum}`) || document.getElementById(`post-${postNum}`);
                    if (threadEl) {
                        threadEl.style.transition = "opacity 0.5s, transform 0.5s";
                        threadEl.style.opacity = "0";
                        threadEl.style.transform = "scale(0.9) skewX(10deg)";
                        setTimeout(() => {
                            threadEl.style.display = "none";
                            alert(t('fake_del_done'));
                        }, 500);
                    }
                }
                break;
            case 'reply':
                const isBoardPage = !!document.querySelector('.thread-list') && !location.pathname.includes('/res/');
                const isOverboard = location.pathname.includes('/overboard/');
                if (isBoardPage || isOverboard) {
                    const targetThread = threadId || postNum;
                    sessionStorage.setItem('quick_reply_to', postNum);
                    window.location.href = `/${boardId}/res/${targetThread}.html#post-${postNum}`;
                } else {
                    window.setReplyTo(postNum);
                }
                break;
            case 'view-thread': window.open(`/${boardId}/res/${threadId}.html#post-${postNum}`, '_blank'); break;
            case 'copy-link':
                const url = `${location.origin}/${boardId}/res/${threadId}.html#post-${postNum}`;
                if (navigator.share) {
                    navigator.share({ title: `Post #${postNum}`, url: url }).catch(()=>{});
                } else {
                    navigator.clipboard.writeText(url)
                        .then(() => showToast(t('link_copied')))
                        .catch(() => showToast(t('copy_err_perm', 'Не удалось скопировать (нет прав?)')));
                }
                break;
            case 'bottle':
                const bm = document.getElementById('bottle-modal');
                if(bm) { bm.querySelector('input[name="post_num"]').value = postNum; openModal('bottle-modal'); }
                break;
            case 'speak': window.speakPost(postNum); break;
            case 'report':
                const rm = document.getElementById('report-modal');
                if(rm) {
                    rm.querySelector('input[name="post_num"]').value = postNum;
                    if(rm.querySelector('#report-post-num')) rm.querySelector('#report-post-num').textContent = '#'+postNum;
                    openModal('report-modal');
                }
                break;
            case 'user-hide':
                if (authorId) {
                    const isActive = document.body.dataset.filteredUser === authorId;
                    if (!isActive) {
                        document.body.dataset.filteredUser = authorId;
                        document.body.classList.add('filtering-by-id');
                        document.querySelectorAll('.post').forEach(p => {
                            if (p.querySelector(`.identicon[data-userid="${authorId}"]`)) {
                                p.classList.add('filter-highlight');
                            } else {
                                p.classList.remove('filter-highlight');
                            }
                        });
                        showToast(t('filter_enabled', 'Фильтр по ID включен'));
                    } else {
                        delete document.body.dataset.filteredUser;
                        document.body.classList.remove('filtering-by-id');
                        document.querySelectorAll('.post.filter-highlight').forEach(p => p.classList.remove('filter-highlight'));
                        showToast(t('filter_disabled', 'Фильтр сброшен'));
                    }
                }
                break;
            case 'hide':
                const postEl = document.getElementById(`post-${postNum}`);
                if (postEl) {
                    postEl.classList.add('hidden');
                    const threadContainer = postEl.closest('.thread-container');
                    if (threadContainer && threadContainer.id === `thread-${postNum}`) {
                        threadContainer.style.display = 'none';
                        const hr = threadContainer.nextElementSibling;
                        if (hr && hr.tagName === 'HR') hr.style.display = 'none';
                    }
                }
                if(window.UserState) {
                    window.UserState.hiddenPosts.add(postNum);
                    localStorage.setItem('hidden_posts', JSON.stringify([...window.UserState.hiddenPosts]));
                }
                showToast(t('toast_hidden'));
                break;
            case 'op-mod':
                const newAction = isHidden ? 'show' : 'hide';
                fetch('/api/op/moderate', {
                    method: 'POST',
                    headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify({ post_num: parseInt(postNum), action: newAction })
                })
                .then(async r => {
                    if (r.ok) {
                        showToast(newAction === 'hide' ? t('op_hidden_toast') : t('op_restored_toast'));
                        const post = document.getElementById('post-' + postNum);
                        const btn = document.querySelector(`.action-menu-trigger[data-post-num="${postNum}"]`);
                        if (post) {
                            if (newAction === 'hide') {
                                post.classList.add('op-hidden');
                                if (!post.querySelector('.op-hidden-overlay')) {
                                   const content = post.querySelector('.post-content');
                                   content.insertAdjacentHTML('afterbegin', `<div class="op-hidden-overlay">${t('op_hidden_label')}</div>`);
                                }
                            } else {
                                post.classList.remove('op-hidden');
                                const ov = post.querySelector('.op-hidden-overlay');
                                if (ov) ov.remove();
                            }
                        }
                        if (btn) {
                            btn.dataset.isHidden = (newAction === 'hide').toString();
                        }
                    } else {
                        showToast(t('error_prefix') + (await r.json()).detail);
                    }
                })
                .catch(() => showToast(t('network_error')));
                break;
            case 'delete': this.adminCall('/api/admin/delete_post', {post_num: parseInt(postNum)}, false, postNum); break;
            case 'ban': this.adminCall('/api/admin/ban_user', {post_num: parseInt(postNum)}, false, postNum); break;
            case 'wipe': this.adminCall('/api/admin/wipe_user', {post_num: parseInt(postNum)}, true); break;
            case 'img-ban':
                if (!confirm(t('admin_ban_img_confirm'))) return;
                fetch('/api/admin/ban_image', {
                    method: 'POST',
                    headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify({ post_num: parseInt(postNum) })
                })
                .then(r => r.json())
                .then(data => {
                    if (data.status === 'ok') {
                        showToast(t('msg_img_ban_result').replace('{0}', data.count));
                        const el = document.getElementById(`post-${postNum}`);
                        if (el) el.remove();
                    } else {
                        showToast("Ошибка: " + (data.detail || "Неизвестно"));
                    }
                })
                .catch(() => showToast(t('network_error')));
                break;
            case 'move':
                const targetBoard = prompt(t('move_prompt'));
                if (!targetBoard) return;
                
                const tId = this.ctx.threadId || this.ctx.postNum; 
                fetch('/api/admin/move_thread', {
                    method: 'POST',
                    headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify({ thread_id: String(tId), target_board: targetBoard })
                })
                .then(async r => {
                    const ans = await r.json();
                    if (r.ok) {
                        showToast(ans.message);
                        setTimeout(() => location.reload(), 1000);
                    } else {
                        showToast("Ошибка: " + ans.detail);
                    }
                })
                .catch(() => showToast(t('network_error')));
                break;
            case 'delete-after':
                if(confirm(t('confirm_delete_after'))) this.adminCall('/api/admin/delete_after', {thread_id: String(threadId), post_num: parseInt(postNum)}, true);
                break;
            case 'shadow':
                const sm = document.getElementById('shadow-ban-modal');
                if(sm) { sm.querySelector('input[name="post_num"]').value = postNum; openModal('shadow-ban-modal'); }
                break;
            case 'stealth': if(typeof StealthEditor !== 'undefined') StealthEditor.open(postNum); break;
            case 'pin':
                 if(!confirm(t('confirm_pin'))) return;
                 fetch('/api/admin/pin_thread', {
                    method: 'POST', headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify({ thread_id: String(threadId), pinned: true })
                }).then(() => { showToast(t('toast_pinned')); setTimeout(() => location.reload(), 500); });
                break;
            case 'endless':
                if(!confirm("Сделать этот тред БЕСКОНЕЧНЫМ?\n\n- Лимит постов увеличится до 1000\n- Старые посты будут удаляться\n- Тред не уйдет в архив")) return;
                const targetTid = threadId || postNum;
                fetch('/api/admin/endless_thread', {
                    method: 'POST', 
                    headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify({ thread_id: String(targetTid), endless: true })
                })
                .then(async r => {
                    if(r.ok) {
                        showToast('🔄 Тред теперь бесконечный (Cyclic)!');
                        setTimeout(() => location.reload(), 1000);
                    } else {
                        showToast('Ошибка: ' + (await r.json()).detail);
                    }
                })
                .catch(() => showToast(t('network_error')));
                break;
        }
    },
    adminCall(url, data, reload=false, removeId=null) {
        if(!confirm(t('confirm_action'))) return;
        fetch(url, {
            method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify(data)
        }).then(async r => {
            if(r.ok) {
                showToast(t('toast_destroyed') || t('done_action'));
                if(reload) {
                    location.reload();
                } else if (removeId) {
                    const el = document.getElementById(`post-${removeId}`);
                    const container = document.getElementById(`thread-${removeId}`);
                    const target = container || el;
                    if(target) {
                        target.style.opacity = 0;
                        setTimeout(() => target.remove(), 300);
                    }
                }
            } else showToast(`${t('error_prefix')}${(await r.json()).detail}`);
        });
    }
};
const SystemDock = {
    audio: new Audio(),
    playlist: [],
    currentIndex: 0,
    isOpen: false,
    init() {
        if (document.getElementById('system-dock')) return;
        const shouldShow = document.querySelector('.thread-list') ||
                           document.querySelector('.chat-list') ||
                           document.querySelector('.op-post') ||
                           document.querySelector('.catalog-grid') ||
                           document.getElementById('favourites-container') ||
                           document.getElementById('history-container') ||
                           document.getElementById('my-posts-feed') ||
                           window.location.pathname.includes('/my/replies');

        if (!shouldShow) return;
        const html = `
            <div class="dock-body" id="dock-body">
                <div class="dock-tabs">
                    <div class="dock-tab active" data-tab="player" title="${t('dock_player', 'Плеер')}">🎵</div>
                    <div class="dock-tab" data-tab="replies" title="Ответы">✉️<div class="dock-tab-badge"></div></div>
                    <div class="dock-tab" data-tab="favs" title="${t('dock_favs', 'Избранное')}">⭐</div>
                    <div class="dock-tab" data-tab="history" title="${t('dock_history', 'История')}">👁️</div>
                    <div class="dock-tab" data-tab="my" title="${t('dock_my', 'Мои посты')}" style="color:var(--accent-primary);">👤</div>
                    <div class="dock-tab" data-tab="tools" title="${t('dock_tools', 'Инструменты')}">🛠️</div>
                </div>
                
                <div class="dock-content active" id="tab-player">
                    <div class="dock-current-info" id="dp-current-info" style="display:none;">
                        <div class="dc-artist" id="dc-artist"></div>
                        <div class="dc-track" id="dc-track"></div>
                    </div>
                    <div class="dock-player-controls">
                        <div class="dock-btns-row">
                            <button class="dp-btn" id="dp-prev" title="${t('dock_prev', 'Пред.')}">⏮</button>
                            <button class="dp-btn" id="dp-play" title="Play/Pause">▶</button>
                            <button class="dp-btn" id="dp-next" title="${t('dock_next', 'След.')}">⏭</button>
                        </div>
                        <div class="dock-volume">
                            <span>🔈</span>
                            <input type="range" id="dp-volume" min="0" max="1" step="0.05">
                        </div>
                    </div>
                    <div class="dock-progress-area">
                        <div class="dock-time-display" id="dp-time">00:00 / 00:00</div>
                        <div class="dock-progress-bar" id="dp-progress">
                            <div class="dock-wave-bg"></div>
                            <div class="dock-wave-fill" id="dp-fill"></div>
                        </div>
                    </div>
                    <div id="dock-playlist"></div>
                </div>

                <div class="dock-content" id="tab-replies">
                    <div class="dock-empty-state">${t('loading', 'Загрузка...')}</div>
                </div>

                <div class="dock-content" id="tab-favs"></div>
                <div class="dock-content" id="tab-history"></div>
                <div class="dock-content" id="tab-my">
                    <div class="dock-empty-state">${t('loading', 'Загрузка...')}</div>
                </div>

                <div class="dock-content" id="tab-tools">
                    <div class="dock-tools-grid">
                        <button class="dock-tool-btn" onclick="window.scrollTo({top:0, behavior:'smooth'})">⬆ ${t('dock_up', 'Наверх')}</button>
                        <button class="dock-tool-btn" onclick="window.scrollTo({top:document.body.scrollHeight, behavior:'smooth'})">⬇ ${t('dock_down', 'Вниз')}</button>
                        <button class="dock-tool-btn" onclick="document.getElementById('theme-toggle').click()">🌗 ${t('dock_theme', 'Тема')}</button>
                        <button class="dock-tool-btn" onclick="window.manualRefresh()">🔄 ${t('dock_refresh', 'Обновить')}</button>
                        <button class="dock-tool-btn" id="dock-clear-history" style="color:var(--action-danger)">🗑 ${t('dock_clear_hist', 'История')}</button>
                        <a href="/my/" target="_blank" class="dock-tool-btn" style="text-decoration:none; color:var(--accent-primary);">${t('dock_link_my_posts')}</a>
                        <a href="/my/replies" class="dock-tool-btn" style="text-decoration:none; color:var(--accent-link);">${t('dock_link_all_replies')}</a>
                    </div>
                </div>
            </div>
            <div id="dock-scroll-controls" class="dock-scroll-controls">
                <button class="dock-scroll-btn" id="dock-scroll-up" title="Наверх">⬆</button>
                <button class="dock-scroll-btn" id="dock-scroll-down" title="Вниз">⬇</button>
            </div>
            <button class="dock-btn" id="dock-toggle-btn">
                <span class="dock-emoji">🧩</span>
                <div class="dock-badge" id="dock-badge" style="display:none">0</div>
            </button>
        `;
        
        const wrapper = document.createElement('div');
        wrapper.id = 'system-dock';
        wrapper.innerHTML = html;
        document.body.appendChild(wrapper);
        
        this.bindEvents();
        this.initAudioSettings();
        
        this.checkUnread();
        setInterval(() => this.checkUnread(), 60000);
    },

    initAudioSettings() {
        const volSlider = document.getElementById('dp-volume');
        if (!volSlider) return;
        const vol = parseFloat(localStorage.getItem('user_volume') || '0.5');
        this.audio.volume = vol;
        volSlider.value = vol;
        volSlider.addEventListener('input', (e) => {
            const v = parseFloat(e.target.value);
            this.audio.volume = v;
            window.globalVideoVolume = v;
            document.querySelectorAll('video, audio').forEach(el => {
                el.volume = v;
            });
            if (window.saveVolume) window.saveVolume(v);
        });
        
        this.audio.onended = () => this.nextTrack();
        
        this.audio.ontimeupdate = () => {
            const bar = document.getElementById('dp-fill');
            const time = document.getElementById('dp-time');
            if (this.audio.duration && isFinite(this.audio.duration)) {
                const pct = (this.audio.currentTime / this.audio.duration) * 100;
                if(bar) bar.style.width = `${pct}%`;
                if(time) time.textContent = `${formatTime(this.audio.currentTime)} / ${formatTime(this.audio.duration)}`;
            }
        };
        const progressContainer = document.getElementById('dp-progress');
        if (progressContainer) {
            progressContainer.onclick = (e) => {
                e.stopPropagation();
                if (!this.audio.duration) return;
                const rect = progressContainer.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const pct = Math.max(0, Math.min(1, x / rect.width));
                this.audio.currentTime = pct * this.audio.duration;
            };
        }
    },
    toggleDock(forceState = null) {
        const body = document.getElementById('dock-body');
        const toggle = document.getElementById('dock-toggle-btn');
        if (!body || !toggle) return;

        const newState = forceState !== null ? forceState : !this.isOpen;
        this.isOpen = newState;
        localStorage.setItem('dock_open', this.isOpen);

        if (this.isOpen) {
            body.classList.add('open');
            toggle.classList.add('active');
            toggle.querySelector('.dock-emoji').textContent = '×';
            this.refreshContent();
        } else {
            body.classList.remove('open');
            toggle.classList.remove('active');
            toggle.querySelector('.dock-emoji').textContent = '🧩';
        }
    },
    bindEvents() {
        const toggle = document.getElementById('dock-toggle-btn');
        const body = document.getElementById('dock-body');
        const scrollCtrl = document.getElementById('dock-scroll-controls');
        const btnUp = document.getElementById('dock-scroll-up');
        const btnDown = document.getElementById('dock-scroll-down');

        if (!toggle || !body) return; 

        toggle.onclick = (e) => {
            e.stopPropagation();
            this.toggleDock();
        };

        document.addEventListener('click', (e) => {
            if (this.isOpen && !body.contains(e.target) && e.target !== toggle && !toggle.contains(e.target)) {
                this.toggleDock(false);
            }
        });
        if (btnUp && btnDown && scrollCtrl) {
            btnUp.onclick = () => window.scrollTo({ top: 0, behavior: 'smooth' });
            btnDown.onclick = () => window.scrollTo({ top: document.body.scrollHeight, behavior: 'smooth' });

            const handleScroll = () => {
                if (!SettingsManager.current.showScrollButtons) {
                    scrollCtrl.classList.remove('visible');
                    return;
                }
                scrollCtrl.classList.add('visible');
                
                const scrollY = window.scrollY;
                const maxScroll = document.documentElement.scrollHeight - window.innerHeight;
                
                btnUp.disabled = scrollY <= 50;
                btnDown.disabled = scrollY >= maxScroll - 50;
            };

            window.addEventListener('scroll', debounce(handleScroll, 100), { passive: true });
            setTimeout(handleScroll, 500);
        }

        document.querySelectorAll('.dock-tab').forEach(tab => {
            tab.onclick = () => {
                const targetId = `tab-${tab.dataset.tab}`;
                const targetContent = document.getElementById(targetId);
                if (!targetContent) return;
                document.querySelectorAll('.dock-tab').forEach(t => t.classList.remove('active'));
                document.querySelectorAll('.dock-content').forEach(c => c.classList.remove('active'));
                tab.classList.add('active');
                targetContent.classList.add('active');
            };
        });
        
        const btnPlay = document.getElementById('dp-play');
        const btnPrev = document.getElementById('dp-prev');
        const btnNext = document.getElementById('dp-next');
        const clearBtn = document.getElementById('dock-clear-history');

        if (btnPlay) btnPlay.onclick = () => this.togglePlay();
        if (btnPrev) btnPrev.onclick = () => this.prevTrack();
        if (btnNext) btnNext.onclick = () => this.nextTrack();
        
        if (clearBtn) {
            clearBtn.onclick = () => {
                if(confirm(t('history_clear_confirm', 'Очистить историю?'))) {
                    localStorage.removeItem('viewed_threads_history');
                    this.renderHistory();
                    showToast(t('history_cleared', 'История очищена'));
                }
            };
        }
    },

    refreshContent() {
        this.scanAudio();
        this.renderFavs();
        this.renderHistory();
        this.renderMyPosts();
        this.renderReplies();
        this.checkUnread();  
    },

    async checkUnread() {
        if (!document.body.dataset.userId) return;
        try {
            const res = await fetch('/api/my/replies/count');
            if (res.ok) {
                const data = await res.json();
                const count = data.count;
                const badge = document.getElementById('dock-badge');
                const tabBadge = document.querySelector('.dock-tab[data-tab="replies"] .dock-tab-badge');

                if (count > 0) {
                    if (badge) {
                        badge.textContent = count > 99 ? '99+' : count;
                        badge.style.display = 'flex';
                        badge.classList.add('pulse-anim');
                    }
                    if (tabBadge) tabBadge.style.display = 'block';
                } else {
                    if (badge) {
                        badge.style.display = 'none';
                        badge.classList.remove('pulse-anim');
                    }
                    if (tabBadge) tabBadge.style.display = 'none';
                }
            }
        } catch(e) {}
    },

    async renderReplies(isUpdate = false) {
        const container = document.getElementById('tab-replies');
        if (!container) return;
        if (!document.body.dataset.userId) {
            container.innerHTML = `<div class="dock-empty-state">${t('dock_token_required', 'Нужен токен (или пост)')}</div>`;
            return;
        }
        if (!isUpdate) {
            container.innerHTML = `<div class="dock-empty-state">${t('loading', 'Загрузка...')}</div>`;
        }
        
        try {
            const res = await fetch('/api/my/replies/list');
            if (!res.ok) throw new Error();
            const replies = await res.json();
            
            if (replies.length === 0) {
                container.innerHTML = `<div class="dock-empty-state">${t('dock_no_new_replies')}</div>`;
                return;
            }

            const hasUnread = replies.some(r => !r.is_read);
            let html = hasUnread ? `<button class="mark-read-all-btn" onclick="SystemDock.markAllRead()">✅ ${t('btn_mark_all_read', 'Пометить все как прочитанные')}</button>` : '';
            
            html += replies.map(r => {
                const date = new Date(r.created_at * 1000).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'});
                const link = `/${r.board_id}/res/${r.thread_id}.html#post-${r.post_num}`;
                const unreadClass = r.is_read ? '' : 'unread';
                const fileIcon = r.has_file ? '📎 ' : '';
                const safePreview = r.preview
                    .replace(/&/g, "&amp;")
                    .replace(/</g, "&lt;")
                    .replace(/>/g, "&gt;")
                    .replace(/"/g, "&quot;")
                    .replace(/'/g, "&#039;");
                
                return `
                <a href="${link}" class="reply-item ${unreadClass}" onclick="SystemDock.handleReplyClick(event, ${r.id}, '${link}')">
                    <div class="reply-meta">
                        <span>/${r.board_id}/ >>${r.parent_num}</span>
                        <span>${date}</span>
                    </div>
                    <div class="reply-preview-text">
                        ${fileIcon}${safePreview}
                    </div>
                </a>`;
            }).join('');
            
            container.innerHTML = html;
        } catch (e) {
            container.innerHTML = `<div class="dock-empty-state" style="color:red">${t('stash_load_error', 'Ошибка загрузки')}</div>`;
        }
    },

    async markRead(id) {
        try {
            await fetch('/api/my/replies/read', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({ ids: [id] })
            });
            this.checkUnread();
            this.renderReplies();
        } catch(e) {}
    },

    async handleReplyClick(e, id, url) {
        e.preventDefault();
        const el = e.currentTarget;
        el.classList.remove('unread');
        
        try {
            await fetch('/api/my/replies/read', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({ ids: [id] })
            });
        } catch(err) { console.error(err); }
        
        window.location.href = url;
    },

    async markAllRead() {
        try {
            const btn = document.querySelector('.mark-read-all-btn');
            if(btn) btn.textContent = '⏳ ...';
            
            await fetch('/api/my/replies/read', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({ ids: [] })
            });
            this.checkUnread();
            this.renderReplies();
            showToast(t('all_replies_read', 'Все ответы прочитаны'));
        } catch(e) {}
    },

    scanAudio() {
        if (!this.audio.paused && this.playlist.length > 0) {
            this.scrollToCurrent();
            return;
        }
        this.playlist = [];
        const seen = new Set();
        document.querySelectorAll('.custom-audio-player').forEach(player => {
            const audioEl = player.querySelector('audio');
            let src = audioEl ? (audioEl.currentSrc || audioEl.src || audioEl.querySelector('source')?.src) : null;
            if (!src) return;
            const tempLink = document.createElement('a');
            tempLink.href = src;
            src = tempLink.href;
            if (!seen.has(src)) {
                seen.add(src);
                let rawName = "";
                const downloadLink = player.querySelector('.cap-download-btn');
                if (downloadLink && downloadLink.download) {
                    rawName = downloadLink.download;
                } else {
                    try {
                        rawName = decodeURIComponent(src.split('/').pop().split('?')[0]);
                    } catch (e) {
                        rawName = src.split('/').pop().split('?')[0];
                    }
                }
                
                const info = AudioManager.parseAudioName(rawName);
                const displayName = info.artist ? `${info.artist} — ${info.track}` : info.track;
                
                this.playlist.push({ url: src, name: displayName });
            }
        });
        const audioRegex = /\.(mp3|ogg|wav|m4a|opus)(\?.*)?$/i;
        document.querySelectorAll('a[href]').forEach(a => {
            if (audioRegex.test(a.href)) {
                const tempLink = document.createElement('a');
                tempLink.href = a.href;
                const absoluteUrl = tempLink.href;
                if (!seen.has(absoluteUrl)) {
                    seen.add(absoluteUrl);
                    const name = a.textContent.trim() || a.dataset.filename || absoluteUrl.split('/').pop().split('?')[0];
                    this.playlist.push({
                        url: absoluteUrl,
                        name: name
                    });
                }
            }
        });
        this.renderPlaylist();
    },
    renderPlaylist() {
        const container = document.getElementById('dock-playlist');
        if (this.playlist.length === 0) {
            container.innerHTML = `<div class="dock-empty-state">${t('dock_no_music', 'Музыки нет 🤷‍♂️')}</div>`;
            return;
        }
        if (!this.audio.src && this.playlist.length > 0) {
            this.audio.src = this.playlist[0].url;
            this.currentIndex = 0;
            this.audio.onloadedmetadata = () => {
                 const time = document.getElementById('dp-time');
                 if(time) time.textContent = `00:00 / ${formatTime(this.audio.duration)}`;
            };
        }
        container.innerHTML = this.playlist.map((track, i) => `
            <div class="dock-playlist-item ${i === this.currentIndex ? 'active' : ''}" 
                 id="dock-track-${i}"
                 onclick="SystemDock.playTrack(${i})">
                ${i + 1}. ${track.name}
            </div>
        `).join('');
        this.scrollToCurrent();
    },
    scrollToCurrent() {
        const el = document.getElementById(`dock-track-${this.currentIndex}`);
        if (el) el.scrollIntoView({ behavior: 'smooth', block: 'center' });
    },
    playTrack(index) {
        if (index < 0 || index >= this.playlist.length) return;
        this.currentIndex = index;
        const track = this.playlist[index];
        
        const infoPanel = document.getElementById('dp-current-info');
        const artistEl = document.getElementById('dc-artist');
        const trackEl = document.getElementById('dc-track');
        
        const parsed = AudioManager.parseAudioName(track.name);
        if (infoPanel && artistEl && trackEl) {
            artistEl.textContent = parsed.artist;
            trackEl.textContent = parsed.track;
            infoPanel.style.display = 'flex';
        }

        this.audio.src = track.url;
        this.audio.play()
            .then(() => {
                const btn = document.getElementById('dp-play');
                if(btn) btn.textContent = '⏸';
                this.renderPlaylist();
                if ('mediaSession' in navigator) {
                    navigator.mediaSession.metadata = new MediaMetadata({
                        title: parsed.track,
                        artist: parsed.artist || 'ТГАЧ Радио'
                    });
                }
            })
            .catch(e => {
                console.error(e);
                const btn = document.getElementById('dp-play');
                if(btn) btn.textContent = '▶';
            });
    },
    togglePlay() {
        if (this.playlist.length === 0) return;
        const btn = document.getElementById('dp-play');
        
        if (this.audio.paused) {
            if (typeof AudioManager !== 'undefined') AudioManager.stopOthers(this.audio);
            
            const playPromise = this.audio.src ? this.audio.play() : this.playTrack(this.currentIndex);
            
            if (playPromise !== undefined) {
                playPromise
                    .then(() => {
                        if (btn) btn.textContent = '⏸';
                    })
                    .catch(e => {
                        console.error(e);
                        if (btn) btn.textContent = '▶';
                    });
            }
        } else {
            this.audio.pause();
            if (btn) btn.textContent = '▶';
        }
    },

    pause() {
        this.audio.pause();
        const btn = document.getElementById('dp-play');
        if(btn) btn.textContent = '▶';
    },
    nextTrack() {
        let next = this.currentIndex + 1;
        if (next >= this.playlist.length) next = 0;
        this.playTrack(next);
    },
    prevTrack() {
        let prev = this.currentIndex - 1;
        if (prev < 0) prev = this.playlist.length - 1;
        this.playTrack(prev);
    },
    async renderFavs() {
        const container = document.getElementById('tab-favs');
        if (!container) return; 

        try {
            const saved = JSON.parse(localStorage.getItem('favourite_threads') || '[]');
            if (saved.length === 0) {
                container.innerHTML = `<div class="dock-empty-state">${t('dock_no_favs', 'Нет избранных')}</div>`;
                return;
            }
            const res = await fetch('/api/favourites', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({ thread_ids: saved })
            });
            if (!res.ok) throw new Error();
            const threads = await res.json();
            if (threads.length === 0) {
                container.innerHTML = `<div class="dock-empty-state">${t('dock_threads_sunk', 'Треды утонули')}</div>`;
                return;
            }
            container.innerHTML = threads.map(t => {
                const title = t.content.text ? t.content.text.replace(/<[^>]*>/g, '').slice(0, 40) : 'Без текста';
                return `
                <a href="/${t.board_id}/res/${t.id}.html" class="dock-list-item">
                    <span style="font-weight:bold;">/${t.board_id}/ ${title}</span>
                    <small>Тред #${t.id}</small>
                </a>
            `}).join('');
        } catch(e) { 
            const saved = JSON.parse(localStorage.getItem('favourite_threads') || '[]');
            if (saved.length === 0) {
                 container.innerHTML = `<div class="dock-empty-state">${t('dock_no_favs', 'Нет избранных')}</div>`;
                 return;
            }
            container.innerHTML = saved.map(id => `
                <a href="/b/res/${id}.html" class="dock-list-item" style="opacity:0.7">
                    Тред #${id} <small>(оффлайн)</small>
                </a>
            `).join('');
        }
    },
    renderHistory() {
        const container = document.getElementById('tab-history');
        if (!container) return;

        try {
            const history = JSON.parse(localStorage.getItem('viewed_threads_history') || '[]');
            if (history.length === 0) {
                container.innerHTML = `<div class="dock-empty-state">${t('dock_history_empty', 'Пусто')}</div>`;
                return;
            }
            container.innerHTML = history.slice(0, 15).map(h => `
                <a href="/${h.boardId}/res/${h.threadId}.html" class="dock-list-item">
                    ${h.title || 'Тред'}
                    <small>/${h.boardId}/ #${h.threadId}</small>
                </a>
            `).join('');
        } catch(e) { container.innerHTML = `<div class="dock-empty-state">${t('stash_load_error', 'Ошибка')}</div>`; }
    },
    async renderMyPosts() {
        const container = document.getElementById('tab-my');
        if (!container) return;
        let ids = [];
        try {
            ids = JSON.parse(localStorage.getItem('my_post_ids') || '[]');
        } catch(e) { ids = []; }

        if (ids.length === 0) {
            container.innerHTML = `<div class="dock-empty-state">${t('dock_no_posts')}</div>`;
            return;
        }
        const targetIds = ids.slice(-60).reverse();
        try {
            const res = await fetch('/api/my-posts-content', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({ post_nums: targetIds })
            });
            if (!res.ok) throw new Error();
            const posts = await res.json();
            if (posts.length === 0) {
                container.innerHTML = `<div class="dock-empty-state">Посты удалены</div>`;
                return;
            }
            container.innerHTML = posts.map(p => {
                let rawText = p.content.text || '';
                rawText = rawText.replace(/>>\d+/g, '').trim();
                let preview = rawText.replace(/<[^>]*>/g, '').slice(0, 100);
                if (rawText.length > 100) preview += '...';
                if (!preview && p.content.files && p.content.files.length > 0) {
                    preview = `[Файл: ${p.content.files[0].type}]`;
                } else if (!preview) {
                    preview = 'Без текста';
                }
                const date = new Date(p.timestamp * 1000).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'});
                return `
                <a href="/${p.board_id}/res/${p.thread_id || p.id}.html#post-${p.id}" class="dock-list-item" style="border-left: 3px solid var(--accent-primary);">
                    <div style="display:flex; justify-content:space-between; font-size:0.8em; color:var(--text-secondary); margin-bottom:2px;">
                        <span>/${p.board_id}/ #${p.id}</span>
                        <span>${date}</span>
                    </div>
                    <div style="white-space: normal; overflow: hidden; text-overflow: ellipsis; max-height: 3em; line-height: 1.2;">
                        ${preview}
                    </div>
                </a>
                `;
            }).join('');
        } catch (e) {
            container.innerHTML = `<div class="dock-empty-state" style="color:red">${t('dock_load_err')}</div>`;
        }
    },
};
const TagSearchManager = {
    page: 1,
    loading: false,
    finished: false,
    tags: null,
    init() {
        const grid = document.querySelector('.gallery-grid');
        if (!grid || !location.pathname.includes('/search/tags')) return;
        
        const params = new URLSearchParams(location.search);
        this.tags = params.get('tags');
        if (!this.tags) return;

        const sentinel = document.createElement('div');
        sentinel.id = 'tag-search-sentinel';
        sentinel.style.cssText = 'height: 20px; width: 100%; clear: both;';
        grid.after(sentinel);

        this.observer = new IntersectionObserver((entries) => {
            if (entries[0].isIntersecting && !this.loading && !this.finished) {
                this.loadMore();
            }
        }, { rootMargin: '400px' });
        
        this.observer.observe(sentinel);
    },
    async loadMore() {
        this.loading = true;
        this.page++;
        
        try {
            const res = await fetch(`/search/tags?tags=${encodeURIComponent(this.tags)}&page=${this.page}`, {
                headers: { 'Accept': 'application/json' }
            });
            const images = await res.json();

            if (!images || images.length === 0) {
                this.finished = true;
                return;
            }

            const grid = document.querySelector('.gallery-grid');
            images.forEach(img => {
                const wrapper = document.createElement('div');
                wrapper.className = 'gallery-item-wrapper';
                
                const thumb = document.createElement('div');
                thumb.className = 'gallery-thumb';
                
                const link = document.createElement('a');
                link.href = `/${img.parent_board_id}/res/${img.parent_post_id}.html#post-${img.parent_post_id}`;
                link.className = 'file-thumb';
                link.dataset.filename = img.filename;
                
                const image = document.createElement('img');
                image.className = 'post-image lazy-load';
                image.dataset.src = img.thumbnail_url || img.original_url;
                image.src = "data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7";
                
                link.appendChild(image);
                thumb.appendChild(link);
                wrapper.appendChild(thumb);
                grid.appendChild(wrapper);
                
                if (typeof SmartLoader !== 'undefined') SmartLoader.scan(wrapper);
            });
            
            if (typeof GalleryManager !== 'undefined') GalleryManager.updatePlaylist();
            
        } catch (e) {
            console.error("Tag scroll error:", e);
        } finally {
            this.loading = false;
        }
    }
};
document.addEventListener('DOMContentLoaded', async () => {
    await loadTranslations();
    document.body.style.setProperty('--text-drag-hint', `"${t('css_drag_hint')}"`);
    document.body.style.setProperty('--text-post-hidden', `"${t('css_post_hidden')}"`);
    if (window.isTgachInitialized) {
        console.log("⚠️ App already initialized, skipping...");
        return;
    }
    SpankAudioManager.init();
    window.isTgachInitialized = true;
    const overlay = document.getElementById('modal-overlay');
    if (overlay) {
        overlay.addEventListener('click', (e) => {
            if (e.target === overlay) {
                if (typeof BackButtonManager !== 'undefined' &&
                    BackButtonManager.stack.length > 0 &&
                    BackButtonManager.stack[BackButtonManager.stack.length - 1] === 'modal') {
                    history.back();
                } else {
                    closeModal();
                }
            }
        });
    }
    document.body.addEventListener('click', (e) => {
        if (e.target.closest('.modal button[onclick*="none"], .modal .btn-secondary')) {
            const btn = e.target.closest('button');
            if (btn && (btn.innerHTML.includes('&times;') || btn.textContent.includes('Закрыть') || btn.textContent.includes('Отмена'))) {
                e.preventDefault();
                e.stopPropagation();
                if (typeof BackButtonManager !== 'undefined' &&
                    BackButtonManager.stack.length > 0 &&
                    BackButtonManager.stack[BackButtonManager.stack.length - 1] === 'modal') {
                    history.back();
                } else {
                    closeModal();
                }
            }
        }
    });
    document.body.addEventListener('click', (e) => {
        if (e.target.closest('.modal-close-btn')) {
            e.preventDefault();
            e.stopPropagation();
            closeModal();
        }
    });
    const safeInit = (name, fn) => {
        try {
            fn();
        } catch (e) {
            console.error(`❌ Ошибка инициализации ${name}:`, e);
        }
    };
    fetch('/api/global_announcement')
        .then(r => r.json())
        .then(data => {
            if (data.text) {
                const div = document.createElement('div');
                div.id = 'global-announcement';
                div.innerHTML = data.text;
                document.body.prepend(div);
                div.style.display = 'block';
            }
        })
        .catch(() => {});
    safeInit('BackButton', () => BackButtonManager.init());
    safeInit('Settings', () => SettingsManager.init());
    safeInit('Form', () => FormManager.init());
    safeInit('Gallery', () => GalleryManager.init());
    safeInit('WS', () => {
        const boardInput = document.querySelector('input[name="board_id"]');
        if (boardInput) WSManager.init(boardInput.value);
    });
    const isIndexPage = document.body.classList.contains('page-index') || location.pathname === '/';
    const isLoginPage = location.pathname.startsWith('/login') || location.pathname.startsWith('/welcome');
    const isAdminPage = location.pathname.startsWith('/admin');
    const isGalleryPage = location.pathname.startsWith('/gallery');
    const isUsefulPage = location.pathname.startsWith('/useful');
    const isRoulettePage = location.pathname.startsWith('/tv/random');
    const isSearchPage = location.pathname.includes('/search');
    const isOverboardPage = location.pathname.includes('/overboard/');
    
    if (typeof StaticMascot !== 'undefined') {
        StaticMascot.init();
    }
    if (!window.isStaticMascotActive && !isIndexPage && !isLoginPage && !isAdminPage && !isUsefulPage && !isRoulettePage && !isGalleryPage && !isSearchPage) {
        safeInit('Mascot', () => MascotManager.init());
    }
    safeInit('UI', () => UIManager.init());
    safeInit('VPN', () => VpnManager.init());
    safeInit('Preview', () => PreviewManager.init());
    safeInit('JumpReturn', () => JumpManager.init());
    safeInit('Scroll', () => ScrollManager.init());
    safeInit('BoardStyle', () => BoardStyleManager.init());
    safeInit('Reactions', () => ReactionManager.init());
    safeInit('Hotkeys', () => HotkeyManager.init());
    safeInit('Favourites', () => FavouritesManager.init());
    safeInit('History', () => HistoryManager.init());
    safeInit('Book', () => BookManager.init());
    safeInit('MediaWall', () => MediaWallManager.init());
    safeInit('MediaStream', () => MediaStreamManager.init());
    safeInit('Offline', () => OfflineManager.init());
    safeInit('Catalog', () => CatalogManager.init());
    safeInit('ReadTracker', () => ReadTracker.init());
    safeInit('Bottle', () => BottleManager.init());
    safeInit('Roulette', () => RouletteManager.init());
    safeInit('ImageRoulette', () => ImageRouletteManager.init());
    safeInit('Reports', () => ReportManager.init());
    safeInit('AdminUtils', () => AdminUtils.init());
    safeInit('Alerts', () => AlertManager.init());
    safeInit('GlobalMenu', () => GlobalMenu.init());
    safeInit('Stealth', () => typeof StealthEditor !== 'undefined' && StealthEditor.init());
    safeInit('Personalization', () => PersonalizationManager.init());
    safeInit('ZalgoRot', () => ZalgoRot.init());
    safeInit('BlurHash', () => BlurHash.init());
    safeInit('SystemDock', () => SystemDock.init());
    safeInit('TrollMechanics', () => TrollManager.init());
    safeInit('TagSearchScroll', () => TagSearchManager.init());
    if (isOverboardPage) {
        safeInit('WS-Overboard', () => {
            WSManager.init('overboard');
        });
    }
    safeInit('Welcome', () => {
        if (!localStorage.getItem('welcome_v5')) {
            setTimeout(() => {
                const modal = document.getElementById('welcome-modal');
                const overlay = document.getElementById('modal-overlay');
                
                if (modal) {
                    if (!overlay) {
                        const newOverlay = document.createElement('div');
                        newOverlay.id = 'modal-overlay';
                        newOverlay.className = 'modal-overlay';
                        document.body.appendChild(newOverlay);
                    }
                    const activeOverlay = document.getElementById('modal-overlay');

                    activeOverlay.style.display = 'block';
                    setTimeout(() => activeOverlay.classList.add('active'), 10);
                    
                    modal.style.display = 'block';
                    setTimeout(() => {
                        modal.classList.add('active');
                        document.body.classList.add('modal-open');
                    }, 10);

                    const btn = document.getElementById('welcome-accept-btn');
                    if (btn) btn.onclick = () => {
                        localStorage.setItem('welcome_v5', 'true');
                        
                        modal.classList.remove('active');
                        activeOverlay.classList.remove('active');
                        
                        setTimeout(() => {
                            modal.style.display = 'none';
                            activeOverlay.style.display = 'none';
                            document.body.classList.remove('modal-open');
                        
                            if (typeof MascotManager !== 'undefined') {
                                const phrases = [
                                    "Проходи, не задерживайся.",
                                    "Ну, привет.",
                                    "Опять ньюфаги...",
                                    "Сап!"
                                ];
                                MascotManager.say(phrases[Math.floor(Math.random() * phrases.length)], 2000);
                            }
                        }, 300);
                    };
                }
            }, 600); 
        }
    });
    safeInit('SonicID', () => {
        SonicID.attach();
    });
    safeInit('ServerPulse', () => {
        ServerPulse.init();
    });
    safeInit('HidePosts', () => {
        UserState.hiddenPosts.forEach(id => {
            const el = document.getElementById(`post-${id}`);
            if (el) {
                el.classList.add('hidden');
                const threadContainer = el.closest('.thread-container');
                if (threadContainer && el.querySelector('.post-num-link')?.textContent.includes(threadContainer.id.replace('thread-', ''))) {
                    threadContainer.style.display = 'none';
                    const hr = threadContainer.nextElementSibling;
                    if (hr && hr.tagName === 'HR') hr.style.display = 'none';
                }
            }
            const catItem = document.querySelector(`.catalog-item[href*="${id}.html"]`);
            if (catItem) catItem.style.display = 'none';
        });
    });
    safeInit('InitPosts', () => {
        const posts = document.querySelectorAll('.post');
        const total = posts.length;
        const chunkSize = 20;
        let i = 0;
        function processChunk() {
            const end = Math.min(i + chunkSize, total);
            for (; i < end; i++) {
                window.initializePostFeatures(posts[i]);
            }
            if (i < total) {
                requestAnimationFrame(processChunk);
            }
        }
        processChunk();
    });
    safeInit('ThreadLimits', () => {
        document.querySelectorAll('.thread-container').forEach(container => {
            checkThreadLimit(container);
        });
    });
    safeInit('CrossBoard', () => CrossBoardManager.resolve());
    const replyTo = sessionStorage.getItem('quick_reply_to');
    if (replyTo && location.pathname.includes('/res/')) {
        setTimeout(() => {
             FormManager.focusMainForm(replyTo);
             sessionStorage.removeItem('quick_reply_to');
        }, 300);
    }
    function highlightTarget() {
        const hash = window.location.hash;
        if (hash && hash.includes('post-')) {
            const id = hash.substring(1);
            const el = document.getElementById(id);
            if (el) {
                el.classList.remove('target-highlight');
                void el.offsetWidth;
                el.classList.add('target-highlight');
                setTimeout(() => el.classList.remove('target-highlight'), 1500);
            }
        }
    }
    window.addEventListener('hashchange', highlightTarget);
    highlightTarget();
    const feedbackForm = document.getElementById('feedback-form');
    if (feedbackForm) {
        feedbackForm.addEventListener('submit', async (e) => {
            e.preventDefault();
            const btn = feedbackForm.querySelector('button[type="submit"]');
            const originalText = btn.textContent;
            btn.disabled = true;
            btn.textContent = t('feedback_sending');
            const formData = new FormData(feedbackForm);
            const data = Object.fromEntries(formData.entries());
            try {
                const res = await fetch('/api/feedback', {
                    method: 'POST',
                    headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify(data)
                });
                if (res.ok) {
                    showToast(t('feedback_done'));
                    closeModal();
                    feedbackForm.reset();
                } else {
                    const err = await res.json();
                    showToast(`❌ ${t('error_prefix')}${err.detail}`);
                }
            } catch (err) {
                showToast(t('network_error'));
            } finally {
                btn.disabled = false;
                btn.textContent = originalText;
            }
        });
    }
    window.addEventListener('dragover', e => e.preventDefault());
    window.addEventListener('drop', e => {
        if (e.dataTransfer.files && e.dataTransfer.files.length > 0) {
            e.preventDefault();
            e.stopPropagation();
            showToast(t('drag_to_form')); 
        }
    }, false);
    const recalcTime = () => {
        document.querySelectorAll('.ts-recalc:not(.processed)').forEach(el => {
            const ts = parseFloat(el.dataset.ts);
            if (!isNaN(ts)) {
                el.textContent = formatTimestamp(ts);
                el.classList.add('processed');
            }
        });
    };
    recalcTime();
    window.manualRefresh = async function() {
        if (typeof WSManager === 'undefined' || !WSManager.boardId) {
            window.location.reload();
            return;
        }
        showToast(t('updating', 'Обновляем...'));
        try {
            const count = await WSManager.refreshContent();
            if (count > 0) {
                showToast(t('new_posts_count', 'Новых постов: {0}').replace('{0}', count));
            } else {
                showToast(t('no_new_posts', 'Новых постов нет'));
            }
        } catch(e) {
            showToast(t('network_error'));
        }
    };
    let refreshBtn = document.getElementById('mobile-refresh-btn');
    if (refreshBtn && !refreshBtn.getAttribute('onclick')) {
        refreshBtn.onclick = (e) => {
            e.preventDefault();
            window.manualRefresh();
        };
    }
    window.addEventListener('pageshow', (event) => {
        document.body.classList.remove('loading');
        const loader = document.getElementById('app-loader');
        if (loader) loader.remove();
        document.body.style.pointerEvents = '';
        if (typeof ScrollManager !== 'undefined') {
            ScrollManager.loading = false;
            const container = document.querySelector('.chat-list') || document.querySelector('.thread-list') || document.querySelector('.replies-list');
            if (container && container.children.length === 0) {
                console.log('[Nav] Empty list detected on restore (bfcache), forcing reload...');
                ScrollManager.loadMore();
            }
        }

        if (typeof WSManager !== 'undefined' && WSManager.boardId) {
            console.log('[Nav] Page showed (Back/Forward or Tab switch). Forcing sync...');
            WSManager.attempts = 0;
            if (!WSManager.socket || WSManager.socket.readyState !== WebSocket.OPEN) {
                WSManager.startWS();
            }
            setTimeout(() => {
                console.log('[Nav] Auto-refreshing content...');
                WSManager.refreshContent();
            }, 150);
        }
    });
    if (window.location.pathname.includes('/search/tags')) {
        setTimeout(() => {
            GalleryManager.updatePlaylist();
            CrossBoardManager.resolve();
            console.log('✅ Tag Search UI initialized');
        }, 500);
    }
});
const StaticMascot = {
    basePath: '/static/img/mascot_art/',
    totalImages: 27,

    init() {
        const path = window.location.pathname;

        if (document.title.includes('Ошибка') || 
            path.includes('/res/') || 
            path.includes('/chat/') || 
            path.includes('/catalog/')) return;

        let mode = null;

        if (path === '/' || 
                 path.startsWith('/about') || 
                 path.startsWith('/faq') || 
                 path.startsWith('/rules') || 
                 path.startsWith('/useful') ||
                 path.startsWith('/welcome')) {
            mode = 'footer';
        }

        if (!mode) return;
        if (Math.random() > 0.9) return;

        this.spawn(mode);
    },

    spawn(mode) {
        const oldMascot = document.getElementById('mascot-wrapper');
        if (oldMascot) oldMascot.remove(); 
        window.isStaticMascotActive = true; 

        const num = Math.floor(Math.random() * this.totalImages) + 1;
        const side = Math.random() > 0.5 ? 'left' : 'right';

        const img = document.createElement('img');
        img.src = this.basePath + encodeURIComponent(`mascot (${num})`) + ".webp";
        
        img.className = `static-mascot mode-${mode} ${side}`;
        img.style.transform = 'translateY(100px)';

        if (mode === 'footer') {
            const footer = document.querySelector('footer');
            if (footer) {
                footer.appendChild(img);
            } else {
                document.body.appendChild(img);
            }
        } else {
            document.body.appendChild(img);
        }

        img.onload = () => {
            img.classList.add('visible');
        };
        
        img.onerror = () => {
            img.remove();
        };
    }
};
const AchievementManager = {
    observed: new Set(),
    count: 0,
    milestones: [100, 500, 666, 999, 1000, 1488, 2000, 5000, 8888, 10000, 15000, 20000],
    init() {
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const id = entry.target.id;
                    if (id && !this.observed.has(id)) {
                        this.observed.add(id);
                        this.count++;
                        this.check();
                    }
                }
            });
        }, { threshold: 0.1 });
        document.querySelectorAll('.post').forEach(p => observer.observe(p));
        const list = document.querySelector('.thread-list') || document.querySelector('.chat-list');
        if (list) {
            const mutationObserver = new MutationObserver(mutations => {
                mutations.forEach(mutation => {
                    mutation.addedNodes.forEach(node => {
                        if (node.nodeType === 1 && node.classList.contains('post')) {
                            observer.observe(node);
                        }
                    });
                });
            });
            mutationObserver.observe(list, { childList: true });
        }
    },
    check() {
        if (this.milestones.includes(this.count)) {
            if (window.SettingsManager?.current?.soundEnabled) {
                const audio = new Audio('/static/sounds/not.mp3');
                audio.volume = 0.5;
                audio.play().catch(()=>{});
            }
            showToast(t('achievement_viewed').replace('{0}', this.count));
        }
    }
};
AchievementManager.init();
document.addEventListener('change', (e) => {
    if (e.target.name === 'picrandom') {
        const icon = e.target.parentNode.querySelector('.picrandom-icon');
        if (icon) {
            if (e.target.checked) {
                icon.classList.add('rolling');
            } else {
                icon.classList.remove('rolling');
            }
        }
    }
});
if ('serviceWorker' in navigator) navigator.serviceWorker.register('/static/sw.js').catch(console.log);
window.speakPost = (num) => {
    const el = document.getElementById(`post-${num}`);
    if(!el) return;
    const text = el.querySelector('.post-text-content').innerText;
    const u = new SpeechSynthesisUtterance(text);
    u.lang = 'ru-RU';
    window.speechSynthesis.speak(u);
};
window.setReplyTo = function(postNum, event) {
    if (!postNum) return;
    if (typeof FormManager !== 'undefined') {
        FormManager.smartReply(postNum, event);
    }
};
window.speakPost = function(postNum) {
    const el = document.getElementById(`post-${postNum}`);
    if (!el) return;
    const text = el.querySelector('.post-text-content').innerText
        .replace(/>>\d+/g, '')
        .replace(/https?:\/\/\S+/g, 'ссылка')
        .trim();
    window.speechSynthesis.cancel(); 
    if (!text) return showToast(t('no_text_to_speak', 'Текст не найден'));
    const u = new SpeechSynthesisUtterance(text);
    u.lang = 'ru-RU';
    const showStopBtn = () => {
        let btn = document.getElementById('tts-stop-btn');
        if (!btn) {
            btn = document.createElement('button');
            btn.id = 'tts-stop-btn';
            btn.innerHTML = t('tts_stop_btn', '🔇 Стоп');
            btn.onclick = () => {
                window.speechSynthesis.cancel();
                btn.remove();
            };
            document.body.appendChild(btn);
        }
    };
    const removeStopBtn = () => {
        const btn = document.getElementById('tts-stop-btn');
        if (btn) btn.remove();
    };
    u.onstart = showStopBtn;
    u.onend = removeStopBtn;
    u.onerror = removeStopBtn;
    window.speechSynthesis.speak(u);
};
function initTooltips(scope = document) {
    scope.querySelectorAll('[title]').forEach(el => {
        el.setAttribute('data-title', el.getAttribute('title'));
        el.removeAttribute('title');
    });
}
initTooltips();
const SmartLoader = {
    queue: [],
    activeCount: 0,
    maxConcurrent: 12,
    observer: null,
    init() {
        if (this.observer) return;
        this.observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                const img = entry.target;
                if (entry.isIntersecting) {
                    if (!img.dataset.loaded && !img.dataset.queued && !img.classList.contains('broken-final')) {
                        this.enqueue(img);
                    }
                }
            });
        }, { rootMargin: "800px 0px" });
        this.scan();
    },
    scan(scope = document) {
        scope.querySelectorAll('.thumb-ambient').forEach(el => {
            const parent = el.closest('.file-thumb');
            if (parent && (parent.dataset.type === 'video' || parent.dataset.type === 'gif')) {
                const vid = document.createElement('video');
                vid.className = 'post-image lazy-load'; 
                vid.muted = true;
                vid.loop = true;
                vid.playsInline = true;
                vid.dataset.src = parent.dataset.src || parent.href;
                vid.style.objectFit = 'cover';
                vid.style.width = '100%';
                vid.style.height = '100%';
                el.replaceWith(vid);
            }
        });
        if (!this.observer) { this.init(); return; }
        scope.querySelectorAll('img.lazy-load, video.lazy-load').forEach(img => {
            if (img.dataset.observed) return;
            img.dataset.observed = "true";
            this.observer.observe(img);
        });
    },
    enqueue(img) {
        if (this.queue.includes(img)) return;
        img.dataset.queued = "true";
        this.queue.unshift(img);
        this.process();
    },
    process() {
        if (this.activeCount >= this.maxConcurrent || this.queue.length === 0) return;
        const img = this.queue.shift();
        if (!img.isConnected) {
            this.process();
            return;
        }
        this.activeCount++;
        const parent = img.closest('.file-thumb, .lazy-media-wrapper');
        if (parent) parent.classList.add('is-loading');
        let targetSrc = img.dataset.src;
        if (img.tagName !== 'VIDEO') {
            const onLoad = () => this.onLoadFinished(img, parent, true);
            const onError = () => this.onLoadFinished(img, parent, false);
            img.onload = onLoad;
            img.onerror = onError;
            img.src = targetSrc;
        } else {
            img.onloadeddata = () => {
                img.classList.add('loaded');
                if (parent) parent.classList.remove('is-loading');
            };
            img.onerror = () => {
                 if (parent) {
                    parent.classList.remove('is-loading');
                    parent.classList.add('broken-media');
                    parent.innerHTML = '<div style="font-size:2em; color:#555;">⚠️</div>';
                }
            };
            
            img.src = targetSrc;
            img.load();
            this.activeCount--;
            this.process();
        }
    },
    onLoadFinished(img, parent, success) {
        this.activeCount--;
        if (this.observer) this.observer.unobserve(img);
        if (success) {
            img.dataset.loaded = "true";
            img.classList.add('loaded');
            if (parent) parent.classList.remove('is-loading');
        } else {
            if (img.classList.contains('post-sticker') && !img.dataset.triedVideo) {
                img.dataset.triedVideo = "true";
                const vid = document.createElement('video');
                vid.className = img.className; 
                vid.classList.add('loaded'); 
                vid.src = img.dataset.src || img.src;
                vid.autoplay = true;
                vid.loop = true;
                vid.muted = true;
                vid.playsInline = true;
                vid.setAttribute('webkit-playsinline', 'true');
                vid.style.display = 'block';
                vid.style.maxWidth = '100%';
                vid.style.maxHeight = '150px';
                vid.onerror = () => {
                    const errDiv = document.createElement('div');
                    errDiv.className = 'broken-media';
                    errDiv.innerHTML = '<span>TGS/Error</span>';
                    vid.replaceWith(errDiv);
                };
                img.replaceWith(vid);
                if (parent) parent.classList.remove('is-loading');
                this.process(); 
                return;
            } else {
                 if (!img.dataset.retried) {
                    img.dataset.retried = "true";
                    const sep = img.dataset.src.includes('?') ? '&' : '?';
                    img.dataset.src = img.dataset.src + sep + 'retry=' + Date.now();
                    delete img.dataset.queued;
                    this.enqueue(img);
                    return;
                } else {
                    img.classList.add('broken-final');
                    img.style.display = 'none';
                    if (parent) {
                        parent.classList.remove('is-loading');
                        parent.classList.add('broken-media');
                        parent.innerHTML = '<div style="font-size:2em; color:#555;">⚠️</div>';
                    }
                }
             }
        }
        this.process();
    }
};
document.addEventListener('DOMContentLoaded', () => SmartLoader.init(document));
const _oldInitPost = window.initializePostFeatures;
window.initializePostFeatures = function(el) {
    if (typeof _oldInitPost === 'function') _oldInitPost(el); 
    if (typeof SmartLoader !== 'undefined') {
        SmartLoader.scan(el);
    }
};
document.addEventListener('click', async (e) => {
    const btn = e.target.closest('.action-copy-link');
    if (!btn) return;
    if (navigator.share) {
        e.preventDefault();
        const postNum = btn.dataset.postNum;
        const boardId = btn.dataset.boardId || document.querySelector('input[name="board_id"]')?.value;
        const threadId = btn.dataset.threadId;
        const url = `${window.location.origin}/${boardId}/res/${threadId}.html#post-${postNum}`;
        try {
            await navigator.share({
                title: `ТГАЧ /${boardId}/ #${postNum}`,
                text: `Чекни этот пост:`,
                url: url
            });
        } catch (err) {
        }
    }
});
document.addEventListener('mousedown', createRipple);
function createRipple(e) {
    const target = e.target.closest('.btn, .header-btn, .board-card, .board-nav a, .dock-btn, .dock-tool-btn');
    if (!target) return;
    
    const circle = document.createElement('span');
    const diameter = Math.max(target.clientWidth, target.clientHeight);
    const radius = diameter / 2;
    const rect = target.getBoundingClientRect();
    
    circle.style.width = circle.style.height = `${diameter}px`;
    circle.style.left = `${e.clientX - rect.left - radius}px`;
    circle.style.top = `${e.clientY - rect.top - radius}px`;
    circle.classList.add('ripple');
    
    target.appendChild(circle);
    setTimeout(() => {
        circle.remove();
    }, 600);
}
window.addEventListener('load', () => {
    setTimeout(() => {
        document.body.classList.add('loaded');
        if (typeof MascotManager !== 'undefined') {
            setTimeout(() => MascotManager.welcome(), 300);
        }
        setTimeout(() => {
            const loader = document.getElementById('app-loader');
            if (loader) loader.remove();
        }, 500);
    }, 100);
});
setTimeout(() => {
    document.body.classList.add('loaded');
}, 3000);
window.videoObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
        const vid = entry.target;
        if (!vid.isConnected) return;
        if (entry.isIntersecting) {
            const isGif = vid.classList.contains('is-gif') || vid.closest('[data-type="gif"]');
            if (vid.readyState >= 2 && isGif) {
                vid.play().catch(() => { 
                    vid.muted = true; 
                    vid.play().catch(()=>{}); 
                });
            }
        } else {
            if (!vid.paused && !vid.classList.contains('sticky-mode')) {
                vid.pause();
            }
        }
    });
}, { threshold: 0.1 });
const _oldInitFeatures = window.initializePostFeatures;
window.initializePostFeatures = function(el) {
    if (_oldInitFeatures) _oldInitFeatures(el);
    el.querySelectorAll('video').forEach(v => {
        window.videoObserver.observe(v);
        if(v.readyState > 0) checkIfVideoIsNote(v);
        else v.onloadedmetadata = () => checkIfVideoIsNote(v);
    });
};
document.addEventListener('click', (e) => {
    const btn = e.target.closest('button');
    if (!btn) return;
    const txt = btn.textContent.trim();
    const isCloseBtn =
        txt === '×' ||
        txt === 'X' ||
        btn.classList.contains('modal-close-btn') ||
        btn.getAttribute('aria-label') === 'Close';
    if (isCloseBtn && btn.closest('.modal')) {
        e.preventDefault();
        e.stopPropagation();
        closeModal();
    }
});
async function changeStream(stream) {
    if(!confirm(t('stream_confirm').replace('{0}', stream.toUpperCase()))) return;
    try {
        await fetch('/api/settings/stream', {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify({ stream: stream })
        });
        window.location.reload();
    } catch(e) {
        showToast(t('stream_err'));
    }
}
const TrollManager = {
    init() {
        this.initAntiCopy();
        this.initConsoleTrap();
        this.initMatrixGlitch();
        this.initFlashbang();
        this.initPhantomVibration();
        this.initPhantomDevice();
        this.initBadBacklight();
        this.initFSBMode();
        this.initSubliminal();
    },
    initAntiCopy() {
        document.addEventListener('copy', (e) => {
            const selection = window.getSelection().toString();
            if (selection.length > 30 && Math.random() < 0.05) {
                e.preventDefault();
                const phrases = t('troll_anti_copy');
                const shame = phrases[Math.floor(Math.random() * phrases.length)];
                const newText = `${selection}\n\n[${shame}]`;
                if (e.clipboardData) {
                    e.clipboardData.setData('text/plain', newText);
                } else {
                    navigator.clipboard.writeText(newText).catch(()=>{});
                }
            }
        });
    },
    initConsoleTrap() {
        const isAdmin = document.body.dataset.isAdmin === 'true';
        if (isAdmin) return;
        
        let flooded = false;
        const flood = () => {
            const styles = [
                'background: #a00; color: #fff; font-size: 16px; font-weight: bold; padding: 2px;',
                'background: #000; color: #0f0; font-size: 12px; font-family: monospace;'
            ];
            console.clear(); 
            console.log('%c⚠ CRITICAL SYSTEM FAILURE ⚠', styles[0]);
            console.log('%c[FSB_NODE] UPLOADING USER DATA...', styles[1]);
            console.warn('DELETING SYSTEM32/DRIVERS...');
            console.error('FATAL ERROR: KERNEL PANIC');
            console.log(`%c${t('troll_console_1')}`, styles[0]);
            console.error(t('troll_console_2'));
            console.groupEnd();
        };
        setTimeout(flood, 2000);
        window.addEventListener('keydown', (e) => {
            if (
                e.key === 'F12' || 
                (e.ctrlKey && e.shiftKey && ['I', 'J', 'C'].includes(e.key.toUpperCase()))
            ) {
                if (!flooded) {
                    setInterval(flood, 200); 
                    flood();
                    flooded = true;
                }
            }
        });
    },
    initMatrixGlitch() {
        const el = document.createElement('div');
        el.className = 'matrix-glitch-artifact';
        document.body.appendChild(el);
        const rainbowGradient = 'linear-gradient(90deg, #f00, #0f0, #00f, #ff0, #0ff, #f0f, #fff, #000, #f00, #0f0, #00f)';
        const verticalGradient = 'linear-gradient(180deg, #f00, #0f0, #00f, #ff0, #0ff, #f0f, #fff, #000, #f00, #0f0, #00f)';
        const triggerGlitch = () => {
            if (document.hidden) {
                setTimeout(triggerGlitch, 30000);
                return;
            }

            const isHorizontal = Math.random() > 0.5;
            if (isHorizontal) {
                el.style.width = '100%';
                el.style.height = (1 + Math.random() * 2) + 'px';
                el.style.top = Math.random() * 100 + '%';
                el.style.left = '0';
                el.style.backgroundImage = '';
                el.style.backgroundSize = '20px 100%';
            } else {
                el.style.width = (1 + Math.random() * 2) + 'px'; 
                el.style.height = '100%';
                el.style.left = Math.random() * 100 + '%';
                el.style.top = '0';
                el.style.backgroundImage = verticalGradient;
                el.style.backgroundSize = '100% 20px';
            }
            el.style.display = 'block';
            setTimeout(() => {
                el.style.display = 'none';
            }, 150 + Math.random() * 100);
            const nextDelay = 600000 + Math.random() * 240000; 
            setTimeout(triggerGlitch, nextDelay);
        };
        setTimeout(triggerGlitch, 600000);
    },
    initFlashbang() {
        const isDark = document.body.classList.contains('theme-dark') || 
                       document.body.classList.contains('theme-cyberpunk') ||
                       document.body.classList.contains('theme-black');
        if (!isDark) return;
        const flash = document.createElement('div');
        flash.className = 'flashbang-overlay';
        document.body.appendChild(flash);
        
        const triggerFlash = () => {
            if (document.hidden) {
                setTimeout(triggerFlash, 60000);
                return;
            }
            
            flash.style.display = 'block';
            void flash.offsetWidth; 
            flash.classList.add('active');
            setTimeout(() => {
                flash.classList.remove('active');
                setTimeout(() => { flash.style.display = 'none'; }, 2000);
            }, 100);
            setTimeout(triggerFlash, 600000 + Math.random() * 900000);
        };

        if (Math.random() < 0.3) {
            setTimeout(triggerFlash, 600000 + Math.random() * 900000);
        }
    },
    initPhantomVibration() {
        if (window.innerWidth >= 768) return;
        if (!navigator.vibrate) return;
        const buzz = () => {
            if (!document.hidden) {
                navigator.vibrate([50, 50, 50]);
            }
            setTimeout(buzz, 600000 + Math.random() * 600000);
        };
        setTimeout(buzz, 600000);
    },
    initInputLag() {
        let keyTimes = [];
        let lagMode = false;
        const LIMIT_CPS = 14;
        document.addEventListener('keydown', (e) => {
            const target = e.target;
            if (target.tagName !== 'TEXTAREA' && target.tagName !== 'INPUT') return;
            if (lagMode) {
                if (e.key.length === 1 && !e.ctrlKey && !e.altKey && !e.metaKey) {
                    e.preventDefault();
                    setTimeout(() => {
                        target.setRangeText(e.key, target.selectionStart, target.selectionEnd, "end");
                        target.dispatchEvent(new Event('input'));
                    }, 400);
                }
                return;
            }
            const now = Date.now();
            keyTimes.push(now);
            keyTimes = keyTimes.filter(t => now - t < 2000);
            if (keyTimes.length > LIMIT_CPS * 2) {
                lagMode = true;
                console.log("🔥 Typing too fast. Cooling down...");
                setTimeout(() => {
                    lagMode = false;
                    keyTimes = [];
                }, 15000);
            }
        }, true);
    },
    initPhantomDevice() {
        if (window.innerWidth <= 768) return;
        const playUsbSound = () => {
            if (!window.SettingsManager?.current?.soundEnabled) return;
            const ctx = new (window.AudioContext || window.webkitAudioContext)();
            const t = ctx.currentTime;
            const osc = ctx.createOscillator();
            const gain = ctx.createGain();
            osc.connect(gain);
            gain.connect(ctx.destination);
            osc.type = 'sine';
            osc.frequency.setValueAtTime(784, t);
            osc.frequency.setValueAtTime(523, t + 0.15);
            gain.gain.setValueAtTime(0.1, t);
            gain.gain.exponentialRampToValueAtTime(0.01, t + 0.3);
            
            osc.start(t);
            osc.stop(t + 0.4);
        };

        const loop = () => {
            const delay = 600000 + Math.random() * 600000;
            setTimeout(() => {
                if (!document.hidden) {
                    playUsbSound();
                }
                loop();
            }, delay);
        };
        setTimeout(loop, 600000);
    },
    initBadBacklight() {
        const flicker = () => {
            if (document.hidden) {
                setTimeout(flicker, 60000);
                return;
            }

            const body = document.body;
            let count = 0;
            const interval = setInterval(() => {
                const brightness = 95 + Math.random() * 5; 
                body.style.filter = `brightness(${brightness}%)`;
                count++;
                if (count > 12) {
                    clearInterval(interval);
                    body.style.filter = ''; 
                }
            }, 50);
            setTimeout(flicker, 1200000 + Math.random() * 300000);
        };
        setTimeout(flicker, 1200000);
    },
    initFSBMode() {
        if (Math.random() > 0.005) return;
        const overlay = document.createElement('div');
        overlay.className = 'fsb-overlay';
        overlay.innerHTML = `
            <div class="fsb-icon">🛡️</div>
            <h1 style="margin:0;">${t('troll_fsb_header')}</h1>
            <p>${t('troll_fsb_text')}</p>
            <div class="fsb-bar"><div class="fsb-progress"></div></div>
            <div id="fsb-log" style="margin-top:10px; font-size:12px; opacity:0.7;">INIT_CONNECTION...</div>
        `;
        document.body.appendChild(overlay);
        document.body.style.overflow = 'hidden';
        const bar = overlay.querySelector('.fsb-progress');
        const log = overlay.querySelector('#fsb-log');
        let progress = 0;
        const interval = setInterval(() => {
            progress += Math.random() * 5;
            if (progress > 100) progress = 100;
            bar.style.width = `${progress}%`;
            log.textContent = `UPLOADING SEGMENT_${Math.floor(progress * 124)}...`;
            if (progress >= 100) {
                clearInterval(interval);
                log.textContent = "DONE.";
                setTimeout(() => {
                    overlay.innerHTML = `<h1 style="color:white">${t('troll_fsb_joke')}</h1><p style="color:#ccc">${t('troll_fsb_watch')}</p>`;
                    setTimeout(() => {
                        overlay.remove();
                        document.body.style.overflow = '';
                    }, 2000);
                }, 500);
            }
        }, 100);
    },
    initSubliminal() {
        const el = document.createElement('div');
        el.className = 'subliminal-flash';
        el.style.cssText = 'position: fixed; bottom: 20px; right: 20px; z-index: 2147483647; pointer-events: none; opacity: 0; font-family: monospace; color: #a00; font-weight: bold; font-size: 24px; text-transform: uppercase; transition: opacity 0.1s ease-out; mix-blend-mode: difference;';
        el.innerHTML = '<div class="subliminal-text"></div>';
        document.body.appendChild(el);
        const words = t('troll_subliminal');
        const flash = () => {
            if (document.hidden) {
                setTimeout(flash, 300000); 
                return;
            }
            el.querySelector('.subliminal-text').textContent = words[Math.floor(Math.random() * words.length)];
            if (Math.random() > 0.7) {
                el.style.bottom = '50%';
                el.style.right = '50%';
                el.style.transform = 'translate(50%, 50%) scale(2)';
            } else {
                el.style.bottom = '20px';
                el.style.right = '20px';
                el.style.transform = 'none';
            }
            el.style.opacity = '0.15'; 
            requestAnimationFrame(() => {
                setTimeout(() => {
                    el.style.opacity = '0';
                }, 150);
            });
            setTimeout(flash, 600000 + Math.random() * 600000); 
        };
        setTimeout(flash, 900000 + Math.random() * 900000);
    },
};
document.addEventListener('DOMContentLoaded', () => {
    const match = document.cookie.match(new RegExp('(^| )stream=([^;]+)'));
    const currentStream = match ? match[2] : 'ru';
    const radio = document.querySelector(`input[name="stream_setting"][value="${currentStream}"]`);
    if(radio) radio.checked = true;
});
